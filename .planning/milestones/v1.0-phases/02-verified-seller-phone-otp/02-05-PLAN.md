---
phase: 02-verified-seller-phone-otp
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(public)/u/[username]/page.tsx
  - components/profile/public-profile-header.tsx
  - components/ui/badge.tsx
  - tests/integration/privacy.contract.test.ts
autonomous: true
requirements: [VERF-04]
must_haves:
  truths:
    - "A verified seller's public profile shows an icon + 'Verified' badge next to their username"
    - "An unverified seller's profile shows NO badge"
    - "The badge is read as a boolean via the is_verified_seller RPC — the public page never reads any PII to render it"
    - "An anonymous fetch still exposes ZERO PII keys; the badge boolean does not leak phone or any private field"
  artifacts:
    - path: "components/profile/public-profile-header.tsx"
      provides: "renders the Verified badge when verified===true"
      contains: "Verified"
    - path: "components/ui/badge.tsx"
      provides: "shadcn Badge primitive"
      contains: "badgeVariants"
    - path: "tests/integration/privacy.contract.test.ts"
      provides: "extended assertion: badge boolean present/derivable, phone + PII absent"
      contains: "is_verified_seller"
  key_links:
    - from: "app/(public)/u/[username]/page.tsx"
      to: "supabase.rpc('is_verified_seller', { profile_id })"
      via: "anon RPC returning a boolean (mirrors active_listing_count)"
      pattern: "is_verified_seller"
    - from: "page.tsx"
      to: "PublicProfileHeader verified prop"
      via: "pass the boolean down; header renders the shadcn Badge + lucide icon"
      pattern: "verified"
---

<objective>
Surface the server-computed Verified Seller badge on the public profile — read as a boolean via the is_verified_seller RPC (never any PII), rendered as a shadcn Badge with a lucide check/shield next to the username. Extend the cross-cutting privacy contract test so the badge boolean is present while phone/PII stay absent.

Purpose: VERF-04 — the badge appears only for sellers who completed email + phone + marketplace terms, computed server-side so it auto-revokes. The public profile is anon-safe and the badge must not change that: it is just a boolean from a SECURITY DEFINER function, exactly like active_listing_count.
Output: badge render on /u/[username] + the privacy contract extended.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-verified-seller-phone-otp/02-RESEARCH.md
@app/(public)/u/[username]/page.tsx
@components/profile/public-profile-header.tsx
@tests/integration/privacy.contract.test.ts
@tests/integration/_supabase.ts

<interfaces>
<!-- From Plan 01 (applied to Staging): -->
// public.is_verified_seller(profile_id uuid) returns boolean — anon & authenticated executable.
// Returns true ONLY when email_confirmed AND phone_verified_at AND marketplace_terms_accepted_at.

<!-- Existing page (app/(public)/u/[username]/page.tsx) ALREADY calls the sibling RPC pattern: -->
//   const { data: activeCount } = await supabase.rpc("active_listing_count", { profile_id: profile.id });
// Add the verified RPC right beside it; pass both to PublicProfileHeader.

<!-- PublicProfileHeader current props (components/profile/public-profile-header.tsx): -->
//   { username, stateProvince, country, memberSince, activeListingCount }
// Add `verified: boolean` and render the badge next to username when true.

<!-- Privacy contract (tests/integration/privacy.contract.test.ts) + _supabase.ts: -->
//   PII_KEYS includes "phone"; PUBLIC_PROFILE_KEYS is the allowed public column set; anonClient().
//   Structural layer proves PII columns are physically absent from profiles_public.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Badge + render the Verified badge on the public profile</name>
  <files>components/ui/badge.tsx, app/(public)/u/[username]/page.tsx, components/profile/public-profile-header.tsx</files>
  <action>
1) Install the Badge primitive: `npx shadcn@latest add badge` (writes components/ui/badge.tsx). Use `--yes` if non-interactive.

2) app/(public)/u/[username]/page.tsx — directly after the existing active_listing_count RPC, add:
   `const { data: verified } = await supabase.rpc("is_verified_seller", { profile_id: profile.id });`
   Pass `verified={Boolean(verified)}` to <PublicProfileHeader/>. Keep the page anon-safe and the existing enumerated-column select untouched (do NOT add force-dynamic; the badge is a boolean from a stable function — same caching posture as today). Add a one-line comment that the badge is server-computed (recomputed each read) so it auto-revokes.

3) components/profile/public-profile-header.tsx — add `verified: boolean` to the props. Render, next to the username, when verified is true (02-RESEARCH Code Example):
   ```tsx
   {verified && (
     <Badge variant="secondary" className="gap-1">
       <BadgeCheck className="size-3.5" aria-hidden /> Verified
     </Badge>
   )}
   ```
   Import Badge from @/components/ui/badge and BadgeCheck from lucide-react. Keep layout tidy (username + badge inline). No badge element rendered when false.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>components/ui/badge.tsx exists; the public profile calls is_verified_seller and passes the boolean to the header; the header renders an icon + "Verified" badge only when verified; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the privacy contract — badge boolean present, PII still absent</name>
  <files>tests/integration/privacy.contract.test.ts</files>
  <action>
Extend the existing privacy.contract.test.ts (keep the current structural + live-row layers intact). Add a describe/it block (still gated by INTEGRATION_ENABLED, anon client only) that proves the badge does not breach the PII boundary:
  - `is_verified_seller is anon-callable and returns a boolean` — `supabase.rpc('is_verified_seller', { profile_id: '00000000-0000-0000-0000-000000000000' })`; expect error null and typeof data === 'boolean' (false for unknown user).
  - Reaffirm the boundary: the badge is derived, so add an assertion comment that the public surface exposes ONLY the boolean — there is no new column on profiles_public and no PII key is reachable. (The existing structural it.each over PII_KEYS already enforces phone-absence; this block documents that the badge addition did not add a PII path.)
Run `npm test`.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx vitest run tests/integration/privacy.contract.test.ts</automated>
  </verify>
  <done>privacy.contract.test.ts asserts is_verified_seller returns a boolean to an anon caller while the PII_KEYS (incl. phone) remain physically absent from any public read; passes (or self-skips without env).</done>
</task>

</tasks>

<verification>
- Verified badge renders on /u/[username] only when is_verified_seller is true.
- The badge is read as a boolean via RPC; the public page reads no PII to render it.
- Privacy gate still green: anon reads expose zero PII keys; the badge boolean does not leak phone or any private field.
- `npm run build` + `npm test` green.
</verification>

<success_criteria>
A Verified Seller badge appears on the public profile only for sellers who completed email + phone + marketplace terms, computed server-side and recomputed each read, with no PII exposure. (VERF-04.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-verified-seller-phone-otp/02-05-SUMMARY.md`
</output>
