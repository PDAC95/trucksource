---
phase: 01-foundation-privacy-model
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/0001_foundation_privacy.sql
  - tests/integration/privacy.contract.test.ts
  - tests/integration/rls.test.ts
autonomous: true
requirements: [ACCT-02, PRIV-01, PRIV-02, PRIV-03]

must_haves:
  truths:
    - "PII (first_name, last_name, email, phone, street_address, postal_code) lives ONLY in profiles_private, which has no anon/public SELECT policy"
    - "An anonymous client reading a public profile row gets exactly username, state_province, country, member_since (+ id) — zero PII keys"
    - "An anonymous SELECT against profiles_private returns 0 rows (RLS default-deny), and RLS is enabled on both tables"
    - "active_listing_count(profile_id) exists and returns 0 in Phase 1 (no listings table yet), callable by anon"
  artifacts:
    - path: "supabase/migrations/0001_foundation_privacy.sql"
      provides: "Extensions, profiles_public/profiles_private split, RLS default-deny + policies, handle_new_user trigger, username rename guard, active_listing_count function"
      contains: "create table public.profiles_private"
      min_lines: 90
    - path: "tests/integration/privacy.contract.test.ts"
      provides: "PII-keys contract test — anon public-profile fetch has zero PII keys (THE cross-cutting gate)"
    - path: "tests/integration/rls.test.ts"
      provides: "RLS enabled on both tables + anon SELECT on profiles_private returns 0 rows"
  key_links:
    - from: "auth.users insert"
      to: "profiles_public + profiles_private"
      via: "handle_new_user security-definer trigger reading raw_user_meta_data"
      pattern: "create trigger on_auth_user_created"
    - from: "profiles_private"
      to: "RLS"
      via: "enable row level security with NO anon select policy"
      pattern: "enable row level security"
---

<objective>
Write the single foundation migration — the load-bearing privacy work of the whole project — and the integration tests that prove the privacy guarantee structurally. The migration creates `pg_trgm`/`unaccent`/`citext`, the `profiles_public` (world-readable) / `profiles_private` (owner-only PII) split with RLS default-deny on both, the `handle_new_user` security-definer trigger that populates BOTH rows atomically from signup metadata, the 30-day username rename guard, and the `active_listing_count` function (returns 0 in P1, rewritten in P5). The PII-keys contract test is the cross-cutting gate every later phase re-runs.

Purpose: Make privacy a property of the schema, not of app discipline. No public surface is safe to build until this exists and is proven.
Output: `supabase/migrations/0001_foundation_privacy.sql` applied to Staging + two passing integration tests.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-privacy-model/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- The migration is authored verbatim from RESEARCH.md Pattern 1 (verified Supabase handle_new_user). -->
<!-- Canonical SQL is in 01-RESEARCH.md lines ~159-245 — copy it, do not re-derive. -->

Tables (final shape):
  profiles_public(id uuid PK→auth.users, username citext unique, state_province text, country text,
                  member_since timestamptz default now(), username_changed_at timestamptz,
                  CHECK username ~ '^[A-Za-z0-9]{3,20}$')
  profiles_private(id uuid PK→auth.users, first_name, last_name, email, phone, state_province, country,
                   street_address null, postal_code null, terms_accepted_at timestamptz not null)

Function: public.active_listing_count(profile_id uuid) returns int  -- returns 0 in P1; SECURITY DEFINER, set search_path=''
</interfaces>

# Supabase CLI is linked (Phase 0.1). Apply with `npx supabase db push` (or `supabase migration up` against the linked Staging project).
# Tests run against Staging using the anon key from .env.local (no service role).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the foundation migration</name>
  <files>supabase/migrations/0001_foundation_privacy.sql</files>
  <action>
Author `supabase/migrations/0001_foundation_privacy.sql` from the canonical SQL in 01-RESEARCH.md Pattern 1 (it is verified against Supabase docs — copy it, do not improvise). Include, in order:
1. `create extension if not exists pg_trgm; unaccent; citext;` (citext for case-insensitive username uniqueness; pg_trgm+unaccent per CLAUDE.md invariant 7 / first-migration requirement).
2. `profiles_public` table — exactly: id, username (citext unique not null), state_province, country, member_since (default now()), username_changed_at, and the `username_format` CHECK constraint. NO PII columns.
3. `profiles_private` table — id, first_name, last_name, email, phone, state_province, country, street_address (null), postal_code (null), terms_accepted_at (not null). street_address/postal_code reserved for later (not collected in P1 form).
4. `enable row level security` on BOTH tables (same migration that creates them — invariant 2).
5. Policies: profiles_public → SELECT to anon+authenticated USING(true); UPDATE to authenticated USING/ WITH CHECK ((select auth.uid()) = id). profiles_private → SELECT to authenticated USING ((select auth.uid()) = id); UPDATE to authenticated USING/WITH CHECK ((select auth.uid()) = id). NO anon select and NO insert policy on profiles_private (the trigger is the only creator).
6. `handle_new_user()` — `language plpgsql security definer set search_path = ''`; inserts profiles_public (username/state_province/country from raw_user_meta_data) AND profiles_private (the 6 PII fields + email from new.email + terms_accepted_at from metadata or now()) in one body; `create trigger on_auth_user_created after insert on auth.users for each row execute procedure public.handle_new_user();`.
7. `guard_username_rename()` BEFORE UPDATE trigger on profiles_public enforcing the 30-day window (RESEARCH.md "Username 30-day rename guard").
8. `active_listing_count(profile_id uuid) returns int language sql security definer set search_path = '' as $$ select 0 $$;` — returns 0 in P1 (no listings table); add a comment that Phase 5 rewrites the body to count active listings. Grant execute to anon, authenticated.
Then apply it: `npx supabase db push` against the linked Staging project.
  </action>
  <verify>
    <automated>npx supabase db push</automated>
  </verify>
  <done>Migration applies cleanly to Staging; both tables exist with RLS enabled; `handle_new_user` trigger on auth.users exists; `active_listing_count` callable. (`npx supabase migration list` shows 0001 applied.)</done>
</task>

<task type="auto">
  <name>Task 2: PII-keys contract test + RLS test (the cross-cutting gate)</name>
  <files>tests/integration/privacy.contract.test.ts, tests/integration/rls.test.ts</files>
  <action>
Both tests use a fresh anon Supabase client built from `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (the same env Vitest loads; use `@supabase/supabase-js` createClient directly, no cookies). Define the PII key denylist once: `["first_name","last_name","email","phone","street_address","postal_code"]`.
**privacy.contract.test.ts (ACCT-02, PRIV-01/02):** Seed one disposable user via `supabase.auth.signUp` with full metadata (firstName..country + username) so the trigger creates both rows — OR, if email-confirm gating blocks the row, insert a known fixture via a service-role-free path is not possible; instead query whatever public rows exist and assert shape. Preferred: sign up a throwaway user (random email), then with an ANON client `select('*').from('profiles_public').eq('username', thatUsername).maybeSingle()` and assert: (a) the row exists, (b) `Object.keys(row)` contains NONE of the PII denylist keys, (c) keys are a subset of `["id","username","state_province","country","member_since","username_changed_at"]`. Clean up the test user if a service path is available; otherwise use a clearly-marked test email prefix. If signUp confirmation prevents trigger insert on Staging, fall back to asserting the contract against the table's returned column shape for any existing public row and document the limitation in the SUMMARY.
**rls.test.ts (ACCT-02):** With the ANON client, `select('*').from('profiles_private')` and assert `data` is an empty array (RLS denies → 0 rows, not an error). Also assert via a public read that `profiles_public` IS readable by anon (returns array, possibly empty) — proving the split direction is correct.
Gate both files with a skip-if-unreachable guard (if `check:supabase` env missing, `describe.skip`) so CI without secrets doesn't hard-fail; document this in the test header.
  </action>
  <verify>
    <automated>npm run test -- tests/integration/privacy.contract.test.ts tests/integration/rls.test.ts</automated>
  </verify>
  <done>Contract test asserts zero PII keys in an anonymous public-profile fetch and a column-subset match; RLS test asserts anon SELECT on profiles_private returns 0 rows and profiles_public is anon-readable. Both pass against Staging.</done>
</task>

</tasks>

<verification>
- `npx supabase migration list` shows 0001 applied to Staging.
- Both tables have RLS enabled; profiles_private has no anon SELECT policy.
- `npm run test -- tests/integration/privacy.contract.test.ts tests/integration/rls.test.ts` passes (the PII-keys gate is green).
</verification>

<success_criteria>
- Privacy is structural: PII physically isolated in profiles_private with owner-only RLS; public profile exposes only the 4 allowed fields + id/member_since.
- The signup trigger atomically populates both tables from metadata.
- The PII-keys contract test (cross-cutting gate) and the RLS test pass against Staging.
- active_listing_count exists and returns 0, wired for the Phase 5 rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-privacy-model/01-02-SUMMARY.md`.
</output>
