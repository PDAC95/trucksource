---
phase: 09-contact-private-chat
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - components/messaging/contact-seller-button.tsx
  - components/messaging/contact-form-modal.tsx
  - components/listings/listing-detail.tsx
  - app/(public)/listings/[id]/page.tsx
autonomous: true
requirements: [MSG-01, MSG-02]
must_haves:
  truths:
    - "Every active listing (viewed by a non-owner) shows a prominent 'Contact Seller About This Part' action (MSG-01)"
    - "Anonymous visitors clicking the CTA are sent to login and returned to the listing afterwards"
    - "A logged-in buyer gets a modal contact form (Name, Email, Phone optional, Message) pre-filled from their own private profile, editable (MSG-02)"
    - "Submitting routes the buyer straight into the chat thread at /messages/[threadId]"
    - "A buyer with an existing thread sees 'View conversation' instead of the form; sold/expired listings show no new-contact CTA"
    - "The listing page shows a kebab Report action"
  artifacts:
    - path: "components/messaging/contact-seller-button.tsx"
      provides: "CTA with the four render states (owner/anon/existing-thread/inactive) + modal trigger"
      exports: ["ContactSellerButton"]
    - path: "components/messaging/contact-form-modal.tsx"
      provides: "RHF + zodResolver(contactSchema) Dialog, pre-filled, submits to submitContact"
    - path: "app/(public)/listings/[id]/page.tsx"
      provides: "Server-side prefill (own profiles_private), existing-thread check, props down to the CTA; ReportMenu(listing) mounted"
      contains: "getExistingThreadId"
  key_links:
    - from: "components/messaging/contact-form-modal.tsx"
      to: "lib/actions/contact.ts"
      via: "submitContact on submit; success → router.push(`/messages/${threadId}`)"
      pattern: "submitContact"
    - from: "app/(public)/listings/[id]/page.tsx"
      to: "profiles_private"
      via: "viewer's OWN row via the cookie client (owner RLS) — prefill passed as props, never fetched client-side"
      pattern: "profiles_private"
---

<objective>
Wire the buyer entry point on the listing page: the "Contact Seller About This Part" CTA with its four render states, the pre-filled modal contact form, and the post-submit jump into the chat thread — replacing the existing placeholder contact affordance in listing-detail.tsx. Mount the listing-level ReportMenu while on this surface.

Purpose: MSG-01/02 — the form is the ONLY door into chat; "open chat" is never the primary action (invariant #5 is already enforced server-side by 09-02; this plan must not create any other path).
Output: CTA + modal components, listing page wiring.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-contact-private-chat/09-RESEARCH.md   (Pattern 2)
@app/(public)/listings/[id]/page.tsx     (server page: already resolves viewerId, isOwner, status)
@components/listings/listing-detail.tsx  (has the placeholder "Contact seller" affordance to replace; SaveButton login-invite precedent)
@components/messaging/report-menu.tsx    (from 09-03)
@lib/messaging/schema.ts                 (contactSchema)
@lib/messaging/queries.ts                (getExistingThreadId)
@lib/actions/contact.ts                  (submitContact)
@components/account/contact-preference-form.tsx  (RHF form style precedent)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactSellerButton + ContactFormModal components</name>
  <files>components/messaging/contact-seller-button.tsx, components/messaging/contact-form-modal.tsx</files>
  <action>
**ContactSellerButton** (client) props: `{ listingId, listingTitle, isOwner, isAuthenticated, listingActive, existingThreadId: number | null, prefill: { name: string; email: string; phone?: string } | null }`. Render states (locked decisions):
- owner → render nothing;
- `existingThreadId` → secondary-prominence Button "View conversation" → `Link` to `/messages/${existingThreadId}` (works even if the listing went sold/expired — existing threads stay open);
- not authenticated → primary Button "Contact Seller About This Part" → `Link` to `/login?next=/listings/${listingId}` (SaveButton login-invite posture);
- authenticated + active + no thread → primary Button opening the modal;
- not active + no thread → render nothing (new contacts closed).

**ContactFormModal** (client): vendored Dialog, RHF + `zodResolver(contactSchema)`, defaultValues from `prefill` (editable — it's the buyer's own PII). Fields: Name, Email, Phone (labeled "Phone (optional)"), Message (Textarea, counter against MESSAGE_MAX_LENGTH). Submit button "Send & start chat" (pending state). Microcopy under the form: "Your message is saved and shared with our team before the chat opens." On submit → `submitContact`; map results: ok → `router.push(\`/messages/\${threadId}\`)`; rate_limited → toast "You've reached the daily contact limit. Try again tomorrow."; contacts_closed → toast "This listing is no longer accepting new contacts."; other errors → generic toast. All copy English.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint pass; grep -n "submitContact\|/login?next=" components/messaging/*.tsx returns hits</automated>
  </verify>
  <done>Both components compile; all five render states implemented; success path routes into the thread.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the listing page (prefill + existing-thread + ReportMenu)</name>
  <files>app/(public)/listings/[id]/page.tsx, components/listings/listing-detail.tsx</files>
  <action>
In the server page (already has viewerId/isOwner/status):
- If authenticated and not owner: fetch the viewer's OWN `profiles_private` row via the cookie client (owner RLS) selecting enumerated `first_name, last_name, email, phone`; build `prefill` ({ name: "First Last", email, phone }) — if the row is missing fields, pass empty strings (form still editable). Fetch `existingThreadId` via `getExistingThreadId(listingId, viewerId)`.
- Pass `prefill`, `existingThreadId`, `isAuthenticated`, `isOwner`, `listingActive` down to ListingDetail → ContactSellerButton.
- In listing-detail.tsx: REPLACE the placeholder "Contact seller" affordance with `<ContactSellerButton …/>` in the same visual slot (primary action area near price/seller info). Mount `<ReportMenu targetType="listing" targetId={listing.id} isAuthenticated={…} />` in the detail header area (kebab, top-right of the detail card), hidden for the owner.
- Keep the page's existing privacy posture: prefill PII goes ONLY into the buyer's own form props (it is the viewer's own data); nothing about the SELLER beyond existing public fields is added.
  </action>
  <verify>
    <automated>npm run build exits 0; grep -n "ContactSellerButton\|ReportMenu" components/listings/listing-detail.tsx returns hits; grep -n "placeholder" on the old contact affordance returns nothing</automated>
  </verify>
  <done>Listing page shows the real CTA per render state; report kebab present; placeholder removed; build green.</done>
</task>

</tasks>

<verification>
- `npm run build && npm run test` green.
- Manual smoke (deferred to the 09-07 checkpoint): anon → login redirect; buyer → prefilled modal → submit → lands in /messages/[threadId]; re-click → "View conversation".
</verification>

<success_criteria>
- MSG-01/02 user-visible flow complete up to the thread redirect (thread UI itself is 09-06).
</success_criteria>

<output>
After completion, create `.planning/phases/09-contact-private-chat/09-05-SUMMARY.md`
</output>
