---
phase: 09-contact-private-chat
plan: 07
type: execute
wave: 4
depends_on: ["09-05", "09-06"]
files_modified:
  - app/(app)/messages/page.tsx
  - components/messaging/thread-list.tsx
  - components/messaging/messages-badge.tsx
  - app/(app)/layout.tsx
  - components/account/message-email-form.tsx
  - app/(app)/account/page.tsx
autonomous: false
requirements: [MSG-05, MSG-06]
must_haves:
  truths:
    - "/messages shows the conversation list (part photo, counterparty username, last-message snippet, unread badge) — split view with the active thread on desktop, list→thread pages on mobile"
    - "A 'Messages' nav entry with a live unread badge is visible to logged-in users in the app header"
    - "The inbox empty state reads 'No messages yet…' with a Browse parts CTA"
    - "Account settings has an 'Email me about new messages' toggle, on by default"
    - "End-to-end (human-verified): contact form → admin email + contact_log row → thread opens → second browser receives the message live → non-participant sees nothing"
  artifacts:
    - path: "app/(app)/messages/page.tsx"
      provides: "force-dynamic inbox; desktop split view via ?thread= reusing ThreadView; mobile list linking to /messages/[threadId]"
      contains: "force-dynamic"
    - path: "components/messaging/messages-badge.tsx"
      provides: "Server component: Messages nav link + unreadThreadCount badge"
    - path: "components/account/message-email-form.tsx"
      provides: "Opt-out toggle writing profiles_private.message_email_opt_out (owner RLS)"
  key_links:
    - from: "app/(app)/messages/page.tsx"
      to: "lib/messaging/queries.ts"
      via: "getMyThreads for the list; getThreadForViewer/getThreadMessages when ?thread= selected (desktop split)"
      pattern: "getMyThreads"
    - from: "app/(app)/layout.tsx"
      to: "components/messaging/messages-badge.tsx"
      via: "MessagesBadge mounted in the header next to Saved"
      pattern: "MessagesBadge"
    - from: "components/account/message-email-form.tsx"
      to: "profiles_private.message_email_opt_out"
      via: "owner-RLS update via server action (contact-preference-form pattern)"
      pattern: "message_email_opt_out"
---

<objective>
Finish the surface: the inbox (desktop split view / mobile pages), the always-visible Messages nav entry with the global unread badge, the email opt-out toggle in account settings — then human-verify the entire trust spine live (contact → persist+admin copy → thread → realtime delivery → privacy).

Purpose: completes MSG-05/06 user-facing scope and gates the phase with the only verification that proves realtime + invariant #5 end-to-end.
Output: inbox + badge + settings toggle; UAT checkpoint passed.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-contact-private-chat/09-RESEARCH.md   (Open Q1 & Q3 recommendations, Pitfall 6)
@lib/messaging/queries.ts                 (getMyThreads, unreadThreadCount)
@components/messaging/thread-view.tsx     (from 09-06 — reused in the desktop split)
@components/messaging/thread-header.tsx   (from 09-06)
@app/(app)/layout.tsx                     (header with Saved link — badge mounts here)
@app/(app)/account/page.tsx + components/account/contact-preference-form.tsx  (settings form pattern)
@app/(app)/saved/page.tsx                 (empty-state pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inbox — /messages with desktop split view</name>
  <files>app/(app)/messages/page.tsx, components/messaging/thread-list.tsx</files>
  <action>
`force-dynamic` server page (getClaims → viewerId; layout already guards):
- `getMyThreads(viewerId)` → `<ThreadList/>`: each row = part photo thumb, part title, counterparty public username, last-message snippet (truncated ~60 chars), relative time, unread dot/Badge; listing-status Badge if sold/expired. Row link: on mobile → `/messages/[threadId]`; on desktop → `/messages?thread={id}` (use `Link` with the query param; one URL scheme — `?thread=` simply also renders the split pane, while the plain row link still works on mobile via responsive classes: the list column is always rendered, the thread pane is `hidden lg:flex`; per research Open Q3 keep it pure layout/CSS, NO Next parallel routes).
- When `searchParams.thread` present and valid (getThreadForViewer): render ThreadHeader + ThreadView in the right pane (reusing 09-06 components 1:1) and call markThreadRead. Invalid/foreign id → render the empty pane, not an error.
- Desktop default (no ?thread=): right pane placeholder "Select a conversation".
- Empty state (no threads): "No messages yet. When you contact a seller — or a buyer contacts you — conversations appear here." + Button "Browse parts" → `/` (clone the saved-page empty-state styling).
- Add a `hideThread` affordance per row (kebab → "Hide conversation", AlertDialog confirm "This hides it from your inbox. It doesn't delete anything.") — optional row action wired to the existing action.
  </action>
  <verify>
    <automated>npm run build exits 0; grep -n "getMyThreads\|force-dynamic" "app/(app)/messages/page.tsx" returns hits</automated>
  </verify>
  <done>Inbox lists threads with unread indicators; split view works by query param on desktop; mobile navigates to the thread page; empty state matches the locked copy.</done>
</task>

<task type="auto">
  <name>Task 2: Global unread badge + email opt-out toggle</name>
  <files>components/messaging/messages-badge.tsx, app/(app)/layout.tsx, components/account/message-email-form.tsx, app/(app)/account/page.tsx</files>
  <action>
**MessagesBadge** (server component): props `{ userId }`; calls `unreadThreadCount(userId)`; renders a ghost Button/Link to `/messages` with a `MessageSquare` lucide icon, label "Messages" (hidden sm:inline like Saved), and — when count > 0 — a small count Badge (cap display at "9+"). Mount in `app/(app)/layout.tsx` header next to the Saved link (the layout is already force-dynamic and has the user id). Per research Open Q1, the (app) header is the v1 mount; the public feed has no header chrome for authed users — note this consciously in a code comment (post-v1: shared header). Badge updates on navigation (no realtime subscription for the badge — locked recommendation).

**Email opt-out toggle**: clone components/account/contact-preference-form.tsx → `message-email-form.tsx`: a labeled switch/checkbox "Email me about new messages" (on by default), description "We send at most one email per conversation until you read it." Server action (inline in the form file or lib/actions/account.ts pattern): getClaims → owner-RLS update `profiles_private.message_email_opt_out = !enabled`. Mount on app/(app)/account/page.tsx below the existing contact-preference section; load the current value server-side from the viewer's own profiles_private row.
  </action>
  <verify>
    <automated>npm run build exits 0; grep -n "MessagesBadge" "app/(app)/layout.tsx" returns a hit; grep -n "message_email_opt_out" components/account/message-email-form.tsx returns hits</automated>
  </verify>
  <done>Logged-in header shows Messages with live unread count; settings toggle persists the opt-out flag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end trust-spine UAT</name>
  <what-built>The complete Phase 9 flow: contact form → contact_log + admin email → private realtime chat → inbox/badge → block/report → privacy guarantees.</what-built>
  <action>Pause for human verification of the live end-to-end flow (realtime needs two browser sessions; admin email needs a real inbox). Present the steps below and wait.</action>
  <how-to-verify>
With `npm run dev` and two browsers (Browser A = buyer account, Browser B = seller account that owns an active listing):
1. Browser A, logged out: open the listing → "Contact Seller About This Part" → confirm redirect to login and return to the listing after signing in.
2. Browser A: click the CTA → modal appears pre-filled with YOUR name/email (editable) → write a message → "Send & start chat" → you land in /messages/[threadId] with your message shown.
3. Verify persistence-first: check the `contact_log` row exists in Supabase Staging (Table Editor) and — if ADMIN_NOTIFICATIONS_EMAIL is the Resend account address — the admin email arrived. NOTE: with the unverified resend.dev sender, emails deliver ONLY to the Resend account address; a missing email elsewhere is expected staging behavior, the DB row is the record.
4. Browser B: header shows "Messages" with an unread badge → open the inbox → the conversation shows the part card, BUYER'S USERNAME ONLY as chat identity (the form's real name appears nowhere in the chat) → open the thread.
5. Realtime: with both threads open, send from B → it appears in A WITHOUT refreshing (and vice versa).
6. Browser A: re-visit the listing → CTA now reads "View conversation" and opens the same thread (no second form).
7. Privacy: in the thread and inbox, confirm the seller's real name/email/phone appear nowhere. Open the thread URL as a third account → 404.
8. Block: B blocks A from the thread kebab → A's next send fails with the blocked message → B unblocks → A can send.
9. Report: use the kebab on the listing, a comment, and a message → "Report submitted" toast; reporting the same item again → "You've already reported this."
10. Mark the listing Sold (Browser B) → thread header shows the Sold badge, messaging still works; a third logged-in account sees no contact CTA on the sold listing.
  </how-to-verify>
  <verify>Human walks the 10 steps above; all behave as described.</verify>
  <done>User types "approved"; any reported issue becomes a gap for /gsd:plan-phase --gaps.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build && npm run test && npm run lint && npm run typecheck` all green.
- Human checkpoint approved (realtime + invariant #5 + privacy verified live).
</verification>

<success_criteria>
- All four ROADMAP Phase 9 success criteria observable and human-verified; MSG-01…MSG-07 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-contact-private-chat/09-07-SUMMARY.md`
</output>
