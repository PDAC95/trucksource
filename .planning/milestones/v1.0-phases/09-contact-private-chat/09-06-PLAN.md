---
phase: 09-contact-private-chat
plan: 06
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - components/messaging/thread-view.tsx
  - components/messaging/thread-header.tsx
  - app/(app)/messages/[threadId]/page.tsx
autonomous: true
requirements: [MSG-05, MSG-06]
must_haves:
  truths:
    - "Opening /messages/[threadId] as a participant shows the conversation with a fixed part card header (photo, title, price, status badge, link to the listing)"
    - "Both sides are identified ONLY by public username/display name — never real name, email, or phone (MSG-06)"
    - "Sending a message appears instantly for the sender and arrives live (no refresh) for the other participant via Supabase Realtime"
    - "A non-participant hitting the URL gets a 404-style state, not the thread"
    - "Each received message has a kebab Report action; either party can block the other from the thread menu"
  artifacts:
    - path: "components/messaging/thread-view.tsx"
      provides: "Client component: initial messages prop + postgres_changes subscription + optimistic composer + block/report affordances"
      min_lines: 80
    - path: "app/(app)/messages/[threadId]/page.tsx"
      provides: "force-dynamic server page: getClaims guard, getThreadForViewer + getThreadMessages + participant name map, markThreadRead"
      contains: "force-dynamic"
  key_links:
    - from: "components/messaging/thread-view.tsx"
      to: "public.messages"
      via: "supabase.channel(`thread:${threadId}`).on('postgres_changes', { event: 'INSERT', table: 'messages', filter: `thread_id=eq.${threadId}` })"
      pattern: "postgres_changes"
    - from: "components/messaging/thread-view.tsx"
      to: "lib/actions/messages.ts"
      via: "sendMessage on composer submit (optimistic append, de-dupe by id on the realtime echo)"
      pattern: "sendMessage"
    - from: "app/(app)/messages/[threadId]/page.tsx"
      to: "lib/messaging/queries.ts"
      via: "getThreadForViewer + getThreadMessages (zero-PII readers) + markThreadRead on open"
      pattern: "getThreadForViewer"
---

<objective>
Build the private chat itself: the thread page and the realtime ThreadView — server-fetched initial messages, live postgres_changes delivery, optimistic sending, username-only identity, part-card header with Sold/Expired badge, block and per-message report affordances.

Purpose: MSG-05/06 — this is the product's private communication surface and the first Realtime usage in the project.
Output: thread page + ThreadView/ThreadHeader components.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-contact-private-chat/09-RESEARCH.md   (Pattern 4 has the exact subscription snippet + Pitfalls 1/2/7)
@lib/supabase/client.ts            (browser client factory — auth token rides on subscribe)
@lib/messaging/queries.ts          (getThreadForViewer, getThreadMessages, MessageRow)
@lib/actions/messages.ts           (sendMessage, markThreadRead, blockUser, unblockUser)
@components/messaging/report-menu.tsx   (from 09-03)
@app/(app)/saved/page.tsx          (force-dynamic getClaims→redirect page-guard precedent)
@lib/seller/badge.ts               (resolvePublicName)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread page (app/(app)/messages/[threadId]/page.tsx) + ThreadHeader</name>
  <files>app/(app)/messages/[threadId]/page.tsx, components/messaging/thread-header.tsx</files>
  <action>
Server page, `export const dynamic = "force-dynamic"` (invariant #6; the (app) layout already getClaims-guards, but re-derive viewerId here via getClaims for the queries):
- `getThreadForViewer(threadId, viewerId)`; null → `notFound()` (no existence leak).
- `getThreadMessages(threadId)` for initial render.
- Build a participant name map `{ [userId]: publicName }` from the thread's two profiles_public identities (resolvePublicName) — this is the ONLY identity source in the chat (locked: buyer's form name never appears as chat identity).
- Fire `markThreadRead(threadId)` server-side on render (clears unread + re-arms the email throttle).
- Render `<ThreadHeader/>` + `<ThreadView/>`.

**ThreadHeader** (server-compatible): fixed part card — listing photo thumb, title (Link to `/listings/[id]`), price, status Badge (Active/Sold/Expired — reuse the existing status badge styling from the inbox/listing patterns), counterparty public name. Sold/Expired keeps the thread usable; only the badge changes (locked decision). Include a small DropdownMenu (kebab) with "Block {username}" / "Unblock {username}" wired to blockUser/unblockUser with a confirm AlertDialog ("They won't be able to message you. You can unblock anytime.") — pass an `isBlocked` prop resolved server-side (owner-RLS read of user_blocks).
  </action>
  <verify>
    <automated>npm run build exits 0; grep -n "force-dynamic\|notFound\|markThreadRead" "app/(app)/messages/[threadId]/page.tsx" returns hits; grep -n "profiles_private" app/(app)/messages -r returns nothing</automated>
  </verify>
  <done>Participant-only thread page renders header + messages server-side; non-participant → notFound; read watermark stamped on open.</done>
</task>

<task type="auto">
  <name>Task 2: ThreadView — realtime + optimistic composer</name>
  <files>components/messaging/thread-view.tsx</files>
  <action>
`"use client"` component. Props: `{ threadId, viewerId, initialMessages: MessageRow[], names: Record<string, string>, sendDisabled?: { reason: string } }`.

- State: `messages` seeded from initialMessages. Render bubbles (own = right/accent, other = left/muted), sender public name + relative timestamp; each NON-own message gets `<ReportMenu targetType="message" targetId={m.id} isAuthenticated />` (subtle, on hover/tap).
- Subscription (research Pattern 4 — copy the snippet): `useEffect` keyed on threadId; `createClient()` from lib/supabase/client.ts; `supabase.channel(\`thread:\${threadId}\`).on("postgres_changes", { event: "INSERT", schema: "public", table: "messages", filter: \`thread_id=eq.\${threadId}\` }, payload => append(payload.new as MessageRow))`; cleanup `supabase.removeChannel(channel)` (Pitfall 7). **Append must de-dupe by message id** (optimistic insert + realtime echo). On `CHANNEL_ERROR`/`TIMED_OUT` subscribe status: `router.refresh()` fallback (messages persist — refresh heals). The channel topic string `thread:{id}` is the FUTURE Broadcast topic — do not change it.
- Composer: Textarea + Send button, maxLength MESSAGE_MAX_LENGTH with counter, Enter-to-send (Shift+Enter newline). On submit: optimistic append a temp message (temp id), call `sendMessage`; on ok replace temp with the returned row; on error remove temp + toast (rate_limited → "You're sending messages too fast. Wait a minute."; blocked_or_invalid → "You can't message this user."). Auto-scroll to bottom on new messages.
- Also call `markThreadRead(threadId)` when a realtime message arrives while the thread is open (keeps the badge + email throttle honest).
- NO typing indicators, NO read receipts (locked). All copy English.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build pass; grep -n "postgres_changes\|removeChannel" components/messaging/thread-view.tsx returns hits</automated>
  </verify>
  <done>ThreadView compiles with subscription lifecycle, optimistic de-duped sending, report menu per message; live behavior verified at the 09-07 checkpoint.</done>
</task>

</tasks>

<verification>
- `npm run build && npm run test` green.
- `grep -rn "getSession\|profiles_private" components/messaging/ app/(app)/messages/` → nothing.
- Live two-browser realtime verification deferred to the 09-07 human checkpoint (needs two sessions).
</verification>

<success_criteria>
- A participant can read and send messages on the thread page; identity is public-name only; realtime wiring matches the documented future-proof topic convention.
</success_criteria>

<output>
After completion, create `.planning/phases/09-contact-private-chat/09-06-SUMMARY.md`
</output>
