---
phase: 07-search-feed-public-profile
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/(public)/u/[username]/page.tsx
  - components/profile/profile-listings-grid.tsx
  - components/profile/profile-sort.tsx
  - tests/integration/search.contract.test.ts
autonomous: true
requirements: [SRCH-01]
must_haves:
  truths:
    - "A seller's public profile shows a grid of their ACTIVE listings in the same card format as the feed"
    - "The grid sorts by recent or price via a sort control (no facet panel)"
    - "An empty profile renders the header normally + a friendly 'sin listings activos' empty state"
    - "The profile listings read exposes zero PII (profiles_public enumerated columns only)"
  artifacts:
    - path: "app/(public)/u/[username]/page.tsx"
      provides: "Profile page with the active-listings grid wired (header already exists from Phase 1)"
      contains: "profile-listings-grid"
    - path: "components/profile/profile-listings-grid.tsx"
      provides: "Seller active-listings grid, same card shape as the feed"
    - path: "components/profile/profile-sort.tsx"
      provides: "recent | price sort control driven by a sort searchParam"
  key_links:
    - from: "app/(public)/u/[username]/page.tsx"
      to: "listings (public-read) filtered by seller_id + status=active"
      via: "enumerated read, no profiles_private"
      pattern: "status.*active"
    - from: "components/profile/profile-listings-grid.tsx"
      to: "the feed listing-card shape"
      via: "same SearchCard render (photo + chip + price)"
      pattern: "coverPhotoUrl|fitmentChip"
---

<objective>
Wire the seller's active-listings grid into the existing `/u/[username]` public profile: a grid of the seller's active listings in the same card format as the feed, with a recent|price sort control and a friendly empty state — no facet panel (sellers rarely have enough inventory in v1). Extend the no-PII contract test to cover the profile-grid payload.

Purpose: The profile is centered on the listings grid — that's what the buyer came to see (LOCKED). The Phase-1 header already renders; only the listings zone is new.
Output: Updated profile page + two profile components + extended contract test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-feed-public-profile/07-RESEARCH.md
@.planning/phases/07-search-feed-public-profile/07-CONTEXT.md
@CLAUDE.md
@app/(public)/u/[username]/page.tsx
@lib/listings/queries.ts
@tests/integration/_supabase.ts

<interfaces>
<!-- The profile page ALREADY exists (Phase 1) and renders PublicProfileHeader + EmptyListings.
     It reads profiles_public with ENUMERATED columns and derives the count via active_listing_count RPC.
     This plan REPLACES the EmptyListings placeholder with the real listings grid. Do NOT touch the header read. -->

Existing profile page reads:
  supabase.from("profiles_public").select("id, username, state_province, country, member_since, display_name, seller_type").eq("username", ...).maybeSingle()
  -> notFound() when null; then active_listing_count RPC + is_verified_seller RPC; resolvePublicName(display_name, username).
  Components: <PublicProfileHeader .../> and <EmptyListings/> (replace this with the grid + conditional empty state).

Reuse the feed card shape from 07-03 (components/search/listing-card.tsx, SearchCard) OR the hydration
  helpers in lib/search/queries.ts. Build the profile read by querying `listings` (public-read) WHERE
  seller_id = profile.id AND status = 'active' AND (expires_at is null or expires_at > now()), then hydrate
  cover photo (listingPhotoPublicUrl) + Make/Model chip exactly as the feed does. NEVER select('*'); NEVER
  profiles_private. The seller name on these cards is THIS profile's publicName (already resolved).

sort param: 'recent' (date_listed desc, default) | 'price' (asking_price asc). searchParams is a Promise in Next 16.
listingPhotoPublicUrl from @/lib/listings/storage; resolvePublicName from @/lib/seller/badge.
PII_KEYS / anonClient() / INTEGRATION_ENABLED from tests/integration/_supabase.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile listings grid + sort + empty state (no facets)</name>
  <files>app/(public)/u/[username]/page.tsx, components/profile/profile-listings-grid.tsx, components/profile/profile-sort.tsx</files>
  <action>
Update `app/(public)/u/[username]/page.tsx`:
  - Add `searchParams: Promise<{ sort?: string }>` to the page props; `const { sort } = await searchParams;` → coerce to `'recent' | 'price'` (default 'recent').
  - Keep the existing header read UNCHANGED. After resolving `profile`, read the seller's active listings: `supabase.from("listings").select("id, title, asking_price, condition_id, date_listed").eq("seller_id", profile.id).eq("status","active").or("expires_at.is.null,expires_at.gt." + new Date().toISOString()).order(sort === 'price' ? 'asking_price' : 'date_listed', { ascending: sort === 'price' })`. Enumerate columns — NEVER `*`. (listings is public-read so RLS doesn't auto-scope; the explicit seller_id filter does.)
  - Hydrate each row into the same card shape as the feed (cover photo via listingPhotoPublicUrl from a batched listing_photos read; Make/Model chip via a batched listing_fitment→models→makes read; condition name; price or "Precio a consultar"). The seller name/username on every card is THIS profile's publicName/username (no extra profiles_public read needed). You MAY import the feed's `<ListingCard>` (07-03) for a single card component — if so this plan depends on those props being stable; otherwise render a local card with the identical shape.
  - Replace `<EmptyListings/>`: when the seller has ≥1 active listing render `<ProfileSort/>` + `<ProfileListingsGrid cards={...}/>`; when 0, render the header normally + a friendly "sin listings activos" empty state (the existing EmptyListings copy is fine). Profile stays valid + shareable either way (LOCKED).

`components/profile/profile-listings-grid.tsx` — the seller's active-listing grid, same responsive card format as the feed (reuse ListingCard or mirror it). No facet panel (LOCKED: profile is list + sort only).

`components/profile/profile-sort.tsx` — a small client control (recent | price) that sets the `sort` searchParam (router.replace). Shareable URL.

Anon-safe: this page touches no PII and no per-user data, so do NOT add force-dynamic (keep the existing cacheable posture; the existing header read is already anon-safe).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>/u/[username] shows the seller's active listings as a feed-format grid with a working recent|price sort; an empty profile shows the header + "sin listings activos"; the read enumerates columns (no select('*'), no profiles_private).</done>
</task>

<task type="auto">
  <name>Task 2: Extend no-PII contract test to the profile-grid payload</name>
  <files>tests/integration/search.contract.test.ts</files>
  <action>
Extend `tests/integration/search.contract.test.ts` (created in 07-01) with a profile-grid case: as anon, read a seller's active listings exactly as the profile page does (`from("listings").select("id, title, asking_price, condition_id, date_listed").eq("seller_id", <a known seeded seller id or any seller_id discovered from a public listing>).eq("status","active")`) and assert the returned rows contain ZERO keys from `PII_KEYS`. Also assert the enumerated select shape is honored (no unexpected PII columns). Self-skip when `!INTEGRATION_ENABLED`. This satisfies the cross-cutting gate: public profile surface renders no PII.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/search.contract.test.ts</automated>
  </verify>
  <done>The contract test asserts the anon profile-grid listings read exposes zero PII keys; full file green against Staging.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- `npx vitest run tests/integration/search.contract.test.ts` green (RPC payload + profile-grid payload both PII-free).
- Profile renders the active-listings grid + sort; empty profile renders header + friendly empty state.
- No facet panel on the profile; no select('*'); no profiles_private touch.
</verification>

<success_criteria>
A buyer can open any seller's `/u/[username]`, see that seller's active inventory as a feed-format grid sorted by recency or price, and an empty seller still has a valid, shareable profile — all with zero PII on the wire.
</success_criteria>

<output>
After completion, create `.planning/phases/07-search-feed-public-profile/07-04-SUMMARY.md`.
</output>
