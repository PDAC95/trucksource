---
phase: 06-fitment-intelligence
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - components/ui/skeleton.tsx
  - components/listings/fitment-suggestions.tsx
  - components/listings/listing-form.tsx
  - app/(app)/sell/page.tsx
autonomous: false
requirements: [FINT-01, FINT-02]
user_setup: []

must_haves:
  truths:
    - "The Fitment section has a single-select Part Category control (the trigger)"
    - "Choosing a part category shows grouped suggestion chips ('Common for X', 'From your garage') in real time"
    - "Clicking a suggestion chip (or 'Add all') moves it into the existing confirmed-fitment list — nothing enters confirmed state without a click (FINT-02)"
    - "A subtle skeleton shows while suggestions compute; the input stays responsive (debounce + useTransition)"
    - "Each suggestion can be dismissed (session-only X) and already-confirmed fitments are not re-suggested"
    - "Accepted categories/search-terms submit via the extended listingSchema (categoryIds/searchTermIds) so they persist"
    - "Edit mode pre-fills persisted categories/terms and does not re-suggest them"
  artifacts:
    - path: "components/ui/skeleton.tsx"
      provides: "shadcn Skeleton for the suggestions loading state"
      contains: "Skeleton"
    - path: "components/listings/fitment-suggestions.tsx"
      provides: "Grouped-chips suggestions zone (accept / add-all / dismiss); NO useEffect auto-apply"
      min_lines: 60
    - path: "components/listings/listing-form.tsx"
      provides: "Part-Category select + suggestions wiring into the single fitment/category/term state path"
      contains: "suggestFitment"
    - path: "app/(app)/sell/page.tsx"
      provides: "partCategories passed into the create form"
      contains: "getPartCategories"
  key_links:
    - from: "components/listings/listing-form.tsx"
      to: "lib/fitment/suggest.ts suggestFitment"
      via: "debounced useTransition fetch on categoryId change"
      pattern: "suggestFitment"
    - from: "components/listings/fitment-suggestions.tsx"
      to: "listing-form setFitment / form.setValue"
      via: "onAcceptFitment / onAcceptTag handlers invoked ONLY by click (no effect)"
      pattern: "onAccept"
    - from: "components/listings/listing-form.tsx"
      to: "ListingInput.categoryIds / searchTermIds"
      via: "submit payload includes categoryIds + searchTermIds"
      pattern: "categoryIds"
---

<objective>
Wire the suggestion layer onto the existing Phase-5 tagging UI: add the Part-Category trigger select, a grouped-chips suggestions zone, real-time debounced fetching of `suggestFitment`, and the accept / add-all / dismiss interactions — with the FINT-02 hard guarantee that NOTHING enters the confirmed-fitment state without an explicit seller click (no `useEffect` ever calls `setFitment`). Accepted chips flow through the EXISTING `setFitment` + `form.setValue("fitment", …)` single-source-of-truth path, and accepted category/search-term tags add to new `categoryIds`/`searchTermIds` state submitted via the extended schema.

This is the seller-facing payoff of Phase 6 and ends with a human-verify checkpoint (live flow), so `autonomous: false`.

Purpose: Deliver FINT-01's visible suggestions and FINT-02's never-auto-applied guarantee on top of the persistence (06-02) and engine (06-03) already built.
Output: vendored Skeleton, the suggestions component, the wired form, the create page passing categories in.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fitment-intelligence/06-CONTEXT.md
@.planning/phases/06-fitment-intelligence/06-RESEARCH.md
@.planning/phases/06-fitment-intelligence/06-02-SUMMARY.md   # persistence + getPartCategories + ListingFormDefaults extension
@.planning/phases/06-fitment-intelligence/06-03-SUMMARY.md   # suggestFitment + the shared types

@components/listings/listing-form.tsx        # the form to wire — fitment state, setFitment + form.setValue path, startTransition usage, ListingFormDefaults
@components/listings/fitment-multi-select.tsx # FitmentSelection shape, the addFitment de-dupe predicate, Badge+X chip pattern to reuse
@lib/fitment/types.ts                         # SuggestedFitment / SuggestedTag / SuggestionGroup / SuggestResult (06-03)
@lib/fitment/suggest.ts                       # suggestFitment("use server") — called debounced from the client (06-03)
@lib/listings/cascade.ts                      # getPartCategories() (06-02)
@app/(app)/sell/page.tsx                      # create page — pass partCategories in
@app/(app)/sell/[id]/edit/page.tsx            # edit page — already passes categoryIds/searchTermIds into defaults (06-02)
@components/ui/badge.tsx                       # Badge (chips)
@components/ui/button.tsx                      # Button ("Add all suggested")

<interfaces>
<!-- The single-source-of-truth fitment path that accepted chips MUST use (from listing-form.tsx today): -->
const [fitment, setFitment] = React.useState<FitmentSelection[]>(defaults?.fitment ?? []);
// onChange/accept ALWAYS does BOTH:
setFitment(next);
form.setValue("fitment", next.map((f) => ({ modelId: f.modelId, configId: f.configId ?? null })), { shouldValidate: true });
// de-dupe predicate (reuse): f.modelId === x.modelId && (f.configId ?? null) === (x.configId ?? null)

<!-- From 06-03: -->
export type SuggestedFitment = { modelId: number; configId: number | null; makeName: string; modelName: string; configName: string | null };
export type SuggestedTag = { kind: "search_term" | "special_filter" | "category"; id: number; name: string };
export type SuggestionGroup = { source: "garage" | "category"; label: string; fitments: SuggestedFitment[]; tags: SuggestedTag[] };
export async function suggestFitment(input: { partCategoryId: number | null }): Promise<{ groups: SuggestionGroup[] }>;

<!-- From 06-02: -->
export type PartCategoryOption = { id: number; name: string; parentId: number | null };
export async function getPartCategories(): Promise<PartCategoryOption[]>;
// ListingFormDefaults gained: categoryIds: number[]; searchTermIds: number[]; (extend here if 06-02 left it minimal)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor Skeleton + build the grouped suggestions component (no auto-apply)</name>
  <files>components/ui/skeleton.tsx, components/listings/fitment-suggestions.tsx</files>
  <action>
**components/ui/skeleton.tsx** — vendor the shadcn Skeleton (it is NOT yet present in components/ui/). Run `npx shadcn@latest add skeleton` if the CLI works in this environment; otherwise create it manually (the standard shadcn skeleton: a `div` with `className={cn("bg-accent animate-pulse rounded-md", className)}` using the repo's `cn` from `@/lib/utils`). Verify it imports `cn` from the same path the other ui components use.

**components/listings/fitment-suggestions.tsx** — NEW `"use client"` component. Props:
```ts
{
  groups: SuggestionGroup[];        // from suggestFitment, already filtered server-side
  isLoading: boolean;               // pending transition → show skeleton
  hasTrigger: boolean;              // a category is selected (drives empty-state copy)
  dismissed: Set<string>;           // session-dismissed keys (parent-owned)
  onDismiss: (key: string) => void;
  onAcceptFitment: (f: SuggestedFitment) => void;
  onAcceptTag: (t: SuggestedTag) => void;
  onAddAll: (group: SuggestionGroup) => void;
}
```
Render rules (CONTEXT):
  - While `isLoading`: a subtle `<Skeleton>` row in the suggestions zone (a few chip-sized skeleton bars). Do NOT clear the prior groups abruptly — show skeleton overlaid/below.
  - For each group: a small group header = `group.label` (the explainability layer — "From your garage" / "Common for Bumpers"), an "Add all suggested" `Button variant="ghost"/"outline" size="sm"` calling `onAddAll(group)`, then the chips. Each chip is a `Badge` (reuse the fitment-multi-select Badge+X visual) showing the fitment label (`[makeName, modelName, configName].filter(Boolean).join(" ")`) or the tag name; clicking the chip body calls `onAcceptFitment`/`onAcceptTag`; a small `X` button calls `onDismiss(key)` (session-only). Visually distinguish the garage group (e.g. a subtle badge/border tint) per CONTEXT "From your garage".
  - Compute a stable `key` per suggestion: fitments → `f-${modelId}-${configId ?? "null"}`; tags → `t-${kind}-${id}`. Filter out dismissed keys before rendering.
  - Empty state: when not loading and `groups` is empty (after dismissals): if `hasTrigger`, show "No automatic suggestions — add fitments manually below."; if no trigger and no garage groups, render nothing or the same brief line. Do NOT hide the section silently (CONTEXT).
  - **HARD RULE (FINT-02):** this component has NO `useEffect` that mutates parent state and NO auto-accept. Acceptance happens ONLY in the chip/Add-all click handlers, which call the parent's `onAccept*`. Add a top-of-file comment stating this is the FINT-02 guarantee and must never be replaced with an effect.
Accessibility: chips are real `<button>`s with `aria-label`; the suggestions region has `role="group"` / `aria-busy={isLoading}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components/ui/skeleton.tsx && ! grep -q "useEffect" components/listings/fitment-suggestions.tsx && echo "NO-EFFECT-OK"</automated>
  </verify>
  <done>Skeleton vendored; fitment-suggestions renders grouped chips with accept/add-all/dismiss + skeleton loading + empty-state copy; the garage group is visually distinguished; the component contains NO useEffect and no auto-apply (FINT-02 enforced in code); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Part-Category trigger + suggestions into listing-form.tsx</name>
  <files>components/listings/listing-form.tsx, app/(app)/sell/page.tsx</files>
  <action>
**components/listings/listing-form.tsx** — extend the Fitment `<section>`:
  - Add a prop `partCategories: PartCategoryOption[]` to the component signature; thread it from both pages.
  - Extend `ListingFormDefaults` to include `categoryIds: number[]` and `searchTermIds: number[]` (if 06-02 already added them, keep; otherwise add). Seed new state from defaults:
    ```ts
    const [categoryId, setCategoryId] = React.useState<number | null>(
      defaults?.categoryIds?.[0] ?? null,   // single-select trigger (CONTEXT v1)
    );
    const [categoryIds, setCategoryIds] = React.useState<number[]>(defaults?.categoryIds ?? []);
    const [searchTerms, setSearchTerms] = React.useState<SuggestedTag[]>(/* hydrate from defaults? minimal: [] */ []);
    const [suggestions, setSuggestions] = React.useState<SuggestionGroup[]>([]);
    const [dismissed, setDismissed] = React.useState<Set<string>>(new Set());
    const [isSuggesting, startSuggest] = React.useTransition();
    ```
    Note the single-select trigger drives suggestions; `categoryIds` is the persisted M2M (CONTEXT: storage M2M, UI single-select). Setting the category select updates BOTH `categoryId` (trigger) and `categoryIds` ([id]).
  - Add a single-select Part Category control above/within the FitmentMultiSelect area: a shadcn `Select` listing `partCategories` (render parent/child indentation — roots vs children; a flat list with child names is acceptable for v1). On change set `categoryId` + `categoryIds`.
  - Add the debounced suggest effect (THE ONLY effect; it sets `suggestions`, NEVER `fitment`):
    ```ts
    React.useEffect(() => {
      const t = setTimeout(() => startSuggest(async () => {
        const res = await suggestFitment({ partCategoryId: categoryId });
        setSuggestions(res.groups);
      }), 250);
      return () => clearTimeout(t);
    }, [categoryId]);
    ```
  - Filter the rendered groups against ALREADY-CONFIRMED state before passing to `<FitmentSuggestions>` (Pitfall 5, edit-mode + preserve-on-re-trigger): drop suggested fitments already in `fitment` (the de-dupe predicate) and suggested tags whose `kind+id` is already in `categoryIds`/`searchTerms`.
  - Accept handlers (the ONLY path into confirmed state — FINT-02):
    - `onAcceptFitment(s)`: de-dupe against `fitment`; if new, `const next = [...fitment, { modelId: s.modelId, configId: s.configId, makeName: s.makeName, modelName: s.modelName, configName: s.configName }]; setFitment(next); form.setValue("fitment", next.map(...), { shouldValidate: true });` — EXACTLY the existing FitmentMultiSelect.onChange path. Clear fitmentError.
    - `onAcceptTag(t)`: if `t.kind === "category"` add to `categoryIds`; otherwise add to `searchTerms` (the `searchTermIds` submit array derives from `searchTerms`). De-dupe by id.
    - `onAddAll(group)`: apply onAcceptFitment to each group.fitments and onAcceptTag to each group.tags (still explicit user action — one click = "add all").
    - `onDismiss(key)`: `setDismissed(prev => new Set(prev).add(key))` (session-only).
  - Render `<FitmentSuggestions groups={filteredGroups} isLoading={isSuggesting} hasTrigger={categoryId != null} dismissed={dismissed} onDismiss=... onAcceptFitment=... onAcceptTag=... onAddAll=... />` inside the Fitment section, between the category select and/or alongside FitmentMultiSelect. Show accepted search-term tags as removable badges near the confirmed fitment list (so the seller sees confirmed slang tags with an X) — reuse the Badge+X pattern.
  - In `onSubmit`, extend the `payload` to include `categoryIds` and `searchTermIds: searchTerms.map(t => t.id)`. The schema already accepts them (06-01). Do NOT alter the barnyard-or-fitment refine logic.
  - `is_barnyard` interaction (RESEARCH Open Q4): category suggestions still work for Barnyard listings; garage/truck suggestions are simply irrelevant — no special handling needed beyond what listMyTrucks naturally returns. Leave the suggestions zone available regardless of the Barnyard toggle.

**app/(app)/sell/page.tsx** — call `getPartCategories()` (server) and pass `partCategories={...}` into `<ListingForm mode="create" ...>`. (The edit page already passes defaults with categoryIds/searchTermIds from 06-02; add `partCategories` there too — note: edit page is owned by 06-02's files_modified, so add the `partCategories` prop pass-through here only if the edit page isn't re-touched; if tsc requires the edit page to pass partCategories, make that one-line addition and note it. The form prop should be optional-with-default `[]` to avoid a hard coupling.)
Make `partCategories` an optional prop defaulting to `[]` so a page that hasn't been updated still type-checks and just shows no category options.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "suggestFitment" components/listings/listing-form.tsx && grep -q "categoryIds" components/listings/listing-form.tsx && grep -c "setFitment" components/listings/listing-form.tsx && grep -q "getPartCategories" app/\(app\)/sell/page.tsx</automated>
  </verify>
  <done>The form has a single-select Part Category trigger; choosing one debounce-fetches suggestFitment into suggestions state (the ONLY effect; never touches fitment); accept/add-all route through the existing setFitment+form.setValue path and the categoryIds/searchTerms state; dismiss is session-only; confirmed items are filtered out of suggestions; submit payload carries categoryIds + searchTermIds; create page supplies partCategories; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the live Fitment Intelligence flow</name>
  <action>Manually verify the full suggestion flow end-to-end on the live create + edit listing pages (this is a checkpoint — Tasks 1-2 and Plans 06-01/02/03 already automated the build). Walk the steps below.</action>
  <what-built>
The full Fitment Intelligence flow on the live create + edit listing pages: a Part-Category trigger select, real-time grouped suggestion chips ("Common for <Category>" and, when the seller has garage trucks, "From your garage"), one-click accept + "Add all", session-only dismiss, a subtle loading skeleton, and persistence of confirmed categories/search-terms that show back on the public listing detail page. Edit mode pre-fills and does not re-suggest confirmed fitments.
  </what-built>
  <how-to-verify>
1. `npm run dev` and log in as a seller who has at least one garage truck (e.g. a Peterbilt 359). Visit http://localhost:3000/sell.
2. In the Fitment section, confirm a "From your garage" suggestion group appears with the seller's truck(s) — and that NOTHING is pre-added to the confirmed Fits list (FINT-02: garage trucks are suggestions, not auto-applied).
3. Choose Part Category = "Bumpers". Within ~250ms a "Common for Bumpers" group appears (subtle skeleton flashes while computing) with a chip like "Large Car". Confirm the input stayed responsive (no jank).
4. Click a suggestion chip → it moves into the existing confirmed Fits / tags list with an X, and disappears from suggestions. Click "Add all suggested" on a group → all its chips move over. Dismiss a chip with its X → it disappears for this session only.
5. Add at least one model/config fitment via the manual selector too. Publish. On the public listing page, confirm the Part categories + Also-tagged (search terms) sections render alongside the Make/Model/Config "Fits" badges (FINT-03 visible).
6. Open the listing's Edit page: confirm the category + confirmed fitments are pre-filled and that already-confirmed fitments do NOT reappear as fresh suggestions.
7. Toggle The Barnyard on a fresh create: confirm category suggestions still work and publishing succeeds with no Make/Model.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (e.g. suggestions auto-applying, jank, missing read-back, edit re-suggesting confirmed fitments).</resume-signal>
</task>

</tasks>

<verification>
- `npm run test` full suite green; `npx tsc --noEmit` clean; `npm run build` green.
- FINT-01: suggestions appear from the chosen category + garage, grouped with explanatory headers.
- FINT-02: confirmed only via explicit click; no useEffect auto-applies (asserted by the grep in Task 1 + code review + the live checkpoint).
- FINT-03: accepted + manual tags persist identically and render on the detail page (live checkpoint).
- Edit-mode read-back works and does not re-suggest confirmed fitments.
</verification>

<success_criteria>
- The seller sees suggested trucks/configs/categories while creating a listing (FINT-01), grouped and explained by source.
- No suggestion enters the confirmed-fitment list without an explicit seller click (FINT-02) — the single setFitment/form.setValue path is the only entry, with no auto-apply effect.
- Confirmed categories + search terms persist and surface on the public listing (FINT-03 verifiable now; search itself is Phase 7).
</success_criteria>

<output>
After completion, create `.planning/phases/06-fitment-intelligence/06-04-SUMMARY.md`.
</output>
