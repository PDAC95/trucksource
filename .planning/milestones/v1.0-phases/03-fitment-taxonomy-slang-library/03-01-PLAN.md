---
phase: 03-fitment-taxonomy-slang-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0003_fitment_taxonomy.sql
autonomous: true
requirements: [FITL-01, FITL-02, FITL-03, FITL-04, FITL-05, FITL-06, FITL-07, FITL-08]
must_haves:
  truths:
    - "Every Phase-3 reference table is anon-readable (browsable taxonomy)"
    - "Anon/authenticated cannot INSERT/UPDATE/DELETE any reference table (writes are service-role-only)"
    - "Every slang term in search_term_targets points at exactly one real make OR model OR config (FK-enforced, num_nonnulls=1)"
    - "Models are scoped under a make; configs are a shared master applied to relevant models"
    - "The migration is applied to Supabase Staging (so downstream seed + tests run against it)"
  artifacts:
    - path: "supabase/migrations/0003_fitment_taxonomy.sql"
      provides: "8 reference tables + model_configurations join + search_term_targets exclusive arc, all RLS default-deny with public SELECT"
      contains: "create table public.search_term_targets"
      min_lines: 120
  key_links:
    - from: "search_term_targets"
      to: "makes / models / configurations"
      via: "three nullable FKs + check (num_nonnulls(make_id, model_id, config_id) = 1)"
      pattern: "num_nonnulls\\(make_id, model_id, config_id\\) = 1"
    - from: "every create table in 0003"
      to: "row level security"
      via: "alter table ... enable row level security in the same migration"
      pattern: "enable row level security"
---

<objective>
Create migration `0003_fitment_taxonomy.sql` defining the complete 8-level fitment schema as reference data: the hierarchical `makes → models → configurations` (with a shared-config master + `model_configurations` applicability join), the polymorphic slang link (`search_terms` + `search_term_targets` exclusive arc), and the flat dimension tables (`part_categories` self-referencing tree, `materials`, `conditions`, `special_filters`). Every table gets RLS enabled in the SAME migration (default-deny), a public `anon, authenticated` SELECT policy, and NO write policy (service-role-only). Apply the migration to Supabase Staging.

Purpose: The schema is the product's quality ceiling — search precision in Phase 7 is only as good as the FK-enforced slang→entity links created here. This plan ships only schema (no data — data is `seed.sql` in Plan 03-02).
Output: `supabase/migrations/0003_fitment_taxonomy.sql`, applied to Staging.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fitment-taxonomy-slang-library/03-CONTEXT.md
@.planning/phases/03-fitment-taxonomy-slang-library/03-RESEARCH.md
@.planning/research/ARCHITECTURE.md

# Established conventions to mirror EXACTLY (read before writing SQL):
@supabase/migrations/0002_verification.sql

<conventions>
From 0001/0002 (locked, do not deviate):
- File name: zero-padded `0003_fitment_taxonomy.sql` in supabase/migrations/.
- Lowercase SQL keywords: `create table`, `alter table ... enable row level security`, `create policy`.
- PK style: `id bigint generated always as identity primary key` (matches otp_send_attempts in 0002; use this for ALL Phase-3 tables — reference data, readable seeds, no need for unguessable ids).
- `created_at timestamptz not null default now()` on every table.
- RLS pattern (Pattern 1 from RESEARCH): enable RLS in the same migration; add ONE `for select to anon, authenticated using (true)` policy per table; add NO insert/update/delete policy (writes = service-role-only by default-deny, exactly as otp_send_attempts/abuse_events do it).
- `citext` is enabled (0001) — use it for `search_terms.term` so slang uniqueness is case-insensitive.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hierarchical core — makes, models, configurations, model_configurations</name>
  <files>supabase/migrations/0003_fitment_taxonomy.sql</files>
  <action>
Create the file with a header comment block (mirror 0002's style: phase, what it does, privacy/RLS invariants restated). Then create the hierarchical tables, each with RLS enabled + a public SELECT policy + NO write policy, in this order (parents first):

1. `makes` — `id bigint generated always as identity primary key`, `name text not null unique` (FITL-01: 'Peterbilt','Kenworth'), `created_at`. The UNIQUE on name is required for idempotent seed `on conflict (name)`.

2. `models` (FITL-02) — `id` PK, `make_id bigint not null references public.makes(id) on delete cascade`, `name text not null`, `created_at`, `unique (make_id, name)` (model scoped under make; composite unique enables `on conflict (make_id, name)` seeding). Add index on `make_id`.

3. `configurations` (FITL-03, shared master per RESEARCH Pattern 3) — `id` PK, `name text not null unique` (the shared set: Day Cab, Sleeper, Flat-top, Aerodyne, Extended, etc. — NOT per-model), `created_at`. UNIQUE on name for seed idempotency. Add a SQL comment noting this DIVERGES from ARCHITECTURE.md's `configurations.model_id` sketch — it is a deliberate, decision-backed refinement (CONTEXT: "shared standard set applied to relevant models"); this keeps `search_term_targets.config_id` pointing at ONE canonical config row.

4. `model_configurations` — applicability join: `id` PK, `model_id bigint not null references public.models(id) on delete cascade`, `configuration_id bigint not null references public.configurations(id) on delete cascade`, `created_at`, `unique (model_id, configuration_id)`. Index both FK columns. RLS + public SELECT, no write policy.

Each of the four tables MUST have, immediately after its `create table`: `alter table public.<t> enable row level security;` and `create policy "<t> public-readable" on public.<t> for select to anon, authenticated using (true);`.
  </action>
  <verify>
  <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0003_fitment_taxonomy.sql','utf8'); const t=['makes','models','configurations','model_configurations']; for(const x of t){ if(!s.includes('create table public.'+x)) throw new Error('missing table '+x); if(!new RegExp('alter table public.'+x+' enable row level security').test(s)) throw new Error('missing RLS on '+x);} console.log('OK core tables + RLS');"</automated>
  </verify>
  <done>makes, models (FK make_id, unique make_id+name), configurations (shared master, unique name), model_configurations (join, unique model_id+configuration_id) all exist with RLS enabled, a public SELECT policy each, and no write policy.</done>
</task>

<task type="auto">
  <name>Task 2: Slang link (exclusive arc) + flat dimensions L5–L8</name>
  <files>supabase/migrations/0003_fitment_taxonomy.sql</files>
  <action>
Append to the same migration (order matters — search_terms/targets reference makes/models/configurations from Task 1):

5. `search_terms` (FITL-04 canonical term) — `id` PK, `term citext not null unique` (citext → case-insensitive uniqueness; '359 Guys','Aerodyne','Large Car'), `created_at`. RLS + public SELECT, no write policy.

6. `search_term_targets` (FITL-04 polymorphic M2M, RESEARCH Pattern 2 — THE load-bearing decision) — `id` PK, `search_term_id bigint not null references public.search_terms(id) on delete cascade`, three nullable FKs: `make_id bigint references public.makes(id) on delete cascade`, `model_id bigint references public.models(id) on delete cascade`, `config_id bigint references public.configurations(id) on delete cascade`, and `constraint exactly_one_target check (num_nonnulls(make_id, model_id, config_id) = 1)`. Then a UNIQUE INDEX for seed idempotency: `create unique index search_term_targets_uniq on public.search_term_targets (search_term_id, coalesce(make_id,0), coalesce(model_id,0), coalesce(config_id,0));`. RLS + public SELECT, no write policy. DO NOT use a `target_type`/`target_id` pattern — it cannot FK-enforce that the target exists (anti-pattern per RESEARCH Pitfall 1).

7. `part_categories` (FITL-05, self-referencing adjacency tree) — `id` PK, `parent_id bigint references public.part_categories(id) on delete restrict`, `name text not null`, `created_at`, `unique (parent_id, name)` (name unique within a parent; supports on-conflict seeding). RLS + public SELECT, no write policy.

8. `materials` (FITL-06) — `id` PK, `name text not null unique`, `sort_order int not null default 0`, `created_at`. RLS + public SELECT.

9. `conditions` (FITL-07) — same shape as materials (`name unique`, `sort_order`). RLS + public SELECT.

10. `special_filters` (FITL-08) — same shape as materials/conditions. RLS + public SELECT.

Every table: `enable row level security` + a `for select to anon, authenticated using (true)` policy, NO write policy. Use the exact policy-naming style from 0001/0002.
  </action>
  <verify>
  <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0003_fitment_taxonomy.sql','utf8'); const t=['search_terms','search_term_targets','part_categories','materials','conditions','special_filters']; for(const x of t){ if(!s.includes('create table public.'+x)) throw new Error('missing '+x); if(!new RegExp('alter table public.'+x+' enable row level security').test(s)) throw new Error('missing RLS on '+x);} if(!/num_nonnulls\(make_id, model_id, config_id\) = 1/.test(s)) throw new Error('missing exclusive-arc CHECK'); if(!/search_term_targets_uniq/.test(s)) throw new Error('missing idempotency unique index'); if(/target_type/.test(s)) throw new Error('forbidden target_type pattern present'); console.log('OK slang arc + L5-L8 + RLS');"</automated>
  </verify>
  <done>search_terms (citext unique), search_term_targets (3 nullable FKs + num_nonnulls=1 CHECK + uniq index), part_categories (parent_id tree), materials/conditions/special_filters (flat, name unique, sort_order) all exist with RLS + public SELECT only. No target_type pattern present.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration 0003 to Supabase Staging</name>
  <files>supabase/migrations/0003_fitment_taxonomy.sql</files>
  <action>
Apply 0003 to the linked Staging project (same cadence Phases 1–2 used — the integration tests run against Staging, so the schema must exist there). The repo links to Staging via `supabase/.temp`. Run:

`npx supabase db push`

If `db push` reports the project is not linked or prompts for auth, STOP and convert this into a checkpoint: ask the user to either run `npx supabase db push` themselves or paste/run the migration SQL in the Supabase Staging SQL editor — Claude must not invent credentials. Do NOT mark the task done until the four core + six dimension tables are confirmed present on Staging.

Confirm application by listing the tables via the anon client (they should be readable — empty until seeded):
`node --env-file=.env.local -e "const {createClient}=require('@supabase/supabase-js'); const c=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); (async()=>{ for(const t of ['makes','models','configurations','model_configurations','search_terms','search_term_targets','part_categories','materials','conditions','special_filters']){ const {error}=await c.from(t).select('*').limit(1); if(error){ console.error(t, error.message); process.exit(1);} } console.log('OK all 10 tables anon-readable on Staging'); })();"`
  </action>
  <verify>
  <automated>node --env-file=.env.local -e "const {createClient}=require('@supabase/supabase-js'); const c=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); (async()=>{ for(const t of ['makes','models','configurations','model_configurations','search_terms','search_term_targets','part_categories','materials','conditions','special_filters']){ const {error}=await c.from(t).select('*').limit(1); if(error){ console.error(t, error.message); process.exit(1);} } console.log('OK'); })();"</automated>
  </verify>
  <done>All 10 Phase-3 tables exist on Staging and return a successful (possibly empty) anon SELECT — confirming RLS public-read works and the schema is applied.</done>
</task>

</tasks>

<verification>
- `0003_fitment_taxonomy.sql` exists, follows 0001/0002 naming + style.
- All 10 tables have RLS enabled in this same migration and exactly one SELECT policy each; zero write policies.
- The exclusive-arc CHECK (`num_nonnulls(...) = 1`) and the `search_term_targets_uniq` index are present.
- No `target_type`/`target_id` polymorphic pattern anywhere.
- Migration applied to Staging; all 10 tables anon-readable.
</verification>

<success_criteria>
The 8-level fitment schema (FITL-01..08) exists as reference tables on Staging, browsable by anon, write-locked to service-role, with the slang link FK-enforced to resolve to exactly one real entity. Ready for the seed (03-02) and tests (03-03).
</success_criteria>

<output>
After completion, create `.planning/phases/03-fitment-taxonomy-slang-library/03-01-SUMMARY.md`.
</output>
