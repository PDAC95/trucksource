---
phase: 03-fitment-taxonomy-slang-library
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/integration/fitment.test.ts
autonomous: true
requirements: [FITL-01, FITL-02, FITL-03, FITL-04, FITL-05, FITL-06, FITL-07, FITL-08]
must_haves:
  truths:
    - "An anon client can READ every Phase-3 reference table (taxonomy is browsable)"
    - "An anon INSERT into a reference table is DENIED (writes are service-role-only)"
    - "Peterbilt + Kenworth and their seeded models are present"
    - "Every seeded slang term resolves to at least one real make/model/config (seed-integrity gate, the gated deliverable)"
    - "The existing privacy/RLS suite still passes (no Phase-3 regression)"
  artifacts:
    - path: "tests/integration/fitment.test.ts"
      provides: "Anon read/write-deny RLS gate for all 10 Phase-3 tables + seed-presence + every-term-resolves assertions"
      contains: "search_term_targets"
      min_lines: 60
  key_links:
    - from: "tests/integration/fitment.test.ts"
      to: "tests/integration/_supabase.ts"
      via: "import INTEGRATION_ENABLED + anonClient (self-skip pattern)"
      pattern: "from \"./_supabase\""
    - from: "fitment.test.ts seed-resolution assertion"
      to: "search_terms / search_term_targets"
      via: "anon SELECT proves no term has zero targets"
      pattern: "search_term_targets"
---

<objective>
Create `tests/integration/fitment.test.ts` — the CI-visible verification gate for Phase 3, mirroring the existing `rls.test.ts` infrastructure exactly. It proves: (1) every Phase-3 reference table is anon-readable; (2) anon writes are denied (service-role-only); (3) the launch data is seeded (Peterbilt/KW + models); and (4) the gated seed-integrity deliverable — every seeded slang term resolves to a real entity. Then run the full suite to confirm Phase 3 did not regress the Phase 1–2 privacy/RLS gates.

Purpose: CONTEXT makes the seed-integrity test a gated deliverable ("schema quality caps everything downstream"). This is the automated proof, layered on top of the FK + num_nonnulls + do-block defenses from 03-01/03-02.
Output: `tests/integration/fitment.test.ts`, full suite green.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-fitment-taxonomy-slang-library/03-RESEARCH.md
@.planning/phases/03-fitment-taxonomy-slang-library/03-01-SUMMARY.md
@.planning/phases/03-fitment-taxonomy-slang-library/03-02-SUMMARY.md

# Mirror these EXACTLY — test conventions, anon client, self-skip pattern:
@tests/integration/rls.test.ts
@tests/integration/_supabase.ts

<interfaces>
From tests/integration/_supabase.ts (use directly — do not re-implement):
- export const INTEGRATION_ENABLED: boolean   // true only when NEXT_PUBLIC_SUPABASE_* are set
- export function anonClient(): SupabaseClient // fresh anon client, no session — worst-case public caller
Pattern: `const d = INTEGRATION_ENABLED ? describe : describe.skip;` so a secret-less CI run self-skips, never hard-fails (matches rls.test.ts lines 12–15).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anon read + write-deny RLS gate for all 10 Phase-3 tables</name>
  <files>tests/integration/fitment.test.ts</files>
  <action>
Create `tests/integration/fitment.test.ts` with `// @vitest-environment node` and the `INTEGRATION_ENABLED ? describe : describe.skip` self-skip pattern (mirror rls.test.ts header + imports from "./_supabase").

describe block "fitment reference tables: anon-readable, anon-write-denied":
- One `it` that iterates the 10 tables [makes, models, configurations, model_configurations, search_terms, search_term_targets, part_categories, materials, conditions, special_filters] and asserts `anonClient().from(t).select('*')` returns `error === null` and `Array.isArray(data)` (public-read works on each). (Cross-cutting RLS gate, FITL-01..08.)
- One `it` asserting an anon INSERT is DENIED: `await anonClient().from('makes').insert({ name: 'Hacker-' + Date.now() })` returns `error !== null` (no write policy → RLS blocks). Mirror the RESEARCH Code Example.
  </action>
  <verify>
  <automated>npm test -- tests/integration/fitment.test.ts</automated>
  </verify>
  <done>fitment.test.ts proves all 10 tables anon-readable and an anon INSERT into a reference table is rejected. Test passes against Staging (or self-skips if env absent).</done>
</task>

<task type="auto">
  <name>Task 2: Seed-presence + every-slang-term-resolves assertions (gated deliverable)</name>
  <files>tests/integration/fitment.test.ts</files>
  <action>
Append a describe block "fitment seed integrity (gated deliverable)":
- `it` "makes seeded": anon SELECT name from makes includes Peterbilt + Kenworth (`expect.arrayContaining(['Peterbilt','Kenworth'])`), mirroring the RESEARCH example.
- `it` "models seeded under makes": SELECT from models is non-empty and includes a known seeded model (e.g. 'W900' and '379').
- `it` "configurations + applicability seeded": configurations non-empty (includes 'Aerodyne'); model_configurations non-empty.
- `it` "L5–L8 dimensions seeded": materials, conditions, special_filters each non-empty; part_categories includes at least one top-level (parent_id null) and one child (parent_id not null).
- `it` "EVERY slang term resolves to a real entity" (THE gated assertion): fetch all `search_terms` ids; fetch all `search_term_targets` `search_term_id`s; assert the set of term ids with NO matching target row is EMPTY. Compute client-side from two anon SELECTs (anon can read both tables). Also assert the 5 doc-cited terms are present in search_terms ('359 Guys','Flat Glass Kenworth','Aerodyne','Large Car','Glider').
- `it` "exclusive arc holds": for a sample of search_term_targets rows, assert exactly one of make_id/model_id/config_id is non-null (the num_nonnulls CHECK already enforces this at write time; this is the read-side double-check).

Keep assertions resilient to the user-reviewed dataset: check for presence/non-emptiness and the locked doc-cited terms, NOT exact counts (the seed breadth is user-tunable).
  </action>
  <verify>
  <automated>npm test -- tests/integration/fitment.test.ts</automated>
  </verify>
  <done>Tests prove the launch taxonomy is seeded and EVERY slang term resolves to a real entity (zero orphan terms), with the 5 doc-cited terms present. The gated seed-integrity deliverable is automated and green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression — Phase 3 did not break the privacy/RLS gates</name>
  <files>tests/integration/fitment.test.ts</files>
  <action>
Run the complete Vitest suite (`npm test`) to confirm the new tables/policies did not regress the Phase 1–2 gates (rls.test.ts, privacy.contract.test.ts, public-profile.contract.test.ts, badge.test.ts) — the cross-cutting privacy gate must remain green with 10 new tables present. If any prior test fails, diagnose: a Phase-3 reference table must NOT have added a write policy or any PII column, and must NOT have altered profiles_*. Fix only by correcting the Phase-3 migration/test (do not weaken prior gates). Re-run until the full suite is green.
  </action>
  <verify>
  <automated>npm test</automated>
  </verify>
  <done>`npm test` exits 0 — the full suite (Phase 1–2 privacy/RLS gates + the new fitment gate) passes, proving Phase 3 added the taxonomy without regressing the privacy guarantee.</done>
</task>

</tasks>

<verification>
- fitment.test.ts mirrors rls.test.ts conventions (node env, self-skip, anonClient).
- All 10 tables proven anon-readable; anon INSERT proven denied.
- Seed presence proven (Peterbilt/KW + models + configs + L5–L8).
- Every slang term proven to resolve (zero orphans); 5 doc-cited terms present.
- Full `npm test` green — no Phase-1/2 regression.
</verification>

<success_criteria>
Phase 3's gated seed-integrity deliverable is automated and passing: the taxonomy (FITL-01..08) is browsable, write-locked, seeded, and every slang term (FITL-04) resolves to a real entity — with the cross-cutting privacy/RLS gate re-verified green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fitment-taxonomy-slang-library/03-03-SUMMARY.md`.
</output>
