---
phase: 10-admin-operations-analytics
plan: 06
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/admin/page.tsx
  - lib/admin/analytics.ts
  - components/admin/kpi-cards.tsx
  - components/admin/trend-chart.tsx
  - components/admin/ranking-list.tsx
  - components/ui/chart.tsx (shadcn add)
  - package.json
autonomous: true
requirements: [ADMA-01, ADMA-02, ADMA-03, ADMA-04]
must_haves:
  truths:
    - "Dashboard shows KPI cards: registered users, active users (30-day MAU), active listings, messages sent, monthly growth %"
    - "Rankings show most-viewed listings and most-searched makes and models for the selected range"
    - "A 7/30/90 days + All time preset selector re-scopes every chart and ranking (no free date pickers)"
    - "All aggregation happens server-side; client chart components receive only label/value arrays"
  artifacts:
    - path: "lib/admin/analytics.ts"
      provides: "live aggregate queries: user stats RPC call, listing counts, view/search rankings, message counts, monthly growth series"
    - path: "app/admin/page.tsx"
      provides: "force-dynamic dashboard: KPI cards top, trends + rankings below"
    - path: "components/admin/trend-chart.tsx"
      provides: "client Recharts wrapper via shadcn chart"
  key_links:
    - from: "lib/admin/analytics.ts"
      to: "admin_user_activity_stats()"
      via: "createAdminClient().rpc"
      pattern: "admin_user_activity_stats"
    - from: "lib/admin/analytics.ts"
      to: "search_events.facets"
      via: "group-by on facets->>'makeId' / 'modelId' joined to makes/models"
      pattern: "facets"
    - from: "app/admin/page.tsx"
      to: "?range searchParam"
      via: "preset selector driving every query interval"
      pattern: "range"
---

<objective>
ADMA-01..04: the analytics dashboard — live service-role SQL aggregates (no pre-aggregation; discretion answered) rendered as KPI cards, trend charts, and rankings with 7/30/90/all presets.

Purpose: operators measure the marketplace; the event streams (search_events, listing_view_events) built in Phases 5/7 finally get their read side.
Output: /admin dashboard replacing the 10-02 placeholder.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@lib/search/events.ts
@app/(public)/page.tsx
@supabase/migrations/0006_listings.sql
@supabase/migrations/0014_search.sql
@lib/admin/auth.ts

<interfaces>
From 10-01: `admin_user_activity_stats()` definer RPC (registered, active_30d), created_at indexes on both event tables.
From 10-02: dashboard placeholder at app/admin/page.tsx inside the gated layout.
Facet keys are camelCase jsonb: `facets->>'makeId'`, `facets->>'modelId'`, plus `fitsModelId` (verified in app/(public)/page.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics query module</name>
  <files>lib/admin/analytics.ts</files>
  <action>
`lib/admin/analytics.ts` (`server-only`). Accept a `range: '7d'|'30d'|'90d'|'all'` param mapped to an interval (null for all). Because several aggregates exceed PostgREST's comfortable query shapes, prefer small `security definer`-free SQL RPCs ONLY where required — default approach: supabase-js queries with `count`/filters where possible, and add tiny SQL helper functions to migration-style SQL ONLY if PostgREST can't express the group-by (e.g. jsonb facet grouping, date_trunc series). If new SQL functions are needed, create migration `0020_analytics_helpers.sql` with execute revoked from anon/authenticated (service-role-only, same posture as admin_user_activity_stats).

Functions (each returns plain serializable data):
- `getUserStats()`: rpc admin_user_activity_stats → { registered, active30d } (ADMA-01).
- `getListingStats(range)`: count status='active' listings (current), plus most-viewed: group listing_view_events by listing_id within range, top 10, joined to listing title/status (include hidden — admin view) (ADMA-02).
- `getSearchRankings(range)`: top 10 makes by facets->>'makeId' and top 10 models by facets->>'modelId' (union fitsModelId into model demand), joined to names; plus top normalized_term strings as a "Top search terms" extra (Research open question 2) (ADMA-03).
- `getMessageStats(range)`: count(messages) in range (ADMA-04).
- `getGrowthSeries()`: monthly buckets (last 12 months) of new users (profiles_public.member_since) and new listings (created_at), plus growth % = current vs previous full month for the KPI card (ADMA-04).

Export a `getDashboardData(range)` that runs all of the above with Promise.all.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>All aggregates return correct shapes against Staging data; any helper SQL is service-role-only.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn chart + build chart/ranking components</name>
  <files>components/ui/chart.tsx, components/admin/kpi-cards.tsx, components/admin/trend-chart.tsx, components/admin/ranking-list.tsx, package.json</files>
  <action>
Run `npx shadcn@latest add chart` (accepts whatever recharts version it pins — Research open question 1; do not hand-pick). Then:
- `components/admin/kpi-cards.tsx` (server-compatible): row of Cards — Registered users, Active users (30d), Active listings, Messages sent ({range label}), Monthly growth % (↑/↓ with color). Props are plain numbers.
- `components/admin/trend-chart.tsx` ("use client"): ChartContainer + Recharts line/area chart for the monthly growth series ({ month, users, listings }[]).
- `components/admin/ranking-list.tsx`: horizontal bar or simple ranked list (rank, name, count) used for most-viewed listings (link to /admin/listings/[id]), most-searched makes, most-searched models, top search terms. A styled ordered list is acceptable where a bar chart adds nothing.
Client components receive ONLY pre-aggregated `{ label, value }`-shaped props (anti-pattern guard: never raw event rows).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Chart component installed and themed with existing tokens; components render from mock props.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard page assembly with range presets</name>
  <files>app/admin/page.tsx</files>
  <action>
Replace the placeholder: `export const dynamic = "force-dynamic"`. Read `searchParams.range` (default '30d', validate against the four presets). Layout per locked decision: KPI cards on top; below, two-column grid — growth trend chart, most-viewed listings, most-searched makes, most-searched models, top search terms. Preset selector as four link-buttons (?range=7d|30d|90d|all) preserving the active state. Wrap data sections in `<Suspense>` with skeleton fallbacks (discretion answered: skeletons from components/ui/skeleton.tsx); fetch via getDashboardData(range). Empty-data states: "No data yet for this range." English copy.
  </action>
  <verify>
    <automated>npm run build && bash -c '! grep -r "SUPABASE_SERVICE_ROLE_KEY" .next/static'</automated>
  </verify>
  <done>/admin shows live numbers matching Staging reality (spot-check counts vs SQL); switching presets changes ranked/aggregate values; route is dynamic (λ).</done>
</task>

</tasks>

<verification>
- KPI numbers match direct SQL spot-checks on Staging.
- Range switch alters messages-sent count and rankings.
- No event rows serialized to the client (inspect RSC payload / props).
</verification>

<success_criteria>
- ADMA-01..04 complete: registered/active users, active + most-viewed listings, most-searched makes/models, messages sent, monthly growth — all live, ranged, service-role-only.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-06-SUMMARY.md`
</output>
