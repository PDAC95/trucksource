---
phase: 10-admin-operations-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/admin/auth.ts
  - lib/admin/audit.ts
  - app/admin/layout.tsx
  - app/admin/page.tsx
  - components/admin/admin-sidebar.tsx
  - .github/workflows/ci.yml
autonomous: true
requirements: [ADMO-01]
must_haves:
  truths:
    - "A non-admin (or anon) visiting any /admin URL gets a 404, not a login hint"
    - "An admin sees the console shell with the locked sidebar: Dashboard, Users, Listings, Reports, Messages, Categories, Fitment Library"
    - "Every admin surface has exactly one gate helper and one audit writer to import"
    - "CI fails if the service-role key name appears in client chunks"
  artifacts:
    - path: "lib/admin/auth.ts"
      provides: "requireAdmin() — the ONE admin gate"
      exports: ["requireAdmin"]
    - path: "lib/admin/audit.ts"
      provides: "logAdminAction() — the ONE audit writer"
      exports: ["logAdminAction"]
    - path: "app/admin/layout.tsx"
      provides: "gated force-dynamic console shell with sidebar"
  key_links:
    - from: "lib/admin/auth.ts"
      to: "supabase.auth.getClaims()"
      via: "verified JWT app_metadata.role check"
      pattern: "getClaims"
    - from: "lib/admin/audit.ts"
      to: "admin_audit_log"
      via: "createAdminClient().from('admin_audit_log').insert"
      pattern: "admin_audit_log"
---

<objective>
Build the admin console shell: requireAdmin() gate on app_metadata.role, logAdminAction() audit writer, the gated `app/admin` layout with the locked fixed sidebar, and the CI scan that proves the service-role key never reaches client bundles (cross-cutting gate).

Purpose: every Wave-2 plan imports these two helpers and mounts pages inside this layout — contracts first.
Output: working /admin shell (404 for non-admins) + CI bundle scan.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@lib/supabase/admin.ts
@lib/supabase/server.ts
@lib/actions/account.ts
@app/(app)/layout.tsx
@components/layout/site-header.tsx
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: requireAdmin() and logAdminAction() helpers</name>
  <files>lib/admin/auth.ts, lib/admin/audit.ts</files>
  <action>
Create `lib/admin/auth.ts` (`import "server-only"`) exporting `requireAdmin(): Promise<{ adminId: string }>` exactly per Research Pattern 1: `createClient()` from lib/supabase/server, `auth.getClaims()` (NEVER getSession — invariant #6), read `claims.app_metadata.role`; if no sub or role !== 'admin' call `notFound()` (404, don't advertise the console). Match the getClaims usage shape already in lib/actions/account.ts.

Create `lib/admin/audit.ts` (`import "server-only"`) exporting `logAdminAction(entry: { adminId: string; action: AdminAction; targetType: 'user'|'listing'|'photo'|'report_group'|'thread'|'taxonomy'|'import'; targetId: string; reason?: string; metadata?: Record<string, unknown> })`. Export an `AdminAction` string-union type listing the action vocabulary from Research Pattern 10 ('listing_hide', 'listing_restore', 'photo_remove', 'user_suspend', 'user_ban', 'user_reactivate', 'username_rename', 'warn_user', 'report_resolve', 'report_dismiss', 'thread_freeze', 'thread_unfreeze', 'thread_content_access', 'taxonomy_create', 'taxonomy_update', 'taxonomy_deactivate', 'taxonomy_delete', 'csv_import', 'bulk_publish'). Insert via `createAdminClient()`. Throw on insert failure — an unaudited admin action must not silently succeed.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Both helpers exist, server-only, typed; AdminAction union exported for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Gated admin layout + sidebar + placeholder dashboard</name>
  <files>app/admin/layout.tsx, app/admin/page.tsx, components/admin/admin-sidebar.tsx</files>
  <action>
`app/admin/layout.tsx`: server component, `export const dynamic = "force-dynamic"`, calls `await requireAdmin()` then renders a fixed left sidebar + main content area. Sidebar (components/admin/admin-sidebar.tsx, client component for active-link highlighting via usePathname) with EXACTLY the locked nav, in this order: Dashboard (/admin), Users (/admin/users), Listings (/admin/listings), Reports (/admin/reports), Messages (/admin/messages), Categories (/admin/fitment/part_categories), Fitment Library (/admin/fitment), plus a secondary "CSV Import" link (/admin/import) and a "Back to site" link to /. Use lucide-react icons + existing Tailwind tokens; desktop-first is fine (admin console), but don't break at mobile widths (collapse to a sheet using existing components/ui/sheet.tsx if trivial, else a simple top nav fallback). All copy in English.

`app/admin/page.tsx`: force-dynamic placeholder Dashboard page ("Analytics dashboard" heading + skeleton cards using components/ui/skeleton.tsx) — plan 10-06 replaces its body; keep the file minimal so the overlap is a trivial rewrite.

Note in layout comments: the layout gate is UX only; every admin Server Action/route handler must call requireAdmin() itself (anti-pattern guard).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build passes; /admin route compiled as dynamic (λ); anon/non-admin gets 404; flagged admin (from 10-01 script after re-login) sees sidebar shell.</done>
</task>

<task type="auto">
  <name>Task 3: CI scan — service-role key never in client bundle</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add a CI step after the existing build step (add a build step if CI doesn't build): fail if the literal string `SUPABASE_SERVICE_ROLE_KEY` appears anywhere under `.next/static`. Use `! grep -r "SUPABASE_SERVICE_ROLE_KEY" .next/static` (or equivalent that works in the workflow's shell). If CI has no build job today, add `npm run build` with dummy NEXT_PUBLIC_* env vars sufficient for build (check how the repo currently builds in CI; reuse its env setup). Keep the step name explicit: "service-role key never in client bundle".
  </action>
  <verify>
    <automated>npm run build && bash -c '! grep -r "SUPABASE_SERVICE_ROLE_KEY" .next/static'</automated>
  </verify>
  <done>Local grep proves the key name is absent from client chunks; CI workflow contains the same gate.</done>
</task>

</tasks>

<verification>
- /admin returns 404 for anon and for a normal authenticated user; renders shell for the flagged admin.
- `grep -r "SUPABASE_SERVICE_ROLE_KEY" .next/static` exits non-zero after a fresh build.
- lib/admin/* both import "server-only".
</verification>

<success_criteria>
- Cross-cutting gate satisfied: service-role isolated to lib/supabase/admin.ts consumers, CI-scanned.
- Downstream plans have their contracts: requireAdmin(), logAdminAction(), AdminAction, layout slots.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-02-SUMMARY.md`
</output>
