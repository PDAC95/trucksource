---
phase: 10-admin-operations-analytics
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/admin/fitment/page.tsx
  - app/admin/fitment/[level]/page.tsx
  - lib/admin/taxonomy-config.ts
  - lib/actions/admin/taxonomy.ts
  - components/admin/taxonomy-crud.tsx
  - components/admin/slang-editor.tsx
  - lib/fitment (picker queries gain is_active filter)
autonomous: true
requirements: [ADMO-05, ADMO-06]
must_haves:
  truths:
    - "Admin can create, edit, deactivate/reactivate values at every taxonomy level: makes, models, configurations, search terms, part categories, materials, conditions, special filters"
    - "Hard delete succeeds only when nothing references the value; otherwise the UI says 'in use — deactivate instead'"
    - "Deactivated values disappear from NEW-listing pickers but existing listings keep them, visible and searchable"
    - "Admin can add/edit/remove slang terms and their canonical targets (search_terms + search_term_targets)"
    - "Categories is reachable from its own sidebar link and lives inside the Fitment Library implementation"
  artifacts:
    - path: "lib/admin/taxonomy-config.ts"
      provides: "level config map: table, label, columns, parent FK, special handling flags"
    - path: "lib/actions/admin/taxonomy.ts"
      provides: "createValue, updateValue, setActive, deleteValue, slang CRUD"
    - path: "app/admin/fitment/[level]/page.tsx"
      provides: "generic CRUD page per level"
  key_links:
    - from: "lib/actions/admin/taxonomy.ts"
      to: "taxonomy tables"
      via: "service-role writes (tables have no public write policies)"
      pattern: "createAdminClient"
    - from: "lib/fitment picker queries"
      to: "is_active = true"
      via: "filter on new-listing/garage pickers only"
      pattern: "is_active"
---

<objective>
ADMO-05/06: Fitment Library management — one generic, config-driven CRUD over the 8 taxonomy levels plus a dedicated slang (search_terms → targets) editor, with is_active lifecycle semantics that never disturb existing listings.

Purpose: adding trucker slang and catalog values is the most frequent ongoing admin op; the taxonomy is the product's first pillar.
Output: /admin/fitment level picker + per-level CRUD + slang editor; pickers filter inactive values.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@supabase/migrations/0003_fitment_taxonomy.sql
@supabase/migrations/0014_search.sql
@lib/fitment
@lib/admin/auth.ts
@lib/admin/audit.ts

<interfaces>
From 10-01: `is_active boolean not null default true` on makes, models, configurations, search_terms, part_categories, materials, conditions, special_filters.
From 10-02: `requireAdmin()`, `logAdminAction()` with taxonomy_* actions.
Note: CONTEXT's "search_synonyms" = this project's `search_terms` + `search_term_targets` (verify exact shape in 0003/0014 before coding).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Level config map + generic taxonomy actions</name>
  <files>lib/admin/taxonomy-config.ts, lib/actions/admin/taxonomy.ts</files>
  <action>
Read 0003 (and 0012/0014 where relevant) to capture each table's real columns, parent FKs, and join tables (models→make_id; configurations master + model_configurations join; part_categories parent_id hierarchy; search_terms + search_term_targets).

`lib/admin/taxonomy-config.ts` (`server-only` ok): export a `TAXONOMY_LEVELS` record keyed by URL slug (makes, models, configurations, terms, part_categories, materials, conditions, special_filters) with: table name, display label, editable columns (name/slug/etc. per table), parent selector spec (e.g. models need make_id picker), and flags for special editors (terms → slang editor, configurations → model_configurations join management). This map drives both the page and the actions — adding a level later is config-only.

`lib/actions/admin/taxonomy.ts` ("use server"), all gated with requireAdmin() + zod + audit:
- `createValue({ level, values })`, `updateValue({ level, id, values })` — generic insert/update via service role using the config map's column whitelist (never spread raw input). Audit 'taxonomy_create'/'taxonomy_update' with { level } metadata.
- `setActive({ level, id, active })` — flips is_active; audit 'taxonomy_deactivate' (or update). Deactivation NEVER touches listings (locked).
- `deleteValue({ level, id })` — hard delete guarded: attempt delete and catch FK violation (restrict FKs already exist) → return { error: "In use by existing data — deactivate instead." }. Audit 'taxonomy_delete' on success.
- Slang: `createSlangTerm({ term, targets })`, `updateSlangTerm`, `setSlangTargets({ termId, targets })`, deleting targets rows as needed — match the real search_terms/search_term_targets shape (target kinds: model/category/etc. per 0014).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Config map covers all 8 levels; generic actions compile and write via service role with audit rows.</done>
</task>

<task type="auto">
  <name>Task 2: Fitment Library pages — level picker, generic CRUD, slang editor</name>
  <files>app/admin/fitment/page.tsx, app/admin/fitment/[level]/page.tsx, components/admin/taxonomy-crud.tsx, components/admin/slang-editor.tsx</files>
  <action>
`app/admin/fitment/page.tsx` (force-dynamic): card grid of the 8 levels with row counts (one service-role count per table) linking to /admin/fitment/[level]. `/admin/fitment/part_categories` is the same generic page — the sidebar "Categories" link from 10-02 already points there (discretion call: categories inside Fitment Library, own nav link).

`app/admin/fitment/[level]/page.tsx` (force-dynamic): validate slug against TAXONOMY_LEVELS (404 otherwise), service-role list of values (include inactive, shown with an Inactive badge + dimmed), search box, Add button. `components/admin/taxonomy-crud.tsx` (client): table + dialogs (add/edit form generated from the config's column list with RHF+zod, parent selector when configured, Deactivate/Reactivate toggle, Delete with confirm — show the "in use — deactivate instead" error from the action). Part categories: render parent_id as an indented tree or parent selector.

`components/admin/slang-editor.tsx`: for level=terms — term list with trigram search, per-term targets editor (add/remove target rows with type+reference pickers per the real search_term_targets shape). This is the highest-frequency admin surface; make add-term + map-target a ≤3-click flow.

All copy English; use existing ui components (table markup can be plain styled table if shadcn table isn't installed — or `npx shadcn@latest add table` per research install line).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Every level browsable/editable; slang term with targets creatable end-to-end; deactivate hides from pickers only; FK-guarded delete messaging works.</done>
</task>

<task type="auto">
  <name>Task 3: is_active filtering on new-listing/garage pickers (and only there)</name>
  <files>lib/fitment (picker query helpers), lib/actions/listings.ts (validation if it re-checks ids)</files>
  <action>
Find every query feeding NEW-value selection UIs: the create/edit listing fitment selector (makes/models/configs), condition/material/special-filter selects, category picker, garage truck pickers, and the 0012 fitment-rules suggestions feeding new listings. Add `is_active = true` filters there. Explicitly DO NOT filter: search/read surfaces, existing listing display, search_listings RPC, synonym expansion at search time — locked decision keeps old values visible/searchable. If server-side listing validation re-resolves taxonomy ids on create, ensure it also rejects inactive ids for NEW listings but does NOT invalidate edits that merely retain an existing inactive value (only block ADDING an inactive value). Add/extend a unit test on the picker query helper asserting inactive values are excluded.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>Deactivated make/model/etc. no longer appears in create-listing pickers; an existing listing using it still renders and is findable via search.</done>
</task>

</tasks>

<verification>
- Deactivate a model → absent from create-listing picker, present on an existing listing's page and in search results.
- Delete a referenced condition → friendly "in use" error; delete an unreferenced test value → gone.
- New slang term maps to a model and search immediately resolves it (synonym path reads search_terms live).
</verification>

<success_criteria>
- ADMO-05 and ADMO-06 complete: full CRUD + activate/deactivate across all 8 levels and slang, with listing-preserving semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-05-SUMMARY.md`
</output>
