---
phase: 10-admin-operations-analytics
plan: 08
type: execute
wave: 3
depends_on: ["10-03", "10-04", "10-07"]
files_modified:
  - app/admin/reports/page.tsx
  - app/admin/reports/[targetKey]/page.tsx
  - lib/admin/reports-queries.ts
  - lib/actions/admin/reports.ts
  - components/admin/report-queue-actions.tsx
autonomous: true
requirements: [ADMO-03]
must_haves:
  truths:
    - "Reports on the same target appear as ONE queue entry with a report counter and aggregated reasons"
    - "Queue is filterable by state (Pending/Resolved/Dismissed) and target type (listing/comment/message)"
    - "Resolve/Dismiss takes an admin note and updates ALL pending reports for that target in one action"
    - "Enforcement actions (hide listing, warn/suspend/ban user, freeze thread) are launchable from the queue entry and reuse the 10-03/10-04/10-07 actions"
    - "Queue actions are audit-logged ('report_resolve'/'report_dismiss')"
  artifacts:
    - path: "lib/admin/reports-queries.ts"
      provides: "grouped queue query (target_key, count, reasons, first_reported) + group detail"
    - path: "lib/actions/admin/reports.ts"
      provides: "resolveReportGroup / dismissReportGroup"
      exports: ["resolveReportGroup", "dismissReportGroup"]
    - path: "app/admin/reports/page.tsx"
      provides: "filterable grouped queue"
  key_links:
    - from: "lib/actions/admin/reports.ts"
      to: "reports.status"
      via: "one UPDATE over all pending rows of the target"
      pattern: "status.*pending"
    - from: "app/admin/reports/[targetKey]/page.tsx"
      to: "lib/actions/admin/enforcement.ts + listings.ts + threads.ts"
      via: "enforcement buttons on the group detail"
      pattern: "suspendUser|hideListing|freezeThread"
---

<objective>
ADMO-03: the abuse-report queue — grouped per target with counters, Pending→Resolved/Dismissed workflow with admin notes, filters, and enforcement actions wired to the already-built ladder.

Purpose: the queue is the operational front door for the whole enforcement system built in Wave 2.
Output: /admin/reports grouped queue + group detail with one-action-resolves-all.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@supabase/migrations/0016_messaging.sql
@lib/admin/auth.ts
@lib/admin/audit.ts

<interfaces>
From 10-01: `reports.status('pending'|'resolved'|'dismissed'), resolved_by, resolved_at, admin_note` + (status, created_at) index.
From 10-03: `warnUser, suspendUser, banUser, renameUsername` (lib/actions/admin/enforcement.ts).
From 10-04: `hideListing` (lib/actions/admin/listings.ts).
From 10-07: `freezeThread` (lib/actions/admin/threads.ts), `getThreadContentJustification` link target for message reports.
Reports exclusive arc: exactly one of listing_id/comment_id/message_id non-null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grouped queue queries</name>
  <files>lib/admin/reports-queries.ts, supabase/migrations/0021_report_queue_rpc.sql (only if PostgREST can't express the group-by)</files>
  <action>
`lib/admin/reports-queries.ts` (`server-only`):
- `getReportQueue({ state: 'pending'|'resolved'|'dismissed', type?: 'listing'|'comment'|'message' })`: grouped per Research's queue SQL — target_key (`listing:<id>` / `comment:<id>` / `message:<id>`), report_count, distinct reasons array, first/last reported, status. Prefer a SQL function (service-role-only execute, new migration 0021) since PostgREST can't group on the coalesce expression; the resolved/dismissed views group the same way filtered by status. Enrich rows app-side with target context: listing title+seller, comment excerpt+listing, message thread participants (batch lookups by id type).
- `getReportGroup(targetKey)`: all report rows for the target (reporter username, reason, note text if the reports table carries details — check 0016 columns), plus the full target context and links: listing → /admin/listings/[id]; message → the thread + /admin/messages/threads/[threadId] (a message report is exactly what unlocks content); comment → comment body + listing link.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Multiple reports on one listing collapse to a single row with count and merged reasons; filters by state/type work.</done>
</task>

<task type="auto">
  <name>Task 2: Queue pages + resolve/dismiss actions with enforcement wiring</name>
  <files>app/admin/reports/page.tsx, app/admin/reports/[targetKey]/page.tsx, lib/actions/admin/reports.ts, components/admin/report-queue-actions.tsx</files>
  <action>
`lib/actions/admin/reports.ts` ("use server"): `resolveReportGroup({ targetKey, adminNote })` and `dismissReportGroup({ targetKey, adminNote })` — requireAdmin, parse targetKey back to the arc column, one service-role UPDATE setting status/resolved_by/resolved_at/admin_note on ALL rows for that target where status='pending' (locked: one action resolves all), audit 'report_resolve'/'report_dismiss' with { targetKey, count } metadata, revalidatePath.

`app/admin/reports/page.tsx` (force-dynamic): state filter tabs (Pending default / Resolved / Dismissed) + type filter select; table: type badge, target summary, report counter badge, reasons, first reported; row → group detail. Pending count badge could feed the sidebar later — keep it page-local for now.

`app/admin/reports/[targetKey]/page.tsx` (force-dynamic; URL-encode targetKey): individual reports list, target context panel, and an Actions panel: context-appropriate enforcement buttons reusing the EXISTING dialogs/actions — listing target: Hide listing + warn/suspend/ban seller; comment target: warn/suspend/ban author (comment hiding only if Phase 8 built a mechanism — do not invent one; otherwise enforcement is against the author); message target: Freeze thread, View thread content (links to the audited 10-07 page), warn/suspend/ban sender. Below: Resolve / Dismiss with required admin note (`components/admin/report-queue-actions.tsx`). The flow "act, then resolve with note" stays two explicit clicks — no auto-resolve on enforcement.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>From a pending group: hide the listing, then Resolve with a note → all its reports leave Pending in one action, audit rows for both steps; Dismiss path works symmetrically.</done>
</task>

</tasks>

<verification>
- File two reports from different accounts on the same listing (Phase 8/9 report UI) → one queue entry, counter = 2.
- Resolve updates both rows; group moves to Resolved tab with note visible.
- Message-report group links into the audited content view.
</verification>

<success_criteria>
- ADMO-03 complete: grouped, filterable queue with notes and one-action group resolution, fully wired to the enforcement ladder.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-08-SUMMARY.md`
</output>
