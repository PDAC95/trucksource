---
phase: 05.1-stakeholder-trust-lifecycle-inserted
plan: 05
type: execute
wave: 3
depends_on: ["5.1-01", "5.1-03"]
files_modified:
  - supabase/migrations/0011_expire_listings_cron.sql
  - app/api/cron/near-expiry/route.ts
  - lib/notifications/queries.ts
  - lib/supabase/admin.ts
  - vercel.json
  - .env.example
  - tests/unit/near-expiry-cron.test.ts
autonomous: true
requirements: [LIST-09]
user_setup:
  - service: vercel
    why: "The near-expiry notify job runs as a Vercel Cron hitting a secret-guarded route handler. The CRON_SECRET env var must be set in Vercel project settings (Claude cannot set Vercel dashboard secrets)."
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random secret; set it in Vercel → Project → Settings → Environment Variables (all environments), and the route handler checks Authorization: Bearer ${CRON_SECRET}"
  - service: supabase
    why: "The daily active→expired flip is a pg_cron job; the extension must be enabled (also flagged in 5.1-01) before cron.schedule runs."
    dashboard_config:
      - task: "Confirm pg_cron is enabled, then the 0011 migration schedules the job"
        location: "Supabase Dashboard → Database → Extensions → pg_cron (enabled)"

must_haves:
  truths:
    - "A daily DB-side job flips ONLY active-past-expiry listings to expired; sold and active-future are untouched"
    - "A daily Vercel Cron hits a secret-guarded route handler that emails + writes an in-app notification ~7 days before expiry"
    - "An unauthenticated call to /api/cron/near-expiry is rejected"
    - "A seller can read their own near-expiry notifications; another user cannot"
    - "Each near-expiry listing is notified once (not re-spammed daily)"
  artifacts:
    - path: "supabase/migrations/0011_expire_listings_cron.sql"
      provides: "pg_cron daily flip job (active→expired) scheduled in the cron schema"
      contains: "cron.schedule"
    - path: "app/api/cron/near-expiry/route.ts"
      provides: "Secret-guarded Vercel Cron target: Resend email + in-app notification insert"
      min_lines: 40
    - path: "lib/notifications/queries.ts"
      provides: "Owner-scoped listMyNotifications / unread count"
      exports: ["listMyNotifications"]
  key_links:
    - from: "app/api/cron/near-expiry/route.ts"
      to: "notifications table (service-role insert)"
      via: "admin client write + Resend email"
      pattern: "notifications|resend|Resend"
    - from: "app/api/cron/near-expiry/route.ts"
      to: "CRON_SECRET guard"
      via: "Authorization: Bearer check"
      pattern: "CRON_SECRET|Authorization"
---

<objective>
Complete LIST-09's automation: the DB-side daily expiry flip and the ~7-day near-expiry email + in-app notification, plus the owner-scoped notification read surface.

Purpose: Scheduling is split (research Open Q3 recommendation): the pure-SQL active→expired flip runs in-DB via pg_cron (runs even if the app is down), while the notify step (Resend email + in-app `notifications` row) runs app-side via a secret-guarded Vercel Cron route handler where the Resend client and app code live. The route handler is the FIRST use of the service-role admin client in this phase — it writes notifications on the system's behalf (the table has no authenticated INSERT policy by design), and it MUST be guarded so it is not publicly triggerable (Pitfall 6).

Output: migration 0011 scheduling the flip, the secret-guarded `/api/cron/near-expiry` route, the admin client (if not already present), the owner-scoped notifications reader (feeding the My Listings "N expiring" surface as a durable record), `vercel.json` cron config, `.env.example` for CRON_SECRET, and a route-guard unit test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-stakeholder-trust-lifecycle-inserted/05.1-CONTEXT.md
@.planning/phases/05.1-stakeholder-trust-lifecycle-inserted/05.1-RESEARCH.md
@.planning/research/PITFALLS.md

# Phase-2 abuse-alert precedent used the service-role admin client + Resend — mirror that posture
@lib/actions/listings.ts
@lib/listings/lifecycle.ts

<interfaces>
<!-- Contracts from 5.1-01 (schema) + 5.1-03 (lifecycle helpers). -->

notifications table (0010, 5.1-01): id, user_id (fk auth.users cascade), kind text,
listing_id (fk listings cascade), body text, read_at, created_at. RLS: owner SELECT +
owner UPDATE only — NO authenticated INSERT policy → inserts come from the SERVICE-ROLE
admin client (the cron route handler), per invariant #3 (service-role server-only).

pg_cron flip (5.1-RESEARCH Example 4) — the EXACT SQL to schedule:
```sql
select cron.schedule('expire-listings', '5 0 * * *',
  $$ update public.listings set status = 'expired'
       where status = 'active' and expires_at <= now(); $$);
```
sold is never touched (terminal); only active-past-expiry flips (Pitfall 3, gate c).

lib/listings/lifecycle.ts (5.1-03): daysUntil(expiresAt, now), isExpiringSoon(status, expiresAt, now).
The route handler reuses these to pick rows within the ~7-day window.

Service-role admin client — CLAUDE.md invariant #3: name SUPABASE_SERVICE_ROLE_KEY (never
NEXT_PUBLIC_), live in ONE `import "server-only"` module lib/supabase/admin.ts, used only by
admin/trusted route handlers. Phase 2's abuse-event store already established this pattern —
reuse lib/supabase/admin.ts if it exists; create it (supabase-js directly, server-only) if not.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pg_cron daily flip migration + admin client</name>
  <files>supabase/migrations/0011_expire_listings_cron.sql, lib/supabase/admin.ts</files>
  <action>
**`supabase/migrations/0011_expire_listings_cron.sql`** — schedule the daily flip with the EXACT Example-4 SQL. Make it idempotent: unschedule any existing job of the same name first (e.g. `select cron.unschedule('expire-listings') where exists (select 1 from cron.job where jobname = 'expire-listings');` guarded so re-apply doesn't error), then `cron.schedule('expire-listings', '5 0 * * *', $$ update public.listings set status='expired' where status='active' and expires_at <= now(); $$)`. The predicate flips ONLY active-past-expiry (sold + active-future untouched — Pitfall 3, gate c).

Apply NON-DESTRUCTIVELY to Staging via `supabase db query --linked -f supabase/migrations/0011_expire_listings_cron.sql` (NOT db reset). REQUIRES pg_cron enabled (user_setup). Verify: `select jobname, schedule, command from cron.job where jobname='expire-listings'` returns the row. Optionally run the inner UPDATE manually once to confirm it only touches active-past-expiry rows.

**`lib/supabase/admin.ts`** — if it already exists (Phase 2 used a service-role client for abuse events), reuse it as-is and skip creation. If NOT present, create it per invariant #3: `import "server-only"` at top, a `createClient` from `@supabase/supabase-js` (NOT @supabase/ssr — no cookie can override the auth header) using `process.env.SUPABASE_SERVICE_ROLE_KEY` (server-only, never NEXT_PUBLIC_). Export a single `createAdminClient()`/`adminClient`. This client bypasses RLS — used ONLY by the cron route handler to insert notifications (the table has no authenticated insert policy).
  </action>
  <verify>
  <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && supabase db query --linked -f supabase/migrations/0011_expire_listings_cron.sql 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <done>0011 idempotently schedules 'expire-listings' (00:05 UTC daily, active-only flip) and is verified in cron.job on Staging; lib/supabase/admin.ts exists as a server-only service-role client (reused or created); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Secret-guarded near-expiry route handler + notifications reader</name>
  <files>app/api/cron/near-expiry/route.ts, lib/notifications/queries.ts</files>
  <action>
**`app/api/cron/near-expiry/route.ts`** — a `GET` (Vercel Cron uses GET) route handler:
1. GUARD FIRST (Pitfall 6): reject unless `request.headers.get("authorization") === \`Bearer ${process.env.CRON_SECRET}\`` → return 401 otherwise. Never run the job for an unauthenticated caller. Mark the route server-only/dynamic.
2. Using the SERVICE-ROLE admin client: select active listings whose `expires_at` falls in the ~7-day window AND that have NOT already been notified for this expiry cycle. To avoid daily re-spam (CONTEXT: single reminder at ~7 days), only notify rows lacking an existing `notifications` row of `kind='listing_expiring'` for that `listing_id` since its current `expires_at` was set — implement the "notify once" check by querying existing notifications for the candidate listing_ids and filtering them out (a left-anti-join in SQL, or fetch existing then diff in JS). Document the chosen approach.
3. For each remaining listing: send a Resend email to the seller (subject/body: "Your listing '{title}' expires in ~7 days — renew in one click", linking to /sell/listings or /sell/{id}/edit; the email NOTIFIES, the renew action happens in-app — NOT a one-click link in the email, CONTEXT lock). Use the existing Resend path/credentials (Resend SMTP/API already wired on Staging). Email send is best-effort (a failure logs but does not abort the batch).
4. Insert a `notifications` row (`kind='listing_expiring'`, `listing_id`, `user_id = seller_id`, `body`) via the admin client (the durable in-app record — the My Listings "N expiring" surface from 5.1-03 derives from expires_at directly, and this row is the inbox-grade durable record for Phase 9/10).
5. Return a small JSON summary `{ notified: N }`.

Resolving seller emails: the seller's email lives in `profiles_private` — the admin client (service-role) MAY read it for the system-initiated email (this is NOT a public surface; it's the one legitimate server-only PII read, like Phase 2's abuse alerts). Read it via the admin client only, never expose it in any response.

**`lib/notifications/queries.ts`** — `listMyNotifications(): Promise<Notification[]>` and an unread-count helper, owner-scoped via the COOKIE client (getClaims sub; the owner SELECT policy scopes rows). Plain async lib (no service-role here — reads are owner-scoped). Returns [] when unauthenticated. This is the in-app read surface (extensible for Phase 9/10).
  </action>
  <verify>
  <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/api/cron/near-expiry rejects non-Bearer-CRON_SECRET calls; on auth, it notifies once per near-expiry active listing (email via Resend + a kind='listing_expiring' notifications row via the service-role admin client, reading seller email from profiles_private server-only); listMyNotifications is owner-scoped via the cookie client; tsc + build green.</done>
</task>

<task type="auto">
  <name>Task 3: Vercel cron config + env docs + route-guard test</name>
  <files>vercel.json, .env.example, tests/unit/near-expiry-cron.test.ts</files>
  <action>
**`vercel.json`** — add (or extend) a `crons` array entry pointing at `/api/cron/near-expiry` on a daily schedule (e.g. `"schedule": "0 1 * * *"` — after the 00:05 UTC DB flip so emails reflect freshly-flipped state; the near-expiry window is independent of the flip but ordering avoids edge confusion). If vercel.json already exists, merge — do not clobber existing config.

**`.env.example`** — add `CRON_SECRET=` with a comment (server-only secret; set in Vercel env; the route checks `Authorization: Bearer ${CRON_SECRET}`). Confirm `SUPABASE_SERVICE_ROLE_KEY` is already documented (it should be from Phase 1/2) — add it if missing, marked server-only / never NEXT_PUBLIC_.

**`tests/unit/near-expiry-cron.test.ts`** — unit-test the route's guard + selection logic (mock the admin client + Resend; jsdom/node as the existing route tests use):
- A request WITHOUT the correct `Authorization: Bearer ${CRON_SECRET}` returns 401 and performs NO email/insert (the critical Pitfall-6 guard).
- A request WITH the correct secret, given a mocked candidate set, inserts a notification + attempts an email only for near-expiry active rows (assert sold / active-future / already-notified rows are excluded — the "notify once" + correct-window logic).
- A Resend failure does not abort the batch (best-effort email) — the notification row still inserts.
  </action>
  <verify>
  <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npx vitest run tests/unit/near-expiry-cron.test.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>vercel.json schedules the daily near-expiry cron; .env.example documents CRON_SECRET (+ service-role key); the unit test proves the 401 guard (no work on bad auth), the near-expiry-only/notify-once selection, and best-effort email; tsc + build + test green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run build` green; `npx vitest run` full suite green.
- Staging: `cron.job` contains 'expire-listings' (daily active-only flip). Manually running the inner UPDATE touches only active-past-expiry rows (gate c).
- The route-guard unit test proves /api/cron/near-expiry is not publicly triggerable (Pitfall 6) and notifies once per near-expiry active listing.
- Privacy cross-cutting gate: the seller email read happens ONLY through the server-only service-role admin client (never a public surface, never returned in a response); the notifications table is owner-read; no PII reaches any client.
- Service-role posture: SUPABASE_SERVICE_ROLE_KEY stays server-only (lib/supabase/admin.ts, `import "server-only"`); CRON_SECRET stays server-only.
</verification>

<success_criteria>
- LIST-09 automation complete: listings flip active→expired daily in-DB (sold/active-future untouched); sellers receive a single ~7-day near-expiry email + in-app notification; the renew/reactivate action (5.1-03) happens in-app, auth-safe; the cron endpoint is secret-guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-stakeholder-trust-lifecycle-inserted/05.1-05-SUMMARY.md`
</output>
