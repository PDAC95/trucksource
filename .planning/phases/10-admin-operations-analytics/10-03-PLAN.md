---
phase: 10-admin-operations-analytics
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/admin/users/page.tsx
  - app/admin/users/[id]/page.tsx
  - components/admin/enforcement-dialogs.tsx
  - lib/admin/queries.ts
  - lib/actions/admin/enforcement.ts
  - lib/admin/email.ts
  - lib/account/restrictions.ts
  - app/(app)/layout.tsx
  - app/(app)/suspended/page.tsx
  - components/messaging/* (read-only composer state)
autonomous: true
requirements: [ADMO-01]
must_haves:
  truths:
    - "Admin can search users by username/email, open a detail view showing PII + restriction state"
    - "Admin can warn, suspend (24h/7d/30d presets), ban, reactivate, and rename a user's username — each action audit-logged and emailed"
    - "Suspending hides the user's listings (reason 'suspension'); ban hides permanently (reason 'ban'); reactivation restores only suspension-hidden listings"
    - "A suspended user logs in and sees the blocked page with date + reason everywhere in the app; chats are read-only"
    - "An expired suspension lazily self-clears: user regains access and suspension-hidden listings are restored without cron"
  artifacts:
    - path: "lib/actions/admin/enforcement.ts"
      provides: "warnUser, suspendUser, banUser, reactivateUser, renameUsername server actions"
      exports: ["suspendUser", "banUser", "reactivateUser", "warnUser", "renameUsername"]
    - path: "lib/account/restrictions.ts"
      provides: "getOwnRestriction() with lazy-expiry sweep"
    - path: "app/(app)/suspended/page.tsx"
      provides: "Account suspended blocked page"
    - path: "lib/admin/queries.ts"
      provides: "getAdminUsers list (no PII) + getAdminUserDetailWithPII"
  key_links:
    - from: "lib/actions/admin/enforcement.ts"
      to: "user_restrictions + listings.hidden_at"
      via: "service-role upsert + listing hide/restore updates"
      pattern: "hidden_reason.*suspension"
    - from: "app/(app)/layout.tsx"
      to: "user_restrictions"
      via: "self-RLS read of own restriction row"
      pattern: "user_restrictions"
    - from: "lib/actions/admin/enforcement.ts"
      to: "lib/admin/audit.ts"
      via: "logAdminAction on every action"
      pattern: "logAdminAction"
---

<objective>
ADMO-01 end to end: admin user management (list, PII detail, full enforcement ladder with audit + email) AND the user-side suspension experience (blocked page, lazy expiry, listing hide/restore, read-only chat).

Purpose: enforcement is the trust spine of the ops console; both sides (admin action, user effect) must ship together to be verifiable.
Output: /admin/users surfaces + enforcement actions + suspension UX.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@lib/admin/auth.ts
@lib/admin/audit.ts
@lib/supabase/admin.ts
@lib/messaging/notify.ts
@lib/actions/account.ts
@app/(app)/layout.tsx

<interfaces>
From 10-02: `requireAdmin(): Promise<{ adminId: string }>` (lib/admin/auth.ts); `logAdminAction({ adminId, action, targetType, targetId, reason?, metadata? })` (lib/admin/audit.ts).
From 10-01 schema: `user_restrictions(user_id pk, state 'suspended'|'banned', reason, suspended_until, created_by, created_at)` — self-select RLS only; `listings.hidden_at/hidden_reason('moderation'|'suspension'|'ban')`; restricted users blocked from messages INSERT at RLS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin users list + PII detail page</name>
  <files>app/admin/users/page.tsx, app/admin/users/[id]/page.tsx, lib/admin/queries.ts</files>
  <action>
Create `lib/admin/queries.ts` (`server-only`) with:
- `getAdminUsers({ q?, page? })`: service-role read of profiles_public (id, username, location fields, member_since) LEFT-joined (separate query is fine) to user_restrictions state; when `q` provided, match username ilike OR email via profiles_private lookup (email search resolves ids first, then fetches public rows). List view shows NO PII columns (Pitfall 7).
- `getAdminUserDetailWithPII(id)`: the ONLY helper that joins profiles_private (first/last name, email, phone) + verification status + restriction row + counts (listings, reports against them). Name it exactly this so reuse on a public surface is self-evidently wrong.

`app/admin/users/page.tsx` (force-dynamic): search input (GET form, ?q=), table (username, location, member since, restriction badge: Active/Suspended until X/Banned), row links to detail. `app/admin/users/[id]/page.tsx` (force-dynamic): PII card, restriction state card, recent listings, enforcement action buttons (wired in Task 2). Both pages call requireAdmin() implicitly via layout BUT the queries are safe regardless; pages themselves need no extra gate (layout) — actions DO (Task 2).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Admin sees searchable user list and a detail view with PII + restriction state; PII appears nowhere in the list query.</done>
</task>

<task type="auto">
  <name>Task 2: Enforcement server actions + email notification</name>
  <files>lib/actions/admin/enforcement.ts, lib/admin/email.ts, components/admin/enforcement-dialogs.tsx</files>
  <action>
`lib/admin/email.ts` (`server-only`): `sendEnforcementEmail(userId, { subject, action, reason, until? })` — clone the lib/messaging/notify.ts posture exactly: raw fetch to api.resend.com, escapeHtml on reason, try/catch swallow (audit row is the record; email is best-effort — Research Pattern 9). Recipient email read from profiles_private via service role. Note in a comment the Staging Resend delivery restriction (only delivers to the account address until domain verification) so UAT doesn't flag it.

`lib/actions/admin/enforcement.ts` ("use server"): every action starts with `const { adminId } = await requireAdmin()` (gate IN the action — anti-pattern guard), validates input with zod, uses createAdminClient(), calls logAdminAction, then sendEnforcementEmail, then revalidatePath:
- `warnUser({ userId, reason })`: no state change; audit 'warn_user' + email with reason.
- `suspendUser({ userId, duration: '24h'|'7d'|'30d', reason })`: upsert user_restrictions (state suspended, suspended_until from preset), hide listings (`set hidden_at=now(), hidden_reason='suspension' where seller_id=$1 and hidden_at is null`), audit 'user_suspend', email with until+reason. (Research code example.)
- `banUser({ userId, reason })`: upsert state banned (suspended_until null), hide listings with reason 'ban' (including overwriting any 'suspension' reason rows: `where seller_id=$1 and (hidden_at is null or hidden_reason='suspension')`), audit 'user_ban', email.
- `reactivateUser({ userId })`: delete restriction row, restore ONLY `hidden_reason='suspension'` OR (for un-ban) `hidden_reason='ban'` listings — restore the reason that matches the prior state; never touch 'moderation' rows; audit 'user_reactivate', email.
- `renameUsername({ userId, newUsername, reason })`: validate against the existing username rules (reuse lib/username or lib/validation schema), service-role update profiles_public.username, audit 'username_rename', email.

`components/admin/enforcement-dialogs.tsx` (client): dialogs using existing ui components — suspend dialog with the three preset radio options + required reason textarea; ban/warn/reactivate/rename dialogs with reason fields; calls the actions, sonner toasts on result. Mount on the user detail page. All copy in English.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>All five actions work against Staging; each writes an admin_audit_log row; suspended user's listings get hidden_at/hidden_reason='suspension'; emails attempted best-effort.</done>
</task>

<task type="auto">
  <name>Task 3: Suspension UX — blocked page, lazy expiry, read-only chat</name>
  <files>lib/account/restrictions.ts, app/(app)/layout.tsx, app/(app)/suspended/page.tsx, components/messaging (composer)</files>
  <action>
`lib/account/restrictions.ts` (`server-only`): `getOwnRestriction()` — normal (non-service-role) server client reads own user_restrictions row via self-RLS. Lazy semantics (Pitfall 3): restricted iff `state='banned'` OR (`state='suspended'` AND `suspended_until > now()`). If the row exists but is EXPIRED, run the sweep via createAdminClient(): delete the row and restore `hidden_reason='suspension'` listings for that user, then return null. (Service-role import here is acceptable — it is a trusted server module; keep it `server-only` and document why.)

`app/(app)/layout.tsx`: after the existing auth resolution, call getOwnRestriction(); if restricted, render the blocked page content INSTEAD of children (or redirect to /suspended — choose render-in-layout so deep links can't bypass; keep /suspended as the canonical page component rendered there). Blocked page copy (English): "Account suspended until {date}" or "Account banned" + "Reason: {reason}" + signed-out-style minimal chrome + logout button. EXCEPTION for suspended (not banned) users: allow READ access to /messages routes per the locked decision (read-only chats) — gate everything else.

Read-only chat: in the thread view/composer components (app/(app)/messages...), accept a `readOnly` reason prop; when the viewer is restricted, replace the composer with "You can't send messages while your account is suspended." Sending is already structurally blocked by the 0019 messages INSERT policy — this is UX. Also surface the frozen-thread state from message_threads.frozen_at: when frozen, BOTH participants see "This conversation has been closed by moderation." instead of the composer (the freeze action ships in 10-07; render off the column now).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Suspended Staging test user sees the blocked page app-wide, can read but not send messages; after suspended_until passes, next visit auto-clears the restriction and restores listings.</done>
</task>

</tasks>

<verification>
- Suspend buyer test account (PatricioBuyer) for 24h: blocked page appears, listings hidden from anon search/detail, composer read-only, counterpart sees thread normally.
- Reactivate: listings visible again, app usable. Audit log has user_suspend + user_reactivate rows.
- Rename: profiles_public.username changes; old name gone from public surfaces.
</verification>

<success_criteria>
- ADMO-01 complete: view/manage users with the full locked enforcement ladder, audited and emailed, with structural + UX suspension semantics and cron-free expiry.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-03-SUMMARY.md`
</output>
