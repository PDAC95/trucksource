---
phase: 10-admin-operations-analytics
plan: 10
type: execute
wave: 4
depends_on: ["10-03", "10-04", "10-05", "10-06", "10-07", "10-08", "10-09"]
files_modified:
  - .planning/phases/10-admin-operations-analytics/10-10-SUMMARY.md
autonomous: false
requirements: [ADMO-01, ADMO-02, ADMO-03, ADMO-04, ADMO-05, ADMO-06, ADMA-01, ADMA-02, ADMA-03, ADMA-04]
must_haves:
  truths:
    - "All three phase success criteria verified live on Staging by the stakeholder"
    - "Privacy/RLS cross-cutting gate re-verified: service-role key absent from client bundles, new tables default-deny, no PII on public surfaces"
    - "Phase 9 realtime regression still passes after the messages INSERT policy change (WALRUS pitfall)"
  artifacts:
    - path: ".planning/phases/10-admin-operations-analytics/10-10-SUMMARY.md"
      provides: "UAT record + phase closure"
  key_links:
    - from: "UAT script"
      to: "ROADMAP Phase 10 success criteria 1-3"
      via: "step-by-step walkthrough"
      pattern: "success criteria"
---

<objective>
End-to-end UAT of the whole admin console + analytics against the three ROADMAP success criteria, plus re-verification of the cross-cutting privacy/RLS and EXIF gates and the Phase 9 realtime regression.

Purpose: Phase 10 closes v1 — this checkpoint is the milestone's last gate.
Output: approved UAT + phase closure.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-admin-operations-analytics/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gate sweep before human UAT</name>
  <files>(no new files — runs existing suites)</files>
  <action>
Run and fix-forward until green:
1. `npm run typecheck && npm run lint && npm run build`.
2. `bash -c '! grep -r "SUPABASE_SERVICE_ROLE_KEY" .next/static'` (cross-cutting gate).
3. `npm test -- --run` (includes the 10-04 hidden/draft regression, 10-09 schema + EXIF tests, existing no-GPS suite).
4. Integration tests against Staging if the suite separates them (check tests/integration conventions).
5. Confirm every app/admin route is dynamic (λ) in build output; none static.
6. Grep audit coverage: every exported function in lib/actions/admin/* calls requireAdmin AND logAdminAction (`grep -L` both patterns across the files; fix any miss).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test -- --run && npm run build && bash -c '! grep -r "SUPABASE_SERVICE_ROLE_KEY" .next/static'</automated>
  </verify>
  <done>All suites green; gates pass; no ungated/unaudited admin action.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Stakeholder UAT — full console walkthrough</name>
  <action>Present the walkthrough below to the user, support them through it (grant the admin flag, prepare the sample CSV with mixed valid/invalid rows, seed any missing test data), and pause for their verdict. Fix-forward small issues immediately; record larger ones as diagnosed gaps.</action>
  <what-built>The complete admin console (users, listings, reports, messages, fitment library, CSV import) and the analytics dashboard, on Staging.</what-built>
  <how-to-verify>
With your admin-flagged account (run `npm run grant:admin -- your@email`, then sign out/in) and the PatricioBuyer test account in a second browser:

A. Access: 1) /admin as anon and as PatricioBuyer → 404. 2) As admin → console with sidebar: Dashboard, Users, Listings, Reports, Messages, Categories, Fitment Library.
B. Users + enforcement (criterion 1+2): 3) Search PatricioBuyer, open detail — PII visible, restriction Active. 4) Warn (reason) → audit logged. 5) Suspend 24h → buyer's browser shows the blocked page with date+reason; buyer's listings vanish from public search/detail; buyer can READ messages but the composer is read-only. 6) Reactivate → everything restored. 7) Rename the username → new name everywhere public.
C. Listings (criterion 1): 8) Hide a listing with reason → gone from search + direct URL as anon; restore → back. 9) Remove one photo from a listing → photo gone from page and Storage.
D. Reports queue (criterion 2): 10) From the buyer account, report the same listing twice (or from two accounts) → ONE queue entry, counter ≥2. 11) From the entry: hide the listing, then Resolve with a note → group leaves Pending. 12) Report a chat message → queue entry; "View thread content" works and shows the "access logged" banner; an unreported thread's content page refuses.
E. Messages (criterion 2): 13) Threads tab shows metadata only. 14) Freeze a thread → neither side can send, both see the moderation notice; unfreeze restores. 15) Contact logs filter by buyer/seller/date.
F. Fitment library (criterion 1): 16) Add a slang term mapped to a model → search finds listings via the new term. 17) Deactivate a material → gone from the create-listing form, existing listings unaffected. 18) Try deleting an in-use condition → "in use — deactivate instead".
G. Analytics (criterion 3): 19) Dashboard shows registered users, active users, active listings, messages sent, growth %; rankings for most-viewed listings and most-searched makes/models. 20) Switch 7d/30d/90d/All → numbers change sensibly.
H. CSV import: 21) Import the sample CSV (valid rows + one bad seller + one bad photo URL) → drafts created for valid rows, failures reported with reasons. 22) Bulk-publish the drafts → live in search; Contact Seller on an imported listing reaches the real seller. 23) Spot-check an imported photo has no GPS EXIF.
I. Regression: 24) Two-browser realtime chat still delivers instantly both ways (Phase 9 script).

Note: enforcement emails on Staging only deliver to the Resend account address (domain not verified yet) — check the audit log row instead of the inbox for other users.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 10 (and v1), or describe any failures</resume-signal>
  <verify>User responds "approved" (or all reported issues are fixed and re-verified)</verify>
  <done>All 24 UAT steps confirmed by the stakeholder; phase success criteria 1-3 observed true on Staging.</done>
</task>

</tasks>

<verification>
- All 24 UAT steps pass or have diagnosed gap entries.
- ROADMAP success criteria 1-3 each demonstrably true on Staging.
</verification>

<success_criteria>
- Stakeholder approval recorded; Phase 10 closed; REQUIREMENTS.md ADMO-01..06 + ADMA-01..04 markable complete.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-10-SUMMARY.md`
</output>
