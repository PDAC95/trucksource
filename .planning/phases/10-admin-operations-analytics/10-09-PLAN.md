---
phase: 10-admin-operations-analytics
plan: 09
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - app/admin/import/page.tsx
  - app/api/admin/import/route.ts
  - lib/admin/import.ts
  - components/admin/import-form.tsx
  - tests/unit (import row schema + EXIF gate test)
  - package.json
autonomous: true
requirements: [ADMO-02]
must_haves:
  truths:
    - "Admin uploads a CSV; valid rows import as DRAFT listings owned by the real seller referenced per row (username or email)"
    - "Failed rows are reported with row number + reason at the end; one bad row never aborts the file"
    - "Every imported photo URL is fetched server-side and passes through stripAndReencode() — zero EXIF/GPS survives (same gate as regular uploads)"
    - "Each import writes a 'csv_import' audit row with imported/failed counts"
    - "Imported drafts are publishable via the 10-04 bulk-publish surface"
  artifacts:
    - path: "lib/admin/import.ts"
      provides: "zod row schema, seller/taxonomy resolution, photo fetch + EXIF gate, per-row import"
    - path: "app/api/admin/import/route.ts"
      provides: "POST route handler with maxDuration, returns per-row results"
    - path: "app/admin/import/page.tsx"
      provides: "upload UI + column reference + results report"
  key_links:
    - from: "lib/admin/import.ts"
      to: "lib/images/strip.ts"
      via: "stripAndReencode() on every fetched photo (never a parallel sharp pipeline)"
      pattern: "stripAndReencode"
    - from: "app/api/admin/import/route.ts"
      to: "requireAdmin()"
      via: "gate inside the route handler"
      pattern: "requireAdmin"
    - from: "imported listings"
      to: "status='draft'"
      via: "insert with draft status, seller_id = resolved real account"
      pattern: "draft"
---

<objective>
CSV bulk import — the cold-start onboarding tool: Papa Parse + zod per-row validation, real-seller ownership, URL-fetched photos through the existing EXIF gate, drafts + results report.

Purpose: seeds the marketplace with native-looking inventory whose contact→chat flow works unchanged (stakeholder-locked).
Output: /admin/import flow producing draft listings + failure report.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@lib/images/strip.ts
@lib/actions/listings.ts
@lib/admin/auth.ts
@lib/admin/audit.ts
@supabase/migrations/0006_listings.sql

<interfaces>
From 10-01: `listings.status` accepts 'draft'.
From 10-02: `requireAdmin()`, `logAdminAction('csv_import')`.
From 10-04: /admin/listings?status=draft bulk-publish surface (link the results page there).
From 10-05 (if landed): taxonomy resolution should prefer is_active values; if is_active filtering helpers exist in lib/fitment, reuse them.
Existing photo pipeline: read lib/actions/listings.ts + lib/images/strip.ts for the bucket name, path convention (seller uid prefix), and stripAndReencode signature — reuse exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import library — row schema, resolution, photo gate</name>
  <files>lib/admin/import.ts, package.json, tests/unit (schema test)</files>
  <action>
`npm install papaparse && npm install -D @types/papaparse`.

`lib/admin/import.ts` (`server-only`):
- Zod row schema per Research Pattern 11 columns: seller (username or email), title, part_number?, asking_price (positive number), condition (name), shipping_option (must map to the existing 3-option enum — read the listing schema), damage_notes?, is_barnyard? (truthy parse), fitments? ("Make Model" entries `;`-separated, optional `:config` suffix), categories? (names `;`-separated), photo_url_1..photo_url_8 (https URLs). Match real column/table shapes from lib/actions/listings.ts and migrations — do not invent fields.
- `resolveSeller(ref)`: profiles_public.username (citext eq) else profiles_private.email via service role; error "seller not found" otherwise.
- `resolveTaxonomy(row)`: names → ids (condition, categories, model/config fitment or barnyard), only is_active values; clear per-field error strings.
- `importPhoto(url)`: https-only, 10MB cap, 10s AbortSignal timeout, fetch → `stripAndReencode(bytes, contentType)` → reject on failure (Research code example). Upload to the listing-photos bucket under the SELLER's uid path per the existing convention.
- `importRow(row, adminId)`: full per-row try/catch returning `{ row, ok, listingId?, error? }`; insert listing with status='draft' + child rows (fitment/categories/photos) only after all photos processed; enforce the listing minimums the publish gate expects where applicable to drafts (drafts may be photo-light; publish-time validation stays the gate).
Unit-test the zod row schema (valid row, bad price, bad URL, missing seller) and extend the EXIF regression: feed a GPS-laden fixture through importPhoto's strip path (reuse the existing no-GPS test fixtures/exifr assertion — cross-cutting EXIF gate).
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>Schema + resolution + photo gate covered by unit tests; GPS fixture comes out clean through the import path.</done>
</task>

<task type="auto">
  <name>Task 2: Import route handler + UI + results report</name>
  <files>app/api/admin/import/route.ts, app/admin/import/page.tsx, components/admin/import-form.tsx</files>
  <action>
`app/api/admin/import/route.ts`: POST, `export const maxDuration = 300` (Vercel plan may cap lower — set and note; imports can be run against Staging locally if prod times out, say so in the UI). Flow: `await requireAdmin()` FIRST (route handlers are directly invocable) → read CSV file from FormData (text is tiny; photos arrive by URL — this dodges the known 4.5MB Vercel body cap) → Papa.parse with header:true, skipEmptyLines, trimmed lowercase headers → cap at 100 data rows (reject larger files with a clear message) → sequential importRow loop → `logAdminAction('csv_import', metadata: { fileName, imported, failed })` → JSON response `{ imported, failed, results: [{ row, ok, error }] }`.

`app/admin/import/page.tsx` (force-dynamic) + `components/admin/import-form.tsx` (client): file input (.csv), an expandable column-reference table documenting the exact schema (copy from the zod schema — keep them in sync), submit with progress state ("Importing — this can take a few minutes for files with many photos"), then the results report: summary line + failed-rows table (row #, seller/title context, reason) so only failures get fixed and re-uploaded (locked decision). Success panel links to /admin/listings?status=draft for review + bulk publish. English copy.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>A test CSV (mix of valid rows + a bad seller + a bad photo URL) imports the valid rows as drafts owned by the right sellers, reports the failures with reasons, and the drafts bulk-publish cleanly.</done>
</task>

</tasks>

<verification>
- Imported draft listing: photos stripped (exifr shows no GPS on the stored object), owner = referenced seller, invisible publicly until published.
- After bulk publish: listing appears in search/feed; Contact Seller flow works against the CSV-imported listing unchanged.
- Failed-row report names row numbers and reasons; re-running with only fixed rows imports cleanly.
</verification>

<success_criteria>
- Bulk-onboarding tooling complete per the locked CSV decisions: real sellers, URL photos through the EXIF gate, per-row validation, draft + review + one-click publish.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-09-SUMMARY.md`
</output>
