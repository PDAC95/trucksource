---
phase: 10-admin-operations-analytics
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/admin/listings/page.tsx
  - app/admin/listings/[id]/page.tsx
  - lib/admin/listings-queries.ts
  - lib/actions/admin/listings.ts
  - components/admin/listing-moderation.tsx
autonomous: true
requirements: [ADMO-02]
must_haves:
  truths:
    - "Admin can list/filter listings by status (draft/active/sold/expired) and hidden state, and search by title/seller"
    - "Admin can hide a listing with a reason and restore it; hidden listings vanish from all public surfaces but stay owner-visible"
    - "Admin can remove an individual rule-violating photo (DB row + Storage object), audit-logged"
    - "Admin can bulk-publish selected draft listings in one click (sets active + date_listed + expires_at)"
    - "Admin never edits seller content — no title/description/price inputs exist in the console"
  artifacts:
    - path: "lib/actions/admin/listings.ts"
      provides: "hideListing, restoreListing, removeListingPhoto, bulkPublishDrafts"
      exports: ["hideListing", "restoreListing", "removeListingPhoto", "bulkPublishDrafts"]
    - path: "app/admin/listings/page.tsx"
      provides: "filterable listing index with draft bulk-publish"
    - path: "app/admin/listings/[id]/page.tsx"
      provides: "listing detail with moderation controls + photo grid"
  key_links:
    - from: "lib/actions/admin/listings.ts"
      to: "listings.hidden_at/hidden_reason"
      via: "service-role update with hidden_reason='moderation'"
      pattern: "hidden_reason.*moderation"
    - from: "removeListingPhoto"
      to: "Storage listing-photos bucket"
      via: "storage.from(...).remove + listing_photos delete"
      pattern: "listing_photos"
    - from: "bulkPublishDrafts"
      to: "listings lifecycle"
      via: "status='active', date_listed, expires_at = now()+90 days"
      pattern: "expires_at"
---

<objective>
ADMO-02: admin listing moderation — filterable index, hide/restore with structural public exclusion, individual photo removal, and the draft bulk-publish that completes the CSV-import flow.

Purpose: moderation must be structural (RLS from 10-01) with the console as the actuator; bulk-publish is the second half of the locked CSV decision.
Output: /admin/listings index + detail with moderation actions.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@lib/admin/auth.ts
@lib/admin/audit.ts
@lib/supabase/admin.ts
@supabase/migrations/0006_listings.sql
@supabase/migrations/0010_stakeholder_lifecycle.sql
@lib/listings

<interfaces>
From 10-02: `requireAdmin()`, `logAdminAction()`.
From 10-01 schema: `listings.status in ('draft','active','sold','expired')`, `listings.hidden_at timestamptz`, `listings.hidden_reason in ('moderation','suspension','ban')`; public-read RLS already excludes hidden/draft.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Listing index with filters + bulk-publish UI</name>
  <files>app/admin/listings/page.tsx, lib/admin/listings-queries.ts</files>
  <action>
`lib/admin/listings-queries.ts` (`server-only`): `getAdminListings({ status?, hidden?, q?, page? })` — service-role read joining seller username (separate profiles_public fetch keyed by seller_id, matching the existing getListing pattern that works around the auth.users FK), cover photo, status, hidden_at/hidden_reason, created/expires dates. Filter params from searchParams. `getAdminListingDetail(id)` for the detail page: full listing + photos + fitment + seller + report count.

`app/admin/listings/page.tsx` (force-dynamic): filter bar (status select incl. Draft, hidden toggle, text search on title via ilike — admin volume, no FTS needed), table with thumbnail/title/seller/status badge/hidden badge/dates, row link to detail. When `?status=draft`: render checkboxes per row + "Publish selected" button submitting to bulkPublishDrafts (Task 2) — this is the locked one-click bulk publish surface. English copy throughout.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Admin can browse/filter all listings including drafts and hidden ones; draft view shows bulk-publish selection.</done>
</task>

<task type="auto">
  <name>Task 2: Moderation actions — hide/restore, photo removal, bulk publish</name>
  <files>lib/actions/admin/listings.ts, app/admin/listings/[id]/page.tsx, components/admin/listing-moderation.tsx</files>
  <action>
`lib/actions/admin/listings.ts` ("use server"), each action: requireAdmin() first, zod input, createAdminClient(), logAdminAction, revalidatePath('/admin/listings'):
- `hideListing({ listingId, reason })`: `update listings set hidden_at=now(), hidden_reason='moderation'` (only if hidden_at is null); audit 'listing_hide' with reason. Optionally email seller via the enforcement email helper if 10-03 has landed (import from lib/admin/email.ts; if not yet present, audit-only and leave a TODO wired in 10-08's queue actions — check the file's existence at execution time, the wave runs in parallel: import defensively by making email a soft dependency).
- `restoreListing({ listingId })`: clear hidden_at/hidden_reason ONLY where hidden_reason='moderation' (never undo suspension/ban hides from here); audit 'listing_restore'.
- `removeListingPhoto({ photoId, reason })`: fetch the listing_photos row (path + listing_id), delete the Storage object from the listing-photos bucket via service role, delete the row, audit 'photo_remove' with { listingId, path } metadata. Do NOT auto-unpublish if photo count drops below 3 (publish-time gate only — Research Pattern 4 note). If the removed photo was the cover (position/first), promote the next photo per the existing ordering convention (read lib/listings to match).
- `bulkPublishDrafts({ listingIds })`: `update listings set status='active', date_listed=now(), expires_at=now()+interval '90 days' where id=any($1) and status='draft'` (one statement; must set expires_at — 0010 lifecycle); audit one 'bulk_publish' row with { count, listingIds } metadata.

`app/admin/listings/[id]/page.tsx` (force-dynamic): listing detail — read-only seller content (title/description/price are TEXT, no inputs — locked: moderate only), status + hidden state, fitment summary, photo grid where each photo has a "Remove photo" button (confirm dialog with reason), Hide/Restore button with reason dialog. `components/admin/listing-moderation.tsx` (client) hosts the dialogs + sonner toasts.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Hide makes a listing 404/absent on public detail, search, profile counts (RLS); restore brings it back; photo removal deletes row+object and is audited; bulk publish activates drafts with expiry set.</done>
</task>

<task type="auto">
  <name>Task 3: Public-surface regression check for hidden/draft listings</name>
  <files>tests/integration (new admin moderation spec)</files>
  <action>
Add an integration test (matching the existing tests/integration conventions and Staging env usage — read an existing spec first) covering Pitfall 2 (RPC vs RLS drift): create (or fixture) a listing, set hidden_at via service role, then assert as anon client: (a) `search_listings` RPC results exclude it, (b) direct `from('listings').select().eq('id',...)` returns empty, (c) `active_listing_count` RPC for that seller does not count it, (d) owner client still sees it. Repeat status='draft' for (a)+(b). Clean up after. If fixtures are impractical, target a dedicated test listing created by the spec itself via service role.
  </action>
  <verify>
    <automated>npm test -- --run tests/integration 2>&1 | tail -5</automated>
  </verify>
  <done>Automated proof that hidden/draft exclusion holds across search RPC, direct reads, and count RPC.</done>
</task>

</tasks>

<verification>
- Hidden listing absent from: feed/search, detail URL (anon), public profile count, saved-listings reads; visible to owner with a hidden indicator if the owner UI shows status.
- admin_audit_log rows exist for every action taken during verification.
</verification>

<success_criteria>
- ADMO-02 complete: view/manage listings with moderate-only powers (hide/restore, photo removal), draft pipeline ready for CSV import.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-04-SUMMARY.md`
</output>
