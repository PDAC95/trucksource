---
phase: 10-admin-operations-analytics
plan: 07
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - app/admin/messages/page.tsx
  - app/admin/messages/threads/[id]/page.tsx
  - lib/admin/messaging-queries.ts
  - lib/actions/admin/threads.ts
  - components/admin/thread-actions.tsx
autonomous: true
requirements: [ADMO-04]
must_haves:
  truths:
    - "Admin sees threads as metadata only: participants (usernames), listing, message count, last activity — no bodies in the list"
    - "Thread content opens ONLY when a report exists on a message in that thread; otherwise a 'no report on this thread' notice renders"
    - "Every content access writes an audit row (admin, thread, justifying report) BEFORE messages render"
    - "Admin can freeze/unfreeze a thread; frozen threads accept no new messages from anyone"
    - "Contact logs are a searchable/filterable table: buyer, seller, listing, date, initial message"
  artifacts:
    - path: "lib/admin/messaging-queries.ts"
      provides: "thread metadata list, contact-log table query, getThreadContentJustification()"
      exports: ["getThreadContentJustification"]
    - path: "app/admin/messages/threads/[id]/page.tsx"
      provides: "audited content view"
    - path: "lib/actions/admin/threads.ts"
      provides: "freezeThread / unfreezeThread"
  key_links:
    - from: "app/admin/messages/threads/[id]/page.tsx"
      to: "admin_audit_log"
      via: "logAdminAction('thread_content_access') before rendering bodies"
      pattern: "thread_content_access"
    - from: "getThreadContentJustification"
      to: "reports.message_id ∈ thread"
      via: "report-on-message-in-thread check (listing reports do NOT unlock)"
      pattern: "message_id"
    - from: "freezeThread"
      to: "message_threads.frozen_at"
      via: "service-role update; INSERT policy arm enforces"
      pattern: "frozen_at"
---

<objective>
ADMO-04: message/contact-log monitoring with the locked privacy posture — metadata by default, report-justified and audited content access, and thread freeze.

Purpose: this is the privacy pillar extended into ops; the audit-the-auditor mechanic is a stakeholder-locked trust feature.
Output: /admin/messages (threads + contact logs tabs) and the audited thread content view.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@supabase/migrations/0016_messaging.sql
@lib/admin/auth.ts
@lib/admin/audit.ts
@lib/messaging

<interfaces>
From 10-01: `message_threads.frozen_at/frozen_by`; messages INSERT policy already blocks writes to frozen threads; `reports.status` queue columns.
From 10-02: `requireAdmin()`, `logAdminAction()` ('thread_freeze', 'thread_unfreeze', 'thread_content_access').
`reports` exclusive arc: exactly one of listing_id / comment_id / message_id (0016 — verify exact column names).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monitoring queries + justification function</name>
  <files>lib/admin/messaging-queries.ts</files>
  <action>
`lib/admin/messaging-queries.ts` (`server-only`), all via createAdminClient():
- `getAdminThreads({ q?, page? })`: message_threads joined to participant usernames (profiles_public), listing title, message count, last_message_at, frozen state, and whether the thread has any message-report (badge "Reported" → that's what unlocks content). NO message bodies selected anywhere in this query.
- `getAdminContactLogs({ buyer?, seller?, listing?, from?, to?, page? })`: contact_log table with username resolution, listing title, created_at, and the initial message text (this IS the admin copy of record — full text is correct here per locked decision). Filters via ilike on usernames / listing title, date range inputs; `ilike` on message_text for search (admin volume — no FTS).
- `getThreadContentJustification(threadId)`: returns the justifying report (id, reason, created_at) iff a report exists whose message_id belongs to a message in this thread; otherwise null. ONE function encodes the rule (Pitfall 8 — a listing report alone must NOT unlock chat content).
- `getThreadMessagesForAdmin(threadId)`: full message bodies + senders, called ONLY by the content page after justification + audit.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Queries return correct shapes on Staging; justification returns null for threads without message reports.</done>
</task>

<task type="auto">
  <name>Task 2: Messages page (threads + contact logs tabs) and freeze actions</name>
  <files>app/admin/messages/page.tsx, lib/actions/admin/threads.ts, components/admin/thread-actions.tsx</files>
  <action>
`app/admin/messages/page.tsx` (force-dynamic): two tabs via ?tab=threads|contacts (default threads; use shadcn tabs if installed — `npx shadcn@latest add tabs` — or simple link-tabs). Threads tab: metadata table (participants, listing, count, last activity, Frozen badge, Reported badge), row actions: "View content" (links to /admin/messages/threads/[id] — only enabled when reported; still navigable, the page itself enforces), Freeze/Unfreeze. Contacts tab: contact-log table with the filter bar (buyer, seller, listing, date range) per locked decision.

`lib/actions/admin/threads.ts` ("use server"): `freezeThread({ threadId, reason })` → requireAdmin, set frozen_at=now()/frozen_by=adminId, audit 'thread_freeze' with reason; `unfreezeThread({ threadId })` → clear both, audit 'thread_unfreeze'. revalidatePath. `components/admin/thread-actions.tsx` (client): confirm dialogs + toasts. (User-facing frozen UI ships in 10-03's composer work.)
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Threads list shows metadata only; freeze immediately blocks both participants from sending (RLS) and shows the moderation notice; contact logs filterable.</done>
</task>

<task type="auto">
  <name>Task 3: Audited thread content view</name>
  <files>app/admin/messages/threads/[id]/page.tsx</files>
  <action>
`app/admin/messages/threads/[id]/page.tsx` (force-dynamic): flow in strict order — requireAdmin (layout covers UX but call again here; this page is the sensitive one) → `getThreadContentJustification(threadId)` → if null, render the locked notice ("No report justifies access to this conversation's content.") with thread metadata only, NOT the messages → if justified, `await logAdminAction({ action: 'thread_content_access', targetType: 'thread', targetId: threadId, metadata: { report_id } })` BEFORE fetching/rendering bodies → render read-only transcript (usernames, timestamps, bodies) with a banner: "Access justified by report #{id} — this access has been logged." Include freeze/unfreeze button here too. No reply/compose capability exists on this page.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Unreported thread → notice, zero bodies fetched; reported thread → transcript renders and an admin_audit_log 'thread_content_access' row exists with the justifying report_id.</done>
</task>

</tasks>

<verification>
- Open content on a thread with a message report: audit row written first (check created_at vs page render), banner shows report id.
- Thread with only a LISTING report: content stays locked (Pitfall 8).
- Freeze: both browsers (Phase 9 two-account setup) blocked from sending; unfreeze restores.
</verification>

<success_criteria>
- ADMO-04 complete: metadata-default monitoring, report-gated + audited content access, freeze, and the contact-log table.
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-07-SUMMARY.md`
</output>
