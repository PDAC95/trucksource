---
phase: 10-admin-operations-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0019_admin_operations.sql
  - scripts/grant-admin.mjs
autonomous: true
requirements: [ADMO-01, ADMO-02, ADMO-03, ADMO-04, ADMO-05, ADMO-06]
must_haves:
  truths:
    - "A suspended/banned user state can be represented and read by the user themselves (powers the blocked page)"
    - "Hidden or draft listings are invisible to the public via RLS, while owners still see their own"
    - "A restricted user or a frozen thread structurally cannot receive new messages (INSERT policy)"
    - "Every taxonomy table can be deactivated per-value without touching existing listings"
    - "An admin can be flagged via script and the flag rides in the verified JWT"
  artifacts:
    - path: "supabase/migrations/0019_admin_operations.sql"
      provides: "user_restrictions, admin_audit_log, report queue columns, listing moderation columns + draft status, thread freeze, taxonomy is_active, analytics indexes, admin_user_activity_stats RPC, policy updates"
      contains: "create table public.user_restrictions"
    - path: "scripts/grant-admin.mjs"
      provides: "manual admin flag script writing app_metadata.role = 'admin'"
  key_links:
    - from: "listings SELECT policy"
      to: "hidden_at / status"
      via: "replaced public-read policy"
      pattern: "hidden_at is null and status <> 'draft'"
    - from: "messages INSERT policy"
      to: "user_restrictions + message_threads.frozen_at"
      via: "extended with check arms"
      pattern: "frozen_at is null"
---

<objective>
Create migration 0019 with ALL Phase-10 schema (enforcement, audit, report queue, listing moderation, thread freeze, taxonomy is_active, analytics indexes, auth-stats RPC) plus the manual grant-admin script. This is the structural foundation every other Phase-10 plan builds on.

Purpose: enforcement must be structural (RLS), not app-side filtering — invariant #2. One migration keeps the boundary reviewable.
Output: applied migration 0019 on Staging + scripts/grant-admin.mjs.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-admin-operations-analytics/10-RESEARCH.md
@.planning/phases/10-admin-operations-analytics/10-CONTEXT.md
@supabase/migrations/0006_listings.sql
@supabase/migrations/0016_messaging.sql
@supabase/migrations/0003_fitment_taxonomy.sql
@scripts/check-supabase.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0019_admin_operations.sql</name>
  <files>supabase/migrations/0019_admin_operations.sql</files>
  <action>
Write one migration implementing Research Patterns 3, 4, 5, 8, 10 plus freeze and taxonomy flags. Read 0006 and 0016 first to copy the EXACT current policy bodies before drop/recreate. Contents:

1. `user_restrictions` table exactly per Pattern 3: PK user_id → auth.users cascade, `state text check in ('suspended','banned')`, `reason text not null`, `suspended_until timestamptz` (null for bans, CHECK that suspended requires it), `created_by`, `created_at`. RLS on; ONE self-select policy `(select auth.uid()) = user_id`; no write policies (service-role only).
2. `admin_audit_log` per Pattern 10: identity PK, admin_id, action, target_type, target_id text, reason, metadata jsonb default '{}', created_at; index on created_at desc; RLS enabled with ZERO policies (default-deny both directions).
3. Report queue columns on `reports`: `status text not null default 'pending' check (status in ('pending','resolved','dismissed'))`, `resolved_by uuid references auth.users(id)`, `resolved_at timestamptz`, `admin_note text`; index `(status, created_at desc)`. No new policies (append-only reporter RLS already protects).
4. Listing moderation per Pattern 4: drop+recreate `listings_status_check` to include `'draft'`; add `hidden_at timestamptz` and `hidden_reason text check (hidden_reason in ('moderation','suspension','ban'))`. DROP the existing public-read SELECT policy on listings and recreate as: `(hidden_at is null and status <> 'draft') or seller_id = (select auth.uid())` for anon+authenticated. Verify/keep any other listing policies untouched.
5. Thread freeze: `alter table public.message_threads add column frozen_at timestamptz, add column frozen_by uuid references auth.users(id);`
6. Messages INSERT policy: drop and recreate the participant-insert policy with its FULL original body (copy from 0016/0018) PLUS two new arms: NOT restricted (`not exists (select 1 from public.user_restrictions r where r.user_id = (select auth.uid()) and (r.state = 'banned' or (r.state = 'suspended' and r.suspended_until > now())))`) AND thread not frozen (`exists (select 1 from public.message_threads t2 where t2.id = messages.thread_id and t2.frozen_at is null)`). Do NOT touch the messages SELECT policy (realtime hot path — Pitfall 4).
7. `is_active boolean not null default true` on all 8 taxonomy tables: makes, models, configurations, search_terms, part_categories, materials, conditions, special_filters (confirm exact table names from 0003 before writing).
8. Analytics indexes: `search_events (created_at desc)` and `listing_view_events (created_at desc)`.
9. `admin_user_activity_stats()` security-definer RPC per Pattern 8 returning (registered, active_30d) from profiles_public count + auth.users last_sign_in_at > now()-30d; `set search_path = ''`; REVOKE execute from anon, authenticated, public.

Comment each section with the requirement ID it serves. All copy/identifiers in English.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | grep -i 0019 || node --env-file=.env.local scripts/check-supabase.mjs</automated>
  </verify>
  <done>0019 file exists covering all nine sections; SQL parses (dry-run or push succeeds in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration to Staging and verify structural enforcement</name>
  <files>supabase/migrations/0019_admin_operations.sql</files>
  <action>
Apply 0019 to the Staging project using the same mechanism previous migrations used (check git history / project convention — likely `npx supabase db push` or the SQL editor flow documented in earlier SUMMARYs). Then verify with service-role queries (extend scripts/check-supabase.mjs style ad-hoc node script or psql):
- `user_restrictions` and `admin_audit_log` exist with rowsecurity = true.
- `select policyname from pg_policies where tablename='listings'` shows the recreated public-read policy.
- Insert a test row into user_restrictions via service role, confirm an anon client cannot read it, the owner can, then delete it.
- Confirm `select admin_user_activity_stats()` fails as anon/authenticated (permission denied) and succeeds via service role.
Fix anything that fails before finishing.
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/check-supabase.mjs</automated>
  </verify>
  <done>Migration applied on Staging; RLS default-deny confirmed on new tables; listings policy hides hidden/draft from anon; definer RPC callable only by service role.</done>
</task>

<task type="auto">
  <name>Task 3: grant-admin.mjs script</name>
  <files>scripts/grant-admin.mjs</files>
  <action>
Create scripts/grant-admin.mjs modeled on scripts/check-supabase.mjs conventions (env loading via `node --env-file=.env.local`). Usage: `node --env-file=.env.local scripts/grant-admin.mjs user@email.com`. Flow: create service-role client (`persistSession: false`) → look up user id by email via `auth.admin.listUsers` filtering by email (or `getUserById` after lookup) → `auth.admin.updateUserById(id, { app_metadata: { role: 'admin' } })` → print success AND a prominent note: "User must sign out and back in before the admin claim appears in their JWT" (Pitfall 1). Add a `--revoke` flag that sets `app_metadata: { role: null }`. Exit non-zero with a clear message when user not found or env vars missing. Add an npm script `"grant:admin": "node --env-file=.env.local scripts/grant-admin.mjs"` to package.json.
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/grant-admin.mjs pdmckinster@gmail.com && node -e "process.exit(0)"</automated>
  </verify>
  <done>Script flags a real Staging user as admin; Supabase dashboard/raw_app_meta_data shows role=admin; re-login note printed.</done>
</task>

</tasks>

<verification>
- 0019 applied; all new tables RLS-on with correct (or zero) policies.
- Anon client: cannot see a listing with hidden_at set; cannot see drafts; cannot read user_restrictions/admin_audit_log; cannot execute admin_user_activity_stats.
- Authenticated restricted user cannot insert into messages (test after 10-03 wires the UI, structurally testable now).
</verification>

<success_criteria>
- Migration 0019 is the single schema source for Phase 10 enforcement/audit/analytics structures.
- scripts/grant-admin.mjs flags admins via app_metadata (the locked "manual DB flag").
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-operations-analytics/10-01-SUMMARY.md`
</output>
