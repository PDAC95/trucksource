---
phase: 02-verified-seller-phone-otp
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/verify/phone.ts
  - lib/verify/schema.ts
  - tests/unit/phone.test.ts
autonomous: true
requirements: [VERF-02, VERF-03]
must_haves:
  truths:
    - "A valid US/CA number normalizes to E.164 (+1...) before any Twilio call"
    - "A non-+1 (international) number is rejected and never reaches a paid send"
    - "A bare 10-digit US input normalizes correctly (default region US)"
    - "The same Zod schema validates phone, OTP code, and terms acceptance on client and server"
  artifacts:
    - path: "lib/verify/phone.ts"
      provides: "toE164Plus1(raw) — returns E.164 string for valid US/CA, else null (geo allowlist, line 1 of defense)"
      contains: "export function toE164Plus1"
    - path: "lib/verify/schema.ts"
      provides: "sendOtpSchema, checkOtpSchema, acceptTermsSchema — the single client+server source of truth"
      contains: "export const sendOtpSchema"
    - path: "tests/unit/phone.test.ts"
      provides: "toE164Plus1 accept/reject/normalize coverage"
      min_lines: 20
  key_links:
    - from: "lib/verify/phone.ts"
      to: "libphonenumber-js parsePhoneNumberFromString"
      via: "parse + isValid + countryCallingCode==='1' + country in {US,CA}"
      pattern: "parsePhoneNumberFromString"
---

<objective>
Build the two pure, dependency-free modules every Server Action and form in this phase relies on: the +1-only E.164 phone normalizer (the geo allowlist's first, free line of defense) and the shared Zod schemas. TDD the phone normalizer since it has crisp input→output behavior.

Purpose: The geo gate must reject non-North-American numbers locally and free, before any Twilio call. The ONE Zod schema must validate identically on the client (UX) and inside the Server Action (trust boundary) — CLAUDE.md invariant.
Output: lib/verify/phone.ts, lib/verify/schema.ts, and a passing unit test for the normalizer.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-verified-seller-phone-otp/02-RESEARCH.md
@lib/validation/auth.ts

<interfaces>
<!-- Existing Zod convention to follow (lib/validation/auth.ts uses zod@4, named exports + inferred types). -->
import { z } from "zod";
export const someSchema = z.object({ ... });
export type SomeInput = z.infer<typeof someSchema>;

<!-- libphonenumber-js (install in Task 1): -->
import { parsePhoneNumberFromString } from "libphonenumber-js";
// p.isValid(): boolean; p.countryCallingCode: string ('1' for NANP); p.country: 'US'|'CA'|...; p.number: E.164 string
</interfaces>
</context>

<feature>
  <name>toE164Plus1 — +1-only E.164 normalizer (geo allowlist)</name>
  <files>lib/verify/phone.ts, tests/unit/phone.test.ts</files>
  <behavior>
    toE164Plus1(raw: string): string | null
    Cases (input -> expected):
      - "+15125550123"            -> "+15125550123"   (valid US, already E.164)
      - "512 555 0123"            -> "+15125550123"   (bare 10-digit, default region 'US')
      - "(512) 555-0123"          -> "+15125550123"   (formatted US)
      - a valid Canadian number   -> its E.164 (+1...) (country 'CA' accepted)
      - "+44 20 7946 0000"        -> null             (UK / non-+1 rejected)
      - "+52 55 1234 5678"        -> null             (Mexico / +52 rejected — v1 is +1 only)
      - "123"                     -> null             (invalid)
      - ""                        -> null
    Implementation note: parse with default region 'US' so bare 10-digit input resolves; reject if !isValid(), if countryCallingCode !== '1', or if country is neither 'US' nor 'CA'. Return p.number on success.
  </behavior>
  <implementation>
Install deps first: `npm install twilio botid libphonenumber-js` (twilio + botid are used by Plan 03 but install them together now so the lockfile is committed once). Then author lib/verify/phone.ts verbatim from 02-RESEARCH.md Pattern 3 (toE164Plus1). RED: write tests/unit/phone.test.ts covering every case above and run it (fails — no module). GREEN: implement toE164Plus1, re-run, all pass.
  </implementation>
</feature>

<task type="auto">
  <name>Task 2: Shared Zod schemas for the wizard (phone, OTP, terms)</name>
  <files>lib/verify/schema.ts</files>
  <action>
Create lib/verify/schema.ts following the lib/validation/auth.ts convention (zod@4, named exports + inferred types). Define the THREE schemas the wizard + Server Actions share:
  - `sendOtpSchema = z.object({ phone: z.string().min(7) })` — raw phone string; the +1/E.164 enforcement is toE164Plus1 in the action (do NOT duplicate that logic in zod, but the field must exist so client+server validate the same shape).
  - `checkOtpSchema = z.object({ code: z.string().regex(/^\d{6}$/) })` — exactly 6 digits.
  - `acceptTermsSchema = z.object({ accept: z.literal(true), termsVersion: z.string().min(1) })` — checkbox must be true (mirrors the registerSchema acceptTerms pattern) + the accepted version string.
Export the inferred types: SendOtpInput, CheckOtpInput, AcceptTermsInput.
Add a comment: this is the single client+server source of truth (CLAUDE.md invariant). Also export a `TERMS_VERSION` constant (e.g. "2026-06-03") so the wizard and action agree on the current marketplace-terms version recorded in marketplace_terms_accepted_at / terms_version.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && node -e "process.exit(0)"</automated>
  </verify>
  <done>schema.ts exports sendOtpSchema/checkOtpSchema/acceptTermsSchema + their types + TERMS_VERSION; typecheck passes.</done>
</task>

<verification>
- `npm test` runs tests/unit/phone.test.ts green; non-+1 numbers return null, US/CA return E.164.
- `npx tsc --noEmit` passes with both new lib/verify modules.
- twilio, botid, libphonenumber-js present in package.json dependencies.
</verification>

<success_criteria>
Non-+1 numbers are rejected locally before any paid path; valid US/CA normalize to E.164; one Zod schema set is exported for client+server reuse. (VERF-02 region gate; VERF-03 terms shape.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-verified-seller-phone-otp/02-02-SUMMARY.md`
</output>
