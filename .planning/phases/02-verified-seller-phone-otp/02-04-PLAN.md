---
phase: 02-verified-seller-phone-otp
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - app/(app)/verify/page.tsx
  - app/(app)/verify/phone-step.tsx
  - app/(app)/verify/otp-step.tsx
  - app/(app)/verify/terms-step.tsx
  - components/ui/input-otp.tsx
  - e2e/verify-wizard.spec.ts
autonomous: false
requirements: [VERF-02, VERF-03]
must_haves:
  truths:
    - "Entering /verify resumes to the correct step from persisted DB state (no phone -> phone step; phone but no phone_verified_at -> OTP step; verified but no marketplace terms -> terms step; all three -> already verified)"
    - "A user can enter a phone, receive a 6-box OTP, enter the code, and accept marketplace terms — completing verification"
    - "The OTP step has a resend button with a live countdown and a 'Change number' link that returns to phone entry"
    - "The wizard never forces a restart from scratch on return"
  artifacts:
    - path: "app/(app)/verify/page.tsx"
      provides: "force-dynamic server component that reads profiles_private state and renders the right step"
      contains: "force-dynamic"
    - path: "app/(app)/verify/otp-step.tsx"
      provides: "6-box InputOTP + resend countdown + change-number link, calls checkOtp/sendOtp"
      contains: "InputOTP"
    - path: "app/(app)/verify/terms-step.tsx"
      provides: "marketplace-terms checkbox + link, calls acceptTerms"
      contains: "acceptTerms"
  key_links:
    - from: "app/(app)/verify/page.tsx"
      to: "profiles_private (phone, phone_verified_at, marketplace_terms_accepted_at)"
      via: "getClaims + owner-RLS select to decide the resume step"
      pattern: "phone_verified_at"
    - from: "phone-step/otp-step/terms-step"
      to: "lib/actions/verify.ts sendOtp/checkOtp/acceptTerms"
      via: "Server Action calls from client components"
      pattern: "sendOtp|checkOtp|acceptTerms"
---

<objective>
Build the authenticated, resume-on-abandon verification wizard at /verify: phone entry -> 6-box OTP -> marketplace terms. Single source of truth for the current step is the user's profiles_private row (same columns the badge reads), so progress survives navigation.

Purpose: CONTEXT locks a guided 3-screen wizard that picks up where the user left off and never forces a restart. The page is per-user and force-dynamic so caching can never resume the wrong user's state. The /verify route path is what BotID protects (wired in Plan 03).
Output: app/(app)/verify/{page,phone-step,otp-step,terms-step}.tsx, the shadcn input-otp component, and an e2e spec covering resume-to-correct-step.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-verified-seller-phone-otp/02-CONTEXT.md
@.planning/phases/02-verified-seller-phone-otp/02-RESEARCH.md
@app/(app)/layout.tsx
@components/auth/register-form.tsx
@lib/supabase/server.ts

<interfaces>
<!-- Server Actions from Plan 03 (import from @/lib/actions/verify): -->
// sendOtp(input): Promise<{ ok:true } | { ok:false; error:'blocked'|'unauthenticated'|'invalid'|'region_unsupported'|'rate_limited'|'spend_cap' }>
// checkOtp(input): Promise<{ ok:true } | { ok:false; error:'unauthenticated'|'invalid'|'no_pending'|'invalid_code' }>
// acceptTerms(input): Promise<{ ok:true } | { ok:false; error:'unauthenticated'|'invalid' }>
// from @/lib/verify/schema: sendOtpSchema, checkOtpSchema, acceptTermsSchema, TERMS_VERSION

<!-- The (app) layout (app/(app)/layout.tsx) is already force-dynamic and guards via getClaims -> redirect('/login'). /verify lives UNDER it, so it inherits the auth gate. Still set force-dynamic on the page for explicit per-user freshness. -->

<!-- Form pattern (components/auth/register-form.tsx): RHF + zodResolver, serialize validated values into FormData / pass to action, surface errors via sonner toast. Radix selects/checkbox need manual value wiring. -->

<!-- Supabase server client (lib/supabase/server.ts): createClient(); getClaims() -> data.claims.sub; owner-RLS select on profiles_private. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn input-otp + the resume-aware /verify page (server component)</name>
  <files>components/ui/input-otp.tsx, app/(app)/verify/page.tsx</files>
  <action>
Install the OTP primitive: `npx shadcn@latest add input-otp` (writes components/ui/input-otp.tsx; pulls the input-otp dep). If the CLI is non-interactive-blocked, add it with `--yes`.

Create app/(app)/verify/page.tsx as an async server component. `export const dynamic = "force-dynamic";` (per-user, must not be cached — PITFALL #6). It lives under the already-guarded (app) layout, but re-read claims for the userId.
  - `const supabase = await createClient(); const { data } = await supabase.auth.getClaims();` (layout already redirects unauth; defensively `if (!data?.claims) redirect('/login')`).
  - Select the caller's own resume signals (owner RLS): `from('profiles_private').select('phone, phone_verified_at, marketplace_terms_accepted_at').eq('id', data.claims.sub).maybeSingle()`.
  - Decide the step (resume-on-abandon, 02-RESEARCH Pattern 5):
      * marketplace_terms_accepted_at set AND phone_verified_at set -> already verified: render a brief "You're verified" panel with a link to their profile / to create a listing. (Bounce target is fine as a link in this phase.)
      * phone_verified_at set, terms not -> render <TermsStep/>.
      * phone set, phone_verified_at null -> render <OtpStep initialPhone={phone}/> (resend allowed).
      * otherwise -> render <PhoneStep initialPhone={registrationPhonePrefill}/>. Pre-fill from the existing registration phone if present (UX convenience) but the user can change it; OTP is mandatory regardless.
  - Wrap in a simple centered card layout consistent with the (auth) pages.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && test -f "components/ui/input-otp.tsx" && echo OK</automated>
  </verify>
  <done>components/ui/input-otp.tsx exists; /verify is force-dynamic and selects the three resume signals, branching to the correct step component; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: The three step components (phone, OTP with countdown + change-number, terms)</name>
  <files>app/(app)/verify/phone-step.tsx, app/(app)/verify/otp-step.tsx, app/(app)/verify/terms-step.tsx</files>
  <action>
All three are `'use client'`, follow the register-form RHF+zodResolver+sonner pattern, and call the Plan 03 Server Actions. Map action error codes to friendly toasts (e.g. region_unsupported -> "We can only verify US/Canada (+1) numbers right now."; rate_limited -> "Too many attempts, try again later."; spend_cap -> "Verification is temporarily unavailable, please try later.").

**phone-step.tsx** — props { initialPhone?: string }. RHF with sendOtpSchema. tel input pre-filled with initialPhone. On submit -> sendOtp({phone}); on {ok:true} advance to OTP entry (lift step state via a parent client wrapper OR navigate by re-rendering: simplest is to keep phone+otp in ONE client component with an internal `mode` state, since after a successful send the page would otherwise need a refresh — implement phone-step and otp-step as a single client island if that is cleaner, but keep them as separate files/exports for clarity. If kept separate, after a successful send call `router.refresh()` so the server page re-resolves to the OTP step from the now-persisted phone). Document the chosen approach in a comment.

**otp-step.tsx** — props { initialPhone: string }. 6-box code entry using `InputOTP maxLength={6}` with six `InputOTPSlot` (02-RESEARCH Code Examples) — gives auto-advance + full-code paste. checkOtpSchema validates 6 digits. On submit -> checkOtp({code}); on {ok:true} -> `router.refresh()` (page re-resolves to terms step). Resend: a button disabled with a LIVE countdown (start at 45s after each send; the useEffect interval pattern from 02-RESEARCH) that calls sendOtp({phone:initialPhone}) again and resets the countdown. A "Change number" link/button that calls a small action or simply routes back to phone entry and makes the user re-enter (re-sending invalidates the prior OTP at Twilio on the next send). Show the masked number being verified.

**terms-step.tsx** — checkbox "I accept the marketplace terms" + a link to /terms (Link). acceptTermsSchema requires accept:true. On submit -> acceptTerms({accept:true, termsVersion: TERMS_VERSION}); on {ok:true} -> router.refresh() (page resolves to the verified panel). Mirror the register-form Checkbox + Link styling.

Use lucide-react icons sparingly; align to shadcn/ui. Keep copy short.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Three step components compile and wire to sendOtp/checkOtp/acceptTerms; OTP step has a 6-box input, a live resend countdown, and a change-number affordance; terms step records TERMS_VERSION; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: E2E spec — resume-to-correct-step</name>
  <files>e2e/verify-wizard.spec.ts</files>
  <action>
Create e2e/verify-wizard.spec.ts following the existing e2e/auth.spec.ts + e2e/public-profile.spec.ts conventions. Since real OTP send/check needs Twilio (and BotID is prod-only / always non-bot locally), scope the e2e to what is deterministic without external calls:
  - Unauthenticated visit to /verify redirects to /login (inherited (app) gate).
  - The /verify page renders the phone-entry step for a fresh authenticated user (if the suite has an auth helper/fixture; if none exists, assert the redirect-to-login behavior and the presence of the phone input on a mocked/seeded session as far as the existing harness allows).
  - Document in a comment that the full happy-path (real SMS) + resume-after-OTP transitions are validated manually in the checkpoint below, because they require a live Twilio Verify service and a confirmed test account; the spec asserts routing/gating and step rendering, not the SMS round-trip.
Keep it green in CI (self-contained, no external network). Run `npm run test:e2e -- verify-wizard`.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx playwright test verify-wizard 2>&1 | tail -15</automated>
  </verify>
  <done>e2e/verify-wizard.spec.ts asserts /verify redirects unauth users to /login and renders the phone step for a fresh user; passes locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the full live wizard (Twilio + BotID)</name>
  <action>Human verification only — no code change. Walk the live wizard end-to-end per the steps below (requires the Twilio Verify service + env vars from Plan 03).</action>
  <what-built>The full /verify wizard wired to Twilio Verify + BotID: phone -> 6-box OTP (resend countdown, change-number) -> marketplace terms, resuming to the correct step on return.</what-built>
  <how-to-verify>
Prereq: the Twilio Verify service + env vars (TWILIO_*, RESEND_API_KEY, ABUSE_ALERT_EMAIL) and OTP_SEND_DAILY_CAP are set on the running env (see Plan 03 user_setup).
1. Log in as a confirmed test account, visit http://localhost:3000/verify — you should land on the PHONE step (pre-filled with your registration phone if present).
2. Enter your real +1 mobile, submit. Expect a real SMS within seconds. The page should now show the 6-BOX code entry; the Resend button shows a live countdown (~45s) then re-enables.
3. Click "Change number" — it returns you to phone entry. Re-enter and re-send.
4. Enter the 6-digit code (paste should fill all boxes). On success the page should advance to the TERMS step.
5. Accept the marketplace terms. The page should show a "verified" confirmation.
6. RESUME TEST: mid-flow (after sending OTP but before entering it), navigate away and back to /verify — you should resume on the OTP step, not restart at phone.
7. NEGATIVE: try a non-+1 number (e.g. a UK +44) — it should be rejected with a friendly message and NO SMS sent.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what broke (which step, what you saw vs expected).</resume-signal>
</task>

</tasks>

<verification>
- /verify is force-dynamic, auth-gated, and resumes to the correct step from profiles_private state.
- 6-box OTP with paste + auto-advance; resend has a visible countdown; change-number works.
- Terms step records marketplace_terms_accepted_at + terms_version.
- Non-+1 rejected before any SMS (verified in checkpoint).
- `npm run build` + `npm run test:e2e -- verify-wizard` green.
</verification>

<success_criteria>
A guided, resume-on-abandon wizard takes a seller from phone entry through OTP to marketplace-terms acceptance, completing the verification needed for the badge. (VERF-02, VERF-03.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-verified-seller-phone-otp/02-04-SUMMARY.md`
</output>
