---
phase: 01-foundation-privacy-model
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app/(auth)/register/page.tsx
  - app/(auth)/register/actions.ts
  - app/(auth)/login/page.tsx
  - app/(auth)/login/actions.ts
  - app/(auth)/check-email/page.tsx
  - app/(auth)/forgot-password/page.tsx
  - app/(auth)/forgot-password/actions.ts
  - app/(auth)/reset-password/page.tsx
  - app/(auth)/reset-password/actions.ts
  - app/(auth)/auth-code-error/page.tsx
  - app/auth/confirm/route.ts
  - app/(app)/layout.tsx
  - app/(app)/page.tsx
  - components/auth/register-form.tsx
  - components/auth/password-strength.tsx
  - components/auth/username-field.tsx
  - components/layout/user-menu.tsx
  - app/api/username-available/route.ts
autonomous: true
requirements: [ACCT-01, ACCT-03, ACCT-04, ACCT-05, ACCT-06]

must_haves:
  truths:
    - "A visitor can submit the registration form with all 6 PII fields + password + terms and is routed to a 'check your email' screen (no app access yet)"
    - "Clicking the email confirm link establishes a session and lands the user in the (app) area; an unconfirmed/no-session visitor hitting (app) is redirected to /login"
    - "A user can log in with email+password, stays logged in across reload (persistent session), and can log out from a header user menu on any (app) page"
    - "A user can request a password reset and set a new password via the recovery link"
    - "Typing a username shows live available/taken feedback; leaving it blank auto-generates a valid one at signup"
  artifacts:
    - path: "app/(auth)/register/actions.ts"
      provides: "'use server' register action: re-validate registerSchema, auto-generate username if blank, signUp with metadata + emailRedirectTo, anti-enumeration generic error, redirect /check-email"
      contains: "signUp"
    - path: "app/auth/confirm/route.ts"
      provides: "GET token-exchange: verifyOtp(token_hash,type) → redirect next or /auth-code-error"
      exports: ["GET"]
    - path: "app/(app)/layout.tsx"
      provides: "Guarded layout: getClaims() → redirect('/login') if absent; renders header + user menu; force-dynamic"
      contains: "getClaims"
    - path: "components/layout/user-menu.tsx"
      provides: "Header dropdown with logout (signOut server action)"
    - path: "app/api/username-available/route.ts"
      provides: "Debounced live username availability check (citext eq)"
      exports: ["GET"]
  key_links:
    - from: "app/(auth)/register/actions.ts"
      to: "supabase.auth.signUp"
      via: "options.data metadata consumed by handle_new_user trigger"
      pattern: "options:\\s*\\{[\\s\\S]*data:"
    - from: "app/(app)/layout.tsx"
      to: "supabase.auth.getClaims"
      via: "redirect if no claims (the confirmation gate)"
      pattern: "getClaims\\(\\)"
    - from: "components/layout/user-menu.tsx"
      to: "supabase.auth.signOut"
      via: "logout server action"
      pattern: "signOut\\(\\)"
    - from: "app/(auth)/register/actions.ts"
      to: "generateUsername"
      via: "auto-generate when username blank"
      pattern: "generateUsername"
---

<objective>
Build the full email/password auth experience on top of the privacy migration: registration (6 PII fields → metadata → trigger), the email-confirmation gate, login with persistent session, logout from a header user menu, forgot/reset password, the `/auth/confirm` token-exchange route, and the guarded `(app)` layout whose `getClaims()` redirect IS the confirmation gate. Forms use react-hook-form + the shared Zod schema (Plan 01), re-validated inside each Server Action (trust boundary). Username field has live availability; blank usernames auto-generate.

Purpose: Delivers ACCT-01/03/04/05/06 — a real account can be created, confirmed, used, and logged out, with privacy preserved end-to-end.
Output: Auth pages + server actions + confirm route + guarded app shell + header user menu.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-privacy-model/01-CONTEXT.md
@.planning/phases/01-foundation-privacy-model/01-RESEARCH.md
@.planning/phases/01-foundation-privacy-model/01-01-SUMMARY.md
@lib/supabase/server.ts

<interfaces>
<!-- From Plan 01 (import directly, do not re-create): -->
lib/validation/auth.ts: registerSchema, loginSchema, forgotSchema, resetSchema
lib/username/generate.ts: generateUsername(isTaken?), USERNAME_REGEX
lib/geo/locations.ts: COUNTRIES, statesForCountry(code)

<!-- Existing server client (lib/supabase/server.ts): -->
export async function createClient(): Promise<SupabaseClient>  // cookie-bound, RLS-enforced

<!-- Verified Supabase auth shapes (RESEARCH.md Patterns 2/3, Code Examples): -->
supabase.auth.signUp({ email, password, options:{ emailRedirectTo, data:{ first_name,last_name,phone,country,state_province,username,terms_accepted_at } } })
supabase.auth.signInWithPassword({ email, password })
supabase.auth.signOut()
supabase.auth.resetPasswordForEmail(email, { redirectTo })
supabase.auth.updateUser({ password })
supabase.auth.verifyOtp({ type, token_hash })   // type: EmailOtpType
supabase.auth.getClaims()                         // verified identity, never getSession()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registration form, username field, strength meter, register action + availability API</name>
  <files>app/(auth)/register/page.tsx, app/(auth)/register/actions.ts, app/(auth)/check-email/page.tsx, components/auth/register-form.tsx, components/auth/password-strength.tsx, components/auth/username-field.tsx, app/api/username-available/route.ts</files>
  <action>
**register-form.tsx** (client): RHF + zodResolver(registerSchema). Fields: First Name, Last Name, Email, Phone, Country `<Select>` (from COUNTRIES), dependent State/Province `<Select>` (statesForCountry(country); resets when country changes), optional Username (`username-field.tsx`), Password (with `password-strength.tsx` meter), and a mandatory Terms+Privacy `<Checkbox>` (acceptTerms). Submit calls the `register` server action via `useActionState`; show field errors and the generic action error via sonner toast.
**username-field.tsx** (client): on debounced input (~400ms), validate format with USERNAME_REGEX client-side first, then GET `/api/username-available?u=...`; render inline green "available" / red "taken"/"invalid".
**password-strength.tsx**: pure inline meter — segment count by length + variety (min-8 per CONTEXT, NIST-aligned, no rigid rules). No external library.
**app/api/username-available/route.ts** (GET): read `u`, format-check, anon-safe `select('id').from('profiles_public').eq('username', u).maybeSingle()`, return `{available: !data}`.
**register/actions.ts** ('use server'): `registerSchema.safeParse(Object.fromEntries(formData))`; on fail return `{error}`. Compute `username = parsed.username || await generateUsername(isTaken)` where `isTaken` queries profiles_public (citext). `await createClient()`; `signUp({ email, password, options:{ emailRedirectTo: `${process.env.NEXT_PUBLIC_SITE_URL}/auth/confirm`, data:{ first_name, last_name, phone, country, state_province, username, terms_accepted_at: new Date().toISOString() } } })`. ANTI-ENUMERATION: do not branch the message on "already registered" — on any non-duplicate error return generic `{error:"Something went wrong"}`; otherwise `redirect('/check-email')`.
**check-email/page.tsx**: "Check your email" screen + a resend button (client) calling `supabase.auth.resend({ type:'signup', email })` with a 60s client cooldown (rate-limit UX; platform enforces real limits). Email may be passed via query param or a small client store.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Registration page renders the 6 PII fields + dependent geo selects + username field + strength meter + terms checkbox; register action validates server-side, auto-generates username when blank, calls signUp with metadata + emailRedirectTo, and redirects to /check-email; availability API returns {available}. `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Login, logout, forgot/reset, confirm route, guarded (app) layout + header</name>
  <files>app/(auth)/login/page.tsx, app/(auth)/login/actions.ts, app/(auth)/forgot-password/page.tsx, app/(auth)/forgot-password/actions.ts, app/(auth)/reset-password/page.tsx, app/(auth)/reset-password/actions.ts, app/(auth)/auth-code-error/page.tsx, app/auth/confirm/route.ts, app/(app)/layout.tsx, app/(app)/page.tsx, components/layout/user-menu.tsx</files>
  <action>
**login** page (RHF + loginSchema) + action: `signInWithPassword({email,password})`; on error generic message; on success `redirect('/')` (home/feed, the (app) landing). Session is persistent by default (@supabase/ssr) — no "remember me" (CONTEXT). Include "Forgot password?" link.
**forgot-password** page + action: `resetPasswordForEmail(email, { redirectTo: `${process.env.NEXT_PUBLIC_SITE_URL}/reset-password` })`; always show a generic "if that email exists, check your inbox" (anti-enumeration).
**reset-password** page + action: the recovery link establishes a session (recovery type); page renders a new-password form (resetSchema) → action `updateUser({ password })` → redirect to `/login` with a success toast.
**app/auth/confirm/route.ts** (GET): copy RESEARCH.md Pattern 3 verbatim — read token_hash + type + next, `await createClient()`, `verifyOtp({type, token_hash})`; on success redirect to `next ?? '/'`, else redirect `/auth-code-error`. NOTE: file is at `app/auth/confirm/route.ts` (NOT inside the (auth) group) so URL is `/auth/confirm`.
**auth-code-error/page.tsx**: friendly "link invalid or expired" + link back to /login and a resend hint.
**app/(app)/layout.tsx**: `export const dynamic = 'force-dynamic';` then `const supabase = await createClient(); const { data } = await supabase.auth.getClaims(); if (!data?.claims) redirect('/login');` — THIS is the confirmation gate (unconfirmed = no session). Render a global header containing `<UserMenu/>` + `{children}`.
**components/layout/user-menu.tsx**: shadcn `dropdown-menu` triggered by username/avatar; a "Log out" item that calls an inline `'use server'` action running `await createClient(); await supabase.auth.signOut(); redirect('/login')`. Visible on every (app) page via the layout (ACCT-06).
**app/(app)/page.tsx**: minimal authenticated home/feed placeholder ("Welcome" + the user's username read from profiles_public) — real feed is Phase 7.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>login/forgot/reset pages + actions work against the verified Supabase shapes; `/auth/confirm` route exchanges the token and redirects; `(app)/layout.tsx` redirects to /login when getClaims has no claims and renders the header user menu with working logout; `npm run build` succeeds and `npm run lint` is clean.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds; `npm run lint` + `npm run typecheck` clean.
- Manual (covered by Plan 05 e2e): register → check-email → confirm → land in (app); login persists across reload; logout from header; forgot/reset flow.
- No `getSession()` anywhere (invariant 6); service-role client not imported (Phase 1 needs none).
</verification>

<success_criteria>
- ACCT-01: registration captures the 6 PII fields → metadata → trigger.
- ACCT-03/04: chosen username live-validated; blank auto-generates a valid handle.
- ACCT-05: login with persistent session.
- ACCT-06: logout from a header user menu on any (app) page.
- Confirmation gate enforced by session absence via getClaims() in the (app) layout.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-privacy-model/01-03-SUMMARY.md`.
</output>
