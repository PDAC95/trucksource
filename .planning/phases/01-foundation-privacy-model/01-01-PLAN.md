---
phase: 01-foundation-privacy-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .env.example
  - lib/validation/auth.ts
  - lib/username/generate.ts
  - lib/geo/locations.ts
  - tests/unit/username.test.ts
  - tests/unit/geo.test.ts
  - tests/unit/validation.test.ts
  - components/ui/form.tsx
  - components/ui/input.tsx
  - components/ui/select.tsx
  - components/ui/dropdown-menu.tsx
  - components/ui/label.tsx
  - components/ui/checkbox.tsx
  - components/ui/sonner.tsx
autonomous: false
requirements: [ACCT-03, ACCT-04, ACCT-01]
user_setup:
  - service: supabase
    why: "Email confirmation gate + emailRedirectTo allowlist for /auth/confirm"
    dashboard_config:
      - task: "Auth → Providers → Email: turn 'Confirm email' ON"
        location: "Supabase Staging project (wmsxoccqgdczgyzivdma) → Authentication → Providers → Email"
      - task: "Add Site URL + Redirect URLs allowlist entries for /auth/confirm (http://localhost:3000/**, the Vercel preview pattern, and prod origin)"
        location: "Supabase Staging → Authentication → URL Configuration"

must_haves:
  truths:
    - "A developer running `npm run check:supabase` connects with a clean bare-URL (no leading space, no /rest/v1/ suffix)"
    - "An auto-generated username matches ^[A-Za-z0-9]{3,20}$ and is built from truck/parts vocabulary + a number (never PII-derived)"
    - "The country dropdown contains exactly USA and Canada; selecting a country yields that country's states/provinces"
    - "The shared register Zod schema rejects missing PII fields, a sub-8-char password, an unchecked terms box, and a malformed username"
  artifacts:
    - path: "lib/validation/auth.ts"
      provides: "Shared Zod schemas (registerSchema, loginSchema, forgotSchema, resetSchema) used client + server"
      exports: ["registerSchema", "loginSchema", "forgotSchema", "resetSchema"]
    - path: "lib/username/generate.ts"
      provides: "Collision-aware truck-word username generator + format helper"
      exports: ["generateUsername", "USERNAME_REGEX"]
    - path: "lib/geo/locations.ts"
      provides: "USA + Canada country/state-province data + lookup helpers"
      exports: ["COUNTRIES", "statesForCountry"]
    - path: "tests/unit/username.test.ts"
      provides: "Format + generator tests"
    - path: "tests/unit/geo.test.ts"
      provides: "Country/state data integrity tests"
    - path: "tests/unit/validation.test.ts"
      provides: "registerSchema validation tests"
  key_links:
    - from: ".env.local"
      to: "@supabase/ssr clients"
      via: "NEXT_PUBLIC_SUPABASE_URL bare project URL"
      pattern: "NEXT_PUBLIC_SUPABASE_URL=https://[a-z0-9]+\\.supabase\\.co$"
    - from: "lib/username/generate.ts"
      to: "registerSchema"
      via: "shared USERNAME_REGEX"
      pattern: "USERNAME_REGEX"
---

<objective>
Establish the Wave-1 foundation that every later Phase 1 plan consumes: fix the blocking `.env.local` URL bug, add the shared Zod validation schemas, the truck-word username generator, the USA/Canada geo data, the unit tests that prove all three, and the shadcn UI primitives the auth forms need. No database or DB-dependent code here — this plan is pure, fast, dependency-free logic plus the Supabase dashboard config the auth flow requires.

Purpose: Downstream plans (migration, auth flows, public profile) import these contracts directly instead of exploring the codebase, and real auth cannot work until the env URL is corrected.
Output: Corrected env, three lib modules with exported contracts, three unit test files (green), shadcn components present, and a verified Supabase dashboard config.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-privacy-model/01-CONTEXT.md
@.planning/phases/01-foundation-privacy-model/01-RESEARCH.md

<interfaces>
<!-- Contracts this plan CREATES for downstream plans. Implement exactly these signatures. -->

lib/validation/auth.ts (shared client + server):
```typescript
import { z } from "zod";
export const USERNAME_REGEX = /^[A-Za-z0-9]{3,20}$/; // re-export from generate or define here, single source
export const registerSchema = z.object({
  firstName: z.string().min(1),
  lastName: z.string().min(1),
  email: z.string().email(),
  phone: z.string().min(7),
  country: z.enum(["USA", "Canada"]),
  stateProvince: z.string().min(1),
  username: z.string().regex(USERNAME_REGEX).optional().or(z.literal("")),
  password: z.string().min(8),
  acceptTerms: z.literal(true),
});
export const loginSchema = z.object({ email: z.string().email(), password: z.string().min(1) });
export const forgotSchema = z.object({ email: z.string().email() });
export const resetSchema = z.object({ password: z.string().min(8) });
export type RegisterInput = z.infer<typeof registerSchema>;
```

lib/username/generate.ts:
```typescript
export const USERNAME_REGEX: RegExp; // ^[A-Za-z0-9]{3,20}$
// isTaken: async predicate the action injects (default no-op for unit tests).
export function generateUsername(isTaken?: (candidate: string) => Promise<boolean>): Promise<string>;
```

lib/geo/locations.ts:
```typescript
export const COUNTRIES: ReadonlyArray<{ code: "USA" | "Canada"; label: string }>;
export function statesForCountry(code: "USA" | "Canada"): ReadonlyArray<{ code: string; label: string }>;
```
</interfaces>

# Observed env bug (RESEARCH.md Pitfall 6) — current .env.local line:
#   NEXT_PUBLIC_SUPABASE_URL= https://wmsxoccqgdczgyzivdma.supabase.co/rest/v1/
# MUST become:
#   NEXT_PUBLIC_SUPABASE_URL=https://wmsxoccqgdczgyzivdma.supabase.co
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix env + add NEXT_PUBLIC_SITE_URL</name>
  <files>.env.local, .env.example</files>
  <action>
In `.env.local`: rewrite the `NEXT_PUBLIC_SUPABASE_URL` line to the bare project URL with NO leading space and NO `/rest/v1/` suffix → `NEXT_PUBLIC_SUPABASE_URL=https://wmsxoccqgdczgyzivdma.supabase.co`. Also strip any stray leading space after `=` on `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY` (supabase-js trims keys but keep it clean). Add a new line `NEXT_PUBLIC_SITE_URL=http://localhost:3000` (used by `emailRedirectTo` / `resetPasswordForEmail`).
In `.env.example`: add `NEXT_PUBLIC_SITE_URL=` under the public section with a comment "Base origin for email redirect links (e.g. https://yourapp.com or http://localhost:3000)", and add a comment on `NEXT_PUBLIC_SUPABASE_URL=` clarifying it must be the BARE project URL (no trailing path, no spaces).
Do NOT change the masked key VALUES in `.env.local` — only the URL line, whitespace, and the added site-url line.
  </action>
  <verify>
    <automated>npm run check:supabase</automated>
  </verify>
  <done>`check:supabase` exits 0; `.env.local` `NEXT_PUBLIC_SUPABASE_URL` is the bare `.supabase.co` URL; `NEXT_PUBLIC_SITE_URL` present in both `.env.local` and `.env.example`.</done>
</task>

<task type="auto">
  <name>Task 2: Username generator, geo data, Zod schemas + unit tests</name>
  <files>lib/username/generate.ts, lib/geo/locations.ts, lib/validation/auth.ts, tests/unit/username.test.ts, tests/unit/geo.test.ts, tests/unit/validation.test.ts</files>
  <action>
**lib/username/generate.ts** — export `USERNAME_REGEX = /^[A-Za-z0-9]{3,20}$/`. Export `async function generateUsername(isTaken?)`: pick a random word from a hard-coded truck/parts pool (e.g. Peterbilt, Kenworth, Chrome, Diesel, Rig, Hood, Stack, Bumper, Visor, Aerodyne, BigRig, Cabover, Deck, Grille) + a random integer (100–9999), assemble `${Word}${num}`, truncate to ≤20 chars. If `isTaken` provided, loop (max ~10 tries) regenerating the number until `isTaken(candidate)` resolves false; throw after max tries. Guarantee output always matches `USERNAME_REGEX`. Never use any PII input. Also export a small denylist guard rejecting reserved words (admin, login, api, auth, u, register) — used by the validator as defense in depth.
**lib/geo/locations.ts** — export `COUNTRIES` = exactly `[{code:"USA",label:"United States"},{code:"Canada",label:"Canada"}]`. Export `statesForCountry(code)` returning the 50 US states + DC for "USA" and the 13 Canadian provinces/territories for "Canada" as `{code,label}[]` (use standard 2-letter codes). Mexico is OUT of scope (deferred). Empty array for unknown code.
**lib/validation/auth.ts** — implement the schemas from the `<interfaces>` block. Import `USERNAME_REGEX` from `lib/username/generate.ts` (single source of truth). `registerSchema.username` accepts a valid handle OR empty string (auto-generate path).
**Tests** (Vitest): `username.test.ts` — `generateUsername()` output matches `USERNAME_REGEX` across 100 runs; retries when `isTaken` returns true once then false; reserved-word guard rejects "admin". `geo.test.ts` — `COUNTRIES` length === 2 and contains USA+Canada only (no Mexico); `statesForCountry("USA")` includes "TX"/Texas; `statesForCountry("Canada")` includes "ON"/Ontario; unknown returns []. `validation.test.ts` — `registerSchema` rejects missing firstName, rejects 7-char password, rejects `acceptTerms:false`, rejects username "ab" (too short) and "has space", accepts a full valid object and a valid object with `username:""`.
  </action>
  <verify>
    <automated>npm run test -- tests/unit/username.test.ts tests/unit/geo.test.ts tests/unit/validation.test.ts</automated>
  </verify>
  <done>All three test files pass; `lib/username/generate.ts`, `lib/geo/locations.ts`, `lib/validation/auth.ts` export the contracts in the interfaces block; `npm run typecheck` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add missing shadcn UI components</name>
  <files>components/ui/form.tsx, components/ui/input.tsx, components/ui/select.tsx, components/ui/dropdown-menu.tsx, components/ui/label.tsx, components/ui/checkbox.tsx, components/ui/sonner.tsx</files>
  <action>
Only `components/ui/button.tsx` exists. Add the components the auth forms + header need via the shadcn CLI: `npx shadcn@latest add form input select dropdown-menu label checkbox sonner --yes`. If the CLI prompts or a component already exists, skip it (do not overwrite button). These are owned components committed to the repo (per stack: shadcn CLI-installed, you own them). After install, run the formatter so they match repo style.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`components/ui/{form,input,select,dropdown-menu,label,checkbox,sonner}.tsx` all exist; `npm run typecheck` passes; `npm run build` is not required here but typecheck is clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Supabase dashboard config for the confirmation gate</name>
  <action>
Code-side prerequisites for the email-confirmation gate are done (env URL fixed, NEXT_PUBLIC_SITE_URL set). The remaining step has no CLI/API in this project's setup and must be done by the user in the Supabase dashboard.
  </action>
  <what-built>Env URL fixed and NEXT_PUBLIC_SITE_URL set; the dashboard config is the manual remainder.</what-built>
  <how-to-verify>
    1. Open the Supabase Staging project (wmsxoccqgdczgyzivdma) → Authentication → Providers → Email. Confirm "Confirm email" is ON.
    2. Go to Authentication → URL Configuration. Set Site URL to your primary origin (e.g. http://localhost:3000 for local dev). Under Redirect URLs, add: `http://localhost:3000/**`, the Vercel preview wildcard (e.g. `https://*-patricio-durans-projects.vercel.app/**`), and the production origin when known. These must cover `/auth/confirm` and `/reset-password`.
    3. Reply "approved" once both are set, or describe what you see.
  </how-to-verify>
  <verify>Manual: Supabase dashboard shows Confirm email ON and the redirect allowlist includes /auth/confirm + /reset-password origins.</verify>
  <done>Confirm email is ON and the redirect URL allowlist covers /auth/confirm and /reset-password for localhost, Vercel preview, and prod.</done>
  <resume-signal>Type "approved" when Confirm email is ON and the redirect allowlist includes /auth/confirm + /reset-password origins.</resume-signal>
</task>

</tasks>

<verification>
- `npm run check:supabase` exits 0 with the corrected URL.
- `npm run test -- tests/unit/username.test.ts tests/unit/geo.test.ts tests/unit/validation.test.ts` all green.
- `npm run typecheck` clean with new lib + shadcn files.
- Supabase dashboard: Confirm email ON + redirect allowlist set (human checkpoint).
</verification>

<success_criteria>
- The blocking env URL bug is gone (bare project URL) and `NEXT_PUBLIC_SITE_URL` exists.
- Username generator, geo data, and Zod schemas exist with the exact exported contracts downstream plans import, all unit-tested.
- shadcn form primitives are present.
- The email-confirmation gate's dashboard prerequisites are confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-privacy-model/01-01-SUMMARY.md`.
</output>
