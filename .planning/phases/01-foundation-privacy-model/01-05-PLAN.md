---
phase: 01-foundation-privacy-model
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - e2e/auth.spec.ts
  - e2e/public-profile.spec.ts
autonomous: false
requirements: [ACCT-01, ACCT-05, ACCT-06, PRIV-01, PRIV-04]

must_haves:
  truths:
    - "An e2e test drives register → check-email screen, then (via confirm route or session injection) reaches the (app) area"
    - "An e2e test proves login persists across a page reload and logout from the header returns to /login"
    - "An e2e test loads a public profile while logged out and sees username + 'State/Province, Country' + 'Member since' + listings count, and asserts no PII strings are present"
    - "The full Phase 1 happy path is confirmed working by a human against a real Supabase confirmation email"
  artifacts:
    - path: "e2e/auth.spec.ts"
      provides: "Register/confirm-gate/login-persist/logout Playwright journey"
    - path: "e2e/public-profile.spec.ts"
      provides: "Anon public-profile render + no-PII assertion"
  key_links:
    - from: "e2e/auth.spec.ts"
      to: "app/(app)/layout.tsx gate"
      via: "asserting redirect to /login when unauthenticated"
      pattern: "login"
---

<objective>
Prove the Phase 1 happy path end-to-end with Playwright, then have a human confirm the one leg automation cannot fully own — clicking a real Supabase confirmation email. The e2e specs cover registration → confirmation gate → login persistence → logout, and the anonymous public-profile render with a no-PII assertion. The human checkpoint validates the live email round-trip and overall UX.

Purpose: Closes Phase 1 success criteria with executable evidence + a human sign-off.
Output: Two e2e spec files + a verified end-to-end run.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-privacy-model/01-RESEARCH.md
@.planning/phases/01-foundation-privacy-model/01-03-SUMMARY.md
@.planning/phases/01-foundation-privacy-model/01-04-SUMMARY.md
@playwright.config.ts

<interfaces>
<!-- Routes built in Plans 03/04: -->
/register  /login  /check-email  /forgot-password  /reset-password  /auth-code-error
/auth/confirm (GET token exchange)
/(app) home (guarded; redirects to /login when no session)
/u/[username] (public)
</interfaces>

# Email confirmation has no click-the-link API. Strategy: the AUTOMATED specs assert the gate
# (register lands on /check-email; visiting (app) unauthenticated redirects to /login) and the
# public-profile no-PII render. The full confirm→app leg is verified by the HUMAN checkpoint
# (real inbox), OR optionally by an admin-confirmed test user if the executor wires a service-role
# test helper — but do NOT add the service-role client to app code; keep any helper in e2e/ only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth + public-profile e2e specs</name>
  <files>e2e/auth.spec.ts, e2e/public-profile.spec.ts</files>
  <action>
**e2e/auth.spec.ts** (Playwright, against `npm run dev` baseURL per playwright.config.ts):
- Test "register routes to check-email": fill /register with a random unique email + all 6 PII fields (select USA → a state), valid password, blank username (exercise auto-generate), check terms, submit → expect URL/UI to be the "check your email" screen.
- Test "confirmation gate blocks app": navigate directly to the (app) home while unauthenticated → expect redirect to /login.
- Test "login persists + logout": this requires a confirmed user. Use a pre-provisioned confirmed test account from env (e.g. `E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD`); if those env vars are absent, `test.skip` with a clear note. When present: log in → expect landing on (app) home → reload page → still authenticated (header user menu visible) → open user menu → click Log out → expect /login.
**e2e/public-profile.spec.ts**:
- Test "anon profile render + no PII": as a logged-out context, visit `/u/${E2E_TEST_USERNAME}` (env; skip if absent) → assert visible: the username, a "{State}, {Country}" location line, "Member since", and a listings count / empty state. Then assert the page's text/DOM contains NONE of the known PII fixture values (the test account's first name, phone, email) — read those from env fixtures. Also assert a not-found state for `/u/__definitely_not_a_user__`.
Keep specs resilient (role/text selectors). Document required env (`E2E_TEST_EMAIL`, `E2E_TEST_PASSWORD`, `E2E_TEST_USERNAME`, and PII fixtures for the no-PII assertion) in the spec header.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/auth.spec.ts e2e/public-profile.spec.ts</automated>
  </verify>
  <done>Both specs run; the gate + register-to-check-email + public-profile-no-PII tests pass; login-persist/logout and authed profile tests pass when E2E_TEST_* env is set (skip cleanly otherwise).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the live Phase 1 happy path</name>
  <action>
Have the user confirm the one leg automation cannot fully own — clicking a real Supabase confirmation email — plus an overall UX pass of register/login/logout and the no-PII public profile. Steps are in how-to-verify below.
  </action>
  <what-built>The complete Phase 1 auth + privacy flow: registration with 6 PII fields, the email-confirmation gate, login with persistent session, logout from the header on any page, forgot/reset password, and the public /u/[username] profile. Automated e2e covers everything except clicking a real confirmation email.</what-built>
  <how-to-verify>
    1. `npm run dev` and open http://localhost:3000/register.
    2. Register with a real email you can access, all 6 PII fields, leave username blank (auto-generate), accept terms → you should see the "check your email" screen.
    3. Open the email from Supabase, click the confirm link → you should land in the app (the (app) home). Before confirming, try visiting the app directly — it should bounce you to /login (the gate).
    4. Reload the app page — you should stay logged in. Open the header user menu → Log out → you should return to /login. Log back in with email+password.
    5. Visit /u/<your-generated-username> in a private/incognito window (logged out) → you should see your username, "State, Country", "Member since <Month Year>", "0 active listings" / empty state — and NONE of your name, phone, email, or address anywhere on the page or in view-source.
    6. (Optional) Test "Forgot password?" → reset email → set a new password → log in with it.
    7. Reply "approved" if all steps work, or describe what broke.
  </how-to-verify>
  <verify>Manual: user confirms the live register→confirm→login→logout flow and the no-PII public profile.</verify>
  <done>User types "approved" after the live email confirmation round-trip works and the public profile shows zero PII.</done>
  <resume-signal>Type "approved" once the live register→confirm→login→logout flow and the no-PII public profile are confirmed, or list the issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm run test:e2e -- e2e/auth.spec.ts e2e/public-profile.spec.ts` passes (with E2E_TEST_* set for the authed legs).
- Human checkpoint confirms the live email confirmation round-trip and no-PII public profile.
</verification>

<success_criteria>
- Phase 1 success criteria are demonstrably met: register+login+logout (ACCT-01/05/06), confirmation gate enforced, and a public profile that exposes only the four allowed facts with zero PII (PRIV-01/04).
- Both e2e specs are committed and runnable in CI (skipping authed legs gracefully when secrets are absent).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-privacy-model/01-05-SUMMARY.md`.
</output>
