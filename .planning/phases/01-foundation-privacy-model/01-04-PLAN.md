---
phase: 01-foundation-privacy-model
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app/(public)/u/[username]/page.tsx
  - app/(public)/u/[username]/not-found.tsx
  - components/profile/public-profile-header.tsx
  - components/profile/empty-listings.tsx
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-03, PRIV-04]

must_haves:
  truths:
    - "Any visitor (logged out) can open /u/<username> and see username, location as 'State/Province, Country', 'Member since <Month Year>', and an active-listings count"
    - "The active-listings count is derived (active_listing_count RPC), not a stored column, and renders 0 with an empty state in Phase 1"
    - "The page reads profiles_public ONLY — the response contains no PII keys (name/phone/email/street/postal)"
    - "An unknown username renders a not-found state, not a crash"
  artifacts:
    - path: "app/(public)/u/[username]/page.tsx"
      provides: "Public profile Server Component reading profiles_public + active_listing_count; no PII"
      contains: "profiles_public"
      min_lines: 25
    - path: "components/profile/public-profile-header.tsx"
      provides: "Compact header: username, 'State/Province, Country', Member Since, active count"
    - path: "components/profile/empty-listings.tsx"
      provides: "0-listings empty state ('This user hasn't posted yet')"
  key_links:
    - from: "app/(public)/u/[username]/page.tsx"
      to: "profiles_public"
      via: "anon SELECT of exactly username, state_province, country, member_since"
      pattern: "from\\(['\"]profiles_public['\"]\\)"
    - from: "app/(public)/u/[username]/page.tsx"
      to: "active_listing_count"
      via: "supabase.rpc('active_listing_count', { profile_id })"
      pattern: "rpc\\(['\"]active_listing_count['\"]"
---

<objective>
Build the public seller profile at `/u/[username]` — a public (anon-readable) Server Component that renders only the four allowed public facts (username, "State/Province, Country", "Member since Month Year", live active-listings count) plus a listings grid that is empty in Phase 1. It reads `profiles_public` only and the count via the `active_listing_count` RPC (derived, not stored), so it is correct now and stays correct once listings land in Phase 5. This is the public surface the cross-cutting PII gate protects.

Purpose: Delivers PRIV-01/02/03/04 — buyers can view a seller's public profile with zero PII exposure.
Output: The `/u/[username]` route + header + empty-listings components.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-privacy-model/01-CONTEXT.md
@.planning/phases/01-foundation-privacy-model/01-RESEARCH.md
@.planning/phases/01-foundation-privacy-model/01-02-SUMMARY.md
@lib/supabase/server.ts

<interfaces>
<!-- From Plan 02 migration (read-only here): -->
profiles_public(id, username citext, state_province, country, member_since timestamptz, username_changed_at)
public.active_listing_count(profile_id uuid) returns int   -- returns 0 in P1

<!-- Existing server client: -->
lib/supabase/server.ts → createClient(): cookie-bound, RLS-enforced (anon read of profiles_public is allowed by policy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public profile page + header + empty state</name>
  <files>app/(public)/u/[username]/page.tsx, app/(public)/u/[username]/not-found.tsx, components/profile/public-profile-header.tsx, components/profile/empty-listings.tsx</files>
  <action>
**page.tsx** (Server Component): params `{ username }`. `const supabase = await createClient();` then `select('id, username, state_province, country, member_since').from('profiles_public').eq('username', username).maybeSingle()` — enumerate exactly these columns (NEVER `select('*')`, never join profiles_private). If no row → `notFound()`. Fetch the derived count: `const { data: count } = await supabase.rpc('active_listing_count', { profile_id: row.id })` (default 0). Render `<PublicProfileHeader username state_province country member_since count />` and, since Phase 1 has no listings, render `<EmptyListings/>` (the grid arrives in Phase 5). Format Member Since as "Member since {Month YYYY}" using `toLocaleDateString('en-US',{month:'long',year:'numeric'})`. Location renders as `{state_province}, {country}` (PRIV-02) — never any street/postal (those columns don't even exist on this table).
**public-profile-header.tsx**: compact header — username (h1), location line, "Member since …", and "{count} active listing(s)". Marketplace-seller feel per CONTEXT (header + grid), not a bare info card.
**empty-listings.tsx**: empty state "This user hasn't posted yet" sized to sit where the grid will go.
**not-found.tsx**: simple "Profile not found" with a link home.
This route is anon-safe (no PII), so default caching is fine — do NOT mark it force-dynamic.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>/u/[username] renders the four allowed facts + empty listings for an existing user and a not-found state for an unknown one; count comes from the RPC; the query selects only public columns from profiles_public. `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Public-profile PII contract test (route-level)</name>
  <files>tests/integration/public-profile.contract.test.ts</files>
  <action>
Add `tests/integration/public-profile.contract.test.ts` (Vitest, anon supabase-js client from NEXT_PUBLIC_* env — same skip-if-unreachable guard as Plan 02). This complements Plan 02's table-level contract test by asserting the EXACT query the page uses returns no PII: run `select('id, username, state_province, country, member_since').from('profiles_public').limit(1)` and assert that for any returned row, `Object.keys(row)` is a subset of `['id','username','state_province','country','member_since']` and contains none of `['first_name','last_name','email','phone','street_address','postal_code']`. Also assert `active_listing_count` RPC is callable by anon and returns a number (0 in P1). If no public rows exist yet, assert the call shape succeeds (no error) and skip the row-key assertion with a logged note.
  </action>
  <verify>
    <automated>npm run test -- tests/integration/public-profile.contract.test.ts</automated>
  </verify>
  <done>The route-level contract test passes: the page's exact select returns zero PII keys and active_listing_count is anon-callable returning a number.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds; `npm run test -- tests/integration/public-profile.contract.test.ts` passes.
- Visiting /u/<known-username> while logged out shows the four public facts + empty listings; /u/<unknown> shows not-found.
- Page queries profiles_public only; active count via RPC.
</verification>

<success_criteria>
- PRIV-01: profile shows exactly username, State/Province, Country, Member Since, active-listings count.
- PRIV-02: location renders only as "State/Province, Country".
- PRIV-03: count derived via active_listing_count RPC, not stored.
- PRIV-04: any visitor can view another user's public profile, with zero PII in the response (route-level contract test green).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-privacy-model/01-04-SUMMARY.md`.
</output>
