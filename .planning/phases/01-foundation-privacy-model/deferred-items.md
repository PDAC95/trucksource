# Deferred / Out-of-Scope Items — Phase 01

Items discovered during execution that belong to another plan or phase.

## From Plan 01-03 execution (auth flows)

- **`tests/integration/public-profile.contract.test.ts(52,17)` TS2352 type error.**
  - Owned by Plan 01-04 (public profile), being executed concurrently by another executor. Not in 01-03's `files_modified` scope.
  - Pre-existing/concurrent — not caused by 01-03 changes. Left untouched for the 01-04 executor to resolve.
  - **RESOLVED** in `2d2a667` (cast row via `unknown`) — kept `npm run typecheck` clean.

## From Plan 01-05 execution (end-to-end verification) — RESOLVED 2026-06-03

- **Configure custom SMTP (Resend) for Supabase auth emails, then verify the live Phase 1 email round-trip.**
  - **Was blocked by:** Supabase's built-in email service is hard-capped at **2 emails/hour** and is **NOT dashboard-raisable** — lifting it requires Custom SMTP.
  - **RESOLVED:** Configured **Resend as Custom SMTP** on the Supabase Staging project (`smtp.resend.com:465`, user `resend`, sender `onboarding@resend.dev` → "Take-Off Parts"). This raised the auth-email rate limit to 30/h and unblocked live verification. The shared `resend.dev` sender can only deliver to the Resend account address (`12gapatricio@gmail.com`), which is fine for Staging verification; a verified own/sub-domain is a pre-launch follow-up.
  - **Verified live (2026-06-03):** register → Resend-sent confirmation email → confirm link → landed in `(app)`; public `/u/Kenworth8561` rendered (logged out) showing only username / `ON, Canada` / `Member since June 2026` / `0 active listings`. View-source of the public page (HTML **and** the serialized RSC payload) contains **zero PII** — no real name, email, phone, or address. Privacy gate confirmed at runtime, not just structurally.
  - **Two real bugs surfaced and fixed while closing the round-trip** (commit `2c250a3`):
    1. `/auth/confirm` only handled `?token_hash&type` but Supabase sends a PKCE `?code` on confirmation links → every confirm hit `/auth-code-error`. Added `exchangeCodeForSession(code)`.
    2. `UserMenu` rendered as a Server Component but Radix DropdownMenu needs client state/IDs → hydration mismatch on every `(app)` page. Marked `"use client"` + extracted the logout server action to `components/layout/logout-action.ts`.

## Minor debt (non-blocking, surfaced 2026-06-03)

- **Scaffold metadata placeholders** in `app/layout.tsx`: `<title>` is still `"Create Next App"` and meta description `"Generated by create next app"`. Replace with Take-Off Parts branding (likely Phase 7 when public pages get real metadata, or sooner).
- **Pre-launch email:** verify an own/sub-domain in Resend (e.g. `takeoffparts.com` or `mail.…`) so auth emails can reach any recipient, not just the Resend account address. Tracked under the centralized 12GA Customs Resend account (one account, one domain per brand).
