---
phase: 07-search-feed-public-profile
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/(public)/page.tsx
  - components/search/feed-grid.tsx
  - components/search/listing-card.tsx
  - components/search/facet-sidebar.tsx
  - components/search/active-filter-chips.tsx
  - components/search/search-bar.tsx
  - components/search/slang-banner.tsx
  - components/search/fits-my-truck-control.tsx
  - components/search/empty-results.tsx
autonomous: false
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05]
must_haves:
  truths:
    - "An anon visitor can browse a grid of active listings at / with no login gate, newest-first"
    - "Typing a keyword + applying facets updates results in-place via the URL (shareable, back/forward works)"
    - "A slang/typo query shows the 'Mostrando resultados para …' banner with a path to the exact term"
    - "Fits-my-truck shows login-prompt (anon), add-truck CTA (empty garage), or truck selector (has trucks) and ANDs into results"
    - "Empty results show a friendly message + a clear/reset-filters action"
    - "Each criteria-bearing search logs a search_event"
  artifacts:
    - path: "app/(public)/page.tsx"
      provides: "Same-screen feed/search Server Component reading searchParams; fires recordSearchEvent"
      contains: "searchParams"
    - path: "components/search/listing-card.tsx"
      provides: "Result card: photo, title+price (or Precio a consultar), condition+location, Make+Model chip, clickable username"
    - path: "components/search/facet-sidebar.tsx"
      provides: "Cascading Make→Model→Config + Part Category + Condition facets (sidebar desktop / drawer mobile)"
    - path: "components/search/fits-my-truck-control.tsx"
      provides: "Three-state fits-my-truck control (anon / empty garage / selector)"
  key_links:
    - from: "app/(public)/page.tsx"
      to: "lib/search/queries.ts searchListings"
      via: "await searchListings(parseSearchParams(sp))"
      pattern: "searchListings\\("
    - from: "app/(public)/page.tsx"
      to: "lib/search/events.ts recordSearchEvent"
      via: "void recordSearchEvent on criteria-bearing search"
      pattern: "recordSearchEvent\\("
    - from: "components/search/facet-sidebar.tsx"
      to: "URL searchParams"
      via: "router.replace with serialized query"
      pattern: "router\\.(push|replace)"
---

<objective>
Build the same-screen feed/search surface at `app/(public)/page.tsx` — anon-open, grid of cards, URL-driven keyword + cascading facets + removable chips + result count, the slang transparency banner, fuzzy "Resultados similares" fallback, the three-state Fits-my-truck control, infinite scroll, and friendly empty state — wired to the `lib/search/` readers and firing `recordSearchEvent`.

Purpose: This is the differentiator's payoff — buyers discover parts without a gate. The feed IS search with empty filters (same screen, locked decision).
Output: One Server Component route + the search component set. Ends at a human-verify checkpoint (live UX + slang/fits behaviour).
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-feed-public-profile/07-RESEARCH.md
@.planning/phases/07-search-feed-public-profile/07-CONTEXT.md
@CLAUDE.md
@lib/search/queries.ts
@lib/search/params.ts
@lib/search/events.ts
@app/(app)/sell/page.tsx

<interfaces>
<!-- Consume the Wave-2 readers (07-02). Reuse existing cascade readers + UI primitives. -->

lib/search/params.ts: parseSearchParams(sp), serializeSearchQuery(query), SearchQuery, hasCriteria(query).
lib/search/queries.ts: searchListings(query) -> { cards: SearchCard[], total },
  expandSlang(raw) -> { canonicalTerm, targets }, autocomplete(prefix) -> { terms, titles }.
  SearchCard = { id, title, price, conditionName, stateProvince, coverPhotoUrl, fitmentChip, sellerName, sellerUsername }.
lib/search/events.ts: recordSearchEvent({ rawTerm, normalizedTerm, facets, resultCount }).

Cascading facet options — REUSE (do not duplicate):
  makes: read inline `supabase.from("makes").select("id, name").order("name")` (the sell page does exactly this — there is NO getMakes reader).
  getModels(makeId), getConfigs(modelId) from @/lib/garage/cascade.
  getConditions(), getPartCategories() from @/lib/listings/cascade.
  listMyTrucks() from @/lib/garage/queries (for fits-my-truck selector).

shadcn/ui primitives already vendored: Sheet (mobile drawer), Command (autocomplete), Badge (chips), Card, Skeleton, Button. lucide-react icons.

Detail route is the EXISTING plural /listings/[id] — card + username links point there (LOCKED: no rename, no redirect).
Next 16: searchParams is a Promise -> `const sp = await searchParams`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feed/search route + card grid + result chips + empty state</name>
  <files>app/(public)/page.tsx, components/search/feed-grid.tsx, components/search/listing-card.tsx, components/search/active-filter-chips.tsx, components/search/empty-results.tsx</files>
  <action>
`app/(public)/page.tsx` — async Server Component, the anon-open marketplace feed/search (LOCKED: feed and search are the same screen; fully open to anon). Add `export const dynamic = "force-dynamic"` so the per-request searchParams + the recordSearchEvent side-effect always run (Pitfall 4 — a cached render undercounts events).
  - `const sp = await searchParams;` → `const query = parseSearchParams(sp);`
  - `const { cards, total } = await searchListings(query);`
  - If `hasCriteria(query)`: resolve the banner via `const slang = query.q ? await expandSlang(query.q) : null;` and `void recordSearchEvent({ rawTerm: query.q, normalizedTerm: slang?.canonicalTerm ?? query.q, facets: { makeId: query.makeId, modelId: query.modelId, configId: query.configId, categoryId: query.categoryId, conditionId: query.conditionId, fitsModelId: query.fitsModelId }, resultCount: total })`.
  - Render: `<SearchBar>` (Task 2), `<FacetSidebar>` (Task 2), `<ActiveFilterChips>` + the "X resultados" count, `<SlangBanner>` (Task 2) when slang corrected/expanded, then `<FeedGrid cards={cards} />` or `<EmptyResults />` when cards.length === 0.
  - No-query feed is newest-first (the RPC already orders by date_listed when p_q null).

`components/search/listing-card.tsx` — Card showing (LOCKED card content): cover photo (`coverPhotoUrl`, fallback placeholder), title + price formatted as currency OR "Precio a consultar" when price null, condition badge + State/Province, a Make+Model fitment chip (`fitmentChip`), and a clickable username → `/u/${sellerUsername}`. The card body links to `/listings/${id}` (plural, existing). Use Tailwind grid-friendly card (Claude's discretion on spacing/typography).

`components/search/feed-grid.tsx` — responsive grid (2–4 cols by width) of `<ListingCard>`. Implement infinite scroll (client component: IntersectionObserver appends the next page by pushing `page+1` into the URL via serializeSearchQuery + router.replace, or a client fetch to a route handler — Claude's discretion; mind back-button/scroll restoration). Keep it accessible.

`components/search/active-filter-chips.tsx` — render each active facet + q + fits as a removable chip ("x" removes that one filter by re-serializing the query without it and router.replace). Show the result count "X resultados" beside them.

`components/search/empty-results.tsx` — friendly "no encontramos resultados" message + a "Limpiar filtros" button that router.replace to `/` (clears all params). Actionable, no dead-end (LOCKED).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>/ renders an anon grid of active listings newest-first; cards show photo/title/price-or-"Precio a consultar"/condition+location/Make+Model chip/clickable username; active filters appear as removable chips with a result count; empty results show a reset action; page is force-dynamic and fires recordSearchEvent on criteria-bearing searches.</done>
</task>

<task type="auto">
  <name>Task 2: Search bar + autocomplete, cascading facet sidebar/drawer, slang banner, fits-my-truck control</name>
  <files>components/search/search-bar.tsx, components/search/facet-sidebar.tsx, components/search/slang-banner.tsx, components/search/fits-my-truck-control.tsx</files>
  <action>
`components/search/search-bar.tsx` — client component: a text input that on submit pushes `q` into the URL (serializeSearchQuery + router.replace). Live autocomplete dropdown (shadcn Command) calling a debounced (~200ms, Claude's discretion) `autocomplete(prefix)` — render suggested slang terms + matching titles; selecting one sets `q`. All-state-in-URL (LOCKED).

`components/search/facet-sidebar.tsx` — client component, sidebar on desktop / Sheet drawer on mobile (a "Filtros" button opens it). Facets (v1 set — Material & Special Filters DEFERRED per locked decision, do NOT render them): Make → Model → Configuration (cascading: choosing Make loads Models via getModels, choosing Model loads Configs via getConfigs — dynamic, avoids empty combinations), Part Category (getPartCategories), Condition (getConditions). Each selection re-serializes the query and router.replace updates results in-place. Makes load via the inline `from("makes")` read (no getMakes reader exists). When a parent changes, clear dependent child params (changing Make clears model/config).

`components/search/slang-banner.tsx` — prop-only: given `{ raw, canonical }`, when canonical !== raw render "Mostrando resultados para '<canonical>' (buscaste: '<raw>')" with a link/button to search the EXACT term (router.replace with a flag that forces the raw query, e.g. `&exact=1`, bypassing expansion). Also used for the fuzzy "Resultados similares" note when trigram fallback supplied near matches. Transparency is LOCKED — never silently swap the query.

`components/search/fits-my-truck-control.tsx` — the STAR feature, prominent (not buried in the facet list). Server-resolve the three states, render the right client control:
  - anon (no getClaims) → a visible button that on click invites login: "Inicia sesión y agrega tu camión para filtrar por fitment" (link to login).
  - logged-in + empty garage (listMyTrucks() === []) → CTA "Aún no tienes camiones. Agrega uno a tu garage para filtrar" → link to add-truck (Phase 4 garage page).
  - has trucks → a selector; choosing a truck sets `fits` (modelId) + `fitsConfig` (configId) in the URL → appears as a removable chip "Fits: <year> <make> <model>". ANDs with keyword + facets (LOCKED). Use getClaims()/getUser(), never getSession().
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <done>Search bar drives q via URL with a debounced autocomplete of slang terms + titles; facet sidebar/drawer cascades Make→Model→Config + Part Category + Condition (no Material/Special-Filter facets) and updates results in-place; slang banner surfaces corrected/expanded terms with a path to the exact term; fits-my-truck shows the correct one of three states and ANDs into results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live feed/search UX (slang, facets, fits-my-truck)</name>
  <action>This is a verification checkpoint. All UI was automated in Tasks 1-2; the executor only needs to start the dev server and pause for the user to confirm the live behaviour described in how-to-verify.</action>
  <what-built>The full anon feed/search surface at /: grid of active-listing cards, keyword search with autocomplete, cascading facet sidebar/drawer with removable chips + result count, slang transparency banner, fuzzy fallback, the three-state Fits-my-truck control, infinite scroll, and friendly empty state — all URL-driven and logging search events.</what-built>
  <how-to-verify>
1. Run `npm run dev`, open http://localhost:3000/ logged OUT → confirm a grid of active listings, newest-first, no login gate.
2. Type a known part keyword → results filter in-place; the URL gains `?q=`; back/forward + refresh preserve state.
3. Type a known slang term (e.g. a seeded Common Search Term) and a near-typo of it → results appear AND the "Mostrando resultados para … (buscaste: …)" banner shows with a path back to the exact term.
4. Open Filtros → pick a Make, then Model, then Configuration (each narrows the next), add Part Category + Condition → result count updates; each filter shows as a removable chip; removing a chip restores results.
5. Search a nonsense term → friendly empty state with a working "Limpiar filtros".
6. Click "Fits my truck" while logged out → login invite. Then log in with an EMPTY garage → add-truck CTA. Add a truck (or use an account with trucks) → selector appears; choosing a truck shows the "Fits: …" chip and narrows results (AND with keyword/facets).
7. Confirm clicking a card opens /listings/[id] (plural) and clicking a username opens /u/[username].
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Anon `/` renders without auth; URL params drive all state.
- recordSearchEvent fires on criteria-bearing searches (verify a search_events row appears via Staging after a live search).
- No Material/Special-Filter facets rendered; card/username links use plural /listings/[id] and /u/[username].
</verification>

<success_criteria>
A buyer (even anonymous) discovers parts on one screen: browse newest-first, search by keyword with autocomplete, filter by cascading Make/Model/Config + Part Category + Condition, recover from typos/slang with a transparent banner, filter to "fits my truck", and never hit a dead-end empty state — with every meaningful search logged for analytics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-search-feed-public-profile/07-03-SUMMARY.md`.
</output>
