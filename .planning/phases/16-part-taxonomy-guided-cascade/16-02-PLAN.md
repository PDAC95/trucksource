---
phase: 16-part-taxonomy-guided-cascade
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/listings/cascade.ts
autonomous: true
requirements: [FITL-05, SRCH-03]
must_haves:
  truths:
    - "A reader returns the direct children of any parent category (root -> subcategory -> item), active-only, alphabetical"
    - "A reader returns the root categories (parent_id is null), active-only, alphabetical"
    - "Both readers return [] on error and never expose anything beyond id + name"
  artifacts:
    - path: "lib/listings/cascade.ts"
      provides: "getChildCategories(parentId) and getRootCategories() server readers"
      contains: "export async function getChildCategories"
  key_links:
    - from: "getChildCategories"
      to: "public.part_categories"
      via: "eq parent_id + eq is_active true"
      pattern: "eq\\(\"parent_id\""
    - from: "getRootCategories"
      to: "public.part_categories"
      via: "is parent_id null + eq is_active true"
      pattern: "is\\(\"parent_id\", null\\)"
---

<objective>
Add the two cascade readers the welcome explorer and `/browse` facets consume to walk the 3-level taxonomy: `getChildCategories(parentId)` (direct children of a node) and `getRootCategories()` (the top level). Both mirror the existing `getModels`/`getConfigs`/`getPartCategories` posture exactly — cookie-bound server client, `is_active = true` picker filter, `.order("name")`, `[]` on error, id+name only.

Purpose: the dependent-select cascades in Plans 03 and 04 need a way to fetch one level at a time (root -> subcategory -> item) instead of the flat `getPartCategories()` list. These readers are the shared contract those two UI plans build against — defining them first prevents a scavenger hunt.
Output: two new exported server functions in `lib/listings/cascade.ts`, reusing the existing `CascadeOption`-style `{ id, name }` shape.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-part-taxonomy-guided-cascade/16-RESEARCH.md

# The file to edit (already holds getConditions + getPartCategories — same posture).
@lib/listings/cascade.ts

# The exact pattern to mirror (getModels: int guard, is_active filter, order(name), [] on error).
@lib/garage/cascade.ts

<interfaces>
<!-- Existing exports the new readers sit beside / mirror:

// lib/garage/cascade.ts
export type CascadeOption = { id: number; name: string };
export async function getModels(makeId: number): Promise<CascadeOption[]>;  // mirror this

// lib/listings/cascade.ts (already present)
export type PartCategoryOption = { id: number; name: string; parentId: number | null };
export async function getPartCategories(): Promise<PartCategoryOption[]>;   // keep for create-listing

// NEW (this plan) — return the same { id; name } shape as CascadeOption:
export async function getChildCategories(parentId: number): Promise<{ id: number; name: string }[]>;
export async function getRootCategories(): Promise<{ id: number; name: string }[]>;
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getChildCategories + getRootCategories to lib/listings/cascade.ts</name>
  <files>lib/listings/cascade.ts</files>
  <action>
    Append two new `"use server"` readers to `lib/listings/cascade.ts` (the file already declares `"use server"` at top and imports `createClient` from `@/lib/supabase/server`). Reuse the existing `PartCategoryOption` import surface; the new readers return `{ id: number; name: string }[]` (same shape as `CascadeOption` from garage/cascade — do NOT re-export a duplicate `CascadeOption` type; if a local alias is needed, define `type CategoryOption = { id: number; name: string }` and export it).

    getChildCategories — direct children of a parent (root -> subcategory -> item), mirror `getModels` EXACTLY:
      ```
      /** Direct child categories of a parent (root -> subcategory -> item), is_active only. */
      export async function getChildCategories(parentId: number): Promise<CategoryOption[]> {
        if (!Number.isInteger(parentId) || parentId <= 0) return [];
        const supabase = await createClient();
        const { data, error } = await supabase
          .from("part_categories")
          .select("id, name")
          .eq("parent_id", parentId)
          .eq("is_active", true)   // ADMO-05 picker-only filter, same as getModels
          .order("name");
        if (error || !data) return [];
        return data as CategoryOption[];
      }
      ```

    getRootCategories — the top level (parent_id null), mirror `getPartCategories`'s root half:
      ```
      /** Root categories (parent_id null), is_active only, alphabetical. */
      export async function getRootCategories(): Promise<CategoryOption[]> {
        const supabase = await createClient();
        const { data, error } = await supabase
          .from("part_categories")
          .select("id, name")
          .is("parent_id", null)
          .eq("is_active", true)
          .order("name");
        if (error || !data) return [];
        return data as CategoryOption[];
      }
      ```

    Do NOT remove or alter `getPartCategories` — the create-listing picker (out of scope this phase) still consumes its flat list. Do NOT use any `LIKE`/`ilike`/name string-match (CLAUDE.md invariant 7 — this is id-based reference data). Keep all UI-facing identifiers in English (no Spanish strings).
  </action>
  <verify>
    <automated>cd "C:/Users/patri/Desktop/12GA/take off parts web/project" && grep -q "export async function getChildCategories" lib/listings/cascade.ts && grep -q "export async function getRootCategories" lib/listings/cascade.ts && grep -q 'is("parent_id", null)' lib/listings/cascade.ts && npx tsc --noEmit 2>&1 | tail -10; echo TS_DONE</automated>
  </verify>
  <done>Both readers exist with the mirrored posture (int guard on getChildCategories, is_active filter, order(name), [] on error); `tsc --noEmit` passes; getPartCategories untouched.</done>
</task>

</tasks>

<verification>
- `getChildCategories` and `getRootCategories` are exported from `lib/listings/cascade.ts`.
- `getChildCategories` filters `parent_id = parentId` + `is_active = true`; `getRootCategories` filters `parent_id is null` + `is_active = true`; both `.order("name")`, both `[]` on error.
- No `LIKE`/`ilike` anywhere; no Spanish strings.
- `npx tsc --noEmit` is clean.
- `getPartCategories` is unchanged (create-listing still compiles).
</verification>

<success_criteria>
- The cascade UIs (Plans 03/04) can fetch one taxonomy level at a time via the two new readers.
- Readers expose id+name only, honor the active-only picker filter, and degrade to [] on error — same trust posture as the garage cascade.
</success_criteria>

<output>
After completion, create `.planning/phases/16-part-taxonomy-guided-cascade/16-02-SUMMARY.md` documenting the exact exported signatures and the option-type name (`CategoryOption` vs reused `CascadeOption`) so Plans 03/04 import the right symbol.
</output>
