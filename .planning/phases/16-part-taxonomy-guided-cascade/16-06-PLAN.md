---
phase: 16-part-taxonomy-guided-cascade
plan: 06
type: execute
wave: 5
depends_on: ["16-05"]
files_modified:
  - components/welcome/welcome-explorer.tsx
  - components/search/facet-sidebar.tsx
  - app/(public)/browse/page.tsx
autonomous: false
requirements: [FITL-05, SRCH-03]
must_haves:
  truths:
    - "On the welcome explorer the buyer picks their truck Year as a step between Model and Category (the reserved seam)"
    - "Picking Year is OPTIONAL — See results stays runnable without a year, and skipping Year still reaches Category"
    - "On /browse a Year select sits after Model; choosing a year sets the single `year` URL param"
    - "Running search forwards the chosen `year` to /browse and the subtree/fitment results respect it; clearing year removes the param"
    - "Year options come from lib/listings/years.ts (no duplicated 1970..2027 range); desktop sidebar and mobile sheet both get the select via shared FacetControls"
  artifacts:
    - path: "components/welcome/welcome-explorer.tsx"
      provides: "A Year step inserted between Model and Category; year flows into runSearch as the `year` param; year chip"
      contains: "yearOptions"
    - path: "components/search/facet-sidebar.tsx"
      provides: "Year select in FacetControls after Model, writing the `year` URL param (covers mobile sheet too)"
      contains: "yearOptions"
    - path: "app/(public)/browse/page.tsx"
      provides: "Reads the year param for the active-filter chip / facet selected state"
      contains: "year"
  key_links:
    - from: "welcome-explorer runSearch"
      to: "/browse ?year="
      via: "single chosen year in URLSearchParams"
      pattern: "\"year\""
    - from: "components/search/facet-sidebar.tsx Year select"
      to: "URL `year` param"
      via: "router.replace set/delete year"
      pattern: "\"year\""
---

<objective>
Surface the new YEAR dimension (shipped in 16-05) in both buyer search surfaces, filling the reserved seam.

Welcome explorer: insert a **Year** step between Model and Category — `Make -> Model -> Year(optional) -> Category -> Advanced`. The buyer picks the single year of their truck from `yearOptions()`; it's optional (skipping it keeps the existing "search now" behavior and still advances to Category). `runSearch` adds `year` to the params when chosen.

/browse: add a **Year** select to `FacetControls` right after Model (one edit covers desktop sidebar + mobile sheet via `browse-toolbar-mobile.tsx`). Selecting a year writes the single `year` URL param; "All years" clears it. The page reads `year` for the facet's selected state and the active-filter chip.

Year is independent of Make/Model (it's the buyer's truck year, options are a static list) — do NOT gate it on a model being chosen.

Output: a Year step in `welcome-explorer.tsx`, a Year select in `facet-sidebar.tsx`, and `year` read in `app/(public)/browse/page.tsx`. All UI copy ENGLISH.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
@C:/Users/patri/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<tasks>

<task n="1">
**Welcome explorer Year step**

In `components/welcome/welcome-explorer.tsx`:
- Add `"year"` to the `Step` union, between `"model"` and `"category"`.
- `pickModel` now advances to `setStep("year")` instead of `"category"` (this is the documented seam at the Model->Category hop). Keep "Any model" working.
- Year step renders the `yearOptions()` list (import from `@/lib/listings/years`) plus a skip affordance ("Any year") AND the shared `seeResults` button (year is optional — search must be runnable here too). Picking a year advances to `"category"`; skipping advances to `"category"`.
- Reset discipline: changing Make or Model clears the chosen year (and everything below). Removing the year chip rewinds to the year step and clears year.
- `runSearch`: set `year` param to the chosen year when present; omit when not. Keep make/model/category/condition behavior unchanged.
- Add a Year chip to the YOUR SEARCH summary (between Model and Category chips); its removal clears year only.

Mirror existing neon token styling and the optional-step pattern already used for category. ENGLISH copy.

Commit: `feat(16-06): add optional Year step to welcome explorer`
</task>

<task n="2">
**/browse Year facet**

In `components/search/facet-sidebar.tsx` (`FacetControls`):
- Add a Year `<Select>` after the Model select, before Configuration. Options from `yearOptions()`; first item "All years" = clear. Always enabled (independent of model).
- onChange: set or delete the `year` URL param via the same `apply()`/`router.replace` idiom the other facets use. Year has no dependents to clear.
- Selected value initialised from the URL `year` param.

In `app/(public)/browse/page.tsx`:
- Read the `year` param and pass whatever `FacetControls` needs for the selected state; add a Year active-filter chip (label e.g. `Year: 2012`) whose removal key is `["year"]` (the existing active-filter-chips component deletes the keys each chip carries — no edit needed there).

Confirm `browse-toolbar-mobile.tsx` renders the same `FacetControls` body so the mobile sheet gets the Year select automatically (no separate edit; if it passes props, thread any new prop through).

Commit: `feat(16-06): add Year facet to /browse sidebar + mobile sheet`
</task>

<task n="3">
**Checkpoint — verify the Year search step (human-verify, blocking)**

Run `npx tsc --noEmit` (clean) and confirm the build compiles. Then STOP and return a structured human-verify checkpoint with these steps for the user:
1. `npm run dev`, open `/`. Make -> Model -> confirm a **Year** step appears with year options + "Any year" + See results.
2. Skip year (Any year) -> See results -> URL has NO `year` param.
3. Pick a year -> continue to Category or See results -> URL carries `year=<picked>`.
4. On `/browse`, the Year select sits after Model; picking a year sets `year=` and updates results + chip; "All years" clears it; works in the mobile Filters sheet too.

Do NOT write SUMMARY/STATE/ROADMAP until the user approves.
</task>

</tasks>

<notes>
Plan 16-07 runs in PARALLEL and edits DIFFERENT files (listing form, schema, actions, listing detail, queries, edit page). Stay strictly within THIS plan's files. Year options MUST come from `lib/listings/years.ts` (16-05) — never re-hardcode the range.
</notes>
