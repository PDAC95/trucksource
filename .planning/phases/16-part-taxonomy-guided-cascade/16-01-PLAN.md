---
phase: 16-part-taxonomy-guided-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0025_part_taxonomy_v2.sql
  - supabase/seed.sql
  - tests/integration/search.subtree.test.ts
autonomous: false
requirements: [FITL-05, SRCH-03, FINT-03]
must_haves:
  truths:
    - "Selecting a root or subcategory in search returns listings tagged at any descendant leaf (subtree match)"
    - "The category tree has the confirmed roots and the full Fuel Tanks subtree (root -> subcategory -> item)"
    - "The old 12-root flat tree no longer appears in any category picker (deactivated, not deleted)"
    - "A fresh `supabase db reset` produces exactly ONE taxonomy (the new one), not old+new"
    - "The search_listings RPC signature and return columns are byte-identical to 0024 (integration gate still green)"
  artifacts:
    - path: "supabase/migrations/0025_part_taxonomy_v2.sql"
      provides: "Recursive-CTE subtree match in search_listings + idempotent re-seed + forward-migrate of old rows/tags"
      contains: "with recursive"
    - path: "supabase/seed.sql"
      provides: "New category seed block replacing the old L146-210 flat tree"
      contains: "Fuel Tanks, Straps & Accessories"
    - path: "tests/integration/search.subtree.test.ts"
      provides: "Subtree-match assertion: ancestor selection returns leaf-tagged listing"
  key_links:
    - from: "supabase/migrations/0025_part_taxonomy_v2.sql"
      to: "public.part_categories"
      via: "recursive CTE walking parent_id -> id"
      pattern: "with recursive subtree"
    - from: "search_listings RPC category arm"
      to: "public.listing_categories"
      via: "join subtree st on st.id = lc.category_id"
      pattern: "join subtree"
---

<objective>
Land the data + query foundation for the 3-level part taxonomy in a single migration `0025_part_taxonomy_v2.sql`, and keep `supabase/seed.sql` correct for fresh environments.

Two coupled changes ship together because they share the `part_categories` / `listing_categories` tables and must be one atomic, idempotent migration:
1. **Subtree search (SRCH-03 / FINT-03):** rewrite ONLY the part-category facet arm of `search_listings` to expand the selected `p_category_id` to itself + all descendants via a recursive CTE, then match listings tagged with any node in that set. Signature, return columns, and `security definer set search_path = ''` posture stay byte-identical to `0024` (the reader `lib/search/queries.ts` and the `search.contract` integration gate call it positionally — a signature change is a breaking regression).
2. **Taxonomy re-seed (FITL-05):** seed the confirmed roots + the full "Fuel Tanks, Straps & Accessories" subtree (root -> subcategory -> item) idempotently, deactivate the old 12-root flat tree (FK is `on delete restrict` — deactivate, never blind-delete), re-tag the staging test seller's listings onto a new leaf, and edit the old `seed.sql` block (L146-210) so a fresh `db reset` yields one taxonomy.

Purpose: every downstream reader and cascade UI in this phase reads this tree and depends on the subtree match. Nothing else can be meaningfully built or tested until the tree exists and the RPC expands subtrees.
Output: migration 0025, an updated seed.sql, and a subtree-match integration test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-part-taxonomy-guided-cascade/16-CONTEXT.md
@.planning/phases/16-part-taxonomy-guided-cascade/16-RESEARCH.md

# The frozen RPC this plan rewrites (body-only) — read it; do not change its signature.
@supabase/migrations/0024_search_slang_target_expansion.sql

# The existing seed block this plan replaces (L146-210).
@supabase/seed.sql

# The integration gate that proves the RPC signature/columns are frozen — must stay green.
@tests/integration/search.contract.test.ts

<interfaces>
<!-- FROZEN signature of search_listings (from 0024). The new migration MUST reproduce
     this parameter list and RETURNS table EXACTLY — only the part-category facet arm body changes.

create or replace function public.search_listings(
  p_q text default null,
  p_model_id bigint default null,
  p_config_id bigint default null,
  p_category_id bigint default null,
  p_condition_id bigint default null,
  p_fits_model_id bigint default null,
  p_fits_config_id bigint default null,
  p_limit int default 24,
  p_offset int default 0
)
returns table (
  id bigint, title text, asking_price numeric, condition_id bigint,
  date_listed timestamptz, rank real, total_count bigint
)
language sql stable security definer set search_path = '' as $$ ... $$;

part_categories: self-referencing adjacency tree.
  parent_id bigint references part_categories(id) on delete restrict,
  unique(parent_id, name), index part_categories_parent_id_idx on (parent_id),
  is_active boolean not null default true (added 0019). RLS public-read.
listing_categories(listing_id, category_id) join, category_id on delete restrict.
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Confirm the canonical root-category list before seeding</name>
  <action>Pause for the stakeholder/user to confirm the root list before Task 1 writes the seed. This is a decision gate, not autonomous work — resolve it, then proceed to Task 1 with the confirmed roots.</action>
  <decision>Confirm the canonical root-category list before seeding (CONTEXT lists ~18, the goal text says 20, the stakeholder board has duplicate rows).</decision>
  <context>
    The migration must seed EXACTLY the confirmed root set — a wrong/duplicate root list ships a permanent taxonomy error to staging. 16-CONTEXT.md ("The 20 root categories") lists 18 numbered roots but notes the board image repeats "Air Cleaners…" and "Light Brackets & Accessories" (root #4 vs #17), and says "Confirm exact names/order with the stakeholder at plan time."

    Executor note: one candidate root, "Lighting", COLLIDES by name with an old root in seed.sql L155 (Pitfall 5). If "Lighting" is kept as a new root, the migration must reactivate-and-keep that specific row, else the new seed's `on conflict do nothing` hits the deactivated old row and the category silently never appears.

    Default if the stakeholder is unavailable: seed the 18 unique roots from 16-CONTEXT.md as-listed and proceed (the admin taxonomy editor can add/rename roots later).
  </context>
  <options>
    <option id="context-18">
      <name>Seed the 18 unique roots from 16-CONTEXT.md as-listed (default)</name>
      <pros>Matches the verified CONTEXT list; unblocks the phase immediately; admin editor can add/rename roots later</pros>
      <cons>If the stakeholder later wants the 2 duplicate rows as distinct roots, a follow-up migration is needed</cons>
    </option>
    <option id="stakeholder-confirmed">
      <name>Use a stakeholder-confirmed list/order supplied at the checkpoint</name>
      <pros>Canonical; no rework</pros>
      <cons>Blocks on stakeholder availability</cons>
    </option>
  </options>
  <resume-signal>Select: context-18 (default) or paste the confirmed root list/order. Confirm whether "Lighting" is kept as a root (collision handling).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Write migration 0025 — subtree-match RPC + idempotent re-seed + forward-migrate</name>
  <files>supabase/migrations/0025_part_taxonomy_v2.sql</files>
  <action>
    Create `supabase/migrations/0025_part_taxonomy_v2.sql` with a header comment explaining the two coupled changes and the frozen-signature constraint. Four ordered sections:

    SECTION A — Redefine search_listings (body-only, signature FROZEN).
    `create or replace function public.search_listings(...)` reproducing the EXACT parameter list and `returns table (...)` from `0024` (see <interfaces>), keeping `language sql stable security definer set search_path = ''`. Copy the entire 0024 body BYTE-FOR-BYTE except the part-category facet arm. Replace ONLY this arm:
      ```
      -- part-category facet
      and (
        p_category_id is null
        or exists (
          select 1 from public.listing_categories lc
          where lc.listing_id = l.id and lc.category_id = p_category_id
        )
      )
      ```
    with the recursive-CTE subtree match:
      ```
      -- part-category facet (subtree match: selected node + all descendants)
      and (
        p_category_id is null
        or exists (
          with recursive subtree as (
            select id from public.part_categories where id = p_category_id
            union all
            select c.id
            from public.part_categories c
            join subtree s on c.parent_id = s.id
          )
          select 1
          from public.listing_categories lc
          join subtree st on st.id = lc.category_id
          where lc.listing_id = l.id
        )
      )
      ```
    Every object schema-qualified (`public.part_categories`, `public.listing_categories`) because `search_path = ''`. Do NOT add, remove, reorder, or rename any parameter or return column. Do NOT touch any other facet arm or the ORDER BY / LIMIT / OFFSET.

    SECTION B — Seed the new tree idempotently (confirmed roots from the decision checkpoint + the full Fuel Tanks subtree), following the EXACT seed.sql idiom:
      - ROOTS: `insert into public.part_categories (parent_id, name) select null, v.name from (values (...)) v(name) where not exists (select 1 from public.part_categories pc where pc.parent_id is null and pc.name = v.name)` — guarded where-not-exists because NULL parent_id does not dedupe in the unique index.
      - SUBCATEGORIES: `insert ... select p.id, v.child from (values ('Fuel Tanks, Straps & Accessories','Fuel Tanks'), ...) v(parent,child) join public.part_categories p on p.parent_id is null and p.name = v.parent on conflict (parent_id, name) do nothing`.
      - ITEMS: `insert ... select sub.id, v.item from (values ('Fuel Tanks','Driver Side Fuel Tanks'), ...) v(subname,item) join public.part_categories sub on sub.name = v.subname and sub.parent_id = (select id from public.part_categories where parent_id is null and name = 'Fuel Tanks, Straps & Accessories') on conflict (parent_id, name) do nothing`.
    Seed ALL 14 Fuel Tanks subcategories AND every item under each, EXACTLY as listed in 16-CONTEXT.md lines 63-188 (subcategories: Fuel Tanks, Fuel Tank Straps, Fuel Tank Strap Covers, Fuel Tank Mounting Components, Fuel Tank Steps, Fuel Tank End Caps, Fuel Tank Fairings & Covers, Fuel Tank Lighting, Fuel Tank Light Brackets & Accessories, Fuel Tank Filler Components, Fuel Tank Protection, Fuel Tank Accessories, Fuel System Components, Fuel Tank Repair Components). Duplicate item names under DIFFERENT subcategories are intentional and allowed (unique is per-parent) — keep them.
    If "Lighting" is a confirmed NEW root that collides with the old one, add an explicit `update public.part_categories set is_active = true where parent_id is null and name = 'Lighting'` AFTER Section C's deactivation so the kept name stays visible (Pitfall 5).

    SECTION C — Forward-migrate old rows (deactivate, never delete — FK on delete restrict, Pitfall 3):
      ```
      update public.part_categories set is_active = false
      where parent_id is null and name in
        ('Hoods & Fenders','Lighting','Mirrors','Exhaust & Stacks','Bumpers','Grilles',
         'Interior','Drivetrain','Suspension','Electrical','Glass','Body & Cab');
      update public.part_categories set is_active = false
      where parent_id in (
        select id from public.part_categories where parent_id is null and name in
          ('Hoods & Fenders','Lighting','Mirrors','Exhaust & Stacks','Bumpers','Grilles',
           'Interior','Drivetrain','Suspension','Electrical','Glass','Body & Cab'));
      ```
    (If "Lighting" is kept as a NEW root, Section B's reactivate runs after this and wins.)

    SECTION D — Re-tag old-category listings onto a valid NEW leaf, then drop the old join rows (the restrict FK blocks deleting the parent category row, NOT the join row):
      ```
      with new_leaf as (
        select id from public.part_categories
        where name = 'Driver Side Fuel Tanks'
          and parent_id = (select id from public.part_categories
                           where parent_id is null and name = 'Fuel Tanks, Straps & Accessories')
        limit 1
      ),
      old_cats as (select id from public.part_categories where is_active = false),
      affected as (
        select distinct lc.listing_id from public.listing_categories lc
        join old_cats oc on oc.id = lc.category_id
      )
      insert into public.listing_categories (listing_id, category_id)
      select a.listing_id, nl.id from affected a cross join new_leaf nl
      on conflict (listing_id, category_id) do nothing;

      delete from public.listing_categories
      where category_id in (select id from public.part_categories where is_active = false);
      ```

    Trailing comment must document: signature frozen; leaf-only tagging is a UI convention (no DB constraint); the YEAR seam is untouched.
  </action>
  <verify>
    <automated>cd "C:/Users/patri/Desktop/12GA/take off parts web/project" && grep -q "with recursive subtree" supabase/migrations/0025_part_taxonomy_v2.sql && grep -q "security definer set search_path = ''" supabase/migrations/0025_part_taxonomy_v2.sql && grep -q "Fuel Tanks, Straps & Accessories" supabase/migrations/0025_part_taxonomy_v2.sql && grep -c "on delete" supabase/migrations/0025_part_taxonomy_v2.sql; echo RPC_AND_SEED_PRESENT</automated>
  </verify>
  <done>Migration file exists; contains the recursive-CTE category arm, the frozen definer posture, the new root + Fuel Tanks subtree seed, deactivation of the 12 old roots + children, and re-tag/cleanup of listing_categories. No signature change.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the old seed.sql category block + add a subtree-match integration test</name>
  <files>supabase/seed.sql, tests/integration/search.subtree.test.ts</files>
  <action>
    PART A — Edit `supabase/seed.sql`. Replace the old category block (the L146-210 region: the "L5) part_categories" comment, the 12-root insert, and the children insert) with the NEW tree so a fresh `db reset` produces exactly ONE taxonomy (Pitfall 1). Reuse the SAME values block authored in migration 0025 Section B (confirmed roots + full Fuel Tanks subtree), keeping the existing seed idiom (guarded where-not-exists for roots, `on conflict (parent_id, name) do nothing` for children/items). Do NOT leave any old root name ('Hoods & Fenders', 'Mirrors', 'Exhaust & Stacks', 'Bumpers', 'Grilles', 'Interior', 'Drivetrain', 'Suspension', 'Electrical', 'Glass', 'Body & Cab') in the seed. Keep the surrounding seed sections (L6 materials etc.) untouched. After this edit, `db reset` runs both the migrations (incl. 0025) and seed.sql — the seed must NOT reintroduce deactivated old rows.

    PART B — Add `tests/integration/search.subtree.test.ts` mirroring the structure of `tests/integration/search.contract.test.ts` (same `@vitest-environment node`, `INTEGRATION_ENABLED` self-skip via `./_supabase`, `anonClient()`). Assert the subtree behavior end-to-end against staging:
      1. Resolve the 'Fuel Tanks, Straps & Accessories' root id and a descendant leaf id ('Driver Side Fuel Tanks') from `part_categories` as anon (public-read).
      2. Call `search_listings` with `p_category_id` = the ROOT id; capture the returned listing ids.
      3. Call `search_listings` with `p_category_id` = the LEAF id; capture those ids.
      4. Assert: every listing returned for the LEAF id is also returned for the ROOT id (ancestor selection is a superset of the leaf — the subtree match works). If zero listings are tagged under the subtree on staging, log and pass (call-shape OK), exactly like the contract test's empty-result branch.
    Keep the existing `search.contract.test.ts` assertions valid — do not change the RPC's returned column set.
  </action>
  <verify>
    <automated>cd "C:/Users/patri/Desktop/12GA/take off parts web/project" && ! grep -qE "Hoods & Fenders|Exhaust & Stacks" supabase/seed.sql && grep -q "Fuel Tanks, Straps & Accessories" supabase/seed.sql && test -f tests/integration/search.subtree.test.ts && npx vitest run tests/integration/search.contract.test.ts tests/integration/search.subtree.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>seed.sql contains only the new tree (no old root names); the subtree integration test exists and runs; the frozen-signature contract test still passes (or self-skips when env absent).</done>
</task>

</tasks>

<verification>
- `grep "with recursive subtree" supabase/migrations/0025_part_taxonomy_v2.sql` matches (subtree CTE present).
- The 0025 function header reproduces the 0024 parameter list and RETURNS table verbatim (diff only the category arm).
- `supabase/seed.sql` contains 'Fuel Tanks, Straps & Accessories' and NO old root names.
- `npx vitest run tests/integration/search.contract.test.ts` stays green (signature/columns frozen).
- New `search.subtree.test.ts` asserts ancestor-result ⊇ leaf-result.
</verification>

<success_criteria>
- A root/subcategory category filter in search returns all leaf-tagged descendant listings (subtree match).
- The category tree carries the confirmed roots + the full Fuel Tanks subtree; the old flat tree is deactivated, not deleted.
- A fresh `supabase db reset` yields exactly one taxonomy.
- The `search_listings` signature + return columns are byte-identical to 0024.
- Staging test-seller listings are re-tagged onto a valid new leaf (no orphaned/old tags, no FK violation).
</success_criteria>

<output>
After completion, create `.planning/phases/16-part-taxonomy-guided-cascade/16-01-SUMMARY.md` documenting: the confirmed root list used, the Lighting-collision resolution, whether staging was forward-migrated vs reset, the new leaf chosen for re-tagging, and the exported root/subcategory/leaf ids that Plans 03/04 will reference in chip-label and cascade testing.
</output>
