---
phase: 16-part-taxonomy-guided-cascade
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - components/welcome/welcome-explorer.tsx
  - app/(public)/page.tsx
autonomous: false
requirements: [FITL-05, SRCH-03]
must_haves:
  truths:
    - "On the welcome explorer, after Make -> Model the user picks a root Category, then drills Subcategory -> Item in the Advanced step"
    - "Picking a parent level loads its children; rewinding a level clears every dependent level below it"
    - "Running the search navigates to /browse with a single `category` param = the deepest category the user picked"
    - "A Year step can later slot between Model and Category without rewriting sibling steps (the seam is preserved)"
  artifacts:
    - path: "components/welcome/welcome-explorer.tsx"
      provides: "Reworked step machine: Make -> Model -> (search) -> Category(root) -> Advanced(sub -> item + Condition)"
      contains: "getChildCategories"
    - path: "app/(public)/page.tsx"
      provides: "Passes root categories (getRootCategories) to the explorer instead of the flat list"
      contains: "getRootCategories"
  key_links:
    - from: "components/welcome/welcome-explorer.tsx"
      to: "lib/listings/cascade.ts getChildCategories"
      via: "load children on pickCategory / pickSubcategory"
      pattern: "getChildCategories"
    - from: "welcome-explorer runSearch"
      to: "/browse ?category="
      via: "single deepest category id in URLSearchParams"
      pattern: "params.set\\(\"category\""
---

<objective>
Rework the welcome explorer cascade (`welcome-explorer.tsx`) from the old flat "Pick a part" step to a guided drill: **Make -> Model -> (search now) -> Category(root) -> Advanced(Subcategory -> Item + Condition)**. The root list comes from `getRootCategories()`; subcategories and items load on demand via `getChildCategories(parentId)`. The search navigates to `/browse` with a SINGLE `category` param set to the deepest category the user chose (item if chosen, else subcategory, else root) — the subtree-match RPC from Plan 01 expands it.

Purpose: this is the buyer-facing payoff of the new taxonomy on the landing page. It replaces a one-shot flat category pick with the stakeholder's general -> specific drill, while preserving the existing reset-on-parent-change discipline and leaving a clean seam for the deferred Year step.
Output: a reworked client step machine in `welcome-explorer.tsx` and a one-line data swap in `app/(public)/page.tsx`.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-part-taxonomy-guided-cascade/16-CONTEXT.md
@.planning/phases/16-part-taxonomy-guided-cascade/16-RESEARCH.md

# The component to rework (current step machine: make|model|part|config|advanced).
@components/welcome/welcome-explorer.tsx

# The page that renders it — currently passes partCategories=getPartCategories().
@app/(public)/page.tsx

# The URL contract the search navigates into (single `category` id is sufficient — subtree match).
@lib/search/params.ts

<interfaces>
<!-- From Plan 16-02 (lib/listings/cascade.ts) — import these:
type CategoryOption = { id: number; name: string };   // (or reused CascadeOption — confirm in 16-02 SUMMARY)
export async function getChildCategories(parentId: number): Promise<CategoryOption[]>;
export async function getRootCategories(): Promise<CategoryOption[]>;

URL contract (lib/search/params.ts): the `category` key is a SINGLE id. Subtree match means
one deepest id is enough; do NOT invent new param keys for subcategory/item.
FEED_PATH = "/browse".

Existing welcome-explorer shape this rework modifies:
  type Step = "make" | "model" | "part" | "config" | "advanced";  // becomes make|model|category|advanced
  pickMake/pickModel/skipModel, removeX clears that level + all below + rewinds step,
  back() maps each step to its removeX, runSearch() builds URLSearchParams and router.push.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the welcome-explorer step machine to the 3-level category drill</name>
  <files>components/welcome/welcome-explorer.tsx</files>
  <action>
    Rework the step machine. The OLD flow is Make -> Model -> Part(flat) -> Config -> Advanced(Condition). The Configuration step and `partCategories` flat prop are being replaced; the NEW flow is Make -> Model -> (search now) -> Category(root) -> Advanced(Subcategory -> Item + Condition).

    Imports: add `import { getChildCategories } from "@/lib/listings/cascade"` (and use the option type exported by 16-02 — check 16-02-SUMMARY for `CategoryOption` vs `CascadeOption`). Keep `getModels`/`getConfigs` import only if Config is still used; per CONTEXT the welcome flow drops the Configuration step — REMOVE `getConfigs` and the config state/step if so (config remains a /browse facet, just not a welcome step). Change the props: replace `partCategories: PartCategoryOption[]` with `rootCategories: CategoryOption[]`.

    Steps type becomes: `type Step = "make" | "model" | "category" | "advanced"`. State:
      - keep `make`, `model`, `condition`, `models`.
      - replace `category: PartCategoryOption|null` semantics with three levels: `rootCategory`, `subcategory`, `item` (each `CategoryOption|null`), plus `subcategories: CategoryOption[]` and `items: CategoryOption[]` lists.

    Handlers (preserve the reset-on-parent-change discipline exactly):
      - `pickMake(m)` -> set make, load models, step "model" (unchanged).
      - `pickModel(m)` / `skipModel()` -> set/clear model, step "category". (No more config load here. This is the (search now) seam — the user can also just hit "See results" from chips at this point if desired, but the guided path continues to Category.)
      - `pickRootCategory(c)` -> set rootCategory, `setSubcategories(await getChildCategories(c.id))`, clear subcategory+item, step "advanced".
      - `skipCategory()` -> clear rootCategory/subcategory/item + their lists, step "advanced".
      - In the Advanced step render a Subcategory select/grid (from `subcategories`) and, once a subcategory is chosen, an Item select/grid: `pickSubcategory(c)` -> set subcategory, `setItems(await getChildCategories(c.id))`, clear item; `pickItem(c)` -> set item. Condition pills stay as-is.
      - removeX functions: `removeRootCategory` clears rootCategory+subcategory+item+lists and rewinds to "category"; `removeSubcategory` clears subcategory+item+items; `removeItem` clears item. Keep `removeMake`/`removeModel` cascading down through the new category levels.
      - `back()`: model->removeMake, category->removeModel, advanced->removeRootCategory.

    runSearch(): build URLSearchParams with make, model, condition as before, and a SINGLE `category` param = the DEEPEST chosen category id: `item?.id ?? subcategory?.id ?? rootCategory?.id`. Do NOT emit separate subcategory/item params (the subtree RPC expands one id; params.ts has no such keys). `router.push(qs ? FEED_PATH+"?"+qs : FEED_PATH)`.

    Chips (left panel): show make, model, and ONE category chip labeled with the deepest chosen level's name (item, else subcategory, else root); its onRemove = removeRootCategory (clears the whole category selection). Plus condition chip. Keep "Start over" = removeMake.

    Step headings: model -> "Pick a {make} model"; category -> "Pick a category"; advanced -> "Refine your search" (subcategory + item + condition). Keep all copy in ENGLISH. Preserve the existing neon styling classes (`optionCard`, `skipCard`, chip styles) — this is a logic rework, not a restyle (do NOT introduce new hardcoded hex; token discipline gate).

    SEAM (Pitfall 7): keep each step's advance/reset isolated so a future Year step can be inserted between "model" and "category" without rewriting siblings — do NOT fuse model-and-category into one handler.
  </action>
  <verify>
    <automated>cd "C:/Users/patri/Desktop/12GA/take off parts web/project" && grep -q "getChildCategories" components/welcome/welcome-explorer.tsx && grep -q 'params.set("category"' components/welcome/welcome-explorer.tsx && ! grep -q "getConfigs" components/welcome/welcome-explorer.tsx && npx tsc --noEmit 2>&1 | tail -10; echo TS_DONE</automated>
  </verify>
  <done>The explorer drills Make -> Model -> Category(root) -> Advanced(sub -> item + Condition); parent change resets dependents; runSearch emits a single deepest `category` id; tsc clean; styling tokens unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Feed root categories into the explorer from the welcome page</name>
  <files>app/(public)/page.tsx</files>
  <action>
    Update `app/(public)/page.tsx` to supply root categories instead of the flat part-category list. Change the import to add `getRootCategories` from `@/lib/listings/cascade` (keep `getConditions`). In the `Promise.all` that currently calls `getPartCategories()` (around L66), call `getRootCategories()` instead, and pass the result to `<WelcomeExplorer rootCategories={...} conditions={...} ... />` (rename the prop from `partCategories` to `rootCategories` to match Task 1). If `getPartCategories` is no longer referenced anywhere else in this file, drop its import. Do not change any other data fetch on the page (brands, fitsState).
  </action>
  <verify>
    <automated>cd "C:/Users/patri/Desktop/12GA/take off parts web/project" && grep -q "getRootCategories" "app/(public)/page.tsx" && grep -q "rootCategories=" "app/(public)/page.tsx" && npx tsc --noEmit 2>&1 | tail -10 && npx next build 2>&1 | tail -15; echo BUILD_DONE</automated>
  </verify>
  <done>The welcome page fetches `getRootCategories()` and passes `rootCategories` to the explorer; `tsc --noEmit` and `next build` succeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the welcome explorer drill</name>
  <action>After Tasks 1-2 build and pass tsc/next build, pause for the user to verify the welcome explorer drill end-to-end per the steps below.</action>
  <what-built>The reworked welcome explorer drill (Make -> Model -> Category root -> Advanced subcategory -> item + Condition), wired to the new taxonomy and the subtree-match search.</what-built>
  <how-to-verify>
    1. `npm run dev` and open http://localhost:3000/ (the welcome landing).
    2. Pick a Make, then a Model (or "Any model").
    3. At "Pick a category", confirm you see the NEW roots (e.g. "Fuel Tanks, Straps & Accessories") and NOT the old ones ("Hoods & Fenders", "Exhaust & Stacks").
    4. Pick "Fuel Tanks, Straps & Accessories" -> in Advanced, confirm the Subcategory options load (Fuel Tanks, Fuel Tank Straps, …). Pick "Fuel Tanks" -> confirm Item options load (Driver Side Fuel Tanks, …).
    5. Remove the category chip on the left and confirm subcategory+item clear and the step rewinds.
    6. Hit "See results" -> confirm the URL is `/browse?...&category=<id>` with the DEEPEST id you picked, and results reflect that subtree (re-tagged staging listing under 'Driver Side Fuel Tanks' should appear when you pick the Fuel Tanks root).
    7. Resize to 360px and confirm the drill is legible (real strings, no overflow).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is wrong (wrong roots, children not loading, stale dependents, wrong category id in URL).</resume-signal>
</task>

</tasks>

<verification>
- Explorer imports `getChildCategories`; page imports `getRootCategories`.
- runSearch emits exactly one `category` param = deepest chosen id; no new param keys.
- Old roots are absent from the Category step (they were deactivated in Plan 01).
- Parent-change resets all dependent levels; `back()` rewinds correctly.
- `tsc --noEmit` + `next build` succeed; no new hardcoded hex.
</verification>

<success_criteria>
- A buyer drills root -> subcategory -> item on the landing page and searches a whole subtree with one click.
- The Model -> Category seam is isolated (Year-step-ready).
- Mobile (360px) renders the drill legibly with real taxonomy strings.
</success_criteria>

<output>
After completion, create `.planning/phases/16-part-taxonomy-guided-cascade/16-03-SUMMARY.md` documenting the final Step enum, how the deepest-id is resolved for the `category` param, and confirmation that the Configuration step was removed from the welcome flow (still a /browse facet).
</output>
