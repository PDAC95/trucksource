---
phase: 09-contact-private-chat
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/integration/messaging.test.ts
  - tests/integration/messaging.contract.test.ts
autonomous: true
requirements: [MSG-04, MSG-06]
must_haves:
  truths:
    - "A non-participant authenticated user cannot read another pair's thread or messages (live RLS test)"
    - "No client (participant or not) can UPDATE or DELETE a messages or contact_log row — the log is append-only (MSG-04)"
    - "A blocked user's message INSERT is rejected by RLS; thread history remains readable to both parties"
    - "The exact thread/message read shapes the UI consumes contain zero PII keys (contract test denylist)"
    - "A user cannot report the same item twice, and cannot report a message in a thread they don't participate in"
  artifacts:
    - path: "tests/integration/messaging.test.ts"
      provides: "Live Staging RLS gates: participant-only threads/messages, append-only, block enforcement, contact_log immutability + seller/buyer visibility, reports exclusive-arc + dedupe"
    - path: "tests/integration/messaging.contract.test.ts"
      provides: "Zero-PII contract over the exact reader shapes (PII-keys denylist) for threads, messages, and the realtime payload columns"
  key_links:
    - from: "tests/integration/messaging.test.ts"
      to: "tests/integration/_supabase.ts"
      via: "shared harness (anon + authed test clients vs live Staging)"
      pattern: "_supabase"
---

<objective>
Gate the 0016 schema with live integration tests against Staging, exactly as 0015 was gated by social.test.ts: prove the RLS surface enforces participant-only access, append-only logging, block enforcement, and zero-PII shapes — before any UI ships on top of it.

Purpose: MSG-04 and MSG-06 are structural claims about the database; only live tests against Staging prove them. This is the phase's Privacy/RLS cross-cutting gate.
Output: Two integration test files, green against Staging.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-contact-private-chat/09-RESEARCH.md   (Pattern 1 schema = what to test)
@tests/integration/social.test.ts          (harness + gate style to clone)
@tests/integration/social.contract.test.ts (PII denylist contract style to clone)
@tests/integration/_supabase.ts            (shared clients/users; extend PII denylist usage, don't fork it)
@supabase/migrations/0016_messaging.sql    (from 09-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live RLS gates (tests/integration/messaging.test.ts)</name>
  <files>tests/integration/messaging.test.ts</files>
  <action>
Clone the social.test.ts harness (anon client + two/three authed test users from `_supabase.ts`, run live vs Staging; service-role client only for fixture setup/teardown — creating a listing, a contact_log row, a thread, cleaning up after).

Gates (each its own test):
1. **contact_log**: buyer can insert self-attributed row; buyer CANNOT insert with a different buyer_id; buyer and the listing's seller can SELECT it; a third user and anon get 0 rows; UPDATE and DELETE by any client (including the buyer) affect 0 rows (immutability).
2. **message_threads**: participant SELECT works for buyer and seller; third user and anon get 0 rows; only the buyer can INSERT (seller/third-party insert rejected); participant can UPDATE watermark columns; third user UPDATE affects 0 rows.
3. **messages append-only**: participant can INSERT self-attributed message; UPDATE/DELETE on an existing message affects 0 rows for EVERY client incl. the sender; non-participant INSERT rejected; non-participant SELECT returns 0 rows; sender spoofing (sender_id = someone else) rejected.
4. **block enforcement**: user A blocks user B (insert into user_blocks) → B's INSERT into the shared thread is rejected by RLS; A and B can still SELECT the existing history; A unblocks → B can send again. Also: blocker_id self-attribution enforced.
5. **reports**: reporter can insert one report per target; second insert on the same target violates the unique index (expect 23505); `num_nonnulls` arc rejects a report with two target columns; a user who is NOT a participant of a thread cannot report a message in it; anon cannot insert; reporter cannot UPDATE/DELETE own report.
6. **realtime publication**: assert via SQL (service-role `select ... from pg_publication_tables`) that messages is in supabase_realtime — cheap regression guard for Pitfall 1.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/messaging.test.ts passes against Staging</automated>
  </verify>
  <done>All six gates green live; fixtures cleaned up in afterAll.</done>
</task>

<task type="auto">
  <name>Task 2: Zero-PII contract test (tests/integration/messaging.contract.test.ts)</name>
  <files>tests/integration/messaging.contract.test.ts</files>
  <action>
Clone social.contract.test.ts: run the EXACT enumerated-column selects the UI readers use (mirror the shapes in lib/messaging/queries.ts — thread list shape, thread detail shape, messages shape) as a participant, and recursively assert no key from the PII denylist in `_supabase.ts` (first_name, last_name, email, phone, street/address, postal, etc.) appears anywhere in the responses.

Also assert:
- The full `messages` row (what Realtime ships to subscribers — `select *` on messages AS a participant) contains only: id, thread_id, sender_id, body, created_at. Snapshot the column list; a new column appearing here must consciously pass this test (realtime payloads ship whole rows — this is the MSG-06 realtime guard).
- The thread shape exposes counterparty identity only as profiles_public fields (username/display_name), never an email/phone/real-name column.
- contact_log read AS the seller (the sanctioned "initial contact context") DOES contain buyer_name/buyer_email — assert it is only reachable by buyer/seller (third user gets 0 rows), documenting that this PII exposure is by design and scoped.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/messaging.contract.test.ts passes against Staging</automated>
  </verify>
  <done>PII denylist contract covers thread list, thread detail, messages full-row (realtime payload), and the scoped contact_log exception.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/integration/messaging.test.ts tests/integration/messaging.contract.test.ts` green vs Staging.
- Full `npm run test` still green (no harness regressions).
</verification>

<success_criteria>
- The Privacy/RLS cross-cutting gate for Phase 9 is automated and green before any UI ships.
</success_criteria>

<output>
After completion, create `.planning/phases/09-contact-private-chat/09-04-SUMMARY.md`
</output>
