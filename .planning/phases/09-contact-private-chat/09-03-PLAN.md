---
phase: 09-contact-private-chat
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/actions/reports.ts
  - components/messaging/report-menu.tsx
  - components/comments/comment-section.tsx
  - tests/unit/report-actions.test.ts
autonomous: true
requirements: [MSG-07]
must_haves:
  truths:
    - "A logged-in user can report a listing, a comment, or a message with a predefined reason + optional detail"
    - "Reporting the same item twice shows a friendly 'already reported' message, not a crash"
    - "Submitting a report fires a best-effort admin email and shows only a confirmation toast (no status tracking)"
    - "Comments in the thread show a kebab menu with a Report action"
  artifacts:
    - path: "lib/actions/reports.ts"
      provides: "submitReport server action (getClaims → zod → rate limit → insert → admin email)"
      exports: ["submitReport"]
    - path: "components/messaging/report-menu.tsx"
      provides: "Reusable kebab DropdownMenu + report Dialog (reason select + optional detail) for listing/comment/message targets"
      exports: ["ReportMenu"]
    - path: "components/comments/comment-section.tsx"
      provides: "ReportMenu mounted on each comment"
      contains: "ReportMenu"
  key_links:
    - from: "components/messaging/report-menu.tsx"
      to: "lib/actions/reports.ts"
      via: "submitReport server action call on dialog submit"
      pattern: "submitReport"
    - from: "lib/actions/reports.ts"
      to: "lib/messaging/notify.ts"
      via: "sendAdminReportCopy after successful insert (best-effort)"
      pattern: "sendAdminReportCopy"
---

<objective>
The complete MSG-07 vertical slice: submitReport action against the reports table (exclusive-arc target, one-report-per-item unique indexes), a reusable kebab ReportMenu component, and its first mount on comments. (Listing-page and chat-message mounts happen in plans 09-05/09-06, which import this component.)

Purpose: Abuse reporting must exist on every social surface; this plan builds it once, reusably, with the admin email so a scam report is never silently invisible pre-Phase-10.
Output: lib/actions/reports.ts, components/messaging/report-menu.tsx, comment mount, unit test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-contact-private-chat/09-RESEARCH.md   (Pattern 5)
@lib/actions/comments.ts            (action template)
@lib/messaging/schema.ts            (from 09-01: reportSchema, REPORT_REASONS)
@lib/messaging/notify.ts            (from 09-01: sendAdminReportCopy)
@components/comments/comment-section.tsx
@components/ui/dropdown-menu.tsx    (already vendored)
@components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: submitReport action (lib/actions/reports.ts)</name>
  <files>lib/actions/reports.ts, tests/unit/report-actions.test.ts</files>
  <action>
`"use server"` action, comments.ts posture. Result union: `{ ok: true } | { ok: false; error: "unauthenticated" | "invalid" | "rate_limited" | "already_reported" | "not_found" }`.

Order:
1. getClaims → unauthenticated.
2. reportSchema parse → invalid.
3. Rate limit: head-count of own reports in trailing 24h, >= 10 → rate_limited (constant `REPORT_DAILY_LIMIT = 10`).
4. Map targetType → column: listing→listing_id, comment→comment_id, message→message_id. Insert `{ reporter_id (RLS self-attributes), [column]: targetId, reason, detail }`. The RLS INSERT policy enforces message-participant authz; a violation collapses to not_found (no existence leak).
5. On unique-index violation (Postgres error code 23505) → `already_reported` (friendly, expected path).
6. On success: `sendAdminReportCopy({ reporterUsername, targetType, targetId, reason, detail })` — fetch the reporter's username from profiles_public for the email; best-effort.
7. Return `{ ok: true }` — no revalidate needed (no UI state changes beyond the toast).

UNIT TEST: guard order; 23505 maps to already_reported; admin email called only after successful insert; rate-limit branch inserts nothing.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/report-actions.test.ts passes</automated>
  </verify>
  <done>submitReport handles all three target types, dedupes gracefully, emails admin best-effort.</done>
</task>

<task type="auto">
  <name>Task 2: Reusable ReportMenu component</name>
  <files>components/messaging/report-menu.tsx</files>
  <action>
Client component `ReportMenu({ targetType, targetId, isAuthenticated }: { targetType: "listing" | "comment" | "message"; targetId: number; isAuthenticated: boolean })`:

- Kebab trigger: vendored DropdownMenu with a ghost icon button (`MoreHorizontal` from lucide-react, `size-4`), single item "Report" with a `Flag` icon.
- If not authenticated: the Report item routes to `/login` (match the SaveButton login-invite posture) instead of opening the dialog.
- Dialog (vendored Dialog + RHF + zodResolver(reportSchema)): title "Report this {listing|comment|message}", reason Select fed from `REPORT_REASONS` labels, optional Textarea "Details (optional)" max 1000, Cancel + "Submit report" buttons (submit disabled while pending).
- On submit → `submitReport`; map results to sonner toasts: success → "Report submitted — our team will review it." ; already_reported → "You've already reported this." ; rate_limited → "Too many reports today. Try again tomorrow." ; otherwise → "Something went wrong. Try again."
- Close the dialog on success. All copy English. Keep the component dumb/reusable — no target-specific logic inside.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint pass; grep -n "submitReport" components/messaging/report-menu.tsx returns a hit</automated>
  </verify>
  <done>One reusable component renders kebab → dialog → toast for any of the three target types.</done>
</task>

<task type="auto">
  <name>Task 3: Mount ReportMenu on comments</name>
  <files>components/comments/comment-section.tsx</files>
  <action>
Add `<ReportMenu targetType="comment" targetId={comment.id} isAuthenticated={...} />` to each comment row (top-level comments AND replies), aligned right in the comment header next to the existing author/timestamp line. Do NOT show it on the viewer's own comments (you don't report yourself — compare `viewerId`). Pass/derive `isAuthenticated` from the props the section already receives (`viewerId != null`). Keep existing delete affordances untouched.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build pass; grep -n "ReportMenu" components/comments/comment-section.tsx returns hits</automated>
  </verify>
  <done>Every non-own comment shows the kebab Report action; existing comment behavior unchanged.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/report-actions.test.ts` green; full `npm run test` still green.
- `npm run build` exits 0.
</verification>

<success_criteria>
- MSG-07 backend + reusable UI complete; comments surface live; listing/message mounts ready for 09-05/09-06 by simple import.
</success_criteria>

<output>
After completion, create `.planning/phases/09-contact-private-chat/09-03-SUMMARY.md`
</output>
