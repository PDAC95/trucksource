---
phase: 06-fitment-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/listings/cascade.ts
  - lib/actions/listings.ts
  - lib/listings/queries.ts
  - components/listings/listing-detail.tsx
  - app/(app)/sell/[id]/edit/page.tsx
  - tests/integration/fitment-intelligence.test.ts
autonomous: true
requirements: [FINT-03]
user_setup: []

must_haves:
  truths:
    - "getPartCategories() returns the seeded 2-level part-category tree (id, name, parentId)"
    - "createListing persists submitted categoryIds + searchTermIds as listing_categories / listing_search_terms rows"
    - "updateListing replaces category + term children the same way it replaces fitment/photos"
    - "An accepted model/config suggestion writes BYTE-IDENTICAL listing_fitment rows as a manual tag (FINT-03)"
    - "The public listing detail page renders confirmed categories + search terms alongside Make/Model/Config"
    - "Edit mode pre-fills the persisted category + search-term ids so they are not re-suggested"
  artifacts:
    - path: "lib/listings/cascade.ts"
      provides: "getPartCategories() reader (mirrors getConditions)"
      contains: "getPartCategories"
    - path: "lib/actions/listings.ts"
      provides: "category + search-term child inserts in createListing/updateListing"
      contains: "listing_categories"
    - path: "lib/listings/queries.ts"
      provides: "categories + searchTerms on ListingDetail; ids in the editable read"
      contains: "listing_categories"
    - path: "components/listings/listing-detail.tsx"
      provides: "Categories + Search-terms display sections"
      contains: "Categor"
    - path: "tests/integration/fitment-intelligence.test.ts"
      provides: "accept-writes-same-rows equivalence assertion (FINT-03)"
      min_lines: 40
  key_links:
    - from: "lib/actions/listings.ts"
      to: "public.listing_categories / public.listing_search_terms"
      via: "bulk child insert + replace-children on update"
      pattern: "from\\(\"listing_(categories|search_terms)\"\\)"
    - from: "lib/listings/queries.ts"
      to: "public.part_categories / public.search_terms"
      via: "embedded select joining category + term NAMES"
      pattern: "listing_categories|listing_search_terms"
    - from: "app/(app)/sell/[id]/edit/page.tsx"
      to: "ListingFormDefaults"
      via: "categoryIds + searchTerms passed into defaults"
      pattern: "categoryId"
---

<objective>
Make the new dimensions REAL end-to-end on the persistence + read side: a `getPartCategories()` reader, category + search-term child writes in the UNCHANGED-shape `createListing`/`updateListing` trust boundary, read-back on the public listing detail page (so FINT-03 is verifiable before Phase 7), and the editable-row read so edit mode pre-fills persisted categories/terms (which the suggestion UI then excludes from re-suggestion).

This plan proves the FINT-03 testable claim: **an accepted model/config suggestion produces byte-identical `listing_fitment` rows as a manual add** — because accepted suggestions flow through the SAME `fitment` path and the SAME action, no separate search plumbing is needed in Phase 6 (actual search surfaces are Phase 7).

Purpose: Without this, the Part-Category selector + suggestion chips (Plan 06-04) would accept into nothing (Pitfall 2). This plan gives them a persistence home and a visible read-back.
Output: extended cascade reader, extended create/update actions, extended detail query + component, extended edit page, the FINT-03 equivalence integration test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fitment-intelligence/06-CONTEXT.md
@.planning/phases/06-fitment-intelligence/06-RESEARCH.md
@.planning/phases/06-fitment-intelligence/06-01-SUMMARY.md   # the tables + extended schema this plan writes against

@lib/listings/cascade.ts        # getConditions — mirror it for getPartCategories
@lib/actions/listings.ts        # createListing/updateListing — extend the child-insert + replace-children blocks (do NOT change fitment handling)
@lib/listings/queries.ts        # getListing/getMyListings — add categories + searchTerms embeds
@components/listings/listing-detail.tsx  # add the display sections (mirror the "Fits" fitment block)
@app/(app)/sell/[id]/edit/page.tsx       # editable-row read — add category + term id read-back into defaults
@lib/listings/schema.ts         # listingSchema now carries categoryIds + searchTermIds (added in 06-01)

<interfaces>
<!-- From 06-01: the new tables + the extended schema. -->
public.listing_categories   (id, listing_id -> listings, category_id -> part_categories)   -- public-read, owner-write-via-EXISTS
public.listing_search_terms (id, listing_id -> listings, term_id -> search_terms)           -- public-read, owner-write-via-EXISTS
public.part_categories (id, parent_id null, name)
public.search_terms    (id, term citext)

// lib/listings/schema.ts (after 06-01):
listingSchema  // now includes: categoryIds: number[] (default []), searchTermIds: number[] (default [])
type ListingInput  // .categoryIds, .searchTermIds present

// existing getConditions to mirror:
export async function getConditions(): Promise<ConditionOption[]>  // anon-public read of conditions(id,name) ordered
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getPartCategories reader + persist category/term children in create/update actions</name>
  <files>lib/listings/cascade.ts, lib/actions/listings.ts</files>
  <action>
**lib/listings/cascade.ts** — add `getPartCategories()` mirroring `getConditions()` (server-side cookie client, anon-public reference read, returns [] on error):
```ts
export type PartCategoryOption = { id: number; name: string; parentId: number | null };
export async function getPartCategories(): Promise<PartCategoryOption[]> {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("part_categories")
    .select("id, name, parent_id")
    .order("parent_id", { nullsFirst: true })
    .order("name");
  if (error || !data) return [];
  return (data as { id: number; name: string; parent_id: number | null }[])
    .map((c) => ({ id: c.id, name: c.name, parentId: c.parent_id }));
}
```

**lib/actions/listings.ts** — extend `createListing` and `updateListing` to persist the new dimensions. DO NOT change the existing `fitment`/`photos` handling (FINT-03 for trucks depends on it staying identical). The schema is already re-validated via `listingSchema.safeParse` in both actions, so `v.categoryIds` / `v.searchTermIds` are available after parse.
  - In `createListing`, AFTER the listing insert and alongside the existing `listing_fitment` / `listing_photos` child inserts, add (same best-effort sequential posture, 05-RESEARCH Open Q3):
    ```ts
    if (v.categoryIds.length > 0) {
      await supabase.from("listing_categories").insert(
        v.categoryIds.map((category_id) => ({ listing_id: listing.id, category_id })),
      );
    }
    if (v.searchTermIds.length > 0) {
      await supabase.from("listing_search_terms").insert(
        v.searchTermIds.map((term_id) => ({ listing_id: listing.id, term_id })),
      );
    }
    ```
  - In `updateListing`, in the "REPLACE CHILDREN" block, add `listing_categories` + `listing_search_terms` to BOTH the delete sweep (`await supabase.from("listing_categories").delete().eq("listing_id", id);` and the same for listing_search_terms) AND the re-insert (same `.insert(...)` shape as create, with `listing_id: id`). Keep the exact ordering: delete all children, then re-insert all children.
  - No new error variants are needed (these mirror the best-effort fitment inserts). The owner-write EXISTS policy (06-01) scopes the writes because the parent listing's seller_id = userId.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "listing_categories\|listing_search_terms" lib/actions/listings.ts && grep -c "getPartCategories" lib/listings/cascade.ts</automated>
  </verify>
  <done>getPartCategories() returns id/name/parentId ordered; createListing inserts listing_categories + listing_search_terms after the listing+fitment inserts; updateListing deletes then re-inserts both alongside fitment/photos; the existing fitment/photo handling is byte-for-byte unchanged; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Read-back — getListing categories/terms + detail display + edit-page id pre-fill</name>
  <files>lib/listings/queries.ts, components/listings/listing-detail.tsx, app/(app)/sell/[id]/edit/page.tsx</files>
  <action>
**lib/listings/queries.ts** — extend `getListing`:
  - Add to the embedded select: `listing_categories ( category_id, part_categories:category_id ( name ) )` and `listing_search_terms ( term_id, search_terms:term_id ( term ) )`. Extend `ListingDetailRow` with these to-many relations.
  - Add to `ListingDetail`: `categories: { id: number; name: string }[]` and `searchTerms: { id: number; term: string }[]`. Map them from the row (filter out any with a null joined name, like the fitment mapping tolerates nulls). Keep the seller resolution + photos + fitment mapping UNCHANGED.
  - Keep the column-enumeration / no-PII discipline (Pitfall 7): the new embeds are public reference tables only (part_categories, search_terms), no profiles touched.

**components/listings/listing-detail.tsx** — add two display sections after the existing "Fits" fitment block, mirroring its markup (a `text-muted-foreground text-sm font-medium` heading + a flex-wrap of `<Badge variant="outline">`), each rendered only when non-empty:
  - "Part categories" → `listing.categories.map((c) => <Badge key={c.id}>{c.name}</Badge>)`
  - "Also tagged" (or "Search terms") → `listing.searchTerms.map((t) => <Badge key={t.id}>{t.term}</Badge>)`
  This makes FINT-03 visually verifiable at the UAT checkpoint (Plan 06-04) before Phase 7 search exists.

**app/(app)/sell/[id]/edit/page.tsx** — extend the owner-scoped editable-row read so the form can pre-fill the persisted dimensions (edit-mode read-back, CONTEXT specific idea + Pitfall 5 "don't re-suggest confirmed"):
  - Add `listing_categories ( category_id )` and `listing_search_terms ( term_id )` to the `.select(...)` and to the `EditableRow` type.
  - Map to `categoryIds: number[]` and `searchTermIds: number[]` and add them to the `ListingFormDefaults` object passed to `<ListingForm>`. (The `ListingFormDefaults` type gains these two fields in Plan 06-04, which owns listing-form.tsx; here, populate them — if tsc flags the type before 06-04 lands, add the fields to `ListingFormDefaults` minimally, but 06-04 is the owner of that type, so coordinate via depends_on. Since 06-04 depends on this plan's read surface, prefer to extend `ListingFormDefaults` in 06-04 and pass the values here.)
  - To avoid a cross-plan type race: in THIS plan, compute `categoryIds`/`searchTermIds` and include them in the `defaults` object. The `ListingFormDefaults` type extension is done in 06-04 (which runs after this plan). If tsc fails here standalone, add `categoryIds: number[]; searchTermIds: number[];` to the `ListingFormDefaults` export in components/listings/listing-form.tsx as the minimal unblocking change and note it for 06-04 to consume.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "categories\|searchTerms" lib/listings/queries.ts && grep -c "categoryIds" app/\(app\)/sell/\[id\]/edit/page.tsx</automated>
  </verify>
  <done>getListing returns categories[] + searchTerms[] (names resolved, no PII); the detail component renders both as outline badges when present; the edit page reads listing_categories/listing_search_terms ids and passes categoryIds/searchTermIds into ListingForm defaults; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: FINT-03 equivalence integration test — accepted suggestion = identical join rows</name>
  <files>tests/integration/fitment-intelligence.test.ts</files>
  <action>
Extend `tests/integration/fitment-intelligence.test.ts` (created in 06-01) with the FINT-03 testable claim. Because the suggestion-accept path writes the SAME `listing_fitment` rows through the SAME `createListing` action, the assertion is a SHAPE-EQUIVALENCE test, not a live-auth-write test (anon can't create listings, and the suite runs anon-only — see _supabase.ts). Implement whichever of these the harness supports cleanly:

  - PREFERRED (pure, no auth): a unit-style assertion that the payload a chip-accept builds maps to the same `listing_fitment` insert shape as a manual add. Construct two `ListingInput` payloads via `listingSchema.parse` — one whose `fitment` entry came from a "manual" add `{ modelId: 5, configId: 7 }` and one representing an "accepted suggestion" with the identical `{ modelId: 5, configId: 7 }` — and assert the normalized fitment arrays are deep-equal. This proves there is no separate plumbing: the accept path and the manual path produce identical action input. Put this in a `describe("FINT-03: accepted suggestion = manual tag (identical fitment rows)")` block. (This does not need Supabase env, so it always runs.)
  - Document in a comment that the LIVE end-to-end equivalence (rows actually present in `listing_fitment` + visible on the detail page) is verified at the Plan 06-04 human-verify checkpoint, and that actual search surfacing is Phase 7 (RESEARCH FINT-03 framing).

Also add a comment block stating the Phase-6 invariant-#8 determination (RESEARCH): NO accept/reject telemetry is logged — the confirmation is reconstructible from the resulting join rows.

Run the full integration + unit suite for this file.
  </action>
  <verify>
    <automated>npm run test -- tests/integration/fitment-intelligence.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The test file asserts the accept-path and manual-path produce identical normalized fitment/category/term payloads (the FINT-03 "no separate plumbing" claim), documents the live + Phase-7 split, and notes the no-telemetry determination; suite green; tsc clean.</done>
</task>

</tasks>

<verification>
- `npm run test` full suite green; `npx tsc --noEmit` clean.
- A listing created with categoryIds/searchTermIds persists listing_categories + listing_search_terms rows (verified live at the 06-04 checkpoint; shape-equivalence asserted in the integration test now).
- The public listing detail page shows the confirmed categories + search terms (FINT-03 visible before Phase 7).
- Edit mode round-trips the persisted category/term ids back into the form defaults.
</verification>

<success_criteria>
- getPartCategories() exposes the seeded 2-level tree for the trigger selector.
- createListing/updateListing persist the new dimensions through the existing owner-write trust boundary with NO change to fitment/photo handling.
- getListing + the detail component surface categories + search terms (FINT-03 verifiable).
- An accepted model/config suggestion is provably identical to a manual tag at the action input layer (no second source of truth).
</success_criteria>

<output>
After completion, create `.planning/phases/06-fitment-intelligence/06-02-SUMMARY.md`.
</output>
