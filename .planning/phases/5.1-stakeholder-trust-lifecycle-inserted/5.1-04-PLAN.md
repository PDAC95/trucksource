---
phase: 5.1-stakeholder-trust-lifecycle-inserted
plan: 04
type: execute
wave: 2
depends_on: ["5.1-01"]
files_modified:
  - lib/listings/duplicates.ts
  - components/listings/duplicate-warning.tsx
  - components/listings/listing-form.tsx
  - tests/unit/duplicate-probe.test.ts
autonomous: true
requirements: [LIST-10]
must_haves:
  truths:
    - "On publish attempt, the seller's own non-sold listings are probed for a fuzzy-similar title"
    - "When similar listings are found, a non-blocking warning with links to them is shown before publish"
    - "The seller can always 'Publish anyway' (one extra click); the warning NEVER blocks"
    - "The probe is scoped to the caller's own listings only (seller_id = auth.uid())"
    - "The duplicate query uses the trigram index (no seq scan)"
  artifacts:
    - path: "lib/listings/duplicates.ts"
      provides: "findSimilarOwnListings advisory Server Action wrapping find_similar_own_listings RPC"
      exports: ["findSimilarOwnListings"]
    - path: "components/listings/duplicate-warning.tsx"
      provides: "Non-accusatory warning dialog with links + 'Publish anyway'"
      min_lines: 30
    - path: "tests/unit/duplicate-probe.test.ts"
      provides: "Proof the probe returns matches but never blocks createListing (gate e)"
      min_lines: 30
  key_links:
    - from: "lib/listings/duplicates.ts"
      to: "find_similar_own_listings RPC (0010)"
      via: ".rpc with tuned threshold"
      pattern: "find_similar_own_listings"
    - from: "components/listings/listing-form.tsx"
      to: "lib/listings/duplicates.ts#findSimilarOwnListings"
      via: "probe on submit before createListing; createListing UNCHANGED"
      pattern: "findSimilarOwnListings"
---

<objective>
Ship the soft same-seller duplicate-title warning (LIST-10): a non-blocking, advisory probe shown on publish attempt that the seller can always override.

Purpose: Sellers legitimately may have multiple similar parts, so the warning must remind, never accuse, and never block. It runs as a SEPARATE advisory Server Action the create form calls on submit — `createListing` stays UNCHANGED (research Open Q4), keeping the probe strictly advisory. The match is same `seller_id` + fuzzy-similar title via `pg_trgm`, index-backed (the trigram index landed in 0010), tuned for precision to avoid false-positive fatigue.

Output: the `findSimilarOwnListings` advisory action, the non-accusatory warning dialog with links + "Publish anyway", the create-form wiring, and a mocked test proving the probe never blocks publish (gate e).
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/5.1-stakeholder-trust-lifecycle-inserted/5.1-CONTEXT.md
@.planning/phases/5.1-stakeholder-trust-lifecycle-inserted/5.1-RESEARCH.md

# Patterns / files to extend
@lib/actions/listings.ts
@components/listings/listing-form.tsx
@tests/unit/listing-actions.test.ts

<interfaces>
<!-- Contract from 5.1-01 (Wave 1). The RPC + trigram index already exist in 0010. -->

find_similar_own_listings (SQL function in 0010, SECURITY INVOKER, owner-scoped):
```sql
-- returns (id bigint, title text, sim real) for the CALLER's own non-sold listings
-- with similarity(title, p_title) >= p_threshold, order by sim desc limit 5.
-- SECURITY INVOKER => owner RLS still applies; the caller only ever sees their own rows.
find_similar_own_listings(p_title text, p_threshold real)
```

createListing today (lib/actions/listings.ts) — DO NOT MODIFY. The form already calls it on
publish (components/listings/listing-form.tsx onSubmit → createListing(payload) → router.push).
The duplicate probe is layered BEFORE createListing in the form; createListing is untouched.

listing-form.tsx onSubmit shape (current, lines ~161-220):
```typescript
function onSubmit(values: ListingInput) {
  React.startTransition(async () => {
    // edit branch: updateListing(...)
    const result = await createListing(payload);
    if (result.ok) { toast.success("Listing published"); router.push(`/listings/${result.id}`); }
  });
}
```
The probe must run BEFORE the createListing call on the CREATE path only (not edit), and only
proceed to createListing on first-time match-found AFTER the seller clicks "Publish anyway".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: findSimilarOwnListings advisory action + probe test</name>
  <files>lib/listings/duplicates.ts, tests/unit/duplicate-probe.test.ts</files>
  <action>
**`lib/listings/duplicates.ts`** — `"use server"`. A SINGLE advisory action:
- `findSimilarOwnListings(title: string): Promise<{ id: number; title: string }[]>` — getClaims identity (return `[]` if unauthenticated; advisory only, never throws). Calls `supabase.rpc("find_similar_own_listings", { p_title: title, p_threshold: 0.6 })`. The RPC is SECURITY INVOKER + owner-scoped, so it returns ONLY the caller's own non-sold listings. Map to `{ id, title }` (drop `sim` from the UI payload; keep it internal if useful). On RPC error return `[]` — the probe must NEVER block publish, so a failure degrades to "no warning" (CONTEXT: never blocks).
- Threshold 0.6 is the precision-tuned start (research Open Q2). Keep it a named const `DUPLICATE_SIMILARITY_THRESHOLD = 0.6` with a comment that it's tunable; the RPC takes it as a parameter so no migration is needed to retune. NOTE for the human-verify gate: validate the threshold against the real launch dataset (reordered-word variants like "Hood Peterbilt 379" vs "Peterbilt 379 Hood" score lower on raw trigram similarity — flag if 0.6 misses them and consider word_similarity).

**`tests/unit/duplicate-probe.test.ts`** (mocked client, mirror `tests/unit/listing-actions.test.ts`): prove gate (e) — the probe returns-but-never-blocks:
- `findSimilarOwnListings` returns `[]` when unauthenticated (no rpc call).
- when the mocked rpc returns rows, the action returns mapped `{id,title}[]` (matches available to the UI).
- when the mocked rpc returns an error, the action returns `[]` (degrades, never throws).
- Assert (documenting gate e) that `createListing` is NOT imported/called by `findSimilarOwnListings` — the probe is purely advisory and cannot block publish (the "Publish anyway" path in the form calls createListing independently). A comment in the test ties this to gate (e).
  </action>
  <verify>
  <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npx vitest run tests/unit/duplicate-probe.test.ts</automated>
  </verify>
  <done>findSimilarOwnListings is owner-scoped via the SECURITY INVOKER RPC, threshold 0.6, returns [] on unauth/error (never blocks), maps to {id,title}[]; the unit test proves return-but-never-block and that the probe never calls createListing; tsc + test green.</done>
</task>

<task type="auto">
  <name>Task 2: Non-blocking warning dialog + create-form wiring</name>
  <files>components/listings/duplicate-warning.tsx, components/listings/listing-form.tsx</files>
  <action>
**`components/listings/duplicate-warning.tsx`** — `"use client"`. A shadcn `AlertDialog`/`Dialog` driven by props `{ open, matches: {id,title}[], onPublishAnyway, onCancel }`:
- Non-accusatory copy (CONTEXT: remind, never accuse): e.g. "You already have similar listing(s). If it's the same part, edit the original — if it's a different part, go ahead." (exact wording Claude's discretion).
- List each match as a LINK to `/sell/{id}/edit` (so the seller can view/edit the original) — links open the existing listing.
- Two actions: "Publish anyway" (primary → `onPublishAnyway`) and "Cancel"/"Edit" (→ `onCancel`). NEVER a blocking-only state.

**`components/listings/listing-form.tsx`** — wire the probe into the CREATE path only (leave the edit branch and `createListing` untouched):
- Add state: `duplicateMatches: {id,title}[]`, `pendingPayload`, `showDuplicateDialog`.
- In `onSubmit`, on the CREATE path (not edit), BEFORE calling `createListing`: call `findSimilarOwnListings(values.title)`. If it returns matches AND this is the first attempt (not already confirmed): open the `<DuplicateWarning>` dialog with the matches and stash the payload; do NOT call createListing yet.
- `onPublishAnyway`: close the dialog, then call the existing `createListing(payload)` exactly as today (success toast + redirect). One extra click — never blocks.
- If the probe returns `[]`: proceed straight to `createListing` (no dialog), unchanged behavior.
- Keep the existing onInvalid toast + RHF flow intact. The probe is additive; a probe failure (returns []) must fall through to normal publish.
  </action>
  <verify>
  <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>On create-publish, similar own listings trigger a non-accusatory dialog with edit-links + "Publish anyway"; "Publish anyway" calls the UNCHANGED createListing; no matches → straight publish; edit path untouched; tsc + build green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run build` green; `npx vitest run` full suite green.
- The duplicate-probe unit test proves gate (e): the probe returns matches but never blocks publish, and degrades to [] on error/unauth.
- MANUAL (phase human-verify): `EXPLAIN ANALYZE` on the find_similar_own_listings query on Staging confirms the trigram GIN index is used (no Seq Scan) — Pitfall 4; and the 0.6 threshold is sane against the real launch titles (tune if reordered-word variants slip through).
- Privacy cross-cutting gate: the probe reads only the caller's own listing titles (owner-scoped RPC); no PII, no other seller's data.
</verification>

<success_criteria>
- LIST-10: when the same seller creates a listing similar to one they already have, a soft non-blocking warning with links is shown; the seller can always publish (one extra "Publish anyway" click); a seller may legitimately keep multiple similar parts.
</success_criteria>

<output>
After completion, create `.planning/phases/5.1-stakeholder-trust-lifecycle-inserted/5.1-04-SUMMARY.md`
</output>
