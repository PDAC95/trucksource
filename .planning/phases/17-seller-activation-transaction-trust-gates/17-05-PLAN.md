---
phase: 17-seller-activation-transaction-trust-gates
plan: 05
type: execute
wave: 2
depends_on: ["17-01", "17-03"]
files_modified:
  - app/(public)/listings/[id]/page.tsx
  - components/listings/listing-detail.tsx
  - components/messaging/contact-seller-button.tsx
autonomous: true
requirements: [MSG-01, MSG-05, VERF-02]
must_haves:
  truths:
    - "An authenticated but unverified buyer's Contact Seller button routes to /verify?require=phone&next=/listings/[id]?contact=1 instead of opening the modal"
    - "The unverified button looks identical to the verified state (gate invisible until click)"
    - "On return to the listing as a now-phone-verified buyer with contact=1, the contact modal auto-opens"
    - "The auto-open does not re-fire on plain refresh or for still-unverified users"
  artifacts:
    - path: "app/(public)/listings/[id]/page.tsx"
      provides: "isPhoneVerified derived from the viewer's own profiles_private (added to the existing prefill select)"
      contains: "phone_verified_at"
    - path: "components/messaging/contact-seller-button.tsx"
      provides: "Unverified branch (route to /verify) + contact=1 auto-open on return"
      contains: "require=phone"
  key_links:
    - from: "app/(public)/listings/[id]/page.tsx"
      to: "components/listings/listing-detail.tsx → ContactSellerButton"
      via: "isPhoneVerified prop threaded down"
      pattern: "isPhoneVerified"
    - from: "components/messaging/contact-seller-button.tsx"
      to: "/verify?require=phone"
      via: "unverified-branch Link before state #5"
      pattern: "require=phone"
---

<objective>
Wire the contact gate UX (CONTEXT locked decisions): an authenticated-but-unverified buyer's "Contact Seller" button looks identical to the verified state but, on click, routes to `/verify?require=phone&next=/listings/[id]?contact=1` instead of opening the modal (early gate — nothing to preserve). On return as a now-verified buyer, the modal auto-opens (zero extra clicks). The server `not_verified` (Plan 01) is the trust boundary; this is the UX.

Purpose: Make contacting feel continuous — verify, return, modal already open — while keeping the gate invisible until click (CONTEXT). Anonymous users keep the existing /login redirect.
Output: isPhoneVerified threaded page → ListingDetail → ContactSellerButton; a new unverified render branch + contact=1 auto-open.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-seller-activation-transaction-trust-gates/17-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- All read directly. The isPhoneVerified prop threads page → ListingDetail → ContactSellerButton. -->

app/(public)/listings/[id]/page.tsx (force-dynamic). The viewer's own private read already runs at
lines 104-111 (only when `userId && !isOwner`), selecting first_name,last_name,email,phone for prefill:
```typescript
supabase.from("profiles_private")
  .select("first_name, last_name, email, phone")   // ADD: , phone_verified_at
  .eq("id", userId).maybeSingle();
```
ADD `phone_verified_at` to THIS select (zero extra round-trips), derive
`const isPhoneVerified = Boolean(privateRow?.phone_verified_at)`, and pass it to <ListingDetail>.
Default false for anon/owner.

components/listings/listing-detail.tsx — props (lines 58-76): isOwner, isSold, saved, isAuthenticated,
listingActive, existingThreadId, prefill. ADD `isPhoneVerified?: boolean` (default false). It renders
<ContactSellerButton …/> at line 292 — pass isPhoneVerified through.

components/messaging/contact-seller-button.tsx — current render states (LOCKED order):
  1 owner → null; 2 existing thread → "View conversation"; 3 not active → null;
  4 anon → <Link href={`/login?next=/listings/${listingId}`}>; 5 authed+active → button opens modal.
ADD a new prop `isPhoneVerified: boolean` and a NEW BRANCH between #4 and #5: authenticated but
!isPhoneVerified → render a button IDENTICAL in appearance to #5 (same className, same text "Contact
Seller About This Part") but as a <Link> routing to:
  `/verify?require=phone&next=${encodeURIComponent(`/listings/${listingId}?contact=1`)}`
(mirror the anon #4 Link shape but to /verify, not /login). The verified path (#5) is unchanged.

AUTO-OPEN on return (RESEARCH Q6, Pattern 4): the button is a client component. Use `useSearchParams()`
to read `contact`. Initialize/open the modal when `contact === "1"` AND `isPhoneVerified` is now true.
Strip the param via `router.replace` (drop ?contact=1) after opening so a refresh doesn't re-open
(Pitfall 4). Do NOT auto-open for still-unverified users.

ALL UI copy in ENGLISH. Behavior freeze: do NOT change ContactFormModal, submitContact, or the
contact-before-thread order (invariant #5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive isPhoneVerified on the listing page and thread it down</name>
  <files>app/(public)/listings/[id]/page.tsx, components/listings/listing-detail.tsx</files>
  <action>
page.tsx:
- In the existing `userId && !isOwner` block (lines 104-124), add `phone_verified_at` to the profiles_private select. Cast the row type to include `phone_verified_at: string | null`.
- Derive `const isPhoneVerified = Boolean((privateRow as { phone_verified_at?: string | null } | null)?.phone_verified_at);` (default false outside the block, i.e. for anon/owner).
- Pass `isPhoneVerified={isPhoneVerified}` to `<ListingDetail … />` (line 131 area).
This adds NO new round-trip (same select) and reads only the caller's OWN row (owner RLS) — the
phone_verified_at flag is non-PII-adjacent but already in the buyer's own private row.

listing-detail.tsx:
- Add `isPhoneVerified?: boolean` to the props type + destructure with default `false`.
- Pass `isPhoneVerified={isPhoneVerified}` into `<ContactSellerButton … />` (line 292).
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg -F "phone_verified_at" "app/(public)/listings/[id]/page.tsx" && rg -F "isPhoneVerified" components/listings/listing-detail.tsx</automated>
  </verify>
  <done>isPhoneVerified is derived from the viewer's own row (no extra round-trip) and threaded page → ListingDetail → ContactSellerButton; tsc clean; only the caller's own row is read.</done>
</task>

<task type="auto">
  <name>Task 2: Add the unverified branch + contact=1 auto-open to ContactSellerButton</name>
  <files>components/messaging/contact-seller-button.tsx</files>
  <action>
1. Add `isPhoneVerified: boolean` to the component props type + destructure it.
2. Add a NEW render branch BETWEEN the anon branch (#4, lines 62-70) and the authed+active modal branch (#5, lines 72-90): when `isAuthenticated && !isPhoneVerified`, return a Link styled identically to #5 (CONTEXT: gate invisible until click):
```tsx
if (isAuthenticated && !isPhoneVerified) {
  const next = encodeURIComponent(`/listings/${listingId}?contact=1`);
  return (
    <Button asChild className="mt-2 w-fit">
      <Link href={`/verify?require=phone&next=${next}`}>
        Contact Seller About This Part
      </Link>
    </Button>
  );
}
```
3. Auto-open on return. Import `useSearchParams` and `useRouter`/`usePathname` from next/navigation. Add an effect that opens the modal once when returning verified:
```tsx
const searchParams = useSearchParams();
const router = useRouter();
const pathname = usePathname();
React.useEffect(() => {
  if (searchParams.get("contact") === "1" && isPhoneVerified && !isOwner && listingActive && existingThreadId === null) {
    setOpen(true);
    router.replace(pathname); // strip ?contact=1 so refresh doesn't re-open
  }
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, []);
```
Place the effect with the other hooks (after `useState(open)`), BEFORE the early returns (hooks must run unconditionally). The guards ensure it never fires for owners, sold/expired listings, existing threads, or still-unverified users (Pitfall 4).

Do NOT change ContactFormModal, the verified #5 branch behavior, or any submit path. English only.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg -F "require=phone" components/messaging/contact-seller-button.tsx && rg -F "contact" components/messaging/contact-seller-button.tsx</automated>
  </verify>
  <done>Unverified authed buyers get a /verify?require=phone link identical in appearance to the verified CTA; returning verified with contact=1 auto-opens the modal once and strips the param; never fires for owner/sold/existing-thread/unverified; modal + submitContact untouched; tsc clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Grep confirms the require=phone link and the contact=1 handling.
- Existing e2e/contact specs (and tests/integration/messaging.contract.test.ts) still pass — invariant #5 order unchanged.
- English-only copy; the unverified button text equals the verified text.
</verification>

<success_criteria>
- Unverified authed buyer → click Contact → /verify?require=phone&next=/listings/[id]?contact=1.
- Return verified → modal auto-opens once, param stripped.
- Anon still routes to /login; owner/sold/existing-thread states unchanged.
- The server-action not_verified (Plan 01) is the authority; this is UX convenience.
</success_criteria>

<output>
After completion, create `.planning/phases/17-seller-activation-transaction-trust-gates/17-05-SUMMARY.md`
</output>
