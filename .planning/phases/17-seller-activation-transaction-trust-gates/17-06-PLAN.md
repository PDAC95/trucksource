---
phase: 17-seller-activation-transaction-trust-gates
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/site-header.tsx
  - components/layout/user-menu.tsx
  - components/layout/mobile-menu.tsx
  - app/(app)/account/page.tsx
autonomous: true
requirements: [VERF-02, VERF-03, VERF-04]
must_haves:
  truths:
    - "Logged-in users see a Sell entry in the desktop header that links to /sell"
    - "Logged-in users see My Listings (/sell/listings) and Account (/account) in the user-menu dropdown"
    - "The mobile menu lists Sell, My Listings, and Account for logged-in users"
    - "Unverified users see a 'Become a verified seller' card on /account linking to /verify?require=seller"
  artifacts:
    - path: "components/layout/site-header.tsx"
      provides: "Always-visible Sell header action (desktop icon row)"
      contains: "/sell"
    - path: "components/layout/user-menu.tsx"
      provides: "My Listings + Account dropdown items"
      contains: "/sell/listings"
    - path: "components/layout/mobile-menu.tsx"
      provides: "Sell / My Listings / Account mobile rows"
      contains: "/account"
    - path: "app/(app)/account/page.tsx"
      provides: "Become-a-verified-seller CTA for unverified users"
      contains: "/verify?require=seller"
  key_links:
    - from: "components/layout/user-menu.tsx"
      to: "/sell/listings and /account"
      via: "DropdownMenuItem links"
      pattern: "/sell/listings"
    - from: "app/(app)/account/page.tsx"
      to: "/verify?require=seller"
      via: "conditional CTA card when unverified"
      pattern: "/verify\\?require=seller"
---

<objective>
Make selling and verifying discoverable. Add the functional nav entries the design doc calls for — Sell (header + mobile + dropdown), My Listings, Account — styled with CURRENT tokens, plus a proactive "Become a verified seller" CTA on /account for unverified users. These are functional entries only; the v1.1 rebrand (CHRM, Phases 11-15) repositions/restyles them later (CONTEXT deferred: final header design).

Purpose: Today the entry points to selling/verifying are orphaned (no nav link to /sell, /account, /verify). This closes the discoverability gap without owning the final visual design.
Output: Three nav surfaces updated + the account CTA.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-seller-activation-transaction-trust-gates/17-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Nav surfaces read directly. NOTE: nav-icon-link.tsx, mobile-menu.tsx are untracked v1.1-in-progress
     files that ALREADY EXIST and are wired into the header. Build on them; do not recreate. -->

components/layout/nav-icon-link.tsx — existing reusable header icon link:
```typescript
<NavIconLink href="/sell" label="Sell" /* exact?: boolean, className?, children: ReactNode */ >
  <SomeLucideIcon className="size-6" />
</NavIconLink>
```
It renders a ghost icon Button with cyan neon glow + tooltip from `label`, active-aware.

site-header.tsx — desktop icon row (lines 56-64), logged-in branch renders:
`<MessagesBadge userId={userId} /> <SavedHeartIcon /> <UserMenu username={username} />`.
Add the Sell entry here. Per CONTEXT it is the PRIMARY conversion → always-visible. Sell may render for
anonymous users too (the (app) layout auth-gates /sell → /login). Use NavIconLink with a lucide icon
(Tag or PlusCircle) and href="/sell".

user-menu.tsx — Radix dropdown. Existing "My profile" item pattern (lines 72-77):
```typescript
<DropdownMenuItem asChild className="gap-3 py-2.5 text-sm">
  <a href={`/u/${username}`}><UserIcon className="size-4" />My profile</a>
</DropdownMenuItem>
```
Add "My Listings" (/sell/listings) and "Account" (/account) items mirroring this, above the separator/logout.
Lucide icons: List (My Listings), Settings (Account).

mobile-menu.tsx — uses a local `MenuLink({ href, icon, label })` helper (lines 108-128). Logged-in list
at lines 52-86 (Messages, Saved, My profile, Log out). Add three MenuLink rows: Sell, My Listings, Account.
Lucide icons consistent with the dropdown (Tag/PlusCircle, List, Settings).

app/(app)/account/page.tsx — already reads profiles_public (line 33) + profiles_private
(message_email_opt_out, line 39). Add phone_verified_at + marketplace_terms_accepted_at to the
profiles_private select, derive `isVerifiedSeller`, and render a CTA card when NOT verified linking
/verify?require=seller. Scoped to /account ONLY (CONTEXT deferred: NOT the public profile).

ALL UI copy in ENGLISH (CLAUDE.md). Labels exactly: "Sell", "My Listings", "Account".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Sell entry to the desktop header + the mobile menu rows</name>
  <files>components/layout/site-header.tsx, components/layout/mobile-menu.tsx</files>
  <action>
site-header.tsx:
- Import a lucide icon (e.g. `Tag` or `PlusCircle`) and `NavIconLink`.
- In the desktop icon row (the `sm:flex` block, lines 56-64), add `<NavIconLink href="/sell" label="Sell"><Tag className="size-6" /></NavIconLink>` as the FIRST item in the logged-in branch (primary conversion, leftmost of the action cluster) — OR render it for both auth states if trivially placed before the auth split. Keep MessagesBadge/SavedHeartIcon/UserMenu order otherwise unchanged.
- Do not touch HeaderSearch, the logo, or the anonymous Sign in/Register buttons.

mobile-menu.tsx:
- Import lucide `Tag`/`PlusCircle`, `List`, `Settings`.
- In the logged-in `<nav>` list (lines 52-86), add three `<MenuLink>` rows using the existing helper: Sell (`/sell`, Tag icon), My Listings (`/sell/listings`, List icon), Account (`/account`, Settings icon). Place Sell first (primary), then Messages/Saved/My profile as today, then My Listings + Account before Log out — or group the three new selling/account rows together; keep Log out last. Match the existing MenuLink markup exactly (it wraps in SheetClose).

English labels only.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg -F "/sell" components/layout/site-header.tsx components/layout/mobile-menu.tsx && rg -F "/sell/listings" components/layout/mobile-menu.tsx</automated>
  </verify>
  <done>Header has an always-visible Sell action; mobile menu has Sell, My Listings, Account rows for logged-in users; tsc clean; no Spanish strings.</done>
</task>

<task type="auto">
  <name>Task 2: Add My Listings + Account to the user-menu dropdown</name>
  <files>components/layout/user-menu.tsx</files>
  <action>
In user-menu.tsx, import lucide `List` and `Settings`. Add two `DropdownMenuItem asChild` rows mirroring the existing "My profile" item (lines 72-77), placed ABOVE the separator that precedes Log out:
```tsx
<DropdownMenuItem asChild className="gap-3 py-2.5 text-sm">
  <a href="/sell/listings"><List className="size-4" />My Listings</a>
</DropdownMenuItem>
<DropdownMenuItem asChild className="gap-3 py-2.5 text-sm">
  <a href="/account"><Settings className="size-4" />Account</a>
</DropdownMenuItem>
```
Keep "My profile", the separators, and the Log out form exactly as-is. English labels only. Do not change the trigger button or the header card.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg -F "/sell/listings" components/layout/user-menu.tsx && rg -F "/account" components/layout/user-menu.tsx</automated>
  </verify>
  <done>Dropdown shows My Listings (/sell/listings) and Account (/account) alongside My profile + Log out; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Become-a-verified-seller CTA to /account</name>
  <files>app/(app)/account/page.tsx</files>
  <action>
In app/(app)/account/page.tsx:
1. Extend the `profiles_private` select (line 39) to also read `phone_verified_at, marketplace_terms_accepted_at` (owner RLS — the caller's own row; these are non-PII verification timestamps).
2. Derive `const isVerifiedSeller = Boolean(priv?.phone_verified_at) && Boolean(priv?.marketplace_terms_accepted_at);`
3. When `!isVerifiedSeller`, render a CTA card ABOVE the settings forms (inside the `mt-8 grid gap-10` block or just before it) using existing UI primitives (Card or a bordered div + Button). Copy (English): heading "Become a verified seller", body "Verify your phone and accept the marketplace terms to publish listings and build buyer trust.", and a primary `<Button asChild><Link href="/verify?require=seller">Become a verified seller</Link></Button>`.
4. When verified, render nothing extra (or an optional subtle "Verified seller" confirmation — keep it minimal; the CTA is the deliverable).

Use current tokens (no hardcoded hex — token discipline). Scope to /account ONLY; do NOT add this to the public profile (CONTEXT deferred). English only.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg -F "/verify?require=seller" "app/(app)/account/page.tsx"</automated>
  </verify>
  <done>Unverified users see a "Become a verified seller" card on /account linking /verify?require=seller; verified users don't; the private read stays owner-scoped to non-PII flags; tsc clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Grep: Sell in header + mobile; My Listings + Account in dropdown + mobile; /verify?require=seller on /account.
- No Spanish strings in any touched file (English-only UI invariant): `rg -i "vender|cuenta|mis anuncios" components/layout app/\(app\)/account` returns nothing.
- Token discipline: no new hardcoded hex in the account CTA.
</verification>

<success_criteria>
- Sell, My Listings, Account are discoverable on desktop (header + dropdown) and mobile.
- /account offers the verified-seller CTA to unverified users only.
- Styled with current tokens; rebrand restyles later (no final-design ownership here).
</success_criteria>

<output>
After completion, create `.planning/phases/17-seller-activation-transaction-trust-gates/17-06-SUMMARY.md`
</output>
