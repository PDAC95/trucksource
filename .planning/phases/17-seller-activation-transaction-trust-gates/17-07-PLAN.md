---
phase: 17-seller-activation-transaction-trust-gates
plan: 07
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-05", "17-06"]
files_modified:
  - e2e/trust-gates.spec.ts
autonomous: false
requirements: [LIST-01, MSG-01, MSG-05, VERF-02, VERF-03, VERF-04]
user_setup:
  - service: vercel
    why: "OTP spend-cap threshold for the now-wider buyer+seller audience (env-only, no code change)"
    env_vars:
      - name: OTP_SEND_DAILY_CAP
        source: "Vercel project → Settings → Environment Variables (set explicitly, e.g. 300–500; unset defaults to 200)"
  - service: twilio
    why: "Billing/usage alert to pair with the wider verification audience"
    dashboard_config:
      - task: "Confirm a usage-trigger billing alert exists for Verify spend"
        location: "Twilio Console → Monitor → Usage / Billing alerts"
must_haves:
  truths:
    - "e2e asserts the gate routing: unverified Publish redirects to /verify (require=seller); unverified Contact routes to /verify (require=phone)"
    - "e2e asserts the nav entries exist (Sell, My Listings, Account) for a logged-in user"
    - "The full Playwright suite is green (no regression to existing specs)"
    - "OTP_SEND_DAILY_CAP is confirmed set on Vercel for the buyer+seller surface"
    - "BotID protect-path coverage of the /verify Server-Action POST is confirmed by a prod smoke test"
  artifacts:
    - path: "e2e/trust-gates.spec.ts"
      provides: "Deterministic gate-routing + nav-presence assertions (no external SMS/BotID network)"
      contains: "require=seller"
  key_links:
    - from: "e2e/trust-gates.spec.ts"
      to: "/verify?require=seller and /verify?require=phone"
      via: "URL assertions after Publish/Contact while unverified"
      pattern: "require="
---

<objective>
Lock the phase behavior with e2e and confirm the two ops items the design doc flagged ("confirm, don't rebuild"). The e2e covers the deterministic slices (gate routing + nav presence) in CI without external SMS/BotID network — mirroring the existing verify-wizard.spec posture; the live-SMS happy path and the BotID prod-matching are validated at the human checkpoint. Plus: confirm OTP_SEND_DAILY_CAP is set on Vercel for the wider audience and a Twilio billing alert exists.

Purpose: Prove the trust boundary holds end-to-end and the anti-abuse pipeline (already built) covers buyers+sellers; no anti-abuse code is rebuilt.
Output: e2e/trust-gates.spec.ts + confirmed env/dashboard ops.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-seller-activation-transaction-trust-gates/17-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- e2e conventions read directly from e2e/auth.spec.ts + e2e/verify-wizard.spec.ts. -->

- Runner: `npm run test:e2e` (playwright test). Spec files live in e2e/.
- Auth legs `test.skip` cleanly when E2E_TEST_EMAIL / E2E_TEST_PASSWORD are absent (a CONFIRMED account).
- Live-SMS + BotID legs are MANUAL (BotID is prod-only / always non-bot locally; SMS needs live Twilio) —
  validated at the human checkpoint, NOT in CI (verify-wizard.spec documents this exact split).
- Staging test accounts (memory/STATE): the VERIFIED seller is RenamedTrucker/12gapatricio; the
  buyer+admin is PatricioBuyer/pdmckinster (pw TakeOff2026!). For the UNVERIFIED-gate legs an account
  WITHOUT phone_verified_at is needed — register a fresh confirmed account, or assert the routing with a
  Radix/role-based selector flow that doesn't require completing OTP.
- Spend cap: lib/verify/ratelimit.ts dailyCap() reads process.env.OTP_SEND_DAILY_CAP, default 200.
  This is an ENV value on Vercel — Claude cannot set production env; it's a user_setup confirm item.
- BotID: instrumentation-client.ts protect:[{path:"/verify",method:"POST"}]; sendOtp calls checkBotId().
  Prod-only enforcement — confirm via prod smoke test (MEDIUM confidence flagged in research).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write e2e/trust-gates.spec.ts — gate routing + nav presence (deterministic)</name>
  <files>e2e/trust-gates.spec.ts</files>
  <action>
Create `e2e/trust-gates.spec.ts` mirroring the verify-wizard.spec posture: assert the DETERMINISTIC gate routing and nav presence with no external SMS/BotID network. Use role-based selectors (getByRole) like the existing specs.

Cover (auth legs `test.skip` when E2E creds absent):
1. NAV PRESENCE (logged-in): after login, the header shows a "Sell" affordance linking /sell; the user menu (open the dropdown) shows "My Listings" → /sell/listings and "Account" → /account. Assert via role/name + href.
2. SELL GATE ROUTING (unverified account): on /sell, the unverified banner is visible; clicking Publish (after filling the minimum required fields, OR asserting the intercept fires) navigates to a URL matching `/verify` with `require=seller` and `next=/sell`. If completing the form is heavy, assert the banner + that the verify link/redirect carries require=seller (a lighter assertion is acceptable — the server gate is unit-covered in Plan 01).
3. CONTACT GATE ROUTING (unverified buyer on an active listing): the "Contact Seller About This Part" button, when clicked, navigates to a URL matching `/verify` with `require=phone` and an encoded `next=/listings/...contact=1`. Assert the destination URL.
4. ANON unchanged: an anonymous user's Contact button still routes to /login?next=… (regression guard).

Keep every leg that needs a verified/unverified live account behind `test.skip` when the relevant env is absent, exactly like auth.spec.ts. Document in a header comment (mirroring verify-wizard.spec) that the LIVE happy path (real OTP round-trip → return → publish/contact) and BotID matching are validated MANUALLY at the plan checkpoint.

Do NOT touch existing specs except trivial brand-string drift if a selector breaks (and if so, update it in the same commit — same-commit test discipline).
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && npx playwright test e2e/trust-gates.spec.ts --reporter=line 2>&1 | tail -25</automated>
  </verify>
  <done>e2e/trust-gates.spec.ts asserts nav presence + sell/contact gate routing (require=seller / require=phone) + anon-login regression; the deterministic legs pass and live legs skip cleanly without creds; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full e2e suite as a regression gate</name>
  <files>e2e/trust-gates.spec.ts</files>
  <action>
Run the WHOLE Playwright suite (`npm run test:e2e`) to confirm no existing spec regressed — Phase 17 added gates + nav, which can shift routing/selectors the existing auth/home/verify-wizard/public-profile specs assert. Per the v1.1 cross-cutting gate: any diff to a behavior-oracle spec must land in the same commit as the surface it covers.

If a pre-existing failure is hit (STATE.md notes auth.spec:95 — unauth /→/login redirect — was already red at HEAD before this phase), confirm it is the SAME known failure and not newly introduced; log it to the phase deferred-items if so. Newly introduced failures MUST be fixed before the checkpoint.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx playwright test --reporter=line 2>&1 | tail -30</automated>
  </verify>
  <done>The full suite is green except the documented pre-existing auth.spec failure (if still present); no NEW failures introduced by Phase 17.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm ops (OTP spend cap + Twilio alert) and validate the live happy paths + BotID</name>
  <action>Human-verify checkpoint. Claude has shipped all code, the migration, and the e2e; the remaining items are not automatable from here: a production env value (OTP_SEND_DAILY_CAP on Vercel), a Twilio billing alert, the two live OTP-round-trip happy paths, and the prod-only BotID matching confirmation. The user performs the steps in how-to-verify and reports back.</action>
  <what-built>
The full Phase 17 trust-gate wiring is deployed: server-action gates (Plan 01), RLS backstop on Staging (Plan 02), parameterized /verify (Plan 03), sell-gate UI + draft (Plan 04), contact-gate UI + auto-open (Plan 05), nav entries + account CTA (Plan 06), and the e2e suite (Tasks 1-2). Two ops items remain that Claude cannot do (production env + live anti-abuse confirmation).
  </what-built>
  <how-to-verify>
1. OTP_SEND_DAILY_CAP (Vercel): In the Vercel project → Settings → Environment Variables, confirm `OTP_SEND_DAILY_CAP` is set explicitly for the buyer+seller surface (recommend 300–500). If unset it defaults to 200, which research flags as low for the wider audience. Set it and redeploy if needed.
2. Twilio billing alert: In Twilio Console → Monitor → Usage/Billing, confirm a usage-trigger alert exists for Verify spend (pairs with the wider audience).
3. SELL happy path (live, on Staging/prod with the unverified test account): go to /sell, fill the form, click Publish → you land on /verify (require=seller). Complete the REAL OTP + accept terms → you return to /sell → the form is rehydrated (everything except photos) with the "re-attach photos" notice → re-attach a photo → Publish succeeds → the draft is gone (refresh /sell: clean form).
4. CONTACT happy path (live): as an unverified buyer on an active listing, click "Contact Seller" → /verify (require=phone) → complete OTP ONLY (no terms required) → you return to the listing → the contact modal auto-opens → submit → thread opens. Refresh the listing: modal does NOT re-open (param stripped).
5. BotID smoke (prod): confirm the BotID protect rule actually scores the /verify Server-Action POST in production (e.g. a known-bot/script request to the OTP send is challenged/blocked, a normal browser is not). This is the MEDIUM-confidence item research flagged — confirm, don't rebuild.
6. RLS backstop (optional, strong): with the public anon key, attempt a raw insert into listings as an unverified user and into contact_log without phone — both must be rejected by RLS (Plan 02 backstop).
  </how-to-verify>
  <resume-signal>Type "approved" once the env cap is set, the two live happy paths work, and BotID coverage is confirmed — or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- `npm run test:e2e` green (minus the documented pre-existing auth.spec failure); trust-gates.spec asserts the gate routing + nav presence.
- OTP_SEND_DAILY_CAP confirmed set on Vercel; Twilio billing alert confirmed.
- Live sell + contact happy paths verified at the checkpoint; BotID prod coverage confirmed.
</verification>

<success_criteria>
- e2e proves: unverified→publish routed to verify(require=seller); unverified→contact routed to verify(require=phone); nav entries present; anon→login unchanged.
- The wider buyer+seller audience is covered by the EXISTING anti-abuse pipeline with an explicit spend cap (no rebuild).
- Both end-to-end happy paths (draft-preserving publish; auto-open contact) work on real verification.
</success_criteria>

<output>
After completion, create `.planning/phases/17-seller-activation-transaction-trust-gates/17-07-SUMMARY.md`
</output>
