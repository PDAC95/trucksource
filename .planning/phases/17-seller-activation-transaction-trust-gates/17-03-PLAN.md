---
phase: 17-seller-activation-transaction-trust-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/verify/page.tsx
  - app/(app)/verify/otp-step.tsx
autonomous: true
requirements: [VERF-02, VERF-03, VERF-04]
must_haves:
  truths:
    - "With ?require=phone, once phone is verified the wizard redirects to ?next without forcing the terms step"
    - "With ?require=seller (or absent), the wizard requires phone AND terms before redirecting to ?next"
    - "On completion the wizard redirects to a safe internal ?next path; an unsafe/external next falls back to the existing panel"
    - "The OTP step's Change-number navigation preserves next and require"
  artifacts:
    - path: "app/(app)/verify/page.tsx"
      provides: "Level-aware completion + safe-next redirect; ?next and ?require parsed"
      contains: "require"
    - path: "app/(app)/verify/otp-step.tsx"
      provides: "Change-number link carries next/require forward"
  key_links:
    - from: "app/(app)/verify/page.tsx"
      to: "redirect(next)"
      via: "safeNext guard on completion"
      pattern: "redirect\\("
    - from: "app/(app)/verify/otp-step.tsx"
      to: "/verify?change=1"
      via: "preserve next/require query params on the change-number push/link"
      pattern: "change=1"
---

<objective>
Parameterize the existing `/verify` wizard with `?next=` (return target) and `?require=phone|seller` (required level) so the Wave-2 gate redirects can drop a user into verification and bounce them back to where they came from. The phone→OTP→terms flow internals are UNCHANGED — the wizard just becomes aware of the required level and the return destination (today it always dead-ends at a "go to dashboard" link).

Purpose: A phone-only gate (contact) must return after OTP without forcing selling terms; a seller gate (publish) must require terms too. Both must return to their origin, not "/".
Output: Level-aware completion + open-redirect-guarded redirect in page.tsx; param-preserving navigation in otp-step.tsx.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-seller-activation-transaction-trust-gates/17-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- The wizard, read directly. force-dynamic; the profiles_private row is the single source of truth for "which step." -->

app/(app)/verify/page.tsx today:
- searchParams typed `{ change?: string }` (line 25) — extend to `{ change?: string; next?: string; require?: string }`.
- Reads phone, phoneVerified, termsAccepted from profiles_private (lines 34-42).
- Completion branch today (line 48): `if (phoneVerified && termsAccepted)` → renders a panel with `<Link href="/">Go to your dashboard</Link>`.
- The else-if at line 60 (`phoneVerified`) routes to <TermsStep/>; line 65 routes to <OtpStep/>; else <PhoneStep/>.
- Uses `import { redirect } from "next/navigation"` ALREADY (line 2).

otp-step.tsx: the "Change number" action does `router.push("/verify?change=1")` (RESEARCH Q1, ~line 130).
Other step components call `router.refresh()` after their action — refresh PRESERVES the URL (good); only explicit push needs the params carried.

Open-redirect guard (RESEARCH Pattern 2): a next is safe only if it starts with "/" AND not "//" AND has no scheme.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Level-aware completion + safe-next redirect in verify/page.tsx</name>
  <files>app/(app)/verify/page.tsx</files>
  <action>
1. Extend the searchParams type to `{ change?: string; next?: string; require?: string }` and destructure `next` and `require` alongside `change`.
2. Add a module-scope safe-next guard:
```typescript
function safeNext(n?: string): string | null {
  return n && n.startsWith("/") && !n.startsWith("//") ? n : null;
}
```
3. Compute the required level and completion:
```typescript
const requireTerms = require !== "phone"; // default "seller" requires terms
const done = phoneVerified && (!requireTerms || termsAccepted);
```
4. Make completion redirect-aware. BEFORE the existing step-branching block, when `done` is true and a safe next exists, redirect there:
```typescript
if (done) {
  const target = safeNext(next);
  if (target) redirect(target);
  // else fall through to the existing "You're a verified seller" panel (line 48 branch unchanged)
}
```
5. CRITICAL — skip the terms step under require=phone: the current `else if (phoneVerified)` branch (line 60) routes to <TermsStep/>. Guard it so that when `require === "phone"` and phone is verified, we DON'T route to terms (the `done` redirect above already handled it; if next was unsafe, fall to the panel). Concretely, only route to TermsStep when `requireTerms && phoneVerified && !termsAccepted`.

Keep the existing panel branch for the no-next / require=seller-complete case. Do NOT change the phone/OTP step rendering, headings, or the force-dynamic export. Validate `next` only via safeNext — never trust it raw (it's attacker-controllable; the guard blocks open-redirect phishing).
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg "safeNext|requireTerms" "app/(app)/verify/page.tsx"</automated>
  </verify>
  <done>page.tsx parses next/require; require=phone completes at phone-verified and skips terms; require=seller requires terms; safeNext guards the redirect; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve next/require across the OTP Change-number navigation</name>
  <files>app/(app)/verify/otp-step.tsx</files>
  <action>
The OTP step needs the gate params so "Change number" doesn't drop them (Pitfall 2 — user finishes verifying and lands on "/" instead of back at /sell or the listing).

1. Make otp-step read the current next/require. Since it's a client component, use `useSearchParams()` from `next/navigation` to read `next` and `require` (do NOT add new props if the param is already in the URL — `router.refresh()` preserved it).
2. Change the "Change number" navigation from `router.push("/verify?change=1")` to carry the params forward, e.g.:
```typescript
const sp = useSearchParams();
const next = sp.get("next");
const require = sp.get("require");
const params = new URLSearchParams({ change: "1" });
if (next) params.set("next", next);
if (require) params.set("require", require);
router.push(`/verify?${params.toString()}`);
```
3. Confirm no OTHER explicit `router.push`/`<Link>` to `/verify` inside otp-step (or phone-step) drops the params. `router.refresh()` calls are fine — leave them. If phone-step has an explicit push to /verify, apply the same param-carry; otherwise leave phone-step untouched.

Do NOT change the OTP submit/verify logic or the anti-abuse calls.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npx tsc --noEmit && rg "useSearchParams|change.*1" "app/(app)/verify/otp-step.tsx"</automated>
  </verify>
  <done>Change-number navigation preserves next and require; tsc clean; OTP verify logic unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Manual reasoning: `/verify?require=phone&next=/listings/5?contact=1` → after OTP, redirects to the listing (no terms). `/verify?require=seller&next=/sell` → requires terms, then redirects to /sell.
- e2e coverage (the round-trips) lands in Plan 07; existing e2e/verify-wizard.spec.ts must still pass.
</verification>

<success_criteria>
- ?require=phone returns after OTP without forcing terms; ?require=seller requires phone + terms.
- ?next redirects only to safe internal paths; unsafe next falls back to the panel.
- next/require survive the OTP Change-number round-trip.
- Wizard internals (phone/OTP/terms steps, anti-abuse) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/17-seller-activation-transaction-trust-gates/17-03-SUMMARY.md`
</output>
