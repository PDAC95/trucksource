---
phase: 11-brand-foundation-token-system
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/(auth)/register/page.tsx
  - app/(auth)/check-email/page.tsx
  - app/(auth)/auth-code-error/page.tsx
  - app/(auth)/login/page.tsx
  - app/(app)/layout.tsx
  - components/layout/site-header.tsx
  - components/account/suspended-screen.tsx
  - README.md
  - package.json
  - package-lock.json
  - e2e/home.spec.ts
  - e2e/auth.spec.ts
autonomous: true
requirements: [BRND-01]
must_haves:
  truths:
    - "Every user-visible Take-Off Parts string reads OG Truck Parts — auth pages, login copy, both header wordmarks, suspended screen, README heading"
    - "package.json visible name is og-truck-parts and package-lock mirrors it"
    - "Zero Take-Off Parts / Create Next App product-copy hits remain in app, components, README, package.json, e2e"
    - "e2e brand-string assertions are updated in the SAME commit as the wordmark rename, and the home spec asserts a DOM element that actually exists"
    - "lib/* email senders are left untouched (Phase 15 deferred)"
  artifacts:
    - path: "components/layout/site-header.tsx"
      provides: "Header wordmark text OG Truck Parts (link)"
      contains: "OG Truck Parts"
    - path: "e2e/home.spec.ts"
      provides: "Brand assertion reconciled to a real DOM element"
      contains: "OG Truck Parts"
  key_links:
    - from: "components/layout/site-header.tsx wordmark link"
      to: "e2e/auth.spec.ts getByRole link assertions"
      via: "link accessible name 'OG Truck Parts'"
      pattern: "OG Truck Parts"
    - from: "e2e/home.spec.ts assertion"
      to: "actual home DOM (header wordmark link)"
      via: "getByRole link not heading"
      pattern: "getByRole\\(\"link\""
---

<objective>
Sweep every user-visible "Take-Off Parts" / "Create Next App" product-copy string to "OG Truck Parts" across auth pages, login copy, both header wordmarks, the suspended screen, README, and the package `name` — and update the e2e brand-string assertions in the same commit so the suite stays a behavior oracle. Reconcile the `home.spec.ts` heading/link role mismatch the researcher flagged.

Purpose: BRND-01 (product named OG Truck Parts everywhere visible; infra/repo slugs unchanged) + the same-commit-test-updates gate.
Output: A clean brand-string sweep with green e2e and a regenerated lockfile.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-brand-foundation-token-system/11-CONTEXT.md
@.planning/phases/11-brand-foundation-token-system/11-RESEARCH.md

<rename_map>
Grep-verified file:line map (RESEARCH §Brand Sweep, confirmed live this session). Replace ONLY these:

| File:line | Current | -> |
| app/(auth)/register/page.tsx:5 | "Create your account · Take-Off Parts" | "Create your account · OG Truck Parts" |
| app/(auth)/check-email/page.tsx:7 | "Check your email · Take-Off Parts" | "Check your email · OG Truck Parts" |
| app/(auth)/auth-code-error/page.tsx:6 | "Link invalid · Take-Off Parts" | "Link invalid · OG Truck Parts" |
| app/(auth)/login/page.tsx:64 | "Log in to your Take-Off Parts account" | "Log in to your OG Truck Parts account" |
| components/layout/site-header.tsx:33 | wordmark "Take-Off Parts" (a Link, role=link) | "OG Truck Parts" |
| components/account/suspended-screen.tsx:35 | wordmark "Take-Off Parts" | "OG Truck Parts" |
| app/(app)/layout.tsx:53 | inline read-only header "Take-Off Parts" | "OG Truck Parts" |
| package.json:2 | "name": "take-off-parts" | "og-truck-parts" |
| README.md:1 | "# Take-Off Parts" | "# OG Truck Parts" |

UI COPY IS ENGLISH (NA market) — these are all already English; do not introduce Spanish.
</rename_map>

<do_not_touch>
EXCLUDED — Phase 15 deferred (need Staging send evidence). Leave EXACTLY as-is:
- lib/admin/email.ts, lib/actions/admin/enforcement.ts, lib/messaging/notify.ts, lib/verify/alert.ts
- app/api/cron/near-expiry/route.ts:123 ("...Sign in to Take-Off Parts..." + https://takeoffparts.com URL) — Phase 15.
LEAVE ALONE — not brand copy:
- tests/integration/* takeoffparts.gsd+...@gmail.com (real test mailbox)
- check.md:439 "OEM Take-Off Parts" (industry term = a type of part, not the product)
Infra untouched: repo name, Vercel/Supabase slugs, env vars, routes — NOT renamed (BRND-01).
This plan does NOT touch app/layout.tsx (root metadata) — that is Plan 02's file. No overlap.
</do_not_touch>

<e2e_reconciliation>
RESEARCH Pitfall 4 (confirmed live): `e2e/home.spec.ts:6` asserts `getByRole("heading", { name: "Take-Off Parts" })` but the home `<h1>` is "Find your part" (app/(public)/page.tsx:119) — the only "Take-Off Parts" on `/` is the header LINK (role=link). Do NOT blindly rename inside a `heading` role that doesn't match the DOM. RECONCILE: assert the header wordmark LINK by accessible name "OG Truck Parts" (matches auth.spec's link assertions for consistency).
e2e/auth.spec.ts:125,131 already assert `getByRole("link", { name: "Take-Off Parts" })` -> rename name to "OG Truck Parts".
</e2e_reconciliation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename product copy in app + components + README</name>
  <files>app/(auth)/register/page.tsx, app/(auth)/check-email/page.tsx, app/(auth)/auth-code-error/page.tsx, app/(auth)/login/page.tsx, app/(app)/layout.tsx, components/layout/site-header.tsx, components/account/suspended-screen.tsx, README.md</files>
  <action>
Apply the exact replacements in the rename_map above — each is a single user-visible string "Take-Off Parts" -> "OG Truck Parts" (auth title suffixes keep the "· " separator; login keeps "Log in to your ... account"; both header wordmarks and the suspended-screen wordmark become "OG Truck Parts"; README h1). Do NOT touch lib/*, the near-expiry cron, tests/integration, or check.md. Do NOT touch root app/layout.tsx (Plan 02). Keep the site-header wordmark as a `<Link href="/">` (role=link) — Plan 04 adds the logo image beside it; here it stays text-only.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && ! rg -n "Take-Off Parts" app/\(auth\) app/\(app\)/layout.tsx components/layout/site-header.tsx components/account/suspended-screen.tsx README.md && rg -c "OG Truck Parts" components/layout/site-header.tsx components/account/suspended-screen.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>All 7 app/component strings + README heading read "OG Truck Parts"; no "Take-Off Parts" remains in those files; lib/* + cron untouched; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename package name + regenerate lockfile</name>
  <files>package.json, package-lock.json</files>
  <action>
Set `package.json` line 2 `"name": "take-off-parts"` -> `"name": "og-truck-parts"` (visible name only — this is NOT the repo/Vercel slug, which stays). Run `npm install` (no new deps) to regenerate `package-lock.json` so its `name` fields (lines 2 and 8) mirror the new name and avoid lockfile drift (RESEARCH Pitfall 5). If Plan 01 already added the `check:contrast` script and Plan 02 already removed next-themes, this install simply reconciles the lock — that's fine.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && node -e "const p=require('./package.json'); process.exit(p.name==='og-truck-parts'?0:1)" && ! rg -n "\"take-off-parts\"" package.json package-lock.json && echo NAME_OK</automated>
  </verify>
  <done>package.json name is "og-truck-parts"; no "take-off-parts" string in package.json or package-lock.json; lockfile regenerated cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Update e2e brand assertions atomically (same-commit gate)</name>
  <files>e2e/home.spec.ts, e2e/auth.spec.ts</files>
  <action>
Reconcile + rename the brand assertions IN THE SAME COMMIT as the wordmark rename (Task 1) — these must land together so the suite stays a true behavior oracle (same-commit-test-updates gate):

- `e2e/home.spec.ts:6`: change `page.getByRole("heading", { name: "Take-Off Parts" })` -> `page.getByRole("link", { name: "OG Truck Parts" })` (the header wordmark is a link, not a heading — Pitfall 4 fix). Update the test title if it says "brand heading" -> "brand wordmark".
- `e2e/auth.spec.ts:125` and `:131`: change link name `"Take-Off Parts"` -> `"OG Truck Parts"` (both, pre- and post-reload).

NOTE on execution ordering: Task 1, 2, and 3 should be committed together (one atomic commit) because the e2e assertions verify the strings Task 1 changes — splitting them would leave the suite red between commits, violating the gate. Run the e2e suite after all three edits.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && rg -n "getByRole\(\"link\", \{ name: \"OG Truck Parts\" \}\)" e2e/home.spec.ts e2e/auth.spec.ts && ! rg -n "Take-Off Parts" e2e && npm run test:e2e 2>&1 | tail -15</automated>
  </verify>
  <done>home.spec asserts the wordmark LINK (not heading) by name "OG Truck Parts"; both auth.spec link assertions renamed; no "Take-Off Parts" in e2e; `npm run test:e2e` passes (brand assertions green against the renamed DOM).</done>
</task>

</tasks>

<verification>
- Gate 2 (brand sweep, scoped): `rg -i "take-off parts|takeoff parts|create next app" app components README.md package.json e2e` returns ZERO hits (lib/ intentionally excluded — Phase 15 email deferral). Note: app/layout.tsx scaffold strings are removed by Plan 02; this gate is fully green only after BOTH 02 and 03 land.
- Gate 5 (e2e green, atomic): `npm run test:e2e` passes with brand assertions updated in the same commit; `npm run test` (vitest) untouched + green (behavior-freeze).
- Pitfall 5: package-lock has no `take-off-parts`.
</verification>

<success_criteria>
- BRND-01: zero remaining "Take-Off Parts"/"Create Next App" product strings in app/components/README/package.json/e2e (success criterion #1).
- Same-commit-test gate honored: e2e brand assertions updated atomically; home.spec mismatch reconciled to a real element.
- Deferred lib/* email senders and infra slugs untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/11-brand-foundation-token-system/11-03-SUMMARY.md`
</output>
