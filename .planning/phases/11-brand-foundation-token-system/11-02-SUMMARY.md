---
phase: 11-brand-foundation-token-system
plan: 02
subsystem: ui
tags: [next-font, typography, metadata, viewport, dark-only, sonner, branding]

# Dependency graph
requires:
  - phase: 11
    plan: 01
    provides: Dark-only :root token foundation; @theme inline font lines left untouched for this plan to fix
provides:
  - Barlow Condensed (display) + Inter (body) registered via next/font with --font-display / --font-body
  - Fixed @theme inline font mapping (--font-sans -> body, --font-heading -> display); self-reference bug gone
  - Root metadata: OG Truck Parts title template + description + OpenGraph + metadataBase
  - viewport export: colorScheme dark + navy themeColor (#0d111b)
  - next-themes removed; sonner toaster hardcoded to dark
affects:
  - 11-03 (rename sweep — per-route titles will render '... | OG Truck Parts' once auth-page title strings are renamed)
  - 11-04 (OG image — app/opengraph-image.png auto-injects via file convention; openGraph.images intentionally omitted here)
  - Phase 12 (component restyle — dark class on <html> still mounted; legacy dark: utils retire there)

# Tech tracking
tech-stack:
  added: []
  removed:
    - next-themes (dark-only app; no runtime theme switching)
  patterns:
    - "Brand fonts via next/font/google with .variable classes on <html>; adjustFontFallback default => zero CLS"
    - "Title template (%s | OG Truck Parts) so per-route titles compose with the brand suffix"
    - "viewport.themeColor hex literal is the canonical Next pattern (allowed token-discipline exception — it is a Next metadata value, not app CSS)"

key-files:
  created: []
  modified:
    - app/layout.tsx
    - app/globals.css
    - components/ui/sonner.tsx
    - package.json
    - package-lock.json

key-decisions:
  - "metadataBase set to https://trucksource.vercel.app (current Vercel project name 'trucksource' from .vercel/repo.json) — trivially swappable, does not touch the deferred infra rename"
  - "Kept Geist_Mono for --font-mono (low-stakes code/mono surfaces); dropped Geist sans in favor of Inter"
  - "Kept the dark class on <html> (locked research decision) so existing dark: utilities stay active until Phase 12"
  - "themeColor #0d111b chosen to match the navy --background first paint and mobile chrome"

requirements-completed: [THEM-02, BRND-03]
requirements-partial:
  - "THEM-04: runtime half done (colorScheme dark, themeColor navy, next-themes removed, sonner dark-pinned); token half landed in 11-01"

# Metrics
duration: ~5min
completed: 2026-06-15
---

# Phase 11 Plan 02: Brand Typography & Metadata Summary

**Wired Barlow Condensed (display) + Inter (body) via next/font, fixed the broken `--font-sans` self-reference in the `@theme inline` mapping, replaced the create-next-app scaffold metadata with the OG Truck Parts title template + OpenGraph + viewport (dark colorScheme, navy themeColor), and finished the dark-only switch by removing next-themes and pinning the sonner toaster to dark.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-06-15T13:36:09Z
- **Completed:** 2026-06-15T13:40:47Z
- **Tasks:** 3
- **Files modified:** 5 (0 created, 5 modified)

## Accomplishments
- Registered Barlow Condensed (`--font-display`, weights 600/700/800 + italic, fallback Arial Narrow/Roboto Condensed) and Inter (`--font-body`) via `next/font/google`; kept Geist_Mono for `--font-mono`. Mounted both `.variable` classes plus the locked `dark` class on `<html>`.
- Fixed the two buggy `@theme inline` lines: `--font-sans` now maps to `var(--font-body)` (was a self-reference) and `--font-heading` to `var(--font-display)` (was `--font-sans`). Headings now resolve to the condensed display face and body to Inter.
- Replaced scaffold `metadata` with `metadataBase`, title `{ default, template: "%s | OG Truck Parts" }`, brand description, and OpenGraph (siteName/type, images intentionally omitted for Plan 04 file convention). Zero "Create Next App" / "Generated by" strings remain.
- Added the `viewport` export: `colorScheme: "dark"` + navy `themeColor: "#0d111b"` so mobile chrome and first paint match the navy base.
- Removed `next-themes` (uninstalled, dropped from `package.json` + lockfile regenerated) and hardcoded `theme="dark"` on the sonner `<Sonner>` — no more `"system"` default on a dark-only app.

## Task Commits

Each task was committed atomically:

1. **Task 1: Register fonts + fix @theme inline font mapping** — `c250e0b` (feat)
2. **Task 2: Replace scaffold metadata + add viewport export** — `ab6c47a` (feat)
3. **Task 3: Pin sonner to dark + remove next-themes** — `dca8177` (chore)

## Files Created/Modified
- `app/layout.tsx` — Barlow Condensed + Inter + Geist_Mono via next/font; `dark` + font-variable classes on `<html>`; brand `metadata` (title template, description, OpenGraph, metadataBase) and `viewport` (colorScheme dark, navy themeColor).
- `app/globals.css` — Fixed the two `@theme inline` font lines (`--font-sans` -> `--font-body`, `--font-heading` -> `--font-display`). Neon/glow tokens and `:root` from Plan 01 untouched.
- `components/ui/sonner.tsx` — Removed `useTheme`/`next-themes`; hardcoded `theme="dark"`. Icons, style vars, classNames unchanged.
- `package.json` / `package-lock.json` — `next-themes` removed from dependencies; lockfile regenerated via `npm uninstall`.

## Decisions Made
- **metadataBase domain:** Used `https://trucksource.vercel.app` (the current Vercel project name `trucksource`, read from `.vercel/repo.json`). It is just a string and is trivially swappable; it does not touch the deferred infra rename. **User should confirm** this is the intended public domain or supply the final one.
- **Kept Geist_Mono** for `--font-mono` (low-stakes mono/code surfaces); dropped Geist sans, replaced by Inter for body.
- **Kept the `dark` class on `<html>`** per the locked research decision, so the existing `dark:` utilities across `components/ui/*` stay active until Phase 12 restyles them.
- **themeColor `#0d111b`** matches the navy `--background` token first paint and mobile browser chrome.

## Deviations from Plan

None — plan executed exactly as written.

(Prettier/eslint pre-commit hooks reformatted the staged files on each commit — formatting only, no semantic change. This is the project's standard pre-commit pipeline, not a deviation.)

## Issues Encountered
None. All three task verification gates passed first time, `npm run build` compiled on each task, and the full vitest suite stayed green (43 files, 304 passed / 1 pre-existing skip) confirming the behavior-freeze — no functional logic was touched.

## UAT Notes (per plan verification)
- **THEM-02:** `getComputedStyle(document.body).fontFamily` should resolve to Inter and headings to Barlow Condensed; the self-reference bug is fixed. (Confirm visually in browser when the dashboard is served.)
- **Gate 4 (zero CLS on headings):** `adjustFontFallback` default `true` is the mechanism. A Lighthouse/CLS spot-check on `/` is recommended at the Phase 11 UAT.
- **BRND-03:** Per-route titles will render `... | OG Truck Parts` once Plan 03 renames the auth-page title strings.

## User Setup Required
- **Confirm `metadataBase` domain.** Currently `https://trucksource.vercel.app` (the Vercel project name). If the public domain differs, update `metadataBase` in `app/layout.tsx` — it is a one-line swap and does not block anything.

## Next Phase Readiness
- Typography and brand metadata are wired; the dark-only runtime is complete (colorScheme dark, themeColor navy, next-themes gone, sonner dark-pinned).
- Plan 03 (rename sweep) can proceed: per-route page titles will compose against the new `%s | OG Truck Parts` template.
- Plan 04 (OG image) can drop `app/opengraph-image.png` — `openGraph.images` was intentionally omitted so the file convention auto-injects.

## Self-Check: PASSED

All claimed artifacts verified on disk and in git history:
- Files: app/layout.tsx, app/globals.css, components/ui/sonner.tsx, package.json, package-lock.json, 11-02-SUMMARY.md
- Commits: c250e0b, ab6c47a, dca8177

---
*Phase: 11-brand-foundation-token-system*
*Completed: 2026-06-15*
