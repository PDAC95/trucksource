---
phase: 11-brand-foundation-token-system
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - scripts/gen-icons.mjs
  - app/icon.png
  - app/apple-icon.png
  - app/favicon.ico
  - app/opengraph-image.png
  - app/opengraph-image.alt.txt
  - app/manifest.ts
  - components/layout/site-header.tsx
  - package.json
  - package-lock.json
autonomous: false
requirements: [BRND-02, BRND-03]
user_setup:
  - service: stakeholder-logo-asset
    why: "Favicon/OG/header-logo are generated FROM the original OG Truck Parts logo; the source must exist before icon generation"
    dashboard_config:
      - task: "Place original logo source (SVG preferred, high-res PNG acceptable) in private/brand/ (git-ignored)"
        location: "private/brand/ in the repo working tree"
must_haves:
  truths:
    - "The browser tab shows the OG Truck Parts favicon (not the create-next-app default)"
    - "A shared link unfurls with the OG Truck Parts OpenGraph card (wordmark on navy with neon glow)"
    - "iOS add-to-home-screen shows the apple-touch icon on an opaque navy background"
    - "The web manifest carries the OG Truck Parts name and navy theme/background colors"
    - "The site header renders the logo icon beside the OG Truck Parts wordmark and scales on mobile"
  artifacts:
    - path: "app/manifest.ts"
      provides: "Typed web manifest with brand name + navy colors + icons"
      contains: "OG Truck Parts"
    - path: "scripts/gen-icons.mjs"
      provides: "sharp + png-to-ico generation of icon/apple-icon/og/favicon from private/brand source"
      min_lines: 30
    - path: "app/opengraph-image.png"
      provides: "1200x630 static brand share card"
    - path: "components/layout/site-header.tsx"
      provides: "Logo image + wordmark side by side"
      contains: "next/image"
  key_links:
    - from: "app/manifest.ts icons"
      to: "app/icon.png"
      via: "/icon.png src reference"
      pattern: "/icon.png"
    - from: "components/layout/site-header.tsx"
      to: "public logo asset"
      via: "next/image with explicit width/height"
      pattern: "Image"
---

<objective>
Integrate the stakeholder logo into the brand surface: generate the full favicon/icon set + the static OpenGraph share card from the source logo, ship the typed web manifest, and render the logo icon beside the wordmark in the header. This is the asset-contingent wave — it depends on the stakeholder logo source existing in `private/brand/`.

Purpose: BRND-02 (logo + favicon set + OG image from brand assets) and the OG-image half of BRND-03.
Output: `app/icon.png`, `app/apple-icon.png`, `app/favicon.ico`, `app/opengraph-image.png` (+ alt), `app/manifest.ts`, a generation script, and a header that renders the logo.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-brand-foundation-token-system/11-CONTEXT.md
@.planning/phases/11-brand-foundation-token-system/11-RESEARCH.md
@.planning/phases/11-brand-foundation-token-system/11-02-PLAN.md
@.planning/phases/11-brand-foundation-token-system/11-03-PLAN.md

<asset_contingency>
BLOCKER (STATE.md): the stakeholder logo package is NOT yet delivered. Plans 01-03 are unblocked and land first; THIS plan is sequenced last and gated on the asset. The first task is a human-action checkpoint to confirm the source logo is in `private/brand/`. If the asset is still missing at execution time, the executor should STOP at the checkpoint and report — do NOT fabricate a logo. (`private/` is already git-ignored at .gitignore:55; originals are NEVER committed — only derived copies in app//public/.)
</asset_contingency>

<pipeline_facts>
- `sharp@0.34.5` already installed (EXIF pipeline) — reuse for re-encode/resize/composite. sharp CANNOT emit .ico.
- Add `png-to-ico` as a DEV dependency for favicon.ico (the only added tool): `npm install -D png-to-ico`.
- Next 16 `app/` FILE CONVENTIONS auto-generate the <link>/<meta> tags — no hand-wired <head>. Files: `app/icon.png` (512²), `app/apple-icon.png` (180², opaque navy bg #0d111b — iOS fills transparency unpredictably), `app/favicon.ico` (from a 32²/48² PNG via png-to-ico), `app/opengraph-image.png` (1200×630) + `app/opengraph-image.alt.txt`.
- navy base hex for flattening/compositing: #0d111b (== oklch(0.18 0.022 264), the --background token).
- Header logo: `next/image` with EXPLICIT width/height (no CLS); icon + wordmark side by side (CONTEXT). Wordmark text already renamed to "OG Truck Parts" by Plan 03 — keep it; add the image beside it. Optionally apply the `.neon-sign` flicker class (Plan 01) to the header logo as THIS phase's single signature flicker moment.
- Auto-tracing the neon PNG logo to SVG posterizes gradients — DON'T. Ship PNG via next/image, or use vector source only if the stakeholder delivers clean SVG.
</pipeline_facts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm stakeholder logo source is in private/brand/</name>
  <action>Blocking gate — confirm the logo asset exists before any generation runs. The executor does NOT do work here; it verifies the asset and pauses for the human.</action>
  <what-built>Plans 01-03 already landed the dark-only tokens, fonts, metadata text, and brand-string sweep. This wave needs the original logo to generate icons/OG/header image.</what-built>
  <how-to-verify>
1. Confirm the original OG Truck Parts logo source exists at `private/brand/` (SVG preferred, or high-res PNG — ideally a square icon mark AND a horizontal wordmark/full logo).
2. Confirm `private/` is git-ignored (it is — .gitignore:55) so the original is never committed.
If the logo is NOT yet delivered: STOP here and report that Plan 04 is blocked on the asset; Plans 01-03 are complete and shippable independently.
  </how-to-verify>
  <resume-signal>Reply "logo ready" (and note the filename(s) in private/brand/) or "logo not delivered" to defer Plan 04.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Icon/OG generation script (sharp + png-to-ico)</name>
  <files>scripts/gen-icons.mjs, package.json, package-lock.json, app/icon.png, app/apple-icon.png, app/favicon.ico, app/opengraph-image.png, app/opengraph-image.alt.txt</files>
  <action>
Install the one tool: `npm install -D png-to-ico` (regenerates lockfile). Then create `scripts/gen-icons.mjs` (Node ESM) that reads the source from `private/brand/` and emits, with sharp:
- `app/icon.png` — 512×512 (transparent or navy bg).
- `app/apple-icon.png` — 180×180, FLATTENED onto opaque navy `#0d111b` (sharp `.flatten({ background: "#0d111b" })`).
- `app/opengraph-image.png` — 1200×630: wordmark/logo composited centered on a navy `#0d111b` canvas (use sharp `composite`; add neon glow either baked from the source or via a subtle red/cyan drop-shadow layer — keep static).
- a 32×32 (or 48×48) PNG buffer -> feed to `png-to-ico` -> write `app/favicon.ico` (REPLACE the create-next-app default).
- a header-sized logo mark into `public/logo-mark.png` (used by Task 2's header render).
Also write `app/opengraph-image.alt.txt` with alt text: "OG Truck Parts — Real Parts. Real Sellers." Add npm script `"gen:icons": "node scripts/gen-icons.mjs"`. Run it once to produce the assets. The script must fail loudly if `private/brand/` source is missing (don't emit blank images).
NOTE: only DERIVED copies (in app//public/) are committed; the originals in private/brand/ stay git-ignored.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && npm run gen:icons && ls -la app/icon.png app/apple-icon.png app/favicon.ico app/opengraph-image.png app/opengraph-image.alt.txt && node -e "const{statSync}=require('fs');for(const f of['app/icon.png','app/apple-icon.png','app/favicon.ico','app/opengraph-image.png']){if(statSync(f).size<1000)throw new Error(f+' too small');}console.log('ASSETS_OK')"</automated>
  </verify>
  <done>All five asset files exist and are non-trivially sized; favicon.ico replaced; script reruns deterministically and fails if the source is absent; png-to-ico in devDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Web manifest + header logo render</name>
  <files>app/manifest.ts, components/layout/site-header.tsx</files>
  <action>
Create `app/manifest.ts` (typed `MetadataRoute.Manifest`): name + short_name "OG Truck Parts", description "Real Parts. Real Sellers.", start_url "/", display "standalone", background_color "#0d111b", theme_color "#0d111b", icons referencing `/icon.png` (512²) and (if a 192² is generated) `/icon-192.png`; at minimum `/icon.png`.

Edit `components/layout/site-header.tsx`: render the logo icon beside the existing "OG Truck Parts" wordmark (Plan 03 already set the text). Use `next/image` with explicit width/height (no CLS) sourcing `/logo-mark.png` from `public/` (emitted by Task 1), inside the existing `<Link href="/">`. Keep the link role (e2e asserts the wordmark LINK by name "OG Truck Parts"). Give the `<Image>` an EMPTY/decorative alt (`alt=""`) so the link's accessible name stays exactly "OG Truck Parts" from the text wordmark — do NOT set alt="OG Truck Parts" (would duplicate/alter the link name and break the e2e link-name assertion). Optionally add the `.neon-sign` class (Plan 01) to the logo wrapper as this phase's single signature flicker moment; it freezes static under prefers-reduced-motion.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && rg -n "OG Truck Parts|background_color|theme_color" app/manifest.ts && rg -n "next/image|Image" components/layout/site-header.tsx && npm run build 2>&1 | tail -5 && npm run test:e2e 2>&1 | tail -10</automated>
  </verify>
  <done>`app/manifest.ts` returns the branded manifest (navy colors, icon refs); header renders the logo image beside the wordmark with explicit dimensions; link accessible name stays "OG Truck Parts" (e2e link assertions from Plan 03 still pass); build + e2e green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full brand-asset render</name>
  <action>Verification-only — all generation/wiring is done by Tasks 1-2; this task pauses for the human to confirm the rendered result.</action>
  <what-built>Full brand-asset integration: favicon, apple-touch icon, OG share card, web manifest, and the header logo+wordmark — all derived from the stakeholder logo on the dark neon foundation from Plans 01-03.</what-built>
  <how-to-verify>
1. `npm run dev` and open `/` — the browser TAB shows the OG Truck Parts favicon (not the Next default), the page is dark navy with no white flash, body text is Inter, headings are Barlow Condensed, and the header shows the logo icon beside "OG Truck Parts".
2. View-source / devtools <head>: confirm `<link rel="icon">`, `apple-touch-icon`, `og:image`, `og:title="OG Truck Parts"`, manifest link are present (Next auto-generated).
3. Paste the staging URL into a link-preview tool (or check the og:image renders at /opengraph-image.png) — the share card shows the wordmark on navy with neon glow.
4. Toggle OS "reduce motion" — any header logo flicker freezes to a static lit glow.
5. Resize to 360px — the header logo+wordmark stays legible and scales.
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering issues.</resume-signal>
</task>

</tasks>

<verification>
- BRND-02: favicon set (ico/png/apple-touch) + OG image generated from the brand asset; header renders the logo.
- BRND-03 (OG image half): `app/opengraph-image.png` file convention auto-injects og:image; combined with Plan 02 metadata, every route serves correct OG.
- Gate 1 (token discipline): the navy `#0d111b` literals here live in `app/manifest.ts` (a Next config value, not a styled className) and in the gen script — these are Next/asset config, not UI hardcoded colors; the brand grep gate scopes to styling hex in app/components classNames. Note any flagged manifest/themeColor hex as known allowed exceptions in the SUMMARY.
- e2e: header link accessible name unchanged ("OG Truck Parts") so Plan 03 assertions stay green.
</verification>

<success_criteria>
- BRND-02 complete: logo integrated as header image + full favicon/icon set + OG share card, all from the stakeholder asset (success criterion #2).
- Web manifest carries brand name + navy colors (tabs, iOS, add-to-home-screen).
- Single signature flicker moment (header logo) honoring prefers-reduced-motion.
- If asset undelivered: plan cleanly blocks at the first checkpoint; Plans 01-03 ship independently.
</success_criteria>

<output>
After completion, create `.planning/phases/11-brand-foundation-token-system/11-04-SUMMARY.md`
</output>
