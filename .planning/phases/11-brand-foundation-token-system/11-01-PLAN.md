---
phase: 11-brand-foundation-token-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - scripts/check-contrast.mjs
  - package.json
autonomous: true
requirements: [THEM-01, THEM-03, THEM-04]
must_haves:
  truths:
    - "The app renders dark-only: navy base, off-white text, no light :root and no .dark toggle block"
    - "Two neon families (red + cyan) exist as @theme tokens, each with a decorative variant and a lightened AA-text variant"
    - "Glow exists only as reusable box-shadow/text-shadow tokens; flicker animates pseudo-element opacity, frozen under prefers-reduced-motion"
    - "color-scheme: dark is declared in CSS so native inputs/scrollbars/autofill render dark; html paints navy on first paint"
    - "A contrast script proves every neon-text token passes AA (>=4.5 on base and card) and decorative neons pass >=3.0, all in sRGB gamut"
  artifacts:
    - path: "app/globals.css"
      provides: "Dark-only :root neon palette + neon/glow @theme tokens + flicker keyframe"
      contains: "--color-neon-red"
    - path: "scripts/check-contrast.mjs"
      provides: "OKLCH->sRGB->WCAG contrast + gamut checker (Gate 3)"
      min_lines: 40
  key_links:
    - from: "app/globals.css :root"
      to: "@theme inline color tokens"
      via: "--background/--foreground/--card resolved by --color-* bridge"
      pattern: "--color-background:\\s*var\\(--background\\)"
    - from: "scripts/check-contrast.mjs"
      to: "app/globals.css neon tokens"
      via: "asserts CR floors on the palette values"
      pattern: "neon-red-text"
---

<objective>
Establish the dark-only neon token foundation in `app/globals.css` — the single file that re-skins ~80% of the app. Delete the light theme and the dead `.dark` toggle block, write the night-navy dark `:root`, add the red/cyan dual-token neon families and the glow/flicker tokens to `@theme`, and declare `color-scheme: dark`. Vendor the contrast/gamut checker that proves the palette hits WCAG AA.

Purpose: THEM-01 (neon token system), THEM-03 (glow as tokens), and the CSS half of THEM-04 (dark-only rendering). Everything later in the milestone resolves through these tokens.
Output: A token-complete, dark-correct `globals.css` and a passing `scripts/check-contrast.mjs`.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-brand-foundation-token-system/11-CONTEXT.md
@.planning/phases/11-brand-foundation-token-system/11-RESEARCH.md

<current_state>
`app/globals.css` confirmed live state (read this session):
- Line 5: `@custom-variant dark (&:is(.dark *));`
- Lines 7-49: `@theme inline` color/font/radius bridge. Line 10 has the broken `--font-sans: var(--font-sans)` (fonts are FIXED IN PLAN 02 — do NOT touch the font lines here).
- Lines 51-84: light `:root` (oklch white base) — DELETE the light values, replace with dark neon values.
- Lines 86-118: `.dark { ... }` block — DELETE entirely.
- Lines 120-130: `@layer base` (`* border-border`, `body bg-background text-foreground`, `html font-sans`).

The repo is effectively light-only today (no ThemeProvider mounted; `.dark` is dead code). 16 `dark:` utility usages exist across 10 `components/ui/*` files — DECISION (research Open Q#2): KEEP the `dark` class on `<html>` in Plan 02 so those `dark:` variants stay active until Phase 12 restyles them. That means the dark token VALUES must live on BOTH a dark `:root` AND remain reachable via the `dark` class. Simplest correct approach: put the dark values on `:root` (single source) — `dark:` variants then layer the SAME values harmlessly. Do NOT keep a separate light `:root`.
</current_state>

<radius_freeze>
BEHAVIOR/GEOMETRY FREEZE: keep `--radius: 0.625rem` and the entire `--radius-*` scale (lines 42-48, 75) byte-for-byte. This phase changes color/glow/type only.
</radius_freeze>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the contrast/gamut checker (Gate 3) — values first</name>
  <files>scripts/check-contrast.mjs, package.json</files>
  <action>
Create `scripts/check-contrast.mjs` (zero runtime deps, pure Node ESM) that converts OKLCH -> linear sRGB (Ottosson OKLab matrices) -> WCAG relative luminance -> contrast ratio, and flags out-of-gamut (any linear channel <0 or >1 before clamp). This is the SAME method that produced the RESEARCH.md palette table.

Hardcode the palette under test as a small array so the script is self-contained and runs before globals.css is edited (interface-first — the script defines the floors the CSS must hit):
  base   = oklch(0.18 0.022 264)
  card   = oklch(0.215 0.025 264)
  foreground       = oklch(0.97 0.005 264)
  muted-foreground = oklch(0.72 0.02 264)
  neon-red         = oklch(0.58 0.232 25)   // decorative
  neon-red-text    = oklch(0.66 0.20 25)    // AA text
  neon-cyan        = oklch(0.78 0.13 195)   // decorative
  neon-cyan-text   = oklch(0.85 0.115 195)  // AA text

Assertions (process.exit(1) on any failure, print a table of CRs):
  - foreground vs base >= 4.5
  - muted-foreground vs base >= 4.5
  - neon-red-text vs base >= 4.5 AND vs card >= 4.5
  - neon-cyan-text vs base >= 4.5 AND vs card >= 4.5
  - neon-red (decorative) vs base >= 3.0
  - neon-cyan (decorative) vs base >= 3.0
  - every color is in sRGB gamut
Expected computed numbers to sanity-check against RESEARCH §Palette: foreground 17.25, muted-fg 7.58, red-text 5.51 (base) / 5.13 (card), cyan-text 12.43 (base) / 11.58 (card), red-dec 3.90, cyan-dec 9.91.

Add an npm script to package.json: `"check:contrast": "node scripts/check-contrast.mjs"`. Do NOT change the package `name` here (that is Plan 03). Do NOT run `npm install` here (no deps added).
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && node scripts/check-contrast.mjs && echo CONTRAST_OK</automated>
  </verify>
  <done>`node scripts/check-contrast.mjs` exits 0, prints a CR table, and the printed numbers match RESEARCH §Palette within rounding (red-dec ~3.90 fails-text-but-passes-3:1 confirmed). `npm run check:contrast` works.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite :root dark-only + add neon palette + delete light/.dark</name>
  <files>app/globals.css</files>
  <action>
Edit `app/globals.css`:

1. DELETE the `.dark { ... }` block (lines 86-118). Leave `@custom-variant dark (&:is(.dark *));` (line 5) IN PLACE — the `dark` class stays on `<html>` (Plan 02) so the 16 existing `dark:` utilities in components/ui/* keep resolving until Phase 12.

2. REPLACE the light `:root` body (lines 51-84) with the dark neon palette. Keep `--radius: 0.625rem` and `--sidebar-*` keys (give sidebar dark-correct values — reuse card/foreground family). New `:root` values (from RESEARCH §Palette, all sRGB-in-gamut, AA-verified by Task 1):
   --background: oklch(0.18 0.022 264);
   --foreground: oklch(0.97 0.005 264);
   --card: oklch(0.215 0.025 264);
   --card-foreground: oklch(0.97 0.005 264);
   --popover: oklch(0.215 0.025 264);
   --popover-foreground: oklch(0.97 0.005 264);
   --primary: oklch(0.58 0.232 25);            /* red — neon CTA */
   --primary-foreground: oklch(0.97 0.005 264);
   --secondary: oklch(0.265 0.02 264);
   --secondary-foreground: oklch(0.97 0.005 264);
   --muted: oklch(0.265 0.02 264);
   --muted-foreground: oklch(0.72 0.02 264);
   --accent: oklch(0.265 0.02 264);
   --accent-foreground: oklch(0.97 0.005 264);
   --destructive: oklch(0.58 0.232 25);        /* red family */
   --border: oklch(1 0 0 / 12%);
   --input: oklch(1 0 0 / 15%);
   --ring: oklch(0.78 0.13 195);               /* cyan ring */
   --radius: 0.625rem;                         /* UNCHANGED */
   keep chart-1..5 for now (Phase 12/14 recalibrates) but shift toward navy-legible values if trivially possible; leaving the existing greys is acceptable this phase.
   --sidebar / --sidebar-* : dark-correct (sidebar=card, sidebar-foreground=foreground, sidebar-primary=red, sidebar-ring=cyan, sidebar-border = border).

3. ADD `color-scheme: dark;` inside `:root`.

4. In `@theme` (NOT `@theme inline` — use a new plain `@theme { }` block, or extend), add the neon families + glow tokens so they emit utilities (`text-neon-red-text`, `border-neon-red`, `shadow-glow-red`, `text-shadow-neon-cyan`, etc.):
   --color-neon-red:        oklch(0.58 0.232 25);
   --color-neon-red-text:   oklch(0.66 0.20 25);
   --color-neon-cyan:       oklch(0.78 0.13 195);
   --color-neon-cyan-text:  oklch(0.85 0.115 195);
   --shadow-glow-red:  0 0 4px oklch(0.58 0.232 25 / 0.7), 0 0 14px oklch(0.58 0.232 25 / 0.45);
   --shadow-glow-cyan: 0 0 4px oklch(0.78 0.13 195 / 0.7), 0 0 14px oklch(0.78 0.13 195 / 0.45);
   --text-shadow-neon-red:  0 0 6px oklch(0.58 0.232 25 / 0.6);
   --text-shadow-neon-cyan: 0 0 6px oklch(0.78 0.13 195 / 0.6);
   --animate-flicker: flicker 2.5s steps(1) infinite;

5. In `@layer base`, add `html { background: var(--background); }` (anti white-flash first paint). Keep the existing `* border-border`, `body bg-background text-foreground`. Leave `html { @apply font-sans; }` for Plan 02.

DO NOT touch lines 7-49 font tokens (`--font-sans`, `--font-heading`, `--font-mono`) — Plan 02 owns the font rewire. NO hardcoded hex anywhere (oklch only).
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && rg -n "oklch\(1 0 0\)" app/globals.css; rg -c "\.dark \{" app/globals.css; rg -n "color-scheme: dark|--color-neon-red|--shadow-glow-red|--animate-flicker|@keyframes flicker" app/globals.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>No light `:root` (`oklch(1 0 0)` background gone), `.dark {` block deleted (rg count 0), `color-scheme: dark` present, all four neon color tokens + both glow shadows + both text-shadow tokens + `--animate-flicker` present, `--radius: 0.625rem` unchanged, `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Add the flicker keyframe + reduced-motion freeze + neon-sign utility</name>
  <files>app/globals.css</files>
  <action>
Append the signature-flicker mechanics to `app/globals.css` (animate pseudo-element OPACITY only — never animate box/text-shadow; per RESEARCH anti-pattern):

```css
@keyframes flicker {
  0%, 18%, 22%, 25%, 53%, 57%, 100% { opacity: 1; }
  20%, 24%, 55% { opacity: 0.55; }
}

@layer components {
  .neon-sign { position: relative; }
  .neon-sign::after {
    content: "";
    position: absolute;
    inset: 0;
    box-shadow: var(--shadow-glow-red);
    border-radius: inherit;
    pointer-events: none;
    animation: var(--animate-flicker);
  }
}

@media (prefers-reduced-motion: reduce) {
  .neon-sign::after { animation: none; opacity: 1; }
}
```

A11Y floor (A11Y-03, audited Phase 15, built-in here): the keyframe is ~1-2 dim blips per 2.5s cycle, well under 3 flashes/second, and never fully off. Reduced-motion freezes to a static fully-lit glow. This utility is the single signature moment for this phase (header wordmark wires it in Plan 04); the second allowed flicker slot is reserved for a Phase 13 signage hero — do not add more flicker call-sites here.
  </action>
  <verify>
    <automated>cd "c:/Users/patri/Desktop/12GA/take off parts web/project" && rg -n "@keyframes flicker|prefers-reduced-motion|\.neon-sign::after|animation: none" app/globals.css && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>`@keyframes flicker` defined, `.neon-sign::after` animates `--animate-flicker`, `prefers-reduced-motion: reduce` sets `animation: none; opacity: 1`, build passes.</done>
</task>

</tasks>

<verification>
- Gate 1 (token discipline) — no NEW hardcoded hex introduced in globals.css (oklch only; globals.css is the one allowed location anyway): `rg "#[0-9a-fA-F]{3,8}\b" app/globals.css` shows no neon hex (this file may legitimately hold hex later but this plan uses oklch exclusively).
- Gate 3 (contrast/gamut): `npm run check:contrast` exits 0.
- Behavior-freeze: `--radius` scale unchanged; no functional code touched. `npm run test` (vitest) stays green — PII/RLS/EXIF invariants untouched.
- `npm run build` succeeds (Tailwind v4 4.3 emits the neon/glow/text-shadow utilities).
</verification>

<success_criteria>
- Dark-only: light `:root` removed, `.dark` block deleted, single dark `:root` with `color-scheme: dark`, `<html>` background navy (success criterion #3, CSS half).
- THEM-01: red + cyan neon families each with decorative + AA-text token, all AA-verified.
- THEM-03: glow box/text-shadow tokens + opacity-only flicker keyframe + reduced-motion freeze.
- `scripts/check-contrast.mjs` is the executable AA proof for any future palette nudge.
</success_criteria>

<output>
After completion, create `.planning/phases/11-brand-foundation-token-system/11-01-SUMMARY.md`
</output>
