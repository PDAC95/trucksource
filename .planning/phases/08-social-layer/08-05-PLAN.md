---
phase: 08-social-layer
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - components/search/listing-card.tsx
  - components/search/feed-grid.tsx
  - app/(public)/page.tsx
  - app/api/search/route.ts
  - app/(app)/saved/page.tsx
  - app/(app)/layout.tsx
  - app/(app)/sell/listings/page.tsx
autonomous: true
requirements: [SOCL-02, LIST-06]
must_haves:
  truths:
    - "Feed/search cards show a save heart: anon → login invite, authenticated → working toggle reflecting actual saved state (including infinite-scroll pages)"
    - "/saved shows the user's saved listings in the Phase 7 card grid; sold/expired saves REMAIN visible with a 'Vendido'/'Expirado' badge and can be removed manually"
    - "/saved has a friendly empty state and is unreachable-but-redirected for anon (auth route group)"
    - "/sell/listings cards show the save count and a new-comments badge, plus a confirmed sold/available toggle"
  artifacts:
    - path: "app/(app)/saved/page.tsx"
      provides: "Owner-scoped saved-listings grid with status badges + empty state"
      min_lines: 40
    - path: "components/search/listing-card.tsx"
      provides: "ListingCard extended with optional save-button overlay + status badge slot"
    - path: "app/(app)/sell/listings/page.tsx"
      provides: "Save count + new-comment badge + sold toggle on the owner's listing rows"
  key_links:
    - from: "app/(public)/page.tsx"
      to: "lib/saves/queries.ts"
      via: "getSavedIds(cards.map(id)) for the first page when authenticated"
      pattern: "getSavedIds"
    - from: "app/api/search/route.ts"
      to: "lib/saves/queries.ts"
      via: "savedIds resolved server-side per infinite-scroll page (cookie client → owner RLS)"
      pattern: "getSavedIds"
    - from: "app/(app)/saved/page.tsx"
      to: "lib/saves/queries.ts getMySavedListings"
      via: "RSC fetch → ListingCard grid"
      pattern: "getMySavedListings"
    - from: "app/(app)/sell/listings/page.tsx"
      to: "lib/listings/queries.ts getMyListings"
      via: "saveCount + newCommentCount rendered per row"
      pattern: "newCommentCount"
---

<objective>
Ship the browse-side and management-side UI of Phase 8: save hearts on feed/search cards (including infinite scroll), the /saved page reusing the Phase 7 grid with sold/expired badges, a nav entry, and the seller's /sell/listings upgrades (save count, new-comment badge, sold toggle). Runs parallel with 08-04 — zero file overlap (08-04 owns the listing detail page/component; this plan owns the card, feed, saved, and management surfaces).

Purpose: SOCL-02 becomes usable end-to-end; LIST-06 gets its second mandated home (the "my listings" management view).
Output: extended ListingCard/FeedGrid/feed page/api route, new /saved page, upgraded /sell/listings.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-social-layer/08-RESEARCH.md
@components/search/listing-card.tsx
@components/search/feed-grid.tsx
@app/(public)/page.tsx
@app/api/search/route.ts
@app/(app)/sell/listings/page.tsx
@app/(app)/layout.tsx
@lib/saves/queries.ts
@components/search/save-button.tsx
</context>

<interfaces>
From 08-03:
```ts
// lib/saves/queries.ts
export type SavedCard = SearchCard & { status: string; expiresAt: string | null; savedAt: string };
export async function getMySavedListings(): Promise<SavedCard[]>;
export async function getSavedIds(listingIds: number[]): Promise<Set<number>>;
// components/search/save-button.tsx
<SaveButton listingId initiallySaved isAuthenticated size? />  // client; stops propagation so card links don't fire
// lib/listings/queries.ts — MyListing now has saveCount: number; newCommentCount: number
// lib/actions/listings.ts — markSold(id) / markAvailable(id)
```
Current facts: `ListingCard({ card }: { card: SearchCard })` is a server component whose whole body is wrapped in a Link to `/listings/[id]` with the username link as a sibling anchor; `FeedGrid` is a client component doing IntersectionObserver infinite scroll against `/api/search`; the feed page `app/(public)/page.tsx` is force-dynamic and calls `searchListings`; `/sell/listings/page.tsx` is force-dynamic, renders MyListing rows with RenewButton and an attention counter.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ListingCard save overlay + feed/API saved-state plumbing</name>
  <files>components/search/listing-card.tsx, components/search/feed-grid.tsx, app/(public)/page.tsx, app/api/search/route.ts</files>
  <action>
**listing-card.tsx:** extend props additively — `{ card: SearchCard; saveState?: { initiallySaved: boolean; isAuthenticated: boolean }; statusBadge?: string }`. When `saveState` is provided render `<SaveButton size="sm" …/>` absolutely positioned top-right over the photo (outside/sibling of the Link content or inside with propagation stopped — SaveButton already preventDefaults). When `statusBadge` is provided render a prominent overlay Badge (e.g. top-center or over the photo) with the text ("Vendido"/"Expirado") — used by the /saved grid. Omitting both props renders the card exactly as today (07-04's profile grid and any other consumer stays untouched).

**app/(public)/page.tsx:** after `searchListings`, resolve viewer auth once (getClaims via the existing server client) and, when authenticated, `getSavedIds(cards.map(c => c.id))`. Pass `savedIds` (as an array — Sets don't serialize to client components) + `isAuthenticated` into `<FeedGrid …/>`.

**feed-grid.tsx:** accept `initialSavedIds: number[]` + `isAuthenticated: boolean`; keep saved ids in state merged from each fetched page; render each card with `saveState={{ initiallySaved: savedSet.has(card.id), isAuthenticated }}`. Mind the react-hooks pre-commit gate (derive the Set with useMemo; no render-time state writes).

**app/api/search/route.ts:** the infinite-scroll pages need saved state too — in the route handler (it runs with cookies → owner RLS), after computing the page's cards call `getSavedIds(pageIds)` (cheap no-op empty Set for anon) and include `savedIds: number[]` in the JSON response. FeedGrid merges them into its saved-state map per page load.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Feed/search cards show working hearts for authenticated viewers across all scrolled pages, login invite for anon; cards without saveState render unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: /saved page + nav entry</name>
  <files>app/(app)/saved/page.tsx, app/(app)/layout.tsx</files>
  <action>
**app/(app)/saved/page.tsx** (force-dynamic, owner-scoped — never cache one user's saves for another, invariant #6; mirror /sell/listings/page.tsx's getClaims → redirect("/login") guard):
- Fetch `getMySavedListings()`. Render the SAME responsive grid the feed uses with `<ListingCard card={…} saveState={{ initiallySaved: true, isAuthenticated: true }} statusBadge={…} />` — `statusBadge` = "Vendido" when status sold, "Expirado" when expired/past-expiry, undefined when active (LOCKED: sold/expired saves REMAIN with a badge; manual removal = tapping the heart, which unsaves + revalidates "/saved").
- Header: "Guardados" + count. Empty state: bookmark/heart icon + "No tienes anuncios guardados — toca el corazón en cualquier anuncio para guardarlo aquí." with a Button linking to `/` (no dead end). `<Toaster />` for SaveButton error toasts.

**app/(app)/layout.tsx:** add a "Guardados" nav entry (Heart or Bookmark lucide icon) alongside the existing account/sell links, matching the current nav markup style. If the (app) layout has no nav list (inspect first), add the link wherever account/sell/garage entries live — smallest consistent change; do not redesign the layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>/saved renders the saved grid with status badges + heart removal + empty state behind auth; nav reaches it.</done>
</task>

<task type="auto">
  <name>Task 3: /sell/listings — save count, new-comment badge, sold toggle</name>
  <files>app/(app)/sell/listings/page.tsx</files>
  <action>
Upgrade the owner's listing rows using the extended `MyListing` (saveCount, newCommentCount from 08-03):

1. **Save count (seller-facing only, never WHO):** per row, a muted "{saveCount} guardados" with a Heart icon (hide or show "0 guardados" — pick one and be consistent; prefer showing only when > 0 to keep rows quiet).
2. **New-comment badge (LOCKED in-app indicator, NOT a notification system):** when `newCommentCount > 0`, a small primary Badge "{n} comentarios nuevos" with a MessageSquare icon, linking to `/listings/[id]` (where viewing as owner fires markCommentsSeen via 08-04's page, clearing the badge). Optionally fold comment-news into the existing attention counter; keep the existing expiring/expired logic untouched.
3. **Sold toggle (LIST-06's second mandated home):** per row, status `active` → "Marcar vendido" button; status `sold` → a "Vendido" Badge + "Marcar disponible" button. Both behind the vendored AlertDialog confirmation, calling `markSold(id)` / `markAvailable(id)` in a transition with sonner feedback (the page is force-dynamic + the action revalidates "/sell/listings", so router.refresh() on success updates the row). Implement as a small `"use client"` subcomponent colocated in this file or reuse the pattern of renew-button.tsx — but do NOT modify components/listings/* (08-04 owns listing-detail.tsx; renew-button.tsx stays untouched).
4. Keep `expired` rows on the existing reactivate path — the sold toggle never shows for them.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run</automated>
  </verify>
  <done>Owner rows show save counts + new-comment badges and a confirmed reversible sold toggle; renew/reactivate flows unchanged; full suite green.</done>
</task>

</tasks>

<verification>
- `npm run build` green (no route collisions, /saved present); `npx tsc --noEmit` clean; `npx vitest run` no regression.
- Grep gates: no `profiles_private` / `select('*')` / `getSession` / `supabase/admin` in touched app/components files.
- File-ownership check: this plan did NOT touch app/(public)/listings/[id]/page.tsx or components/listings/* (08-04's files).
</verification>

<success_criteria>
SOCL-02 is end-to-end usable (save from feed/search/detail, view + manage at /saved with sold/expired badges) and LIST-06 is operable from the my-listings management view; the seller sees save counts and new-comment indicators without any notification infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/08-social-layer/08-05-SUMMARY.md`
</output>
