---
phase: 08-social-layer
plan: 06
type: execute
wave: 4
depends_on: ["08-04", "08-05"]
files_modified: []
autonomous: false
requirements: [SOCL-01, SOCL-02, LIST-06]
must_haves:
  truths:
    - "A buyer can post a public comment on a listing, shown attributed to their username only (phase success criterion 1)"
    - "A buyer can save a listing and view their saved list (phase success criterion 2)"
    - "A seller can mark their own listing as Sold, updating its public status while the page stays reachable (phase success criterion 3)"
    - "Comments display no PII anywhere; saves are owner-scoped (cross-cutting privacy/RLS gate)"
  artifacts: []
  key_links: []
---

<objective>
Final verification of Phase 8 against the ROADMAP success criteria: an automated sweep (build, types, full test suite, privacy greps) followed by a single human-verify checkpoint of the live social flows.

Purpose: phase close-out — proves SOCL-01, SOCL-02, LIST-06 work as a user experiences them, with the privacy gate re-verified.
Output: user approval (or gap reports feeding /gsd:plan-phase --gaps).
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-social-layer/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase sweep</name>
  <files></files>
  <action>
Run the full verification battery and report results:
1. `npx tsc --noEmit` — clean.
2. `npm run build` — green, `/saved` route present, single `/` route.
3. `npx vitest run` — full suite green including the live Staging gates (social.test.ts: depth-1 rejection, sold-closed insert rejection, owner-only saves, audit default-deny, RPC anon-revoked; social.contract.test.ts: zero-PII shapes).
4. Privacy greps across lib/comments, lib/saves, lib/actions/comments.ts, lib/actions/saves.ts, components/comments, components/search/save-button.tsx, app/(app)/saved: zero hits for `profiles_private`, `select('*')`, `getSession`, `supabase/admin`.
5. Confirm by inspection: markSold/markAvailable update payloads contain no `expires_at`; the saved page reader does not call `search_listings`.
Start the dev server (`npm run dev`) for the checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run</automated>
  </verify>
  <done>All automated gates green; dev server running for the human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live social-layer walkthrough</name>
  <action>Pause for the user to walk the live social flows below against the dev server. Do not proceed until the user responds; on issues, record them as gaps for /gsd:plan-phase --gaps.</action>
  <verify>User responds "approved" (or reports issues, which become gap inputs)</verify>
  <done>User approval received — Phase 8 success criteria confirmed live</done>
  <what-built>Phase 8 complete: public comments with one-level replies and seller moderation, save/bookmark with a /saved page, and reversible mark-as-sold — all on top of the Phase 7 feed.</what-built>
  <how-to-verify>
Con el dev server corriendo (http://localhost:3000), verifica en orden:

**Comentarios (SOCL-01):**
1. Abre un listing activo con sesión iniciada → escribe un comentario → aparece arriba (más nuevo primero) atribuido SOLO a tu username (link a /u/...).
2. Responde a un comentario → la respuesta aparece debajo del padre; la respuesta NO tiene botón "Responder" (un solo nivel).
3. Borra tu propio comentario (confirmación) → desaparece junto con sus respuestas.
4. Como vendedor del listing, borra un comentario ajeno → funciona.
5. En ventana incógnito (anon): el hilo se ve, el composer muestra "Inicia sesión para comentar".

**Guardados (SOCL-02):**
6. Desde el feed `/`, toca el corazón en una card → se marca; en incógnito el corazón invita a iniciar sesión.
7. Abre /saved → el listing guardado aparece en el grid; tocar el corazón lo quita.
8. En la página de detalle también hay corazón.

**Vendido (LIST-06):**
9. Como vendedor, en el detalle de tu listing o en /sell/listings, "Marcar como vendido" (con confirmación) → el listing desaparece del feed y la búsqueda, PERO su URL pública sigue viva con badge "Vendido" y los comentarios quedan cerrados (los existentes visibles).
10. El listing vendido guardado en /saved muestra badge "Vendido" (no desaparece).
11. "Marcar como disponible" lo revierte y vuelve al feed.
12. En /sell/listings ves el contador de guardados y el badge de comentarios nuevos (que se limpia al abrir tu listing).
  </how-to-verify>
  <resume-signal>Escribe "approved" o describe los problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
User approval at the checkpoint = Phase 8 success criteria 1-3 verified live + privacy gate green.
</verification>

<success_criteria>
All three ROADMAP Phase 8 success criteria confirmed by the user on the live app; automated suite + privacy greps green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-social-layer/08-06-SUMMARY.md`
</output>
