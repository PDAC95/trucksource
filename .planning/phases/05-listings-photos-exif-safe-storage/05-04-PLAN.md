---
phase: 05-listings-photos-exif-safe-storage
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - components/ui/radio-group.tsx
  - components/ui/textarea.tsx
  - components/listings/listing-form.tsx
  - components/listings/photo-uploader.tsx
  - components/listings/fitment-multi-select.tsx
  - app/(app)/sell/page.tsx
  - app/(app)/sell/[id]/edit/page.tsx
autonomous: false
requirements: [LIST-01, LIST-02, LIST-04, LIST-05]
user_setup: []

must_haves:
  truths:
    - "A seller sees a single sectioned page (Part data → Fitment → Photos → Shipping) to create a listing"
    - "A seller can add multiple Make/Model/Config fitment combinations to one listing (multi-fit), or toggle The Barnyard to skip fitment"
    - "A seller can drag-drop photos to reorder them; the first photo is the cover"
    - "Each selected photo uploads immediately with a per-photo spinner; EXIF is stripped server-side"
    - "A seller can pick a shipping option (Shipping Available / Local Pickup Only / Shipping Assistance Requested)"
    - "On successful publish the seller is redirected to the published listing's public page"
    - "Editing a listing reuses the same form, pre-filled, and saves via updateListing"
  artifacts:
    - path: "components/listings/listing-form.tsx"
      provides: "The single sectioned create/edit form (RHF + zodResolver(listingSchema))"
      contains: "listingSchema"
      min_lines: 80
    - path: "components/listings/photo-uploader.tsx"
      provides: "dnd-kit sortable photo grid, immediate upload, per-photo state, first=cover"
      contains: "uploadListingPhoto"
    - path: "components/listings/fitment-multi-select.tsx"
      provides: "Reused Make→Model→Config cascade producing a multi-fit list + Barnyard toggle"
      contains: "getModels"
    - path: "app/(app)/sell/page.tsx"
      provides: "Create-listing route rendering ListingForm"
      contains: "ListingForm"
    - path: "app/(app)/sell/[id]/edit/page.tsx"
      provides: "Edit route rendering the same ListingForm pre-filled (LIST-05)"
      contains: "ListingForm"
  key_links:
    - from: "components/listings/photo-uploader.tsx"
      to: "lib/actions/listings.ts"
      via: "calls uploadListingPhoto on each selected file"
      pattern: "uploadListingPhoto"
    - from: "components/listings/listing-form.tsx"
      to: "lib/actions/listings.ts"
      via: "submit calls createListing or updateListing then router redirects"
      pattern: "(createListing|updateListing)"
    - from: "components/listings/fitment-multi-select.tsx"
      to: "lib/garage/cascade.ts"
      via: "reuses getModels/getConfigs for the dependent selects"
      pattern: "getConfigs"
---

<objective>
Build the seller-facing listing experience: one sectioned create/edit form with a multi-fit fitment selector (reusing the Phase-4 cascade), a dnd-kit photo uploader that strips EXIF at selection time, a shipping radio, and redirect-to-public-page on publish. Includes a live human-verify checkpoint.

Purpose: This is where LIST-01/02/04/05 become a usable flow. CONTEXT locks: single long sectioned page (NOT a wizard), no draft state, edit reuses the same component pre-filled, redirect to the public listing on publish, up to 8 photos / drag-drop / first=cover / immediate per-photo upload, multi-fit, Barnyard toggle.
Output: shadcn radio-group + textarea primitives, the three listing components, and the sell/edit routes.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-listings-photos-exif-safe-storage/05-CONTEXT.md
@.planning/phases/05-listings-photos-exif-safe-storage/05-RESEARCH.md

# Reuse these proven Phase-4 UI patterns (cascade, dialog wiring, force-dynamic owner page):
@app/(app)/profile/garage/truck-cascade.tsx
@app/(app)/profile/garage/add-truck-dialog.tsx
@app/(app)/profile/garage/page.tsx

# Depends on Wave-2 outputs:
@.planning/phases/05-listings-photos-exif-safe-storage/05-03-SUMMARY.md

<interfaces>
<!-- From 05-02 / 05-03 (import these): -->
import { listingSchema, type ListingInput, type ListingFormValues } from "@/lib/listings/schema";
import { getModels, getConfigs, type CascadeOption } from "@/lib/garage/cascade";   // REUSE — do not re-create
import { getConditions, type ConditionOption } from "@/lib/listings/cascade";
import { createListing, updateListing, uploadListingPhoto, removeListingPhoto } from "@/lib/actions/listings";
import { getListing } from "@/lib/listings/queries";   // for the edit page pre-fill

<!-- dnd-kit (installed in 05-01): -->
import { DndContext, closestCenter } from "@dnd-kit/core";
import { SortableContext, arrayMove, rectSortingStrategy, useSortable } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";

<!-- Cascade reader convention is "use server" functions called from a client component
     (exactly how truck-cascade.tsx calls getModels/getConfigs). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn primitives + the photo uploader (dnd-kit, immediate upload, EXIF at selection)</name>
  <files>components/ui/radio-group.tsx, components/ui/textarea.tsx, components/listings/photo-uploader.tsx</files>
  <action>
Add missing shadcn primitives: `npx shadcn@latest add radio-group textarea` (writes components/ui/radio-group.tsx + textarea.tsx — repo owns shadcn components).

Create `components/listings/photo-uploader.tsx` (`"use client"`) — the genuinely new UI (05-RESEARCH "Don't Hand-Roll" → use @dnd-kit/sortable, NEVER native HTML5 DnD):
- Props: `value: UploadedPhoto[]`, `onChange(next: UploadedPhoto[])` where `UploadedPhoto = { id: string; path: string | null; previewUrl: string; status: "uploading" | "ready" | "error" }`.
- File input accepting `image/jpeg,image/png,image/webp` (CONTEXT formats), `multiple`. On select: enforce max 8 total; for each file create an immediate local `URL.createObjectURL` preview, push with status "uploading", then call `uploadListingPhoto(formData with file)` per photo. On `{ok:true,path}` set status "ready" + store path; on error set status "error" and toast a friendly message (HEIC/unsupported → "Please upload JPG, PNG, or WebP — HEIC isn't supported; your phone can export JPG." per Pitfall 2). Per-photo spinner overlay while uploading (NOT one global bar — CONTEXT).
- dnd-kit sortable grid: wrap items in `DndContext`+`SortableContext` (rectSortingStrategy), each tile a `useSortable` item; onDragEnd `arrayMove` and call onChange. The FIRST tile is the cover — show a small "Cover" badge on index 0 (no separate set-cover button — CONTEXT).
- Each tile has a remove (X) button → call `removeListingPhoto(path)` if it had a path, then drop from the list.
- Expose the ordered `path[]` of ready photos to the parent form (this becomes `photoPaths`).
Revoke object URLs on unmount to avoid leaks.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/listings/photo-uploader.tsx','utf8'); if(!s.includes('use client')) throw 0; if(!s.includes('uploadListingPhoto')) throw new Error('must upload per photo'); if(!s.includes('@dnd-kit/sortable')) throw new Error('use dnd-kit, not native DnD'); if(/withMetadata|keepMetadata|keepExif/.test(s)) throw new Error('no metadata methods in UI'); console.log('OK')"</automated>
  </verify>
  <done>Photos upload the moment they're selected (per-photo spinner), EXIF stripped server-side via uploadListingPhoto, drag-drop reorder via dnd-kit, first tile is the cover, remove drops the staged object; ≤8 enforced; HEIC/unsupported shows a friendly message.</done>
</task>

<task type="auto">
  <name>Task 2: Fitment multi-select (reuse cascade) + the sectioned listing form</name>
  <files>components/listings/fitment-multi-select.tsx, components/listings/listing-form.tsx</files>
  <action>
Create `components/listings/fitment-multi-select.tsx` (`"use client"`) — REUSE the truck-cascade Make→Model→Config dependent-select logic (import `getModels`/`getConfigs` from `@/lib/garage/cascade`; configs scoped THROUGH model_configurations, never the full master). Differences from garage:
- An "Add this fitment" button appends the current `{ modelId, configId }` to a list shown as removable Badges (e.g. "Peterbilt 379 — Day Cab"). This is the multi-fit list (CONTEXT: do not collapse to single-fit).
- A "The Barnyard" toggle (Checkbox/Switch): when ON, hides/relaxes the selects and sets `isBarnyard` true (CONTEXT: explicit toggle; relaxes the Make+Model requirement).
- Props expose `fitment: FitmentEntry[]`, `onChange`, `isBarnyard`, `onBarnyardChange`, plus the `makes` list (passed from the server page like garage does).

Create `components/listings/listing-form.tsx` (`"use client"`) — the single sectioned form (CONTEXT: ONE page, sections, not a wizard). RHF with `useForm<ListingFormValues, unknown, ListingInput>({ resolver: zodResolver(listingSchema), defaultValues })`. Sections in order:
1. **Part data** — Title (required Input), Part Number (optional Input), Asking Price (required numeric Input, USD; format display via Intl.NumberFormat), Condition (required Select from getConditions), Damage Notes (optional Textarea).
2. **Fitment** — render `<FitmentMultiSelect>`; wire its fitment/isBarnyard into form state.
3. **Photos** — render `<PhotoUploader>`; map ready photo paths into `photoPaths`.
4. **Shipping** — RadioGroup of the three shipping_option values (LIST-04). Also DISPLAY the account contact preference as read-only reference text (CONTEXT: listing form only displays it, never edits it) — accept it as a `contactPreference` prop string from the server page; if not yet available, render a neutral note. (The control itself lives in 05-05's account page.)
- Submit: `startTransition` → `createListing(values)` (or `updateListing(id, values)` in edit mode); on `{ok:true,id}` `router.push(`/listings/${id}`)` (CONTEXT redirect-to-public-page) + success toast; on error map the typed error to friendly copy (invalid_combo, invalid_photo_path, not_found, etc.) like add-truck-dialog does, keeping the form mounted.
- Accept `mode: "create" | "edit"`, optional `listingId`, and `defaults` (for edit pre-fill).
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('components/listings/fitment-multi-select.tsx','utf8'); const b=require('fs').readFileSync('components/listings/listing-form.tsx','utf8'); if(!a.includes('getConfigs')) throw new Error('reuse cascade'); if(!b.includes('listingSchema')||!/createListing|updateListing/.test(b)) throw new Error('form must use schema + actions'); if(!b.includes('RadioGroup')) throw new Error('shipping radio missing'); console.log('OK')"</automated>
  </verify>
  <done>The form is one sectioned page (part data / fitment / photos / shipping); fitment supports multi-fit + Barnyard via the reused cascade; shipping is a 3-option radio; contact preference shows read-only; submit creates/updates and redirects to the public listing on success.</done>
</task>

<task type="auto">
  <name>Task 3: Sell (create) and edit routes</name>
  <files>app/(app)/sell/page.tsx, app/(app)/sell/[id]/edit/page.tsx</files>
  <action>
Create `app/(app)/sell/page.tsx` (Server Component, `export const dynamic = "force-dynamic"` — owner-only, never cache one seller's form for another, mirrors garage/page.tsx):
- `getClaims` defensive redirect to /login if no claims.
- Load `makes` (anon-public read) and `conditions` (via getConditions) and the account contact preference (read profiles_public.contact_preference for the current user — added in 05-05; if the column isn't present yet at execution time, default to "messaging_only" so this page never hard-fails).
- Render `<ListingForm mode="create" makes={makes} conditions={conditions} contactPreference={pref} />` + a `<Toaster/>`.

Create `app/(app)/sell/[id]/edit/page.tsx` (Server Component, force-dynamic) — LIST-05:
- `getClaims` redirect if unauthenticated.
- `const listing = await getListing(Number(id))`; if null → notFound(). Confirm `listing.seller... ` belongs to the caller — but getListing returns public data; instead fetch ownership via a quick `seller_id` check (select seller_id eq id) and if it isn't the caller's, `notFound()` (no edit-others, no existence leak).
- Map the listing into `defaults` for the form (title, partNumber, askingPrice, conditionId, shippingOption, damageNotes, isBarnyard, fitment[], existing photos as ready UploadedPhotos with their public URLs + paths).
- Render `<ListingForm mode="edit" listingId={listing.id} defaults={defaults} makes={makes} conditions={conditions} contactPreference={pref} />`.
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('app/(app)/sell/page.tsx','utf8'); const b=require('fs').readFileSync('app/(app)/sell/[id]/edit/page.tsx','utf8'); if(!a.includes('force-dynamic')||!b.includes('force-dynamic')) throw new Error('owner pages must be force-dynamic'); if(!a.includes('ListingForm')||!b.includes('ListingForm')) throw new Error('render the shared form'); if(!b.includes('getListing')&&!b.includes('seller_id')) throw new Error('edit must load + ownership-check'); console.log('OK')"</automated>
  </verify>
  <done>/sell renders the create form (force-dynamic, getClaims-gated); /sell/[id]/edit loads the listing, ownership-checks (notFound on non-owner), and pre-fills the same form for editing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the live listing create + edit flow</name>
  <action>BLOCKING human-verify checkpoint — Claude has finished building the create/edit flow; the user manually exercises it (steps below) before the phase is considered done. No further code is written in this task; on "approved" the plan completes, on issues Claude fixes them.</action>
  <verify>Manual — the user walks the how-to-verify steps below and types "approved".</verify>
  <done>The user confirms create + multi-fit + Barnyard + drag-drop photo upload (EXIF-stripped) + shipping + publish-redirect + edit-prefill all work, and types "approved".</done>
  <what-built>The full seller listing flow: create at /sell (sectioned form — part data, multi-fit fitment + Barnyard toggle, drag-drop photo upload with EXIF strip + first-as-cover, shipping radio), publish → redirect to the public listing, and edit at /sell/[id]/edit (same form pre-filled). Photos are EXIF-stripped server-side (the P0 gate from 05-01 is already proven by an automated test).</what-built>
  <how-to-verify>
1. Run `npm run dev`, log in as a verified seller, visit http://localhost:3000/sell.
2. Fill Part data (title, price, condition; leave part# / damage notes empty to confirm they're optional). Add 2+ fitment combos (e.g. Peterbilt 379 — Day Cab, Kenworth W900). Confirm the multi-fit badges appear and are removable.
3. Toggle The Barnyard on/off — confirm it relaxes the fitment requirement.
4. Select 3-4 photos (include a real phone photo with location on if you have one). Confirm each shows an immediate preview + spinner, then a "ready" state; drag to reorder; confirm the first tile shows "Cover"; remove one.
5. Pick a shipping option. Confirm the account contact preference is shown read-only (not editable here).
6. Publish → confirm you're redirected to the public listing page showing your photos + fitment.
7. (EXIF spot-check) Right-click → save one displayed photo and confirm it carries no GPS (or trust the automated no-GPS test, which is the authoritative gate).
8. Visit /sell/[that id]/edit → confirm the form is pre-filled; change the title/price, save, confirm the public page updates.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (layout, upload behavior, redirect, pre-fill).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm run build` green; full suite green.
- No `withMetadata`/`keepMetadata`/`keepExif` anywhere in components (the strip is server-only).
- Human-verify checkpoint approved (create + edit + photo upload + redirect).
</verification>

<success_criteria>
- A seller can create a listing through one sectioned page with multi-fit fitment (or Barnyard), drag-drop photos (first=cover, immediate EXIF-stripped upload), and a shipping option, then lands on the public listing.
- A seller can edit their own listing via the same pre-filled form.
</success_criteria>

<output>
After completion, create `.planning/phases/05-listings-photos-exif-safe-storage/05-04-SUMMARY.md`.
</output>
