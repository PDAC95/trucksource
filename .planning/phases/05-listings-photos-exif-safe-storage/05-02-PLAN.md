---
phase: 05-listings-photos-exif-safe-storage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/listings/schema.ts
  - lib/listings/cascade.ts
  - supabase/migrations/0008_active_listing_count.sql
  - tests/unit/listing-schema.test.ts
autonomous: true
requirements: [LIST-01, LIST-04, LIST-05]
user_setup: []

must_haves:
  truths:
    - "A single listingSchema validates a listing on the client (UX) and inside the Server Action (trust boundary)"
    - "Title and Asking Price are required; Part Number and Damage Notes are optional"
    - "Asking Price must be a positive USD amount (> 0, 2 decimals)"
    - "A listing must have at least one fitment combination UNLESS it is marked The Barnyard"
    - "Shipping option is one of shipping_available / local_pickup / shipping_assistance"
    - "The Condition select is populated from the fitment library conditions reference table"
    - "A public profile's active-listings count reflects real active listings (no longer a hardcoded 0)"
  artifacts:
    - path: "lib/listings/schema.ts"
      provides: "listingSchema (zod) — single client+server source of truth for a listing"
      contains: "listingSchema"
    - path: "lib/listings/cascade.ts"
      provides: "getConditions reference reader for the Condition select (reuses garage cascade conventions)"
      contains: "getConditions"
    - path: "supabase/migrations/0008_active_listing_count.sql"
      provides: "active_listing_count rewritten to count active listings (PRIV-03 promise from Phase 1)"
      contains: "active_listing_count"
    - path: "tests/unit/listing-schema.test.ts"
      provides: "Unit coverage for required/optional fields, price rules, barnyard-vs-fitment rule"
      contains: "listingSchema"
  key_links:
    - from: "lib/listings/schema.ts"
      to: "zod refine"
      via: "isBarnyard OR fitment.length >= 1"
      pattern: "refine"
    - from: "supabase/migrations/0008_active_listing_count.sql"
      to: "public.listings"
      via: "count where seller_id = profile_id and status = 'active'"
      pattern: "from public.listings"
---

<objective>
Define the listing contracts and the Condition reader that the Wave-2 actions and Wave-3 UI build against — the `listingSchema` (zod, client+server source of truth), the `getConditions` cascade reader, and the long-promised rewrite of `active_listing_count` to count real active listings.

Purpose: Interface-first. Downstream plans (actions, form) must NOT re-derive validation rules; they import `listingSchema`. This also fulfils the Phase-1 deferred promise that `active_listing_count`'s body is rewritten in Phase 5 (PRIV-03). Independent of 05-01 (no file overlap) → runs in parallel.
Output: `lib/listings/schema.ts`, `lib/listings/cascade.ts`, migration 0008, and the schema unit test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-listings-photos-exif-safe-storage/05-CONTEXT.md
@.planning/phases/05-listings-photos-exif-safe-storage/05-RESEARCH.md

# Mirror these proven Phase-4 patterns EXACTLY (zod source-of-truth + cascade reader):
@lib/garage/schema.ts
@lib/garage/cascade.ts

<interfaces>
<!-- The listingSchema shape downstream plans import (05-RESEARCH Pattern 6): -->
const fitmentEntry = z.object({
  modelId: z.coerce.number().int().positive(),
  configId: z.coerce.number().int().positive().nullable().optional(),
});
export const listingSchema = z.object({
  title: z.string().trim().min(1).max(120),
  partNumber: z.string().trim().max(80).optional().or(z.literal("")),
  askingPrice: z.coerce.number().positive().multipleOf(0.01),
  conditionId: z.coerce.number().int().positive(),            // required
  shippingOption: z.enum(["shipping_available","local_pickup","shipping_assistance"]),
  damageNotes: z.string().trim().max(2000).optional().or(z.literal("")),
  isBarnyard: z.boolean().default(false),
  fitment: z.array(fitmentEntry).default([]),
  photoPaths: z.array(z.string()).max(8).default([]),
}).refine(v => v.isBarnyard || v.fitment.length >= 1,
  { message: "Add at least one fitment, or mark The Barnyard.", path: ["fitment"] });
export type ListingInput = z.infer<typeof listingSchema>;
export type ListingFormValues = z.input<typeof listingSchema>;

<!-- conditions reference table (Phase 3, anon-public read): -->
public.conditions (id bigint, name text, sort_order int)

<!-- The active_listing_count signature established in 0001 (DO NOT change its name/args,
     ONLY its body) — the public profile page already calls it via rpc: -->
active_listing_count(profile_id uuid) returns integer  -- currently returns 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: listingSchema (zod) + its unit test</name>
  <files>lib/listings/schema.ts, tests/unit/listing-schema.test.ts</files>
  <action>
Create `lib/listings/schema.ts` per the interfaces block (05-RESEARCH Pattern 6). Mirror `lib/garage/schema.ts`'s header-comment intent: the SAME schema validates client + server; the schema does NOT enforce model_configurations applicability (that's a server-side DB re-check in the actions plan), and it does NOT enforce that submitted photo paths belong to the caller (also server-side). Export `listingSchema`, `ListingInput` (`z.infer`), and `ListingFormValues` (`z.input`, the RHF working type before coercion).
Notes embedded as comments:
- Title/Price required; partNumber/damageNotes optional with `""` accepted (empty field = absent).
- askingPrice uses `z.coerce.number().positive().multipleOf(0.01)` (USD, >0, cents) — never a float column (Pitfall 6 lives in the DB plan; this is the matching client rule).
- The `.refine` enforces "Make+Model required unless Barnyard" via `isBarnyard || fitment.length >= 1` with `path: ["fitment"]` so the error attaches to the fitment section.
- `photoPaths` capped at 8 (CONTEXT limit).

Create `tests/unit/listing-schema.test.ts` (vitest, jsdom is fine — pure zod). Cover:
- valid minimal listing (title, price, conditionId, shippingOption, one fitment) parses.
- missing title → fail; empty title → fail.
- askingPrice 0 or negative → fail; `19.99` → pass; price as string `"19.99"` coerces → pass.
- partNumber omitted AND `""` → both pass (optional).
- no fitment AND not barnyard → fail (the refine); no fitment BUT isBarnyard true → pass.
- invalid shippingOption value → fail.
- more than 8 photoPaths → fail.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/listing-schema.test.ts</automated>
  </verify>
  <done>listingSchema exists with the exact field rules + barnyard/fitment refine; all schema unit tests pass including the price coercion and barnyard-relaxation cases.</done>
</task>

<task type="auto">
  <name>Task 2: getConditions cascade reader + active_listing_count rewrite</name>
  <files>lib/listings/cascade.ts, supabase/migrations/0008_active_listing_count.sql</files>
  <action>
Create `lib/listings/cascade.ts` mirroring `lib/garage/cascade.ts` (the listing form reuses the garage Make→Model→Config readers from `@/lib/garage/cascade`, so DO NOT duplicate getModels/getConfigs — only add what's new). Add:
- `"use server";` header, `import { createClient } from "@/lib/supabase/server";`
- `export type ConditionOption = { id: number; name: string };`
- `export async function getConditions(): Promise<ConditionOption[]>` — selects `id, name` from `conditions` ordered by `sort_order` (then name). Anon-public read; no owner scope. Returns [] on error. This drives the required Condition select in the form's Part-data section.

Create `supabase/migrations/0008_active_listing_count.sql` — fulfils the Phase-1 promise (STATE decision: "active_listing_count ships returning 0 in P1; Phase 5 rewrites only its body"). Use `create or replace function public.active_listing_count(profile_id uuid) returns integer language sql stable security definer set search_path = '' as $$ select count(*)::int from public.listings where seller_id = profile_id and status = 'active' $$;`
- Keep the EXACT name + signature (the public profile page already calls `rpc('active_listing_count', { profile_id })`).
- SECURITY DEFINER + `set search_path = ''` matches the repo convention (0001/0002 functions). It must remain anon-callable and return only an integer (no PII) — same posture as is_verified_seller.
Apply non-destructively to Staging: `supabase db query --linked -f supabase/migrations/0008_active_listing_count.sql`. Verify: calling the RPC for a seller with N active listings returns N (0 when none).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/0008_active_listing_count.sql','utf8'); if(!s.includes('active_listing_count')||!s.includes('from public.listings')||!s.includes(\"status = 'active'\")) throw new Error('count body wrong'); if(!s.includes('security definer')) throw new Error('must stay security definer'); console.log('OK')"</automated>
  </verify>
  <done>getConditions returns id+name conditions ordered by sort_order; active_listing_count now counts active listings for the seller (same name/signature, still SECURITY DEFINER + anon-safe integer). Applied + verified on Staging.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/listing-schema.test.ts` green.
- `npx tsc --noEmit` clean (the new lib modules compile).
- Full suite still green (no regression): `npx vitest run`.
- On Staging: `active_listing_count` returns a real count (seed a quick listing manually or confirm 0 for a fresh seller); `getConditions` returns the seeded conditions.
</verification>

<success_criteria>
- listingSchema is the single client+server contract: required title/price, optional part#/damage, USD price rule, barnyard-or-fitment refine, 8-photo cap.
- getConditions feeds the required Condition select from the library.
- active_listing_count counts real active listings (PRIV-03 promise fulfilled), unchanged name/signature/security posture.
</success_criteria>

<output>
After completion, create `.planning/phases/05-listings-photos-exif-safe-storage/05-02-SUMMARY.md`.
</output>
