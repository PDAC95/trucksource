---
phase: 05-listings-photos-exif-safe-storage
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - supabase/migrations/0009_contact_preference.sql
  - lib/account/schema.ts
  - lib/actions/account.ts
  - app/(app)/account/page.tsx
  - app/(app)/account/contact-preference-form.tsx
  - app/(public)/listings/[id]/page.tsx
  - components/listings/listing-detail.tsx
  - lib/actions/listing-view.ts
  - next.config.ts
  - tests/integration/contact-preference.test.ts
autonomous: true
requirements: [LIST-07]

must_haves:
  truths:
    - "A seller can set an account-level contact preference: Email Only / Email + Phone / Marketplace Messaging Only"
    - "The contact preference defaults to Marketplace Messaging Only (most private)"
    - "The contact preference is account-scoped (set once in account settings), not per-listing"
    - "Anyone can view a public listing detail page showing photos, fitment, price, and the seller's public username"
    - "Viewing a listing detail page records a listing-view event on every view (the page is not statically cached, so the event is non-reconstructible per invariant #8)"
    - "The public listing page exposes zero seller PII"
  artifacts:
    - path: "supabase/migrations/0009_contact_preference.sql"
      provides: "contact_preference column on profiles_public, default messaging_only (non-PII enum)"
      contains: "contact_preference"
    - path: "app/(app)/account/contact-preference-form.tsx"
      provides: "The account-settings control for LIST-07 (the only place it's edited)"
      contains: "updateContactPreference"
    - path: "app/(public)/listings/[id]/page.tsx"
      provides: "Public buyer-facing listing detail page (redirect target on publish); force-dynamic so view events fire per request"
      contains: "getListing"
    - path: "lib/actions/listing-view.ts"
      provides: "Fire-and-forget listing-view event insert (invariant #8)"
      contains: "listing_view_events"
  key_links:
    - from: "app/(app)/account/contact-preference-form.tsx"
      to: "lib/actions/account.ts"
      via: "updateContactPreference Server Action (getClaims, owner RLS on profiles_public)"
      pattern: "updateContactPreference"
    - from: "app/(public)/listings/[id]/page.tsx"
      to: "lib/listings/queries.ts"
      via: "getListing(id) reads public columns + profiles_public only"
      pattern: "getListing"
    - from: "app/(public)/listings/[id]/page.tsx"
      to: "lib/actions/listing-view.ts"
      via: "force-dynamic page fires a view event on every render"
      pattern: "listing_view_events|recordListingView"
    - from: "app/(public)/listings/[id]/page.tsx"
      to: "Next.js render mode"
      via: "force-dynamic disables static caching so the per-view event always runs"
      pattern: "force-dynamic"
---

<objective>
Complete the phase's account + public surfaces: the account-level contact preference control (LIST-07), the public buyer-facing listing detail page (the publish redirect target), and listing-view event logging instrumented from day one (invariant #8).

Purpose: LIST-07 is account-scoped, not per-listing — it lives in account settings and defaults to the most-private option. The public listing page is what `createListing` redirects to and must expose zero PII (reads `profiles_public` only). View-event logging must ship NOW because the data is non-reconstructible later — and because it is non-reconstructible, the page must render per-request (force-dynamic) so the event fires on EVERY view, not just the first cached render. Separate files from 05-04 → runs in parallel (both Wave 3, depend only on 05-03).
Output: migration 0009, account schema/action/page/form, public listing page + detail component, the view-event action, next.config image host whitelist, and the contact-preference RLS test.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-listings-photos-exif-safe-storage/05-CONTEXT.md
@.planning/phases/05-listings-photos-exif-safe-storage/05-RESEARCH.md

# Mirror these proven patterns:
@supabase/migrations/0001_foundation_privacy.sql
@app/(public)/u/[username]/page.tsx
@lib/actions/garage.ts
@tests/integration/privacy.contract.test.ts

# Depends on Wave-2 output:
@.planning/phases/05-listings-photos-exif-safe-storage/05-03-SUMMARY.md

<interfaces>
<!-- From 05-03 (import these): -->
import { getListing, type ListingDetail } from "@/lib/listings/queries";

<!-- profiles_public is world-readable, owner-update RLS already exists (0001):
     create policy "owner updates own public profile" ... (select auth.uid()) = id.
     contact_preference is NON-PII (an enum of 3 contact modes) → it belongs on
     profiles_public (05-RESEARCH Open Q1 recommendation): the Phase-9 contact surface
     needs to read it. The actual email/phone it unlocks stay in profiles_private. -->

<!-- LIST-07 values: 'email_only' | 'email_phone' | 'messaging_only' (default). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contact_preference migration + account schema/action/page/form (LIST-07)</name>
  <files>supabase/migrations/0009_contact_preference.sql, lib/account/schema.ts, lib/actions/account.ts, app/(app)/account/page.tsx, app/(app)/account/contact-preference-form.tsx</files>
  <action>
Create `supabase/migrations/0009_contact_preference.sql`:
- `alter table public.profiles_public add column if not exists contact_preference text not null default 'messaging_only' check (contact_preference in ('email_only','email_phone','messaging_only'));`
- Rationale comment: NON-PII enum, default = most-private (CONTEXT); lives on profiles_public because Phase-9's contact flow must read it; the email/phone it governs stay in profiles_private. The existing owner-update policy on profiles_public already scopes writes — no new policy needed.
Apply non-destructively to Staging: `supabase db query --linked -f supabase/migrations/0009_contact_preference.sql`. Verify the column exists with the default + CHECK.

Create `lib/account/schema.ts`: `export const contactPreferenceSchema = z.object({ contactPreference: z.enum(["email_only","email_phone","messaging_only"]) });` (client+server source of truth).

Create `lib/actions/account.ts` (`"use server"`, mirror garage.ts trust boundary): `updateContactPreference(input: unknown)` → getClaims (never getSession) → safeParse contactPreferenceSchema → `supabase.from("profiles_public").update({ contact_preference }).eq("id", userId).select("id")`; zero rows → not_found; else ok. Owner RLS on profiles_public is the boundary; NO service-role.

Create `app/(app)/account/page.tsx` (Server Component, `force-dynamic` — owner data): getClaims redirect if unauthenticated; read the user's current `contact_preference` from profiles_public (`.eq("id", userId).maybeSingle()`); render a settings page with `<ContactPreferenceForm current={pref}/>` + `<Toaster/>`. (Keep the page minimal — a settings shell with the one control; other account settings are out of scope.)

Create `app/(app)/account/contact-preference-form.tsx` (`"use client"`): RHF + zodResolver(contactPreferenceSchema), a RadioGroup of the three options with helper text (Marketplace Messaging Only = most private, the default). Submit in startTransition → updateContactPreference → toast + router.refresh(). This is the ONLY place the preference is edited (the listing form merely displays it).
  </action>
  <verify>
    <automated>npx vitest run tests/integration/contact-preference.test.ts</automated>
  </verify>
  <done>contact_preference column added to profiles_public (default messaging_only, CHECK on 3 values); /account renders the radio control; updateContactPreference writes owner-scoped via getClaims; the integration test confirms the column + default + anon-can't-write.</done>
</task>

<task type="auto">
  <name>Task 2: contact-preference RLS test + next.config image host whitelist</name>
  <files>tests/integration/contact-preference.test.ts, next.config.ts</files>
  <action>
Create `tests/integration/contact-preference.test.ts` mirroring privacy.contract.test.ts / garage.test.ts (node env, self-skip via INTEGRATION_ENABLED, anonClient):
- anon SELECT of `profiles_public` (which now includes contact_preference) returns rows but the value is a NON-PII enum — assert no PII keys leak (extend or reference the existing PII denylist) and contact_preference is one of the 3 allowed values.
- anon UPDATE of `contact_preference` on a row is DENIED (no anon update policy on profiles_public → error). This proves only the owner can change it.

Update `next.config.ts` to whitelist the Supabase Storage host for `next/image` (05-RESEARCH Pattern 4 note): add `images.remotePatterns` with `{ protocol: "https", hostname: "<project-ref>.supabase.co", pathname: "/storage/v1/object/public/**" }`. Derive the hostname from `NEXT_PUBLIC_SUPABASE_URL` if possible (parse the host) so it works across Staging/Prod without hardcoding the ref. This lets the listing detail page render photos via next/image against the public Storage URL (the project is plan-agnostic — no Image Transformations dependency, per Open Q2).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('next.config.ts','utf8'); if(!s.includes('remotePatterns')) throw new Error('whitelist supabase storage host for next/image'); console.log('OK')" && npx vitest run tests/integration/contact-preference.test.ts</automated>
  </verify>
  <done>The RLS test proves contact_preference is owner-only-writable and exposes no PII; next.config whitelists the Supabase Storage host so next/image can render listing photos.</done>
</task>

<task type="auto">
  <name>Task 3: Public listing detail page (force-dynamic) + per-view event logging (invariant #8)</name>
  <files>app/(public)/listings/[id]/page.tsx, components/listings/listing-detail.tsx, lib/actions/listing-view.ts</files>
  <action>
Create `lib/actions/listing-view.ts` (`"use server"`): `export async function recordListingView(listingId: number)` — best-effort insert into `listing_view_events` (listing_id, viewer_id = getClaims sub or null for anon). NO IP/PII (Pitfall). Swallow errors (never block the page render) — the event row is the durable record, like the abuse-alert pattern. This is the invariant-#8 instrumentation that Phase 10 analytics consumes.

Create `components/listings/listing-detail.tsx` (Server Component or simple client for the gallery) — renders the buyer-facing view from a `ListingDetail`: a photo gallery (next/image, cover first by sort_order), title, formatted USD price (`Intl.NumberFormat('en-US',{style:'currency',currency:'USD'})`), condition, shipping option label, damage notes (if any), Barnyard badge (if barnyard), the fitment combos as badges, and the seller's PUBLIC identity ONLY (username → link to /u/[username], state/province, country). NO contact button yet (Phase 9) — a placeholder "Contact seller" affordance is fine but out of scope to wire.

Create `app/(public)/listings/[id]/page.tsx` (Server Component) — the publish redirect target. It is anon-readable (listings public-read), BUT it must record a view event on EVERY request, and that view event is the ONLY non-reconstructible data in the phase (invariant #8). A fire-and-forget call inside a *statically cached* RSC would NOT run on cached renders, so view events would silently undercount. Therefore this page renders per-request:
- `export const dynamic = "force-dynamic";` at the top, with a comment: "force-dynamic so recordListingView runs on every view — invariant #8, the view event is non-reconstructible; mirrors the force-dynamic owner pages in this phase." (Equivalent no-cache directives like `export const revalidate = 0` are acceptable, but force-dynamic matches the rest of the phase — pick force-dynamic.)
- `const listing = await getListing(Number(id))`; if null → `notFound()`.
- Fire `void recordListingView(listing.id)` after the listing is found, BEFORE rendering (best-effort; never await-block in a way that can fail the page — wrap in try or let recordListingView swallow its own errors). Because the page is force-dynamic, this runs on every request.
- Render `<ListingDetail listing={listing}/>`.
- CRITICAL: this page reads ONLY public columns + profiles_public (getListing already enforces this). Re-confirm against the privacy contract: NO profiles_private, NO PII in the RSC payload (Pitfall 7).
  </action>
  <verify>
    <automated>node -e "const p=require('fs').readFileSync('app/(public)/listings/[id]/page.tsx','utf8'); const d=require('fs').readFileSync('components/listings/listing-detail.tsx','utf8'); if(!p.includes('getListing')) throw new Error('use getListing'); if(!/recordListingView|listing_view_events/.test(p)) throw new Error('must log a view event'); if(!/force-dynamic|revalidate\\s*=\\s*0/.test(p)) throw new Error('public listing page must NOT be statically cached, or per-view events undercount (invariant #8)'); if(/profiles_private|first_name|last_name|\\bphone\\b/.test(p+d)) throw new Error('PII must not appear on the public listing page'); console.log('OK')"</automated>
  </verify>
  <done>The public listing page is force-dynamic (not statically cached), so visiting it records a listing-view event on EVERY view (invariant #8 — the event is non-reconstructible); it shows photos (next/image), price, condition, shipping, fitment, and the seller's public username only (no PII).</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/integration/contact-preference.test.ts` green; full suite green.
- `npx tsc --noEmit` clean; `npm run build` green.
- Grep the public listing page: it declares `force-dynamic` (or `revalidate = 0`) so it is never statically cached — the per-view event fires on every request (invariant #8).
- Grep the public listing page + detail component: NO `profiles_private`, no PII field names.
- On Staging: contact_preference column present (default messaging_only); each view of a listing inserts a listing_view_events row (re-load the page twice → two rows).
</verification>

<success_criteria>
- LIST-07: a seller sets an account-level contact preference (default Marketplace Messaging Only) in /account; the listing form only displays it.
- The public listing page renders the full buyer view with zero PII and is the publish redirect target.
- Listing-view events are logged on every view from day one (invariant #8) — the page is force-dynamic so cached renders cannot skip the event.
</success_criteria>

<output>
After completion, create `.planning/phases/05-listings-photos-exif-safe-storage/05-05-SUMMARY.md`.
</output>
